# КАДАСТР-ПЛАН v0.3.1 → v0.3.2 (применяется ПОСЛЕ сносов C/D) · 707 · 2026-08-30
**Статус:** ПОДГОТОВЛЕН, НЕ ПРИМЕНЯТЬ (диспатч-2 200, п.2). Приложится к houses.yaml когда C/D в каноне.

## Что изменится в городе после MICRO-PACK C (dead-code)
| Здание | Изменение | Почему |
|---|---|---|
| **scenes B11** | −1 этаж: PitchModule.tsx + usePitch + featureFlag уходят | dead-code прогона #1 (снос чист) |
| **quest B02** | −1 этаж: RecordingPanel.tsx + RecordingPanel.module.css (пара) | dead-code прогона #1 |
| **billy B07** | −0 этажей формально, НО: под 🔴 Никиты CatalogBillyChat — если KILL: −1 спящий этаж (skill-registry + BillyMessageRenderer + css каскадом); если SHELVED — этаж остаётся с меткой `shelved` | прогон #1 + досье для Никиты |
| **studio B01** |PitchModule вычитается из loc: 293 → пересчёт | метрики перегенерятся city-metrics |
| **notes B06** | Этаж «Питч-детекция» — остаётся живым (YIN не тронут) | — |

## Что изменится после MICRO-PACK D (docs)
| Объект | Изменение |
|---|---|
| Подземка infra | Слой доков-дублей (handoff/docs 22) уходит из дерева — в кадастре НЕ отражено (доки не этажи src), НО регистрирую в open-seams как закрытый шов |
| masks | css-скелёт .masks-* (styles.css:4929-5108) — если пойдёт опцией D+ — этажа в городе нет (маски никогда не были зданием), фиксирую в open-seams |
| Доки-трупы (003: slot-matrix-v2.2, MAC-PC-BRIDGE-SPEC, TC-094-095) | Не город — but DOC-SYNC-MAP 003 уже ведёт их; кадастр не трогает |

## Новые этажи (план, после решений)
| Кандидат | Откуда | Этаж |
|---|---|---|
| **rnnoise-wasm** (если досье 005 подтвердится) | Охота №3, Apache-2.0 | studio B01: новый этаж «Шумодав микрофона» (MicSourceV3 → denoise-ворклет) — кандидат Этапа 1 моста |
| **pitchy A/B** (если Суд возьмёт как микро-апгрейд ARC-2e) | Охота №1, 0BSD | notes B06: этаж «A/B-эталон» — только после вердикта |
| **pYIN-слой + FFT-путь** (005_2, наука) | Охота 005_2 №1 | notes B06: кандидат «Питч v2» — через 200 → 007, отдельный ARC |
| **audio-reactive V3-tap** 🆕 | Находка 201 (15-AUDIO-BUG-POSTMORTEM P2) | **backlog-этаж**: слеп в V3 (ищет V2-узлы stereoMerger/instrumentalGain — audio-reactive.ts:43 connect failed); FullAvatar (читает --bl-audio-* CSS-вары) + спектр мертвы. Эквивалент vocalReferenceTap (ARC-2e, pre-fader, fader-independent) для спектра ещё не сделан. Здание: studio B01 или scenes B11 (визуал, ведомый звуком — северная звезда). Кандидат в ARC-очередь после BRG — зависит от решения 200/007 |

## Метки тёмных этажей (новое в контракте v0.3.2)
```yaml
# floors-элементы получают опциональный суффикс-метку:
#   [dark-code] — нет в графе импортов (снос подтверждён прогоном)
#   [dark-doc]  — есть в доках, нет в коде (008-идея, ждёт реализации)
#   [shelved]   — спящая фича: код готов, не смонтирован, идея жива (CatalogBillyChat)
#   [backlog]   — живой код с мёртвой зависимостью: работает в V2-контуре, ждёт V3-аналога (audio-reactive)
```
city-gen v3.2 (малый патч): парсинг `[метка]` в конце строки этажа → в state как `room.dark: true`
(+ `room.backlog: true` для [backlog]), рендер HTML: тёмные/спящие этажи пунктиром, backlog —
штрихом с янтарной точкой («жив, но ждёт провода»).

## Сверка итогов (после применения C/D — прогон приёмки 707)
1. `node city-gen.mjs` — exit 0, 0 ошибок, этажи-метки в state
2. Пересчёт loc: scenes/billy/quest худеют на снесённые строки
3. t30 — по ТЗ (city-metrics), свет окон = f(t30)
4. 17 зданий / 48→44 этажей / 9 районов / 14 линий метро (после вычета тёмных)

— 707 · картограф
