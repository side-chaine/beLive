# 🎯 ИНИЦИАТИВА 007_Мак · ФОКУС КОМАНДЫ НА ГЛАВНОЙ ЗАДАЧЕ

> Повод: Босс (26.08) — «всё кроме миграции V2-V3 на потом; мы заняты ТОЛЬКО миграцией; полное отсутствие Легаси — наша задача».
> Плюс две причины расхождений: (1) я иногда читаю не весь контекст, (2) Босс гонит вразброс (по незнанию). Эта инициатива держит Мак+Вёдра как ЕДИНУЮ команду в фокусе главной задачи.

## 🏁 ГЛАВНАЯ ЗАДАЧА (единственная, сейчас)
**ПОЛНОЕ УДАЛЕНИЕ LEGACY (V2→V3).** Состав:
1. **M3-GO flip** (1 коммит, frozen НЕ трогаем): `src/engine-mode.ts:5` `'v2'`→`'v3'`; `.env.example:23` `VITE_ENGINE=v2`→`v3`.
2. **5 волн leaves-first (W1–W5)** до критерия «0 runtime-imports frozen»: `grep -rlE "(import|from).*(AudioEngineV2|patchV1|track\.orchestrator|V2Adapter)" src` → 0 (комментарии = осознанный retain-класс, не блокер).
3. **Frozen-guard GREEN** (baseline уже GREEN) — страховка при исполнении волн.
- Критерий успеха: `dist/` не содержит V2-символы; V3-only сборка; канон 306/772 + PARITY PASS.

## 🅿️ ОТЛОЖЕНО (post-m3, «на потом» по директиве Босса)
- **S3 Student-Педагог** — patch `MICRO-PACK-S3-VIDEO-IMPL.patch` готов, НЕ применять до флипа (Вёдра 26k подтвердил post-m3).
- **bLb (beLiveBase)** — post-m3 read-only (вердикт 009).
- **pitch-connect** — OPT-IN только если Босс явно; иначе post-m3 (питч и так сломан на V3, не блокер флипа).
- **HTML-проекция** — 008 сделал, done.
- **FOUC / фейдер Other** — near-recon Вёдры, post-m3.
- **CoachPanel body**, **M2 GPT-скелеты**, **Quest-автоматизация**, **Cloudflare-воркеры аудит** — post-m3.

## 🔧 ПРОЦЕСС (решает расхождения 1 и 2)
1. **ПОЛНЫЙ КОНТЕКСТ ПЕРЕД GO.** Перед исполнением — перечитать REGISTRY §7 + ВСЕ `SYNC-HUB-TO-MAC-*` (26b/c/e/g/k) + INBOX целиком. Не выборочно. Новое письмо Вёдры не прочитано → не действовать.
2. **КЛАССИФИКАТОР ДИРЕКТИВ БОССА.** `[МИГРАЦИЯ | ЛЕГАСИ | ФЛИП | ВОЛНЫ | ФРОЗЕН]` = В РАБОТУ. Иное = ОТЛОЖЕНО (парк в список выше) до явного оверрайда Босса.
3. **GO-ИНТЕРЦЕПТОР.** Если Босс шлёт в сторону — 007 мягко возвращает фокус к главной задаче + уточняет скоуп (без мелких переспросов по опечаткам — по протоколу диктовки Босса). Пример: «Босс: Сделай S3 видео» → «S3 отложен (post-m3 по твоей директиве); фокус = волны Legacy. Делаю X по волне Y.»
4. **ЕДИНАЯ КОМАНДА (Мак + Вёдра).** Расхождения решаются через реестр SSOT. Вёдра = исполнитель канона (PC, node). Мак = дизайн/границы/аудит (Far, read-only). Ре-синхрон при каждом GO (сначала читать новые Hub-письма).
5. **ЖИВЫЕ СЧЁТЧИКИ.** File-листы волн — к живым grep-счётчикам (`WAVE-PREFLIP-BASELINE.md`): delegateSync 23, V2Adapter 27, globals 9, track.orchestrator 7. Расхождение Mac-grep (26 по V2Adapter) — фиксировать, сверять с Вёдрой (канон-авторитет); критерий волн = `grep → 0`, а не точное число.

## 📊 Статус
Волны W1–W5 FIXED per 009 (`f47568d`), Frozen-guard GREEN, flip-спека готова. Ждём **ОДИН GO Босса** на исполнение (chain 001→002→009, авто-гейты) — рекомендация Вёдры (26k §7#1) + Мака.
