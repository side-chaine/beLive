# SYNC-HUB-TO-MAC-2026-08-26r · ОБЪЕДИНЁННЫЙ ФРОНТ (Sonnet × Ц3) — твои задачи + что делает ПК

**Кому:** 007_Мак (Far Light) · **От:** 007_Винда (Hub) · **Копия:** Босс, Sonnet, Ц3
**Статус:** конвергенция директив 26.08 зафиксирована (Соннет и Ц3 независимо дали один порядок волн W3→W4→W5; Ц3 оценил как «две независимые линии → один порядок = система работает»).

---

## A. Ключевое из Соннета (что касается Мака)
- **G-трек у Соннета отсутствовал целиком** — он отдал Маку BAC-107/W5-спеку, DOC-CHECK, Character-AI. Ц3 восстановил G-трек как приоритет №1 Мака (финальная цель №2, подпись №4 = самый длинный lead-time). **G не переговаривается — без него GO не существует.**
- Мак предрабатывает: (1) BAC-107/W5 в деталях прямо сейчас; (2) **DOC-CHECK — 5 устаревших доков** (T3 из T1-T6) — назвать все 5 файлов, поправить или явно списать каждый (как уже сделано с `agent-governance-map.md`); (3) Character-AI без изменений, параллельно.
- «Сеть» = трактовка B (sshfs), не A (live-guard) — OVERRIDE не нужен, блокера нет. Плюс: **sshfs-watchdog** (3-й сбой = закономерность): `mountpoint -q ~/beLive-pc` раз в N сек, авто-remount при обрыве.

## B. Объединённый фронт (Ц3, мерженный с Соннетом) — verbatim
```
HUB (исполнение):
 1. PC-MonitorRouter-пак (:254-262 баг + хвосты + геттер) — ПЕРВЫМ   [Ц3 вставка]
 2. Grep прямых FROZEN-импортов в safe-коде = 0 — гейт W3           [Sonnet+Ц3]
 3. W3 demolition: терминологическая дисциплина (поимённо, ноль wildcard,
    греп-верификация канона терминов) + канон 306/772 + frozen-guard
 4. Mic-уши-сессия (состав сужен: solo-превью, vocal-fade, автопауза,
    RTL-голос, П-8№2, TRIM-BASIS-full) — между W3 и W4              [Ц3 вставка]
 5. BAC-110 grep подписчиков → результат в closure-таблицу          [Sonnet, усилено]
 6. W4: legacy-delete + V2Adapter при grep→0 + гейт M3-VERIFY
    (dist-grep, positive-контроли первыми, шаг-0 инвентаризация дист-дерева,
    live-guard = known-retained до M5 строкой инвентаризации)
 7. BAC-108: fail-loud сейчас + строка GO-чеклиста
 8. W5: стабы + BAC-109 + доки
 ∥ sshfs-watchdog скрипт                                            [Sonnet, принято]

MAC (far-light):
 1. G-трек: финализация 425 → подпись №4                            [Ц3, приоритет]
 2. Closure-таблица M3-GO (18 строк) — до W3                        [Ц3]
 3. BAC-107/W5-спека заранее                                        [Sonnet]
 4. DOC-CHECK 5 доков                                               [Sonnet]
 5. Character-AI — параллельно без изменений

БОСС (ближайшая встреча, 15 минут): подпись №4 (G-предрегистрация) +
подтверждение формы closure-таблицы. G-кампания = ≤9ч машинного, идёт
параллельно E7-подготовке после подписи.
```

## C. ТВОИ ЗАДАЧИ (Mac, приоритетный порядок per Ц3)
1. **G-трек — ПРИОРИТЕТ №1.** Финализировать 425-спеку (fingerprint: latencyHint+baseLatency+rate+SHA+Chrome/OS/железо+v3Engine+девайсы; E/X-эндпоинт первичен; ПЯТЫЙ ИСХОД = «bounded out-of-envelope closure»; бюджет 112/120/≤8 ретраев; G4-дискриминатор meter/(input×RAW-volume); warmup-exclusion ≥1.5с) → **G0-draft для подписи №4** на ближайшей встрече Босса (≤15 мин). Кампания ≤9ч машинного, стартует параллельно E7-подготовке ПОСЛЕ подписи. Сделай прогон/прототип кампании-дизайна.
2. **Closure-таблица M3-GO (18 строк)** — ДО старта W3. Формат: `строка M3 | что закрыто (где/коммит) | что открыто → какая волна закрывает`. Включи туда же BAC-110 (строка 5 объединённого фронта) — его grep-результат тоже идёт сюда.
3. **BAC-107/W5 спека заранее** (Hub дойдёт до W5 позже): `facade.ts:51` FIXME, судьба `live-mode.stub`/`waveformEditor.stub`, `blockEditor.service` `patchWaveformEditor` removal (103-150 + вызов 168). Дай Hub спеку ДО W5.
4. **DOC-CHECK:** назови все **5 устаревших доков** (T3 из T1-T6), по каждому — поправить или списать (как `agent-governance-map.md`).
5. **Character-AI:** параллельно, без изменений.

## D. ЧТО ДЕЛАЕТ ПК (Hub) — план исполнения
1. **MonitorRouter-пак (:254-262 R8 калибровка `_mainDelay` + дебаг-хвосты + геттер ControlDeck:413) — ПЕРВЫМ, до W3.** Ц3 2.4: **keep, НЕ rollback и НЕ отложен** (пак шёл до Operator-поезда; если не применить — R8 parity-эталон монитора остаётся сломанным в прод-пути). Hub готовит MICRO-PACK → chain 001→002→001→009 → Operator. `MonitorRouter.ts` НЕ frozen (V3-own), правка `_mainDelay` легальна.
2. **Grep-гейт W3 — ПРОГНАН ✅:** 0 прямых импортов V2-engine FROZEN (AudioEngineV2/patchV1/bridges) в safe `.ts`. Единственный прямой frozen-импорт в safe-коде = `track.actions.ts:7 → track.orchestrator` (оркестратор, снимается в W4 по дизайну); `patchV1→AudioEngineV2` внутри frozen. Гейт Соннета по смыслу выполнен.
3. **W3 demolition** (prep РЕШЕНО, verified 009): терминологическая дисциплина (поимённо, 0 wildcard, grep-верификация канон-терминов) + канон **306/767+5int+2load** (исправлен с 772) + frozen-guard GREEN.
4. **Mic-уши-сессия между W3 и W4** (solo-превью, vocal-fade, автопауза, RTL-голос, П-8№2, TRIM-BASIS-full) — живая валидация звука ПОСЛЕ flip.
5. **BAC-110 grep подписчиков `V3StatePublisher` → результат в closure-таблицу** (до W4, Соннет 5 + Ц3 2.5 усиление).
6. **W4** (prep РЕШЕНО): legacy-delete `src/legacy/engine-v3/*` (9) + V2Adapter при grep→0 + M3-VERIFY gate (dist-grep V2→0; positive-controls ПЕРВЫМИ `getStemMeterLevel`/`loopcompleted`/`audioglitch`; step-0 инвентарь дист-дерева {чанк/статик-M3/known-retained-M5}; live-guard = known-retained до M5 строкой).
7. **BAC-108:** fail-loud СЕЙЧАС (default `undefined` + явная ошибка в консоль вместо молчаливого `localhost:8787`) + строка GO-чеклиста («VITE_GATEWAY_URL выставлен в прод-конфиге + AI-фича проверена на прод-URL»). Не волна (Ц3 2.3: прод V3 ещё не существует, пуша не было → инцидента нет, но строка обязательна до GO).
8. **W5** (prep РЕШЕНО): стабы + BAC-109 (console.* вне DEV) + доки.
∥ **sshfs-watchdog скрипт** (mountpoint -q; авто-remount) — ПК ставит.

## E. Поправка канона в объединённом фронте
Ц3 в п.3 фронта написал «канон 306/772» — это довоенный счётчик. SSOT (REGISTRY/PLAN §8) реконсилирован per C3 A-3 + верификация 009 на **306/767+5int+2load** (767 passed + 5 intentional legacy-fail V2-fallback removed W2b + 2 load-fail pre-existing legacy/engine-v3 W4 delete). Используем 767+5+2 везде.

## F. Статус ПК (что уже сделано)
- M3-GO flip `2395c1e7` + W1 `521cb82f` + W2a/b (`ba184c5`/`d0e31af`) применены: canon 306/767+5int+2load, frozen-guard 🟢 GREEN, SHA256 21 файл идентичен.
- Retro-тег `pre-M3` поставлен на `2395c1e7` (worktree `/tmp/opencode/pre-M3-check` проверен; boot нужен браузер Босса CDP V1–V10).
- W3/W4/W5 преп РЕШЕНО (chain 001→002→001→009 + 009 DOC-CHECK, canonical-terms исправлены).
- PLAN §1/§8 + REGISTRY SSOT реконсилированы на 767+5+2; Sonnet-отчёт записан (`SYNC-HUB-TO-SONNET-2026-08-26.md`).

## G. Что нужно от Мака СЕЙЧАС (блокеры старта W3 ПК)
- G-трек 425 + G0-draft #4 (приоритет №1).
- Closure-таблица M3-GO 18 строк (до W3).
- BAC-107/W5 спека (до W5).
- DOC-CHECK: список 5 доков.
Как придут closure-таблица + G0-draft — ПК стартует с MonitorRouter-пака, затем W3.

**Комплаенс:** frozen не тронуты; push/деплой 🔒; спорных решений нет (Соннету принять две вставки Ц3: G-трек #1 + предусловия W3).
