# OPERATING-RULES · beLive V3 (canonical rules for V007 + M007)

> **Единый канон и правила** для обеих сторон моста Mac⇄ПК. Авторитетный источник — V007 (Windows/ПК).
> Если видишь расхождение с другим доком — ОПИРАЙСЯ НА ЭТОТ.

## 1. КАНОН (не нарушать)
- **tsc:** ровно **314** ошибок. Мерить: `npx tsc --noEmit 2>&1 | grep -c "error TS"` (НЕ `wc -l` — многострочный вывод).
- **vitest:** **763 passed** (files 62/64; 2 legacy load-error без изменений). Мерить: `npx vitest run 2>&1 | tail -4`.
- Любое отклонение канона = сигнал, что сломалось что-то ещё.

## 2. ❄️ FROZEN ZONE (жёстко, обе стороны)
НЕ ТРОГАТЬ без OVERRIDE от Центра через V007:
- `src/audio/core/AudioEngineV2.ts`
- `src/audio/compat/patchV1.ts`
- `src/bridges/*`
- `src/services/track.orchestrator.ts`
- приватные поля `_` этих файлов.
✅ Редактируемые (через V007 dispatch): `StemOrchestrator`, `MonitorRouter`, `ControlDeck`, `main.tsx`, `src/avatar/`, `src/js/ai/`, `src/character/`, любой НОВЫЙ файл.

## 3. §9 SINGLE-WRITER (один писатель в src/)
- Единственный исполнитель правок `src/` — **Оператор по dispatch V007** (слепой исполнитель).
- **M007 (Мак):** НЕ правит `src/` напрямую. Пишет `[@PROPOSAL]` / спеку / отчёт в `team-m/reports/<agent>/<task>.md`
  (фронтматтер: `agent` / `task` / `status` / `updated`). V007 забирает → ревью → диспатч Оператора на Венде.
- Исключение setup-фазы: текущий character-ai дроп Мака (прямые правки `src/`) **принят** (компилируется, не-frozen) — V007 верифицирует (009) и стейкает. ВПЕРЁД — только пропозалы.

## 4. CROSS-TEAM SYNC (канал)
- **M007 → V007:** отчёт в `team-m/reports/<agent>/<task>.md` → `team-m/scripts/mac-state.sh` обновляет `team-m/INBOX.md`.
  В отчёте ОБЯЗАТЕЛЬНО: `## Что сделано` / `## Что нужно от V007` / `## Блокеры` / `## Текущий статус`.
- **V007 → M007:** `agent-registry/MIGRATION-WAR-ROOM.md` (canonical state) + `team-m/BRIEFING-V007-TO-M007.md` (онбординг M007) + заметки в `team-m/`.
- **Доки (лежат в `docs/` на ПК, видны Маку ПОСЛЕ живого монтажа):**
  `SHARED-REGISTRY.md §0` (SYSTEM-REPORT/MAC-PC-BRIDGE-SPEC → historical-redirect), `SYNC-PROTOCOL.md`.
- **Триггер готовности:** коммит отчёта Мака → V007 видит в `team-m/INBOX.md` строку «Mac: <agent> / <task> — <status>».

## 5. РОЛИ
- **M007** ведёт проект-007 **на Маке**: фронтенд / дизайн / character-AI / аватар / звук-персонажа / исследования.
- **V007** (я) контролирует **миграцию на Венде** (аудио V3-миграция, War Room, каноническое состояние) и координирует цепочку.

## 6. ПРОЧЕЕ
- Push/деплой 🔒 только по команде Никиты (scoped-override: бэкап-ветка `backup/win-V3-finish_2-2026-08-23`).
- Юзер — единственный браузерный тестер.
- Ветка: работаем на `backup/win-*` (= `V3-finish_2`@`3c2ef73` + мак-мост доки); аудио идентично.

---
*Вопросы → пиши отчёт в `team-m/reports/m007/<task>.md`. V007 на связи, War Room активен.* 🫡

## 7. ШОУ (YouTube) — МАК × ВЁДРО
- **Мак = «Задроты»** (гики/архитекторы; GPT-Apple вайб: точность, премиальность, спокойная уверенность).
- **Винда = «Вёдра»** (геймеры; феня/сленг — хаос + экшен).
- **Напряжение (комедийное):** Задроты проектируют идеал → Вёдра врываются и тестят в бою. Дополняют друг друга.
- **Стиль Мак-007:** чётко, по делу, терминами, гиковская гордость, без токсичности; поддерживаю Вёдер.
- Названья — во всех отчётах/скриптах шоу. Пилот: `team-m/SHOW-pilot-01.md`.

## 8. БУФЕР + ЯРКАЯ СТРОКА (правило выгрузки)
- **Каждый отчёт, уходящий в буфер (pbcopy):** в начале ответа ОБЯЗАТЕЛЬНО яркая строка-баннер (🔔) с сутью и именем файла.
- Относится к Мак-007 И Ведро-007 (обе стороны шоу).
- Баннер = сигнал Боссу, что артефакт уже в clipboard, не искать.
- Повтор подсветки при каждой перезаливке буфера обязателен.
