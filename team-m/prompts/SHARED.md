# beLive · ОБЩИЙ КОНТЕКСТ ЦЕПОЧКИ
Проект beLive: миграция аудио V2→V3 (TypeScript · React · WebAudio). Ты в цепочке координатора 007.
Фактов для задачи не хватает — не выдумывай: верни блок «БЛОКЕР: <чего не хватает>».

## FROZEN-ZONE — читать можно и нужно; менять/предлагать правки — НИКОГДА
src/audio/core/AudioEngineV2.ts · src/audio/compat/patchV1.ts · src/bridges/* · src/services/track.orchestrator.ts
Задача требует их тронуть → ответ ровно «СТОП: FROZEN» + список конфликтующих требований. Без интерпретаций.

## ПРАВИЛА ЦЕПОЧКИ
- REGISTRY (team-m/REGISTRY.md) = SSOT. Нет строки в реестре — фронта не существует.
- Рабочая ветка backup/win-V3-finish_2; push — только по GO владельца.
- `_`-поля легальны только внутри V3-собственных файлов (не frozen).
- Вопрос про библиотеку/API/CLI/версию → сначала context7 (resolve-library-id → query-docs), цитируй источник.
- Формат вывода задаёт твоя роль и диспатч координатора, а не этот файл.
- **ПАРАЛЛЕЛИЗМ (ЖЁСТКО):** параллельно запускаются ТОЛЬКО скауты (arch/gateway/sync/explore/general) по разным фронтам — **и ДО 001 (фактура), и ПО ХОДУ** прогона (момент и число решает ведущий: 007/Hub на PC, Мак/Far на Far). Агенты 001/002/005/008/009/Operator — **СТРОГО ПОСЛЕДОВАТЕЛЬНО** (каждый читает вывод предыдущего, контекст накапливается). Параллельный диспатч этих агентов ЗАПРЕЩЁН (даже по разным волнам). Модель: `team-m/AGENT-EXEC-MODEL.md`. ⚠️ Среда: named-spawn мёртв, скауты/console-go блипают → фолбэк на general/инлайн, цепь скаутом не блокировать.

## РЕЕСТР ВЕРДИКТОВ ЦЕПОЧКИ (единственные допустимые наборы)
- 002: ДЕРЖИТ | ТРЕБУЕТ ПАТЧА | ПАДАЕТ
- 009: РЕШЕНО | ОТЛОЖЕНО | ОТКЛОНЕНО
Другие шкалы (PASS/FAIL/GUILTY и т.п.) в этой цепочке запрещены.
