# MICRO-PACK · ВОЛНА A (де-фриз, мёртвый код): 3 файла — СНОС · 2026-09-01

**Автор:** 007 (спека по WAVE-ПЛАНУ 200/двух скаутов + личная верификация фактов) · **Статус:** СПЕКА ГОТОВА — ЖДЁТ 🔴 GO Никиты (диспатч 200 11:30: «жди GO Никиты»)
**HEAD-SSOT:** `1d2746b` · канон: tsc=290 🔴 · vitest=808+0int+0load 🟢 (69) · PARITY PASS · frozen 21×SHA
**OVERRIDE-база:** Никита ~11:00 — «я даю согласие на обработку этой зоны! избавление от Frozen — наш приоритет!»

## Состав (3 файла, риск ~0.1/5)

| # | Файл | Доказательство смерти (007, живое дерево `1d2746b`) |
|---|---|---|
| A-1 | `src/audio/compat/patchV1.ts` | 0 вызовов (grep: только комменты в js/audio-engine.js). Единственный `new AudioEngineV2()` в проекте — живёт здесь, уходит вместе с файлом. **FROZEN-файл — снос по OVERRIDE Никиты** |
| A-2 | `js/audio-engine.js` | Не грузится: index.html не содержит (только audio-facade-v3.js:397); ссылка единственная — коммент «совместимость с v1-stub API» на фасаде (:214, обновить коммент) |
| A-3 | `js/worklets/recorder-processor.js` | 0 ссылок по src/js/index.html; нет динамического импорта; не грузится |

## Правки-спутники
- `js/audio-facade-v3.js:214` — коммент «совместимость с v1-stub API (js/audio-engine.js)» → «v1-stub-эпоха закрыта (де-фриз Волна A, 01.09)»
- `.gitignore`/docs — не трогаем (WAVE-план де-фриза доков — отдельный фронт 003)

## Гейты применения
1. `tsc=290` Δ0 (patchV1 вне ошибок? — проверить ДО: ни одна из 290 ошибок не в patchV1/audio-engine.js/recorder-processor — js/ вне include, patchV1 проверить grep)
2. `vitest=808+0int+0load (69)` Δ0 (0 тестов трекают эти файлы — проверено)
3. `git grep "patchV1\|recorder-processor"` → 0 (кроме архива/доков-истории)
4. Frozen-инвентарь обновить: 21 → 18 файлов (21×SHA → 18×SHA — коммит-месседж зафиксировать); frozen-zones-v2.md:2 «4 зоны · 19 файлов» → «4 зоны · **16 файлов**» (3 из frozen-зоны ушли: patchV1 + bridges? НЕТ — Волна A = patchV1 (frozen) + 2 js (вне frozen-списка). Итог: frozen 19→18, строки пересчитать wc-фактом)
5. PARITY PASS · live-guard/MonitorEngine/фасад — НЕ тронуты

## НЕЛЬЗЯ
- НЕ трогать: live-guard.ts (живой bridge!), MonitorEngine (dual-mode фасад), v2-ветки ENGINE_MODE (dormant failover), AudioEngineV2.ts (Волна D — после B/C, решение Центра)
- НЕ трогать Волну B/C (отдельные паки по GO)
- Каждый bridge из Волны C — только с diff bridge↔wrapper перед сносом (WAVE-ПЛАН)

## Маршрут
🔴 GO Никиты (Волна A) → стресс 002 → Оператор → гейты → коммит 007 → 006 смоук-глазами (прогон пустой — риск 0.1) → Волна B (следующий пак).

— 007 · 01.09 · Волна A на старте, ждёт GO
