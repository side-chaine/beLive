# W2b RECON · re-point remaining delegateSync app callers → V3 (apply spec)

> Apply spec for WAVE2 phase B. V3 REPLACEMENT MAP + exact edits. Post-M3-GO: V2 engine is NEVER born (No-Birth, W1 cut activation), so re-pointing V2Adapter delegateSync to getTransport()/useStemStore is CORRECT — old "silence V2" blocks now correctly target the live V3 engine.

## V3 REPLACEMENT MAP
- seekTo(t) → getTransport()?.seek(t)
- play() → getTransport()?.play()
- pause() → getTransport()?.pause()
- stop() → getTransport()?.stop()
- getCurrentTime → getTransport()?.currentTime
- setCurrentTime(t) → getTransport()?.seek(t)
- setPlaybackRate(r) → getTransport()?.setPlaybackRate(r)
- setStemVolume(id,v) → useStemStore.getState().setStemVolume(id,v)
- setStemMute(id,m) → useStemStore.getState().setStemMute(id,m)
- setInstrumentalVolume(v) → useStemStore.getState().setStemVolume('instrumental',v)
- setVocalsVolume(v) → useStemStore.getState().setStemVolume('vocals',v)
- setStemsEnabled(false) → mute all stems: stemIds.forEach(id => useStemStore.getState().setStemMute(id, true))
- setVocalMixEnabled(v) → useAudioStore.getState().setVocalMixEnabled(v)

## FROZEN (never read, never edit, never name): AudioEngineV2.ts, patchV1.ts, bridges/*, track.orchestrator.ts

## FILES + EDITS

### 1) src/foundation/event-bus/wrappers/rehearsal-trigger-writer.ts
- Remove field `_v2: V2Adapter` and ctor `this._v2 = V2Adapter.getInstance()`.
- getCurrentTime(): return getTransport()?.currentTime ?? 0
- getPlaybackRate(): return getTransport()?.playbackRate ?? 1
- play(): try { getTransport()?.play() } catch {}
- pause(): try { getTransport()?.pause() } catch {}
- seekTo(time): try { getTransport()?.seek(time) } catch {} (x2 lines)
- setStemVolume(id,vol): try { useStemStore.getState().setStemVolume(id,vol) } catch {}
- setPlaybackRate(rate): try { getTransport()?.setPlaybackRate(rate) } catch {}
- isPlaying: getTransport()?.state === 'playing'
- import: `import { V2Adapter } from '../../../audio/engine-v3/V2Adapter'` → `import { getTransport } from '../../../audio/engine-v3'\nimport { useStemStore } from '../../../stem/stem.store'`

### 2) src/components/MonitorMixPanel.tsx
- L410: `try { V2Adapter.getInstance().delegateSync('pause') } catch {}` → `try { getTransport()?.pause() } catch {}`
- L564: `try { V2Adapter.getInstance().delegateSync('seekTo', savedPositionRef.current || 0) } catch {}` → `try { getTransport()?.seek(savedPositionRef.current || 0) } catch {}`
- L566: `try { V2Adapter.getInstance().delegateSync('play') } catch {}` → `try { getTransport()?.play() } catch {}`
- L626: same as L564
- L628: same as L566
- import: delete `import { V2Adapter } from '../audio/engine-v3/V2Adapter';` (getTransport already imported)

### 3) src/hooks/useKeyboardShortcuts.ts
- L61-65: replace `const v2 = V2Adapter.getInstance(); const ct = v2.delegateSync('getCurrentTime')...; const d = v2.getSync<number>('duration')...; if (d>0) v2.delegateSync('seekTo', ...)` with `const transport = getTransport(); const ct = transport?.currentTime ?? 0; const d = transport?.duration ?? 0; if (d>0) transport?.seek(...)`
- L92-95: replace `const v2 = V2Adapter.getInstance(); const isPlaying = v2.getSync<boolean>('isPlaying')...; if (isPlaying) {v2.delegateSync('pause')} else {v2.delegateSync('play')}` with `const transport = getTransport(); const isPlaying = transport?.state === 'playing'; if (isPlaying) {transport?.pause()} else {transport?.play()}`
- import: `import { V2Adapter, getTransport } from '../audio/engine-v3'` → `import { getTransport } from '../audio/engine-v3'`

### 4) src/main.tsx
- L128-152: DELETE the entire MP-18 monkey-patch block (V2Adapter guard).
- L232: `try { V2Adapter.getInstance().delegateSync('stop') } catch {}` → `try { getTransport()?.stop() } catch {}`
- L257: `const offset = (V2Adapter.getInstance().delegateSync('getCurrentTime') as number) ?? 0` → `const offset = getTransport()?.currentTime ?? 0`
- L265: `V2Adapter.getInstance().delegateSync('setStemMute', id, true)` → `useStemStore.getState().setStemMute(id, true)`
- L266: `setStemVolume(id, 0)` → `useStemStore.getState().setStemVolume(id, 0)`
- L268: `setInstrumentalVolume(0)` → `useStemStore.getState().setStemVolume('instrumental', 0)`
- L269: `setVocalsVolume(0)` → `useStemStore.getState().setStemVolume('vocals', 0)`
- L270: `setStemsEnabled(false)` → `stemIds.forEach(id => useStemStore.getState().setStemMute(id, true))`
- L271: `delegateSync('stop')` → `getTransport()?.stop()`
- import: `import { V3StatePublisher, V3DataInterceptor, getTransport, V2Adapter, setStatePublisher, MonitorRouter } from './audio/engine-v3'` → remove `V2Adapter` from it.
- (comments mentioning V2Adapter may stay; only code edits above.)

### 5) src/foundation/event-bus/wrappers/position-sync.ts
- L37: delete `const v2 = V2Adapter.getInstance()`
- L46: `const t = v2.getV2Engine()?.getCurrentTime?.() ?? -1` → `const t = getTransport()?.currentTime ?? -1`
- import: `import { V2Adapter, getTransport } from '../../../audio/engine-v3'` → `import { getTransport } from '../../../audio/engine-v3'`

### 6) src/foundation/reactions/stem-engine-sync.ts  (structural V3-only conversion — DROP V2 fallback branches)
- L3 comment: scrub "V2Adapter" string (optional).
- L13: `import { V2Adapter, getTransport } from '../../audio/engine-v3'` → `import { getTransport } from '../../audio/engine-v3'`
- L63: delete `const v2 = V2Adapter.getInstance()`
- L80-88: replace V2 poll branch with V3-only: `const t3 = getTransport(); if (t3 && isV3Master()) { t3.setPlaybackRate(state.playbackRate) }`
- L90-99: V2 poll → `if (!isV3Master()) { pollTimer = setInterval(() => { if (isV3Master()) { coldSync(); clearInterval(pollTimer); pollTimer=null } }, V2_POLL_INTERVAL) } else { coldSync() }`
- L110-112: delete `function isV2Ready(v2: V2Adapter)` 
- L114: `function coldSync(v2: V2Adapter)` → `function coldSync()`
- L124: `applyAll(v2, current)` → `applyAll(current)`
- L131-132: delete `const v2 = V2Adapter.getInstance()` + `const isV2 = ...`
- L149-151, L161-163, L174-176, L191-197, L218-220: delete the `else if (isV2) { safeDelegate(v2, ...) }` blocks.
- L234: `function applyAll(v2: V2Adapter, state)` → `function applyAll(state)`
- L255-269: delete the `else { V2 path ... safeDelegate(v2, ...) }` block (keep V3 branch).
- L284-290: delete `function safeDelegate(v2, method, ...args)` (it uses V2Adapter).
- NOTE: ensure `useStemStore`/`useAudioStore` imported if used by new code; add import if missing.

## POST-GATE
- grep -rnF "delegateSync" in the 6 files → 0
- grep -rnF "V2Adapter" in the 6 files → 0 (imports dropped; comment strings allowed to remain but should be 0 code refs)
- tsc count = 306, frozen-guard GREEN, SHA256 FROZEN-IDENTICAL
