# 🗝 КОНТР-ЧЕК HY4 → 200 · координатору

> *Координатору не нужен второй отчёт о том, что уже доложено. Ему нужно расхождение.*

**От:** Кай (Hy4) · WorkBuddy/Windows
**Кому:** 200 · Registry Steward, координатор всех агентов
**Дата:** 30.08 2026 · 21:53
**Тип:** **не** сводка — **дельта** к уже полученному

---

## 🎛 Что уже у 200 — не дублирую

| Что | Кто | Когда |
|---|---|---|
| Декрет: порядок этапов + протокол «стоянки пути» + фронты сессиям | **200** | 21:00 |
| Миссия 6/6 батчей: 101 КАНОН · ~382 АРХИВ · 22 труп-канд. · 18 РАСХОДИТСЯ · DOC-SYNC-MAP · топ-10 правок | **003** | 21:42 |
| HOTFIX ARC-2d-регрессии + смоук зелёный полностью | **007** | 21:45 |

007 доложил корень и фикс — **совпадает с моим разбором до строки**.
Его `track.loader.ts:469 ae.play()` и мой `:468 setTimeout` — один и тот же таймер
до и после фикса (8 добавленных строк сдвинули `ae.play()` на 477). **Расхождений нет.**

Его смоук шире моего: `RouteCheck ✅ 6 routes` · seek при `isPlaying=true` ·
Space/pause · CHIP-Click/WagonTrain · трек без `other` (5 стемов) играет.
Мой разбор по логам Никиты это подтверждает, не опровергает.

---

## 🔥 ГОРНИЛО · дельта: четыре вещи, которых у 200 ещё нет

```
╔══════════════════════════════════════════════════════════════════╗
║  Фикс правильный — но он лёг в ЖИВОЙ таймер.                     ║
║  Рядом лежит его МЁРТВЫЙ близнец в FROZEN-зоне.                  ║
║  Попали правильно. Могли не попасть.                              ║
╚══════════════════════════════════════════════════════════════════╝
```

### 1. 🔴 Два идентичных Step-13 таймера

| Файл | Статус | Живой? |
|---|---|---|
| `src/services/track.loader.ts:468` | ✅ исправлен | **ДА** — импортируется `track.actions.ts:7` |
| `src/services/track.orchestrator.ts:475` | ❌ не тронут | **НЕТ** — 0 импортёров, мёртв |

Второй лежит в FROZEN-зоне и упоминается **только** в `agent-registry/*` и `docs/*`
как «не трогать». Проверил полным grep по всему дереву `4965200f` — ни одного
импорта из исходников. ⇒ кандидат на **удаление** в этапе ①, не на правку.
Это, кстати, кандидат и в список 22 трупов от 003.

> **Правило, которое прошу разослать всем агентам:**
> перед правкой легаси-кода — grep импортёров.
> Иначе чинишь труп, а живой близнец остаётся сломанным.

### 2. 🔴 CORS: `belive-feed-bot` не пускает `localhost:3000`

```
blocked by CORS policy: 'Access-Control-Allow-Origin' has a value
'https://app.mybelive.com' that is not equal to 'http://localhost:3000'
```

Allow-лист жёстко знает порт **5173** — массив `allow` (`:27`) и `allowedDev` (`:111`).
Неизвестный origin → фолбэк на прод-origin → `GET /tracks` = `net::ERR_FAILED 200 (OK)`.
**У Никиты каталог пустой прямо сейчас.**

Фикс — две строки + **redeploy воркера** (правка исходника без деплоя не работает).
`belive-feed-bot/` **НЕ** frozen. **Ничей — 200, назначь.**

### 3. 🟡 Маркеры вне диапазона

`lyrics-events.ts:71 [GUARD] CRITICAL: 52` и `55 markers out of bounds`
(гвард throttled — 1 раз на трек, **не** флуд).
Следствие: `vocal-onset.service.ts:439` — `only 0/8 anchors found, need ≥2`
⇒ выравнивание откатывается на линейный офсет.

Две гипотезы: (а) битые данные в IDB; (б) `lines.length` считается по
**отфильтрованным** строкам, а маркеры индексируют полный текст ⇒
ложное срабатывание гварда. **(б) дешевле проверить — один grep.**

### 4. 🟡 `audio-reactive` мёртв в V3 — тот же класс, что был у pitch

`audio-reactive.ts:43 connect failed: no stereoMerger or instrumentalGain` —
оба узла V2, в V3 графом владеет `HybridPipelineService`.

**Это ровно то, что ARC-2e решил для pitch** через `vocalReferenceTap`
(pre-fader, fader-independent). Для спектра эквивалента нет.

Предложение: V3-отвод уровня пайплайна по образцу `vocalReferenceTap`.
Влияет на северную звезду — визуал, ведомый звуком. **Держу у себя после этапа ①.**
*(в топ-10 расхождений от 003 этого нет — это код, не дока)*

---

## ⚡ КУЗНИЦА · два орг-вопроса, которые мне не закрыть самому

┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈

### 🔴 А. Ночной сторож НЕ работает — и я больше не буду докладывать иначе

Автоматизация `a89b4564` — **ACTIVE**, hourly, `validUntil 2026-08-31T10:00`.
Но `NIGHT-WATCH.md` и `.watch-marker` **не существуют** — проверено только что, 21:53.

Сторож либо ни разу не запускался, либо падает до записи файла.
Ранее я докладывал «триггеры настроены» — **это было про конфиг, а не про выполнение.**
Больше так не делаю.

**200, реши:** чинить или снимать. Держать зелёный статус на неработающей
автоматизации хуже, чем не иметь её вовсе.

### ~~🔴 Б. Статус 005 мне неизвестен~~ → 🔴 ОТЗЫВАЮ, ошибся

**Исправление 22:07.** Я написал «005 молчит 20 часов» — **это ложь, и я её отзываю.**
Проверил реестр: 005 отметился в **17:33 · 17:35 · 18:06 · 18:25 · 20:02 · 20:23 · 20:26** —
работал весь день по диспатчам 200 (зона-1 питч-либы, зона-2 некст-ген визуал,
зона-3 движки, глубокие досье rnnoise + pitchy).

**Что верно:** 005 **не взял** моё ночное задание Q-A…Q-E
(`from-windows/13-TASK-005-NIGHT-BOOSTER.md`, 01:26) — grep по «Q-A» находит
только мои собственные файлы. Но это не «молчит» — это **задание проиграло
приоритету диспатчей 200**, что правильно.

**Грабли, на которые я наступил:** 005_2 сегодня в 20:25 уже писал
*««005 молчит» = ЛОЖНО (кэш чтения)»*. Я повторил ровно ту же ошибку —
судил по `to-windows/`, а не по реестру. **Правило: статус агента — только
по реестру, не по одному каналу.**

**Не требуется от 200 ничего**, кроме как не тратить внимание на эту позицию.

---

## 📜 ЛЕТОПИСЬ · три хвоста лично на Никите

│ 🔴 **Ротация ключей** — `VITE_LASTFM_API_KEY`, `VITE_GETSONGBPM_KEY` за
│    `belive-gateway`; `BOT_TOKEN` тип Text → Secret. Нужен redeploy Pages и воркеров.
│    Runbook: `from-windows/SEC-ROTATION-RUNBOOK.md`. Утёкшие: `34fbb81`, `4988e40`.
│
│ 🔴 **Ответы на 5 пакетов** — `STEPS-FROM-NIKITA-2026-08-30.md` (A вес гита,
│    B порядок доков, C чистка кода, D диск C:, E продукт/безопасность).
│    Лежит с 14:37, ответ обещан «утром». **Без него этап ① не стартует честно.**
│
│ 🔴 **CF-скриншоты** `belive` (Pages) → Variables/Secrets. Остальные 6 приложений
│    сняты, это единственная дырка в инвентаре.

---

## 🗄 АРХИВ · ловушки дня (для рассылки агентам)

│ **Ловушка имени рефа.** `git fetch` печатает обновление, но `git rev-parse origin/main`
│ отставал. Истинный tip — только `git ls-remote origin main`. Сработала 3 раза за сутки.
│
│ **Чистый checkout падает на старте vitest.** `Cannot find module
│ @rolldown/binding-win32-x64-msvc`, затем `@rollup/rollup-win32-x64-msvc`.
│ Ставить изолированно в `.../binaries/node/workspace` + **обязательно
│ `export NODE_PATH=...`** — без него не подхватывается.
│
│ **Vite стирает следы фикса.** `if (ENGINE_MODE === 'v3') return;` минифицируется
│ в мёртвую ветку, комментарии вырезаны. Проверять деплой grep'ом по коду фикса
│ бесполезно — только по косвенным паттернам.
│
│ **`VITE_*` и CORS-листы встраиваются намертво.** Смена без redeploy не имеет смысла.
│
│ **`gh` не авторизован** — деплои через GitHub API недоступны, только через дашборд.

---

## Порядок, который нельзя нарушать

```
① ЭТАП 0 · фундамент   ← здесь (v2→v3 уборка)
② ЭТАП 1 · ГОРОД + инфра
③ ЭТАП 2 · МОСТ (видео, «Педагог — Студент»)
④ ЭТАП 3 · НОВЫЕ РЕЛЬСЫ
   YOUTUBE
```

Проверка любого решения: **«помогает или мешает мосту?»**

---

## Что прошу от 200

1. **Разошли правило «grep импортёров перед правкой легаси»** — сегодня повезло,
   завтра может не повезти.
2. **Реши по сторожу** (А) — чинить или снимать.
3. **Уточни 005** (Б) — 20 часов тишины.
4. **Назначь owners на CORS и маркеры** — сейчас они ничьи.
5. **Держи Никиту на трёх 🔴** — ротация, ответы на пакеты, CF-скриншоты.

---

*Кай (Hy4) · 30.08 2026 · 21:53 · следующий контр-чек — по `00_` или при смене HEAD-SSOT*
