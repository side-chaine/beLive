# SYNC-HUB-TO-MAC — 2026-08-29 · City Audit + Pivot (вводный бриф)

_От: Hub (007, Linux). Кому: Mac-007. Прочитай ДО начала работы — репозиторий сменил «живой фронт»._

## 0. Главное (прочитай первым)
Ты, вероятно, работаешь с **устаревшим снимком** репо (как Хай4 до тебя — он сидел на `origin/main` от 2026-07-14). **Живой фронт сейчас = Linux-`main`** (HEAD `78b19f4`, плюс свежие коммиты Hub поверх: `1dfb7b1`, `bb159ba`). **Сделай `git pull` (или rebase на свежий `origin/main`) перед любой работой.** Иначе будешь править несуществующие файлы.

## 1. Что УЖЕ сделано на живом фронте (не повторяй)
- **Легаси удалён.** `src/legacy/` — нет. `V2ResurrectionDetector` — нет. `V2AudioCage` — нет. Дока-план «Финальная срезка Легаси» (ADR-0016) **устарел → НЕ ИСПОЛНЯТЬ** (помечено в `docs/modernization/02-PROGRAM-ROADMAP.md` как `⛔ НЕ ИСПОЛНЯТЬ`).
- **V3 в проде** (тег `v2-final-production`, deploy пройден 28.08).
- Твои городские коммиты 28.08 (city-prep / REPO-TO-CITY plan / city design) **уже в main** — не дублируй.

## 2. Новая метафора и задача: CITY AUDIT (Городская инвентаризация)
Босс утвердил метафору **«город»**: репо = город, код = здания/коммуникации, мусор = dead-code/старые связи. Цель — **аудит города перед заселением**, а не прямая перепись легаси.

Бриф скаутов: **`docs/modernization/03-CITY-AUDIT-BRIEF.md`** (прочитай целиком). 5 скаутов (Хаб координирует 001/002/005/009):
- Scout-4 SECRETS — **ВЫПОЛНЕН** (отчёт `docs/audit/04-SECRETS.md`, ключевой вывод: MVSEP-ключ утёк публично, ротируется).
- Scout-2 COMMUNICATIONS, Scout-1 CADASTRE, Scout-3 DEAD-CODE, Scout-5 DOCS — **очередь**.

### ТВОИ 2 задачи (INBOX `mac-007`):
1. **`security-audit-pivot`** (GO) — помоги Хабу в City Audit с точки зрения безопасности: где ещё plaintext-секреты / хардкод / debug-эндпоинты в живом фронте (вне уже найденного MVSEP). Доклад в `docs/audit/` по шаблону Scout-4.
2. **`city-description`** (GO) — опиши город для Genplan'а: районы (модули), артерии (потоки данных), границы (публичные API), снос (что можно удалить). Положи в `docs/modernization/` (формат — как договоришься с Хабом, но читабельно для Босса).

## 3. ЖЕЛЕЗНОЕ ПРАВИЛО для всех скаутов (rule #0)
**СКАУТЫ ТОЛЬКО ЧИТАЮТ.** Никаких правок кода. Хай4 однажды случайно `git rm src/legacy` снёс весь `src/` (591 файл) — восстановлено, но урок: разведка = read-only. Правишь ТОЛЬКО через Hub→Оператор по канону.

## 4. FROZEN ZONE (не трогать ни при каких обстоятельствах)
`src/audio/core/AudioEngineV2.ts`, `src/audio/compat/patchV1.ts`, `src/bridges/*`, `src/services/track.orchestrator.ts`, приватные поля `_`. **Решение Босса:** `AudioEngineV2` НЕ удаляем (это «электростанция»), мигрируем его 11 потребителей на V3-контракт (ADR-0004 step 2). Сами frozen-файлы нетронуты. Если любой агент упомянет frozen-файл даже в контексте — Хаб СТОПает цепочку и спрашивает Босса (OVERRIDE).

## 5. MVSEP-ключ (контекст безопасности)
- Босс: «удали везде, новый ключ бесплатный». **Живые файлы очищены:** `belive-mvsep/DEPLOY.md:21` → `${MVSEP_API_KEY}` (коммит `1dfb7b1`); `.env.production.local` (gitignored) захарден. В рабочем дереве ключа нет (grep = 0).
- **История git:** ключ остаётся в 2 старых коммитах (`4d59e67`, `1e3f9cb`). Локальная перепись истории **заблокирована** (нет `git-filter-repo`, сеть закрыта, `filter-branch` в этом окружении не переписывает блобы). Решение: Босс ротирует ключ → историческая копия нейтральна. Полный scrub истории возможен только с `git filter-repo` + `gh auth login` (Windows) + force-push — пока OPEN.
- **Для тебя:** просто `git pull`, чтобы получить чистый `DEPLOY.md`. Новый ключ Босс возьмёт сам.

## 6. Как начать
1. `git pull` / rebase на свежий `origin/main` (HEAD `bb159ba`).
2. Прочитай `team-m/REGISTRY.md` (новая запись `2026-08-29 · MVSEP KEY PURGE`) + `docs/modernization/03-CITY-AUDIT-BRIEF.md` + `docs/audit/04-SECRETS.md`.
3. Стартуй `security-audit-pivot` и `city-description` (read-only!); клади отчёты в `docs/audit/` и `docs/modernization/`, статус — в `team-m/INBOX.md` (строки `mac-007 | security-audit-pivot` / `city-description`).
4. По готовности — положи отчёт в `team-m/reports/mac-007/` и жди GO Хаба на применение (через Оператора, по канону tsc/vitest).

_Канон на живом фронте (для справки): tsc=296, vitest=761+0int+0load, PARITY PASS. Frozen-guard GREEN._
