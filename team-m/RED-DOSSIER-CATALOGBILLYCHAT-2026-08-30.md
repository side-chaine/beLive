# 🔴-ДОСЬЕ: CatalogBillyChat — kill или shelved? · 707 для Никиты · 2026-08-30

**Одна страница. Решение — одно слово: KILL / SHELVED.**

## Что это
Полный самостоятельный AI-чат Билли **внутри каталога**: `src/catalog/components/CatalogBillyChat.tsx`
(151 строка). Собственный стриминг ответов, quick-questions («Как загрузить трек?», «Что такое ZIP?»,
«С чего начать?»), выбор «навыка» через `skill-registry`, рендер сообщений через BillyMessageRenderer.

## Жив ли сейчас
**НЕТ — не смонтирован ни в одном экране** (grep импортёров = 0). Это «спящая фича»:
компонент готов, но никто его не показывает пользователю.

## Каскад (что уходит вместе с ним)
| Файл | Строк | Кто импортирует |
|---|---|---|
| `CatalogBillyChat.tsx` | 151 | 0 (нигде не смонтирован) |
| `src/billy/skill-registry.ts` | 20 | только CatalogBillyChat |
| `src/billy/BillyMessageRenderer.tsx` | 81 | только CatalogBillyChat |
| `src/billy/BillyMessageRenderer.css` | ~50 | только рендерер |

**Итого каскад: 4 файла, ~300 строк.** В манифесте 301 этих трёх не было — нашёл стресс-002 (прогон #1).

## Кто звал фичу (история)
Идея жива в **доках 008**: «Билли в каталоге» — помощник прямо у полок с треками: спрашиваешь
«как загрузить трек?» — он отвечает на месте, не уходя из каталога. Также связана с визией
«каталог = площадь города» (кадастр: PORT) и будущей правой рейкой чата (VIS-7/BLB-13-14 —
переезд чата из дока в правую часть уже в рекомендациях 🔴-списка).

## Что теряем / что чистим
| Вариант | Теряем | Чистим | Риск |
|---|---|---|---|
| **KILL** (снос 4 файлов) | Идею «Билли в каталоге» — в коде её не останется совсем; возрождение = с нуля | ~300 строк мёртвого кода | Идея 008 остаётся только в доках |
| **SHELVED** (архив в docs/operations/archive/ + указатель) | Ничего: код читаем в git-истории | 0 строк из дерева | Место под «спящую» фичу — вернётся при VIS-7 (правая рейка) |

## Рекомендация 707
**SHELVED** — по паттерну «мост + указатель» (STORAGE-POLICY §5): компонент уже написан, идея
подтверждена документами 008, а VIS-7 (переезд чата в правую рейку) — рекомендация A в твоём
списке 🔴. Когда дойдём до рейки — этот код может стать заготовкой. Снос — только если хочешь
радикальный «ноль мусора» прямо сейчас.

**Мой нюанс:** в отличие от мёртвых файлов пакета C (0 импортёров и никогда не было), у этого —
**история и судьба**: он писался под идею, которая ещё в планах. Это не мусор, это «спящий этаж».

— 707 · картограф · по диспатчу 200 от 19:20 п.3
