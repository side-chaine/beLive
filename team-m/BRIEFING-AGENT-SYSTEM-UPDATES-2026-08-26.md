# BRIEFING-AGENT-SYSTEM-UPDATES — для 007_Винда (Hub) · 2026-08-26
> От: 007_Мак (Far Light). Назначение: новые пункты в систему агентов — прими и работай по той же схеме (скауты параллельно, основные последовательно, полный контекст на входе).

## Новые пункты (внесены в `team-m/TRIGGER-PROTOCOL.md`)
1. **§8 Последовательность основных агентов + параллельные скауты.**
   - Основные агенты цепи (`001→002→001→009`, `005→002→009`, `GO_009=[009]`, `GO_002=[002]`) — **СТРОГО ПОСЛЕДОВАТЕЛЬНО**: каждый видит вывод предыдущего + весь контекст. Задача прорабатывается и передаётся логично.
   - **СКАУТЫ** (explore/general, read-only разведка) — **ПАРАЛЛЕЛЬНО**, в т.ч. **ДО** запуска 001 (подготовить фактуру) и **ПО ХОДУ** (по решению ведущего). Скауты НЕ решают, только собирают/подсвечивают факты (file:line, grep/git-выводы, инвентари).
   - Ведущий агент (Мак/Far Light) решает момент и число скаутов.
2. **§9 Полный контекст на блюдечке (принцип Босса).** Каждому агенту — ВСЁ сразу: выводы всех предыдущих агентов (полностью), схемы «кто кого слушает» (event/listener map: `ASSISTANT_RESPONSE_COMPLETED` → `CharacterSoundManager` + будущий `avatar.store`; цепь `aiHub.sendMessage.onDone` → звук+аватар), структура волн W1–W5, grep-гейты, точные file:line, зафиксированные решения (REGISTRY/SYNC/вердикты 009/flip-коммит 2395c1e). Цель — **ноль re-derivation**, агент бьёт по существу.

## Канонические цепи (registry §6, не менялись)
- `GO_001` = `[001 → 002 → 001 → 009]`
- `GO_005` = `[005 → 002 → 009]`
- `GO_009` = standalone `[009]`
- `GO_002` = standalone `[002]`

## Применяй так же
Скауты параллельно (до 001 и по ходу) → основные агенты последовательно → каждому на входе ПОЛНЫЙ контекст. Far Light уже прогнал аудит миграции по этой схеме (001 recon → 3 скаута параллельно → 002 stress → 001 re-recon → 009 verdict). Делай симметрично на своей стороне.
