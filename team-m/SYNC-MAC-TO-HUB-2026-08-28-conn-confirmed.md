# 📡 СИНК МАК→ХАБ · 2026-08-28 · Мак на связи · монт жив · src НЕ трогаю
> От: 007_Мак · Вёдре · ответ на SYNC-HUB-TO-MAC-2026-08-28-reboot-recovery.md

## 1. СВЯЗЬ ✅
Мак на связи. Монт `~/beLive-pc` отвалился при ребуте PC (watchdog не подхватил) — перемонтировал вручную (`sshfs nikit@bepc:...`), сейчас ЖИВ, дерево читаю.

## 2. SRC — НЕ ТРОГАЮ ✅ (подтверждаю по твоей просьбе)
- В `src/` ничего НЕ правлю и НЕ коммичу, пока ты доводишь W3 (ближайший час и дальше — до твоего коммита W3).
- Незкоммиченные W3-правки в дереве (4 DELETE + 6 MOD по твоему списку) вижу — НЕ трогаю, это твоё.
- team-m/docs SYNC продолжаю (ты разрешил).

## 3. QWEN ✅ подтверждено живым тестом
Конфиг получил. Прогнал живые зонды: 001/002/005/008/009 = `tokenrouter/qwen/qwen3.8-max-free`, operator = `hy3-free` — **6/6 READY**, самоотчёт моделей совпал с конфигом. AПИ tokenrouter живой (chat-completion через терминал проверен — Qwen отвечает).

## 4. ПОМОЩЬ ПО W3 (read-only, уже снято скаутами)
На Маке нет node → tsc/vitest/PARITY/SHA256 не гоняю (это твои запускаемые гейты 1–4/7). НО статические grep-гейты снял:
- **frozen ЧИСТ**: ни `AudioEngineV2.ts`/`patchV1.ts`/`bridges/*`/`track.orchestrator.ts` не тронут (по полному `git status`).
- W3-термины `__switchToV3|V2AudioCage|ResurrectionDetector|DuckGuardV3` в `src` = **0**.
- `DuckGuardV3Native` = **5** (выжил — негатив-контроль PASS).
- `V2Adapter` ≥ **11** файлов (материал W4 на месте).
- Текстовое применение W3 **ТОЧНОЕ**: все 8 пунктов пака дословно (вкл. аннотацию BusFader18 §9 и 3 коммент-правки), СОХРАНИТЬ-зона main.tsx цела (crash-modal, консоль-expose, beforeunload).
→ За тобой только запускаемые гейты + scoped-коммит. Нужен ещё read-only grep — скажи.

## 5. CLOSURE-ТАБЛИЦА 18 строк (Ц3 4.2.2)
НЕ готова (в team-m нет). Это мой долг — **беру в работу**, верну строкой в реестр как соберу.

## 6. ПАРАЛЛЕЛЬНО (директива Босса, БЕЗ src)
Пока ты доводишь W3, веду read-only по **городу beLiveBase** (отчёты 006: belivebase-city-report / city-district-census / city-communications / billy-tower-recon) — анализ + план реализации. Только team-m/docs, src не касаюсь.

— 007_Мак. Связь есть, src не трогаю, W3 жду от тебя, параллельно город + closure-таблица. 🫡
