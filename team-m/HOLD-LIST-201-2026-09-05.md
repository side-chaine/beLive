# HOLD-LIST v0.1 · beLive · автор **201 (Кай, Windows)** · 2026-09-05 14:2x

**Назначение:** D-4-шаг-1. Без него флип `verify:reach` (G-1) из `warn` в `fail` обрывает деплой
первым же пушем — это ровно причина «НЕТ» от CEO_1 05.09 10:14, и я принял её с последствием на себя.

**HEAD-SSOT:** `6ce938ae1`. **Все цифры — с ЧИСТОГО дерева** (`git worktree add C:/Users/nikit/wt201-hold 6ce938ae1`,
дерево чистое по построению). Моя рабочая ветка `w2-legacy-removal` (`9cc70245`) расходится с SSOT на
**208 файлов / +12 918 −7 874** ⇒ мерить на ней канон нельзя (правило CEO_1 «канон = чистое дерево»).
**Прогон:** `node scripts/verify-reach.mjs` → **121 unreachable src/ file(s)** — сошлось с каноном бит-в-бит.
Сырой список: `tmp/reach-201-6ce938ae1.txt`.

---

## 0. ГЛАВНАЯ ЦИФРА ДНЯ

🔴🔴 **G-1 (`verify:reach`) имеет структурную ложно-положительную долю 57 % — 69 файлов из 121 это тесты.**

Граф G-1 идёт от корней `index.html` + `src/main.tsx`. Тесты **не импортируются приложением по построению** —
их запускает vitest (68 файлов / 812 тестов). Следовательно:

> **G-1 нельзя флипать в `fail`, пока он не исключает тесты. Иначе 57 % его вывода — вечный красный,
> и деплой ляжет на первом же пуше не из-за мусора, а из-за устройства самого гейта.**

Это и есть измеренная причина, по которой «все пять гейтов в fail до D-4» было бы ошибкой. Моя утренняя
оценка 08:45 («~37 % лжи») была заниженной — **реальность 57 %**, и она не «ошибка гейта», а его свойство.
Лечение — не правка гейта, а **hold-лист + исключение класса H-1 до флипа**.

---

## 1. ФОРМАТ ЗАПИСИ

Запись = **правило (glob) + именованные исключения**, а не 121 строка вручную.
Поля: `glob` · `class` · `reason` · `owner` · `until`.
Обоснование формы: носитель G-7 учит «истина в данных, одна строка = один ассерт» — 121 рукописная
строка проживёт меньше недели, 6 правил переживут квартал.

---

## 2. ШЕСТЬ КЛАССОВ (сумма = 121, сходится)

### H-1 · ТЕСТЫ — 70 файлов (57 %) · `hold` **навсегда**

| поле | значение |
|---|---|
| `glob` | `src/**/__tests__/**` · `src/**/*.test.ts` · `src/**/*.test.tsx` · `src/test/setup.ts` |
| `reason` | запускает vitest; граф G-1 от корней приложения не достигает тестов **по построению** |
| `owner` | гейт (не долг команды) |
| `until` | **никогда** — это свойство графа, не мусор |

🔴 **Следствие для D-4:** до флипа `verify:reach` в `fail` в скрипт вносится исключение класса H-1.
Ожидаемый остаток после исключения: **121 − 70 = 51 файл**, из которых реальный мусор — предмет triage 007.

---

### H-2 · ДИАГНОСТИКА ДВИЖКА — 18 файлов (14 %) · `DORMANT`

| поле | значение |
|---|---|
| `glob` | `src/audio/engine-v3/diagnostics/**` |
| `reason` | ручные прогоны из консоли и из Node (`.mjs` / `.console.js` / `.browser.js`), в графе приложения не участвуют; версионные хвосты v6.32.x и 007-верификаторы; внутри — `CaptureWorklet.ts` |
| `owner` | **007** |
| `until` | «пока живы ручные прогоны» — ⚠️ **с открытием консольной эпохи (006 водит Chrome через мост) эти файлы становятся первыми кандидатами на снос**: то, что теперь делается через `window.audioEngine` вживую, больше не нуждается в 18 статических прогонах |

---

### H-3 · ОСТРОВ БИЛЛИ — 6 файлов (4 %) · `DORMANT`

| поле | значение |
|---|---|
| `glob` | `src/billy/**` (5: `BillyMessageRenderer.tsx`, `context-builder.ts`, `skill-registry.ts`, `skills/scout.skill.ts`, `types.ts`) · `src/catalog/components/CatalogBillyChat.tsx` |
| `reason` | ИИ-ассистент выключен до выпуска; 006 подтвердил вживую: Билли за `isTrusted`-гейтом, окно «AI Оператор» скрыто в DOM (`ai-chat-window hidden`), анти-петля 3/3 |
| `owner` | **Никита** |
| `until` | **«выпуск с ИИ-ассистентом»** |

---

### H-4 · ПАРКОВКА КОММЕНТАРИЕМ — 4 файла (3 %) · `DORMANT` 🔴 **находка этого паса**

| поле | значение |
|---|---|
| `glob` | `src/components/VolumeControls.tsx` · `src/components/ControlPanel.tsx` · `src/components/AIChatPanel.tsx` · `src/components/BpmControl.tsx` |
| `reason` | записи в реестре динамических импортов **`src/deck/modules.ts:10-11, :22-23, :56-57` закомментированы** ⇒ файлы целы, провода выключены. Это **класс ⑲ «перебитый провод», но в графе импортов**: строка жива, правило мёртво. `BpmControl` — транзит: его единственный импортёр — сам `VolumeControls.tsx:6` |
| `owner` | **007** (установить, кто комментировал — по `git log -S` на `src/deck/modules.ts`) |
| `until` | «обратная подмотка `modules.ts`, либо явное решение Никиты — снос» |

🔴🔴 **ПОБОЧНЫЙ УРОК — МОЯ СОБСТВЕННАЯ ОШИБКА, НАЗЫВАЮ САМ.**
Моя КОМ-карта считала `VolumeControls.tsx` **живым** (5 чтений `window.audioEngine`) и `BpmControl` живым (1 чтение).
Файлы недостижимы ⇒ **эти 6 чтений мертвы, моя цифра «живых 111» завышена минимум на 6.**
Причина ровно та, которую я сам сформулировал для G-1 04.09 08:45: **«недостижимо по графу» ≠ «мёртво»,
но и «читает глобал» ≠ «живо».** Обе шкалы врут порознь; правда — только в их **сверке**.
⇒ Это готовый аргумент под пару `kom-marker-file` (v0.2 G-7): маркер ↔ файл достижим по графу.

---

### H-5 · ЛЕНТА (FEED) — 3 файла (2 %) · 🔴 **НЕ hold — развилка Никиты**

| поле | значение |
|---|---|
| `glob` | `src/catalog/feed/EventList.tsx` · `src/catalog/feed/HeroStack.tsx` · `src/catalog/feed/TrackScroll.tsx` |
| `reason` | воркер **`belive-feed-bot` живой в CF и исправно работает**, а лента в приложении не подключена к графу ⇒ стык «живой бэк ↔ мёртвый фронт» |
| `owner` | **Никита** |
| `until` | развилка: **подключить ленту / снести фронт ленты / оставить как есть**. Hold-статуса не даю — это решение, не отсрочка |

---

### H-6 · ОСТАТОК — 20 файлов (16 %) · **triage 007, статус не присвоен**

По декрету ⑥ («не маркировать неизвестное») статус этим файлам **не присваиваю** — это вход на разбор 007:

```
src/audio/pitch/pitch-visual-bridge.ts
src/character/layer2-report-emitter.ts
src/components/NowPlaying.tsx
src/components/TrackInfo.tsx
src/components/onboarding/OnboardingStep.tsx
src/exercises/components/RecipeCardPopover.tsx
src/exercises/index.ts
src/foundation/event-bus/wrappers/block-editor-events.ts
src/foundation/event-bus/wrappers/index.ts
src/foundation/event-bus/wrappers/mode-switch-events.ts
src/foundation/event-bus/wrappers/rehearsal-trigger-writer.ts
src/js/main.js
src/services/lrc-parser.service.ts
src/services/monitor.state.ts
src/slot-matrix/compute-slot-canvas.ts
src/slot-matrix/compute-slot-groups.ts
src/stem/index.ts
src/stores/pitch.store.ts
src/triggers/index.ts
src/utils/storage.ts
```

⚪ **Одно наблюдение без статуса:** `src/js/main.js` — V1-точка входа, **отсутствует в `index.html`**
(проверено) ⇒ кандидат №1 в снос по программе V2-похорон. Присваивать маркер не мне — это зона 007.

---

## 3. ЧТО ЭТО МЕНЯЕТ В КАЛЕНДАРЕ D-4

1. **Флип `verify:reach` в `fail` — только после исключения H-1.** Порядок: исключение тестов →
   hold-лист в репо → triage H-6 (007) → флип. Иначе 57 % ложной зелени превращаются в 57 % ложной красноты.
2. **`verify:ci` завтра = events + parity + inert** (3 гейта, компромисс CEO_1) — **не меняется**, моих
   цифр на откат нет. `reach` + `docvis` → `verify:ci-full` по календарю D-4, с этим листом на руках.
3. **Новый вход для G-7 v0.2:** пара `kom-marker-file` получила **первую живую жертву** (H-4, 6 ложных
   чтений в моей же карте) ⇒ правило §6 CEO_1 выполнено: не гипотеза, а пойманная руками невидимка.

---

## 4. ЧЕСТНОЕ «НЕ ЗНАЮ»

- Не проверял, **существует ли альтернативный загрузчик** помимо `index.html` + `main.tsx` (например,
  Service Worker или второй HTML-вход) — если он есть, часть H-6 жива, и G-1 врёт и в эту сторону.
- Не устанавливал автора комментариев в `src/deck/modules.ts` (нужен `git log -S`, зона 007).
- H-6 не классифицирован **сознательно**: присвоить статус незнакомому файлу = нарушить декрет ⑥.

---

— **201 · Кай · Windows · 05.09 14:2x** · 121 = 70 + 18 + 6 + 4 + 3 + 20, сходится · главная цифра дня: **57 % лжи в G-1 — структурно, не случайно** 🧾🪤㊱
