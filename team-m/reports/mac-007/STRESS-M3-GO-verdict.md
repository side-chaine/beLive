# STRESS-M3-GO · ВЕРДИКТ 002 Стресс-тестера
**Жертва:** `team-m/reports/mac-007/FINAL-M3-GO-CHECKLIST-draft.md` (001 Со-Архитектор) · 2026-08-25
**Метод:** каждое проверяемое утверждение сверено с рабочим деревом (grep/sed/git-log, только чтение). Frozen не тронут.

---

## §1. АТАКА НА 3 ТОЧКИ

### Точка 1. Дрейф канона 313/769 → 306/770 — **001 ПРАВ, дрейф реален, но он же оставил второй источник истины протухшим**
Аргументы:
- REGISTRY.md:140 фиксирует дословно: «tsc **306** (улучшен на 7 vs базовых 313), vitest **770 passed**, verify:ci PARITY PASS» + mic-race `82e1c76` в хвосте той же записи (tsc 306). Хронология родовитая: 769→770 = +1 тест B-SLICE (REGISTRY:129), 313→306 = подчистка доканонных ошибок Оператором. Никаких более новых цифр в team-m/ и git-логе нет — **306/770 = последний факт, подтверждено**.
- PLAN-v3.3-CANONICAL.md §2 (стр.31) при этом продолжает говорить «tsc 313, vitest 769 (база с 25.08)». R1 описан верно, но митигация неполна: Этап 0 «переприсваивает базовое число замером» — а в самом документе-каноне останется 313/769 до ручной правки. Любой следующий агент, открывший PLAN, воспроизведёт дрейф.
Вердикт: **утверждение доказано** (я пытался опровергнуть цифру — не смог). Усиление см. §3-У1.

### Точка 2. «Флип ×3» против 12 ридеров `?? 'v2'` — **001 ПРАВ по числам, НО обоснование варианта (а) содержит фактическую ошибку**
Аргументы:
- Пересчёт независимо: ровно **12 сайтов `?? 'v2'` в 8 файлах**, список A13 совпадает символ в символ (App:93; ControlDeck:362; MixerPanel:139,:309; TakesPanel:499,:555; TakesControlStrip:174,:571; takes.recorder:70; VolumeControls:91,:105; recording.store:48). Это ВСЕ использования VITE_ENGINE в src/ (+1 комментарий main.tsx:301). `.env:5`=`'v3'`, `.env.example:23`=`v2` — тоже точно.
- Контра: «×3» фактически «×2» — `.env` уже v3, менять нечего; коммит из одной правки example + одного дефолта. Критерий «ровно 3 сайта» считает несуществующую работу.
- СЕРЬЁЗНЕЕ: тезис варианта (а) «env всегда задан → фоллбек мёртвый» **неверен в тестовом окружении**: `vitest.config.ts` не определяет VITE_ENGINE, `src/test/setup.ts` не стабит env → под vitest `import.meta.env.VITE_ENGINE === undefined` и фолбэк `'v2'` — ЕДИНСТВЕННОЕ живое значение на всех 12 сайтах. Сегодня это безобидно (0 тестов импортирует любой из 8 файлов — проверено), но вариант (а) навсегда легализует сплит test-env('v2') vs prod('v3') на этих ветках.
- Бонус-находка к A5: проверочная команда `grep -rn "__v3Active" src/ | grep "="` даёт ~15 присвоений в BusFader18.test.ts + 3 в stem-engine-sync.test.ts. Формулировка «присвоения только в main.tsx:135/:157» буквально ложна без `--exclude-dir=__tests__`.
Вердикт: **числа безупречны, риск R2 настоящий и заранее прикрыт вариантами а/б** — но вариант (а) требует допусловия (см. §3-У4), иначе 002 (я) завернул бы именно его.

### Точка 3. Mic-методология ждёт фасад-аксессоры, которых B-SLICE-FINAL не создал — **001 ПРАВ, подтверждено железно**
Аргументы:
- Фасад `js/audio-facade-v3.js`: членов `audioContext`/`isPlaying` НЕ существует вовсе (комментарий :5 обещает — не реализовано); `setInstrumentalVolume/setVocalsVolume` — no-op (:37) И вдобавок заблокированы upstream безусловно (`main.tsx:147–150`, «master-volume blocked»). Дифф фасада в B-SLICE-FINAL (`287cf5d`, +31 строка) = только `awaitStemReady`+`getStemAudioBuffer`; E6 снял H4.1-гард с volume-членов, гард остался лишь на setStemVolume/setStemsEnabled (main.tsx:321–322). Ревивала нет — 001 описал дизайн точно.
- Методология требует обратного: §0-P3 «фасад оживлён (audioContext/isPlaying/setVocalsVolume/setInstrumentalVolume живые)», §7 identity-тест `window.audioEngine.audioContext === __belive.pipeline.ctx`. На HEAD левая часть = `undefined` → тест математически невыполним. P5 («легит UI-записи проходят») тоже выполняется лишь наполовину: гарды сняты, но под ними всё равно no-op.
- При этом страх 001, что сессия развалится, преувеличен: П1/П2/П3 реально идут мимо фасада — duckProgram через pipeline.setBusVolume/muteStem (`takes.duck.ts:14–50` ✓), автопауза уже отвязана от ae.isPlaying (`useTakesPlayback.ts:99` через takes.time.isPlaying(), TAKES-AUDIO). Ломаются только P3-предусловие и §7-чеки.
Вердикт: **R3 корректен и хорошо откалиброван** (правка методологии, не добавление аксессоров — согласен: оживление сеттеров вскрыло бы заново writer-conflict поверхность, которую B-SLICE-FINAL только что закрыл single-writer `_applyEffectiveGain`). Готовый текст правки — §3-У2.

---

## §2. ПРОЧИЕ ДЫРЫ

**Д-1 🔴 ГЛАВНАЯ: dist-инвентарь A10 упрётся в протухшую сборку, порядок Этапа 2 это гарантирует.**
Текущий `dist/` собран **24.08 11:56** — ДО всего Operator-поезда 25.08 (`287cf5d` 18:11, `b13de92/dbb46bf/ea13b6b/b0b9b8e/82e1c76`). Positive-контроли A10 (`getStemMeterLevel`, `'loopcompleted'`, `audioglitch`) существовали уже в старой сборке → против протухшего dist они дадут **false-PASS на вчерашнем коде**, а новые поверхности паков 25.08 (`awaitStemReady`, `getProgramCaptureStream`, `'master-volume blocked'`) контролями вообще не покрыты. M3-GO-VERIFY шаг 10 инвентаризирует существующее дерево; единственный билд в плане (dual-tag pre-M3, A14) идёт ПОЗЖЕ dist-grep в порядке Этапа 2. Как написано — подписанный GO может опереться на неподтверждённый артефакт. **FIX: prepend «npm run build на HEAD» перед шагом 0 инвентаря + расширить positive-контроли маркерами паков 25.08.**

**Д-2 🟡 `rg` отсутствует на Mac-машине** (`zsh: command not found: rg` — проверено). Все команды чек-листа и M3-GO-VERIFY шаг 10 написаны под rg (A10/A12/A18) — как написаны, они падают или исполняются молча не тем инструментом. FIX: grep-эквиваленты (`grep -rEl`) либо установка ripgrep + инструментальная самопроверка в Этап 0.

**Д-3 🟡 Финальный аудит Этапа 4 невыполним буквально.** Критерий `AudioEngineV2|patchV1|track.orchestrator|V2Adapter → 0 по src/` ломается о: (а) комментарии-упоминания минимум в 11 файлах — список §2.0 неполон (пропущены `IV2PublicContract.ts:3,:11` и `MixerPanel.tsx:6`, оба комментарии — вывод «ноль runtime-импортёров» при этом ВЕРЕН, проверил импорты); (б) `V2Adapter.ts` не внесён ни в одну волну удаления Волн 1–5, хотя аудит требует 0 его упоминаний. FIX: волна удаления V2Adapter после Волны 2 (последний вызыватель умирает там), ИЛИ критерий «0 runtime-imports», ИЛИ явный retain-класс.

**Д-4 ⚪ нит: «музейный тест» BusFader18 §9.** H4.1-describe — контракт-зеркало, self-contained (не импортирует main.tsx — прочитано :358–400). После Волны 3 (снос ae-guard/V2Interceptor) тест продолжит зелёно проверять поведение, которого в проде больше нет. Гейты не сломаются (потому и нит), но «регресс-нетто BusFader18 не трогаем» оставляет ложную уверенность. FIX: Волна 3 п.6 тем же паком аннотирует/урезает §9.

**Д-5 ⚪ ниты точности:** «~20 файлов» Волны 2 — фактических caller-файлов вне demolition-списков 13 (инвентарь ПОЛОН — независимый `delegateSync`-обход дал ровно те же 13 + самоуничтожающиеся cage/resurrection/adapter/legacy); «61 упоминание» предиката — мой счёт 47 без тестов / 68 с тестами, цифра 61 между ними, метод счёта не указан; мелкий дрейф якорей App.tsx:100 (факт :97/:99), MonitorRouter ":254–261" (факт 251–258), lyrics-events ":71,:75" (факт ≈69–77); кавычки `.env` `'v3'` vs unquoted `v2` в example. Ничего не меняет по сути, но ⛔-исполнитель по якорям спотыкается — правило «якорь сверять контекстом ±5 строк».

---

## §3. ✅ УСИЛЕНИЯ / АКТИВЫ

- **У1 (R1/A16):** правило синхронизации SSOT — канон-число обновляется ОДНОВРЕМЕННО в REGISTRY:140 и в PLAN §2 (один MICRO-PACK на две строки). Сейчас PLAN = второй, устаревший источник истины; без У1 дрейф 313/769 воспроизведётся каждым новым читателем.
- **У2 (R3):** готовый текст правки MIC-SESSION (закрывает за 5 минут): P3 → «блок-канал подтверждён: консольный `delegateSync('setInstrumentalVolume',0)` при v3 → `[delegateSync] master-volume blocked`, звук не меняется; фасад-аксессоры сознательно НЕ оживлены»; §7 identity-тест → `typeof window.__belive?.pipeline?.ctx === 'object' && window.__belive.ctx === window.__belive.pipeline.ctx`; P5 → «H4.1 на volume снят (E6), остаточный гард setStemVolume/setStemsEnabled сохранён».
- **У3 (A10):** после пересборки добавить positive-контроли свежей поверхности: `awaitStemReady` (B-SLICE VOC), `getProgramCaptureStream` (program-capture), `'master-volume blocked'` (E5), memoize-маркер mic-race — инвентарь начинает доказывать СЕГОДНЯШНИЙ код, а не легаси-артефакт.
- **У4 (A13):** третий вариант (в): оставить 12 ридеров как есть (вариант а) + одна строка stub `import.meta.env.VITE_ENGINE ??= 'v3'` в `src/test/setup.ts` — убивает test/prod сплит нулевой ценой и делает критерий greppable: `grep -rn "?? 'v2'" src --exclude-dir=__tests__ → 0`.
- **У5 (подтверждение прочности):** leaves-first граф Волн 1→4 перепроверен независимо: AudioEngineV2 ← patchV1:6 ← featureFlag:6 ← App.tsx:47 — цепочка полная, других runtime-импортёров нет; 6 потребителей track.actions совпали все шесть; GUARD-паритет lyrics-events ↔ frozen bridge текстуально идентичен; R10 (`__restoreV2Engine → 0`) — отличный анти-no-op контроль; B8 с целенаправленным zombie-тестом внутри 5с-окна — правильно, что вынесено явно после пилота.
- **У6 (A12):** инвентарь V2-recovery полный: tryActivateV2/restore/birth-counter/autoplay-timer — все четыре подтверждены на диске, вызовы tryActivateV2 действительно единственные (App.tsx:95).

---

## §4. ИТОГ

**ВЕРДИКТ: SHIP С ПАТЧАМИ (не REWORK).** Чек-лист готов к защите 001. Все три атаковые точки выдержали проверку против кода: числа 306/770, 12/8 ридеров, отсутствие фасад-аксессоров — доказаны независимо. Архитектура legacy-cut (leaves-first, канон-гейты, SHA256-инвентарь, R6/R9-ограничители) звукая.

Обязательно до исполнения (блокеры процедуры, не архитектуры):
1. **Д-1**: precondition «fresh build на HEAD» перед dist-шагом 0 + контроли паков 25.08;
2. **Д-2**: инструментальный фикс rg→grep/установка.

Желательно: У1 (SSOT канона), У2 (текст правки методологии), Д-3 (судьба V2Adapter + критерий Этапа 4).

---
*002 Стресс-тестер, звено Far Light · read-only, ничего не коммичено · все file:line сняты с рабочего дерева 25.08.*
