# BRIEFING — 2 ROUNDS (Mac ↔ Вёдра, 2026-08-26)
> Синхронизация перед финальной ленточкой миграции. Два круга: Round 1 = что Мак несёт Вёдре; Round 2 = что Мак забрал из REGISTRY (Вёдра готов). Итог — точки сверки.

## Round 1 (Mac→PC / Вёдра): свежий контекст Мака
- **Agent-system ЗАКРЕПЛЁН в систему 007** (коммиты `cb09f9a` §8, `79f1217` §9, `ced134b` §10):
  - §8 — основные агенты цепи **строго последовательно**; **скауты параллельно** (до 001 и по ходу).
  - §9 — **полный контекст на блюдечке** (все выводы, схемы кто-кого-слушает, file:line, решения) → ноль re-derivation.
  - §10 — **Context-Refresh Scout Pass**: на (пере)старте/компактификации/апдейте — параллельный пакет скаутов (git/exec, audit, decisions, agent-system) собирает компактное досье. Босс подтвердил, Вёдра внёс свои пункты.
- **Аудит готовности миграции (chain 001→002→001→009) = ОТЛОЖЕНО:**
  - правки **безопасны** (Frozen чист, флип `2395c1e` обратим), НО **сертификация волн по текущим гейтам ЗАПРЕЩЕНА** (false-GREEN).
  - находки: V2-глобалы истинная досягаемость **~62 файла / ~250+ сайтов** (через `window.X` + `(window as any).X` + `w.`-алиас) — план базировал W1/BAC-105 на «9/12» (grep-артефакт) → **W1 недооценён ~20×**, держится backward-compat шимом; исправленные гейты все FAIL на живом дереве (ожидаемо, волны идут); топ-риск **False-Green DONE**.
- **Директива Босса:** 26.08 Update #2 = **ТОЛЬКО МИГРАЦИЯ** (S3/bLb/pitch post-m3); сейчас добавлено: **«JS не нужен, никакого Легасив, только React + современная архитектура»**.
- **Флаг (context-refresh):** в рабочем дереве Вёдры висят удаления `team-m/SYNC-*` и `*AUDIT*` (наши отчёты закоммичены → безопасны) + незакоммиченные правки `registry.ts`/`package.json`/wave-доков.

## Round 2 (PC→Mac, считано из REGISTRY.md): что Вёдра сделал / готов
- **Вёдра готов (ack)** — владеет структурой, гонит волны chain 001→002→009 post-M3-GO.
- **Статус волн:** WAVE-HANDOFF 1-5 дизайн = РЕШЕНО (`f47568d`); W1 `521cb82`, WAVE2a `ba184c5`, WAVE2b `d0e31af` **ЗАКОММИЧЕНЫ** (executing); W3/W4/W5 дизайн РЕШЕНО, apply pending.
- **Frozen-boundary guard v1 APPROVED (GO)**, baseline GREEN.
- **Канон:** PC pre-W2b 306/772; post-W2b леджер = 306/767+5int+2load. Mac-паки при apply сверяются по 772.
- **Модель-канон:** факт = **opencode/hy3-free** (CHAIN-SMOKE GREEN 26.08). REGISTRY §4/§6 пишут `x-preview-f-free` — расхождение к сверке (уже помечено Mac-side update #1, строка 272).
- **Отложено (post-m3):** bLb (GO_005 РЕШЕНО read-only), S3 video (patch готов, НЕ применять до флипа), pitch (connection РЕШЕНО, quality ОТЛОЖЕНО).
- **Владение зонами (§1):** Mac = avatar/CSS/CoachPanel/design/html-projections; Hub = audio-graph/V-Mix/mic/takes/lyrics/char-AI логика; Frozen = НИКТО.

## ТОЧКИ СИНХРОНИЗАЦИИ (must-fix до сертификации волн)
1. **Базовая линия globals СТАРАЯ:** REGISTRY (строка 289) и `WAVE-PREFLIP-BASELINE.md` пишут **globals 9** — аудит доказал **~62 файла / ~250+ сайтов**. Переписать на 62/~250 и скорректировать W1/BAC-105 scope (DOC-CHECK #1/#4, Hub ещё не применил).
2. **V2Adapter счётчик:** REGISTRY Mac-side = 26 (raw) / WAVE-PREFLIP = 27; аудит = 18 после исключения def/barrel. Сверить.
3. **Модель-канон:** обновить §4/§6 REGISTRY на `hy3-free`.
4. **Гейты W2/W4/W5:** текущие формулировки = false-GREEN (self-match V2Adapter, missing require()/alias). Применить исправленные гейты из `REPORT-MIGRATION-AUDIT-2026-08-26.md` как единственный критерий завершения волн.

— 007_Мак. Контекст поднят скаутами (§10), реестр прочитан, готов к финальной ленточке.
