# VERDICT-FINAL-ROADMAP · 009 Независимый Эксперт · 2026-08-25
**Объект суда:** `team-m/reports/mac-007/FINAL-ROADMAP-draft.md` (001) + его источники: FINAL-M3-GO-CHECKLIST-draft.md (001) и STRESS-M3-GO-verdict.md (002).
**Метод:** read-only. Frozen-зона не тронута (patchV1.ts/AudioEngineV2.ts/bridges/track.orchestrator — только чтение). Ничего не коммичено.
**Канон-снапшот суда:** tsc=306 / vitest=770.

---

## ВОПРОС
Допустима ли карта FINAL-ROADMAP к исполнению как финальный маршрут «СЕЙЧАС → репо без Легаси»: верифицируемы ли её якоря, корректна ли логика гейтов (канон на каждом этапе / fresh-build перед dist-grep / leaves-first срез / mic-уши как блокер M3-GO), и критичен ли хвост 001 по sshfs-таймауту на пересчёте ридеров `?? 'v2'`.

## ЗА
1. **Архитектура среза доказана независимо трижды.** Цепочка графа подтверждена мной по звеньям: `AudioEngineV2` ← `patchV1.ts:6` ← `featureFlag.ts:6` (`tryActivateV2`, :10) ← единственный вызов `App.tsx:95`. Leaves-first Волн 1→4 безопасен: каждый шаг снимает текущий лист графа, канон-гейт после каждой волны, V2Adapter помещён в Волну 4 строго после смерти последнего вызывателя в Волне 2 (Д-3 закрыт корректно).
2. **Логика гейтов здравая.** Канон 306/770 — единый канон-гейт всех этапов; правило «число меняется только вниз» + У1 SSOT-синхрон REGISTRY:140↔PLAN:31 убивает дрейф 313/769 у будущих читателей. Fresh-build precondition вшит ПЕРЕД dist-grep (закрывает Д-1 структурно: даже протухший факт не может дать false-PASS, т.к. исполнитель обязан пересобрать). Mic-уши стоят Этапом 1 ДО исполнения M3-GO и замоделированы как внешняя зависимость Босса честно: A4-гейт открывается только после B-slice+F-2 (REGISTRY:133), zombie-тест B8 и program-capture e2e (R4/R5) вынесены явно — то, что пилот не покрывал.
3. **Все 6 усилений и все 5 дыр вшиты без потерь** (§3 карты): У1→0.1, У2→0.4, У3+Д-1→Этап 2, У4→флип (в), У5→граф, У6→A12-форма; Д-2/Д-3/Д-4/Д-5 имеют адресные места. Спорных пунктов между 001/002 по существу нет — защита 001 полная, без затирания атак.
4. **Числа карты точны.** Точечная верификация: `.env:5`=`'v3'`, `.env.example:23`=`v2`, `package.json:24`=verify:ci, ledger=144 строки, REGISTRY:140 = запись Operator-поезда с каноном, `main.tsx:148`='master-volume blocked', `main.tsx:155–158`=единственный writer `__setV3Active`, фасад :9-10/:48/:75 = restore-save/awaitStemReady/getProgramCaptureStream.

## ПРОТИВ
1. **Факт карты «текущий dist = протухший 24.08 11:56» устарел на момент суда.** На дереве dist/assets пересобран **25.08 22:37 — в ту же минуту, что HEAD `84b59e4`** (последний коммит af/ag-цепочки, REGISTRY:152). Это не ломает процедуру (precondition обязывает пересобрать всё равно), но: (а) текст карты содержит мёртвый факт; (б) критерий свежести «dist-mtime > последнего коммита» при равенстве минут неоднозначен — нужна строгая форма (строго больше ИЛИ bundle-hash сверка), иначе свежесть недоказуема в обе стороны.
2. **Хвост 001 (sshfs-таймаут на полном пересчёте 12 ридеров) — реален и воспроизведён судом:** мой полный grep `?? 'v2'` по src/ через sshfs лёг по таймауту >120с. НЕ критично для карты (см. ДОКАЗАТЕЛЬСТВА Д-вопрос), но инструментально запрещить sshfs-полные свипы надо явно, а не по умолчанию.
3. **Мелкие дрейфы координат** (уже покрытые правилом ±5 строк Д-5): фасад restore-save фактически :9–10 против «:8–10», birth-счётчик :97/:99 против A13-эры «App.tsx:100». Не блокер, но ⛔-исполнитель обязан сверять контекстом, не строкой.

## ДОКАЗАТЕЛЬСТВА
| # | Якорь | Заявление карты | Факт дерева | Статус |
|---|---|---|---|---|
| 1 | patchV1.ts:6 | единственный runtime-importer frozen AudioEngineV2 | `import { AudioEngineV2 } from '../core/AudioEngineV2';` ровно :6 | ✅ |
| 2 | App.tsx:93/95/97-99 | дефолт `?? 'v2'`; вызов tryActivateV2; birth-counter | все три точно на заявленных строках | ✅ |
| 3 | main.tsx:148/:155–158/:186 | 'master-volume blocked'; writer __setV3Active; restore-вызов | подтверждено все три | ✅ |
| 4 | audio-facade-v3.js:10/:48/:75 | __restoreV2Engine save / awaitStemReady / getProgramCaptureStream | подтверждено (±1 строка) | ✅ |
| 5 | .env:5 vs .env.example:23 | `'v3'` vs unquoted `v2` | подтверждено дословно | ✅ |
| 6 | vitest.config.ts + src/test/ | VITE_ENGINE не определён (премиса У4) | grep → пусто (EXIT=1) | ✅ |
| 7 | Ридеры `?? 'v2'` | 12 сайтов / 8 файлов | выборка 4/12 (ControlDeck:362, recording.store:48, MixerPanel:139, takes.recorder:70) — все точны; полный свип упёрся в sshfs-таймаут | ✅ точечно |
| 8 | REGISTRY:140 + package.json:24 + known-ts-errors=144 | канон-запись поезда; verify:ci; ledger | подтверждено | ✅ |
| 9 | «dist = 24.08 11:56, протухший» | обоснование Д-1 precondition | **dist = 25.08 22:37 == HEAD `84b59e4` 22:37:52** — факт устарел, процедура обязательна как прежде | ⚠️ |

**Ответ на вопрос суда №4 (хвост sshfs):** НЕ критичен, оставить под подтверждением допустимо, потому что:
- цифра 12/8 уже независимо подтверждена дважды (001 grep + 002 пересчёт) и точечно мной (4/4 совпадений);
- арбитражным инструментом является сам критерий флипа Этапа 2 — `grep -rnF "?? 'v2'" src --exclude-dir=__tests__ → 0` — который исполняется на PC (правило R8 карты) в гейтованный момент;
- условие: пометить в карте «12/8 = двойное независимое подтверждение + точечная выборка суда; финальный арбитр — гейт флипа на PC; полные свипы через sshfs запрещены».

## «ВАРИАНТ, КОТОРЫЙ НЕ ПРЕДЛОЖИЛ НИКТО»
Спор шёл между (а) «×3 set-сайта», (б) «сменить дефолт во всех 12», (в)/У4 «stub в setup.ts». Все три лечат значения, но сохраняют сам класс проблемы: 12 независимых точек чтения env с локальными фоллбеками. **Не предложен вариант (г) — структурный: централизовать резолв в один модуль** (`src/engine-mode.ts`: `export const ENGINE_MODE = import.meta.env.VITE_ENGINE ?? 'v3'`), re-point 12 ридеров на него одним MICRO-PACK (Волна 5 п.2 или сразу Этап 0). Эффект: флип становится правкой ОДНОЙ константы; критерий превращается из «отсутствия строки» в позитивный «`import.meta.env.VITE_ENGINE` встречается ровно в 1 файле»; test/prod-сплит умирает сам (stub У4 остаётся переходной страховкой до re-point); класс R2 закрывается навсегда, а не на этот релиз. Стоимость ~13 механических правок, нулевое поведение-дельта, отлично режется на канон-гейте.

## ВЕРДИКТ
# РЕШЕНО
Карта FINAL-ROADMAP принимается к исполнению как финальный маршрут «СЕЙЧАС → v3-only». Архитектура, порядок волн, гейты и поглощение всех атак 002 — верифицированы против кода.

**Условия (обязательные, не блокируют старт Этапа 0):**
1. **Свежесть dist:** факт «dist = 24.08 11:56» в карте заменить при фиксации (на дереве dist уже 25.08 22:37 ≈ HEAD `84b59e4`); precondition «npm run build на HEAD» остаётся обязательным всегда; критерий свежести — строгий (mtime строго > времени HEAD-коммита ИЛИ hash-сверка чанков), не равенство минут.
2. **Хвост 12/8 ридеров** — оставить под подтверждением с формулировкой из ДОКАЗАТЕЛЬСТВ (двойное независимое + арбитраж гейтом флипа на PC; sshfs-свипы запрещены).
3. **Порядок Этапа 0 неизменяем:** У1 SSOT-пак (REGISTRY:140 ↔ PLAN:31) выполняется первым, до любых других действий — иначе дрейф 313/769 воспроизведёт следующий же читатель.
4. Вариант (г) зафиксировать в бэклог Волны 5 как рекомендуемую эволюцию варианта (в); немедленного применения не требует.

---
*009 Независимый Эксперт · read-only · frozen не тронут · ничего не коммичено · все проверки сняты с рабочего дерева 2026-08-25 (HEAD `84b59e4`).*
