# DCASE7-002 · КРУГ-2 · СТРЕСС FOUC-ГЕЙТА (002)

**002 · 2026-09-02 · HEAD 710f9d7 · канон 282/803/67.**

## РЕЗЮМЕ: ТРЕБУЕТ ПАТЧА
Гейт 001 армится по НЕСИНХРОННОМУ источнику: ld.textBlocks наполняется в параллельном V2-потоке (track.loader Step 8), track-loaded стреляет из V3-потока (main.tsx:317 fire-and-forget) — на классе треков (typed-markers без blocksData) FOUC жив; watchdog 500мс проигрывает jank.

## УДАРЫ
- **У-1 [КРИТИЧНО]:** в момент track-loaded (V3-поток) ld.textBlocks может быть ПУСТ (наполняется V2 Step 8/11a позже) → гейт не армится → raw-flash жив на треках с маркер-синтезом. Патч: арм на **before-track-change** (tc.tracks[index] уже fresh — loader :56-57 ДО Step 3), дискриминатор = **track.blocksData?.length>0 || typed syncMarkers** (синхронные данные каталога), guard по detail.toTrackId.
- **У-2 [ВЫСОКО]:** watchdog 500 vs setTimeout(300)+jank (52 сцены строятся на main thread после track-loaded) → при X>200мс стреляет раньше blocks. Патч: 500→**1200мс** (BAC-001 5000мс на lyricsReady остаётся страховкой).
- **У-5 [СРЕДНЕ]:** blocks в сторе ДО арма (upload-флоу) → подписка молчит → 500мс лишнего холда. Патч: instant-check blocks.length>0 при арме (при арме на before-track-change — не нужен: blocks.store УЖЕ пуст, facade-порядок publish→dispatch).
- **У-4 [СРЕДНЕ]:** недостающие тесты: (а) порядок guards lyricsReady×structurePending; (б) watchdog→raw при lines>0; (в) двойной свитч; (г) гонка-дискриминатор (инвертируется патчем У-1); (д) ранние blocks.
- **У-3 [снят сам]:** два track-loaded без before-track-change — невозможен (Step 3 безусловен, interceptor зовётся только из handler'а).

## КЛЕЙМЫ, КОТОРЫЕ НЕ СМОГ УБИТЬ
WagonTrain сам скрывается без блоков (:90) · Karaoke raw = легитимный режим · NowPlaying/LiveSubtitle = не центр · порядок guards цел (React 18 батчит) · watchdog 500/5000 не конфликтуют (разные флаги) · blocks.store чист.

## ПАТЧИ-УСЛОВИЯ
1. Арм на before-track-change по tc-данным (blocksData|typed syncMarkers + toTrackId-guard) — закрывает У-1+У-5.
2. Watchdog 1200мс.
3. (Если арм остаётся на track-loaded — instant-check; при патче 1 излишен.)
4. Тесты (а)-(д) — 3-4 новых кейса.
5. Усиление: flush-ветка BAC-001-подписчика дополнительно сбрасывает structurePending при blocks>0.
