# ARC-2D · Круг-4 · 009 (Суд Независимый) · ВЕРДИКТ по финальной спеке контракта аудио-фасада V3 · 2026-08-30

**HEAD `d024a41` · канон-снапшот: tsc=293 🔴 · vitest=779+0int+0load 🟢 (66 файлов) · PARITY PASS 🟢 · V3=дефолт**
Метод: полное чтение дела (скаут-пакет + Круги 1-3) + личная сверка каждого клейм-факта по дереву. Read-only, frozen не тронуты, числа канона взяты из снапшота диспатча (запусков не делал — правилами запрещено).

## ВЕРДИКТ: РЕШЕНО С УСЛОВИЯМИ

Ядро спеки (S1 publish + S2 20 наполнений + S3 22 теста + S4 инвариант) подтверждено код-чтением по всем критическим клеймам: ошибок, ломающих канон или семантику, **не найдено**. Все удары обвинения (a)-(g) диспатча проверены лично и либо опровергнуты кодом, либо уже закрыты патчами У-2/У-4 внутри спеки. Однако по правилу неоспоримости найден **факт, который не рассмотрела ни одна сторона**: teacher-side сетевые broadcast'ы идут ТОЛЬКО через фасадные вызовы, а живой React-UI учителя играет через `getTransport()` напрямую, мимо фасада — hijack этих путей не ловит. Это НЕ дыра спеки (apply-сторона студента полностью фасадная, смоук-клейм честен для сценария «загрузка трека → студент играет»), но это дыра ВЕРИФИКАЦИОННОЙ РАМКИ: 007 обязан тестировать teacher-триггер через живой фасадный путь (autoplay), иначе смоук ложно провалится. Отсюда условия, не патчи кода.

## ДОКАЗАТЕЛЬСТВА (file:line)

**(a) Publish-точка и синхронность — ПОДТВЕРЖДЕНО:**
- `src/main.tsx:91` `const transport = getTransport()`; `:92` `if (!transport) return // V2 not available`; вызов `bootAether()` синхронен на module-eval (до DOMContentLoaded-блока и React-mount) → publish в S1 (после :91, до :92) **синхронен и предшествует async-IIFE пайплайна** (:139+). У-2-сценарий (3× WASM-fail → `handleV3BootFailure(); return` :151) не затрагивает строку publish — принцип «в точке создания» 002 соблюдён.
- `src/audio/engine-v3/index.ts:36-44`: `getTransport()` try/catch → singleton `_sharedTransport`, dispose не зовётся — пересозданий нет, публикация одноразова.
- `src/audio/core/audioContext.ts:19-25`: `getAudioContext()` конструктор без try → может кинуть → `getTransport()` вернёт null → publish null. Гейт фасада `b?.transport` → falsy → resolve-пустышки. **NaN не возникает ни в одном пути** (getPlaybackRate→1, playbackRate→1, audioContext→null, meter→0): обвинение (a) опровергнуто.
- Паттерн `(window as any).__belive` уже в каноне: main.tsx:89-90, :155-162 (async-IIFE), :170-175 — **tsc-дельта 0 обоснована**; `__belive` не типизирован в `src/env.d.ts` (только `__eventBus` + `signalsmith-stretch` module) — проверено лично.

**(b) play/pause/seek/stop сигнатуры — ПОДТВЕРЖДЕНО (личное чтение TransportV3 + HybridClock):**
- `src/audio/engine-v3/core/TransportV3.ts:130-134`: `async play(initialOffset?)` — `if (idle||ended) { this.clock.seek(initialOffset ?? 0); _setState('ready') }` → NaN-вход невозможен (`?? 0` + HybridClock.ts:90 `Math.max(0, time)`).
- Resume-игнор offset: докстринг TransportV3.ts:118-124 («a plain resume from 'paused' always continues…») + HybridClock.ts:118-123 `pause()` запоминает `_startOffset = getCurrentTime()` — обвинение «double NaN/потеря resume» снято.
- `:204` `async seek(time)`: `if (state !== 'playing' && state !== 'paused') return` — seek no-op при 'ended' → У-4-патч (playWithWatchdog с offset) — единственное лечение, семантика верна: из 'ended' `play(t)` → `clock.seek(t)` старт с t; из 'paused' offset игнорируется (докстринг).
- `:185` stop идемпотентен (`if (state === 'idle') return`).

**(c) MonitorRouter:276 — ПОДТВЕРЖДЕНО:** `src/audio/engine-v3/monitor/MonitorRouter.ts:276` `attachProgramSource(node: AudioNode, _opts: { kind: string })` → `node.connect(this._captureGain)`; capture-граф `programInput → _captureGain → captureStream` (constructor, личное чтение). Сигнатура совместима с вызовом `src/takes/hooks/useTakesPlayback.ts:162` `ae?.attachProgramSource?.(gain, { kind: 'preview' })` (без engineMode-гейта — живой потребитель подтверждён). `detach`-метода в MonitorRouter НЕТ (static graph «0 disconnect», шапка файла) — пустышка detach обоснована; зомби-gain молчит (stopped source), задокументировано в S2.

**(d) loop-events — ПРОВЕРЕНО, обоснование У-5 Круга-3 КОРРЕКТНО:** `src/foundation/event-bus/wrappers/loop-events.ts:26-36` (check) и `:51-73` (syncLoopToEngine): живой V3-детект `__v3Active || (t3.state !== 'idle' && t3.orchestrator.all().length > 0)` → `t3.setLoop` напрямую, **фасад не читается**; V2-ветка `:62` `ae.setLoop(s,e)` достижима только при отсутствии живого transport (практически мертва — singleton существует всегда после bootAether при живом AudioContext). `__v3Active=true` ставит V3DataInterceptor.ts:164 при загрузке трека → edge «стемы + idle» 001-Круга-3 покрыт детектором НЕ через фасад (t3.setLoop), либо через фасад в genuinely-V2 мире. Наполнение → true инертно в V3-прогоне, контракт `boolean true` честнее `false` — принято. loop.bridge RETIRED (main.tsx:316 закомментирован `initLoopBridge()`) — подтверждено личным чтением main.tsx.

**(e) HPS-сигнатуры — ПОДТВЕРЖДЕНО grep'ом (лично):** `src/audio/engine-v3/pipeline/HybridPipelineService.ts`: `:135` `get ctx(): AudioContext`; `:276` `async play(offset: number, rate: number)`; `:531` `setStemVolume`; `:559` `setStemMuted` (→ `muteStem`); `:564` `soloStem`; `:571` `getStemMeterLevel`; `:600` `getStemAnalyser` — имя-мэппинги фасада (setStemMute→setStemMuted, setStemSolo→soloStem) точны. `playbackRate` в HPS публичного НЕТ — rate живёт в TransportV3.ts:103-104 `get playbackRate()` (clock, синхронно обновляется setPlaybackRate :243-252) — свойство фасада через `__belive.transport.playbackRate` корректно.

**(f) Фасад — ПОДТВЕРЖДЕНО личным чтением `js/audio-facade-v3.js` (82 строки):** getCurrentTime :12-17 (кэш `__belive.currentTime`), guard `:81` `if (!window.audioEngine) window.audioEngine = facade`; текущие пустышки play/pause/stop :30, seekTo/setCurrentTime :31, loadTrack :32 (resolve), стемы :34-35, rate :67, attach/detach :68, setLoop/clearLoop :78 (`return false`). index.html: component-scripts порядок `js/audio-facade-v3.js` → lyrics-display → track-catalog → marker-manager → monitor-mix (личное чтение index.html body) — eval-порядок шапки соблюдён; V2-`audio-engine.js` НЕ подключён → guard `:81` всегда true в V3.

**(g) CSP/eval — ПОДТВЕРЖДЕНО:** index.html head (личное чтение, строки 1-40): только theme-color/mobile-web-app/viewport/og/twitter-mетас — **CSP-меты НЕТ**; jsdom CSP не применяет. Прецедент `?raw`: `src/audio/engine-v3/pipeline/__tests__/BusFader18.test.ts:22` `import pipelineSrc from '../HybridPipelineService.ts?raw'` — в каноне (779 включает). `vitest.config.ts` include `src/**/*.{test,spec}.{ts,tsx}` — `src/audio/audio-facade-v3.test.ts` проходит, файлов 66→67. `src/test/setup.ts` глобально мокает AudioContext/HTMLAudioElement — фасадным тестам НЕ нужен (мок `__belive` — plain objects), конфликтов нет.

**(h) Ключевые пересечения (мои доп-сверки):**
- `src/services/track.actions.ts:57-58` (deleteTrack, личное чтение): `dispatchEvent(new Event('before-track-change'))` **без detail** → main.tsx handler `const trackId = payload?.toTrackId; if (!trackId) return` (ранний выход ДО stop) → `w.audioEngine.stop()` — **единственный** стоп в этом пути. Сегодня (пустышка) удалённый трек играет дальше — наполнение чинит живой баг. Аналогично clearAllTracks (диспатч без detail + `w.audioEngine.stop()` — тот же путь). Двойной стоп невозможен (main.tsx рано выходит; loadTrack-путь с detail стопает сам, фасад не звётся).
- `src/Rehearsal/bridge/rehearsal-trigger.bridge.ts:116-117` `if (role === 'teacher') this.hijackAudioTransport()` — **студент НЕ hijack'ает** → двойного транспорт-пути (обвинение b) НЕ СУЩЕСТВУЕТ: у учителя hijack→origPlay (фасад→transport)→broadcast; у студента playWithWatchdog:318-336 `void ae.play().catch(...)` — фасад→transport. Один путь на роль.
- Замерзшее время (обвинение c) — опровергнуто: HybridClock.ts:88-90 `if (_state !== 'playing') return _startOffset`; pause():84 фиксирует `_startOffset = getCurrentTime()`; 'ended' → TransportV3 `_handleTrackEnded` → clock.pause() → позиция конца. TransportV3.ts:213-216 (seek) диспатчит 'seek' → V3StatePublisher._onSeek → publishSeek (кэш) — комментарий «иначе замерзает на паузе (006/TASK-010)» описывает кэш, а приоритет transport.currentTime в фасаде (S2 №1) чинит именно 50ms-кэш-шум (002 У-3/У-1), НЕ вводя заморозку. AMEND-1 (V3StatePublisher._onStateChange) синхронизирует кэш на statechange — источники согласованы.
- V3StatePublisher.ts (личное чтение): подписки statechange/ratechange/seek в constructor; publishSeek пишет `__belive.currentTime`; tick ≥50ms — всё соответствует скауту :88-89.

**(i) ФАКТ, НЕ РАССМОТРЕННЫЙ НИ ОДНОЙ СТОРОНОЙ (правило неоспоримости):** Живой React-UI учителя управляет транспортом **напрямую через `getTransport()`, мимо фасада**:
- `src/hooks/useKeyboardShortcuts.ts:44-83` (Space → `transport.play()/pause()`; стрелки → `transport.seek(nt)`) — монтируется в App.tsx:81;
- `src/components/TransportBar.tsx:40-49` (handleSeek → `transport.seek(newTime)`);
- `src/components/WagonTrain.tsx:106-136` (seek по клику на вагоны/блоки);
- `src/components/MonitorMixPanel.tsx:409-427,561-627`; `src/takes/components/TakesControlStrip.tsx:206,681`; `src/takes/components/TakesPanel.tsx:1163,1244,1277`; `src/takes/hooks/useTakesPlayback.ts:99,191` (play через `tr.play()`).
hijack (rehearsal-trigger.bridge.ts:123-152) патчит **только `window.audioEngine.play/pause/seekTo/setCurrentTime`** → эти пути он НЕ перехватывает. Единственный живой фасадный транспорт-вызов учителя — `src/services/track.loader.ts:469` (Step-13 autoplay: `try { await ae.play(); }`). Следствие: network-broadcast от учителя реально происходит при ЗАГРУЗКЕ ТРЕКА (autoplay) и при snapshot-цикле (sync-request → sendSnapshot → applySnapshot у студента — тоже фасад студента: :355-365 `ae?.seekTo/setPlaybackRate/play/pause`); ручные Space/seek учителя НЕ транслируются — и ДО, и ПОСЛЕ спеки (не регресс, спека этот путь не заявляет и не чинит). Смоук-клейм «сеть играет у студента» ВАЛЕН для сценария «учитель загрузил трек → autoplay → hijack → broadcast play → студент applyPlayPauseSeek → фасад→transport → звук». Но верификация 007 обязана знать это ограничение (см. условия).

## ОБВИНЕНИЕ (риски) — по пунктам диспатча

- **(a) publish null-transport → NaN-гейты Rehearsal — ОПРОВЕРГНУТО.** Publish синхронен на module-eval main.tsx — ДО любого Rehearsal-кода (сессия требует user gesture). При null: гейты дают 1/null/0/resolve — vclock.anchor(t, 1) — то же, что сегодня; NaN-путь отсутствует (доказательство (a)).
- **(b) двойной транспорт-путь hijack vs playWithWatchdog — ОПРОВЕРГНУТО.** Студент не hijack'ает (:116-117); playWithWatchdog зовёт ae.play() — фасад; У-4-патч не «мост зовёт transport напрямую», а «playWithWatchdog передаёт offset в ae.play» — остаётся один путь (доказательство (h)-2). Ретрай watchdog без offset — resume от clock — приемлемо (микро).
- **(c) замерзший getCurrentTime — ОПРОВЕРГНУТО, наоборот: чинится.** HybridClock хранит позицию паузы; 006/TASK-010 — про кэш publisher, не про clock (доказательство (h)-3).
- **(d) transport реальный vs мок — НЕ ПРОБЛЕМА.** S3 мокает `__belive` (plain objects), не создаёт TransportV3; setup.ts-моки излишни, но не мешают (доказательство (g)).
- **(e) tsc 293 «0-дельты» — ПОДТВЕРЖДЕНО.** Паттерн `(window as any)` многократно в каноне main.tsx; env.d.ts не расширяется; `?raw` покрыт `types: ["vite/client"]` (tsconfig.json, личное чтение) — прецедент BusFader18:22; transport used ниже :95+ — noUnusedLocals не задет.
- **(f) пустышки — ВСЕ ЗАДОКУМЕНТИРОВАНЫ в S2-таблице** (loadTrack/interceptor, setStemsEnabled, pan/mode, mic-группа, vocalMix-пара с честной UI-ложью TakesPanel, detachProgramSource/static-graph, ensureInstrumentalBuffer). Нюанс: счёт в шапке диспатча «7 пустышек» занижен — фактических позиций 9 (пары enable/disable считать по одной). Косметика, не блокер.
- **(g) новое (моя находка):** teacher-UI-bypass (доказательство (i)) — риск ЛОЖНОГО смоук-провала у 007, если триггер выбран неверно (Space вместо загрузки трека). Плюс тест-риск: `?raw`-импорт из `js/` (вне src/) не имеет прямого прецедента в каноне (BusFader18 — внутри src); vite root='.' покрывает, но это единственное непроверенное звено тест-механики (запуск запрещён суду).

## ЗАЩИТА (сильнейшие аргументы, подтверждены кодом)

1. **Транспорт-роутинг через TransportV3, не HPS** — ядро держит: HPS.play(:276) не трогает clock/state/publisher (V3StatePublisher слушает transport.statechange — личное чтение) → прямой HPS = isPlaying-ложь; transport сам вычисляет offset (:157 `clock.getCurrentTime()`) и rate (:158) → вопрос «откуда rate» растворён; double-lock исключён единственным `_restartStemsAt` (TransportV3:304-310 → один pipelineController.play).
2. **Минимальность**: 2 строки main.tsx (не frozen), 2 строки rehearsal-trigger.bridge (не frozen, `src/Rehearsal/bridge/` ≠ `src/bridges/` — лично проверил путь файла), фасад вне tsconfig, frozen 0 правок.
3. **True-контракт setLoop/clearLoop**: loop-events V2-ветка глушит rAF-fallback при `applied === true` — а там где applied=true, transport.setLoop реально применился (stems+pipeline+clock+reseek, TransportV3:256-268 — личное чтение); IV2-контракт boolean честен.
4. **22 пути-теста**: гейты, имя-мэппинги (setStemMute→setStemMuted), hijack-resolve-контракт (без __belive play() → resolved Promise — rehearsal-trigger:327 `.catch` не ломается), guard-инвариант :81 (повторный eval не затирает) — покрывают регрессии фасада.
5. **У-2 degraded-режим**: publish в точке создания — varispeed-фолбэк сохраняет сеть (главный риск 002 закрыт в S1 дословно).

## УСЛОВИЯ/ЧЕК-ЛИСТ ДЛЯ OPERATOR (дословно, без интерпретаций)

**У-С1 (обязательное, смоук-методология):** В чек-лист Босса/верификацию добавить строку: «teacher→student smoke проверяется через ЗАГРУЗКУ ТРЕКА у учителя (autoplay → track.loader:469 `ae.play()` → hijack → broadcast) и/или sync-request→snapshot цикл. Ручные Space/TransportBar-действия учителя НЕ транслируются (UI зовёт getTransport() напрямую, мимо hijack) — известное отсечение ARC-2d scope, НЕ провал смоука». Текст-формулировка для wired-чека: «Смоук сеть: студент играет после загрузки трека учителем и по snapshot-resync; Space-путь учителя вне сетевого контракта (ARC-2e бэклог)».
**У-С2 (дословность S1):** Publish строго МЕЖДУ `const transport = getTransport()` (main.tsx:91) и `if (!transport) return` (:92), 2 строки, паттерн `;(window as any).__belive = (window as any).__belive || {}` + `;(window as any).__belive.transport = transport`. НЕ внутри async-IIFE, НЕ после guard. При null публикуем null — это контракт.
**У-С3 (дословность У-4):** `playWithWatchdog(retriesLeft = 2, offset?: number)` — offset проксируется в `ae.play(offset)`; вызов в applyPlayPauseSeek play-ветке: `this.playWithWatchdog(2, t)`. Существующие вызовы `playWithWatchdog()` без изменений (default-параметры сохраняют семантику).
**У-С4 (тест-механика, обязательные инварианты):** beforeEach `delete window.audioEngine` + сброс `__belive` (guard :81 иначе молча не переустанавливает — кейс 22 обязателен, СТОП-условие S4 его держит). Если `?raw`-импорт `'../../js/audio-facade-v3.js?raw'` упадёт в vitest (единственное непроверенное звено — js/ вне src), fallback в ТОМ же тест-файле: `readFileSync` + `new Function` — НЕ перенос файла в src/ (радиус правок не расширять), count тестов не меняется.
**У-С5 (косметика, необязательное):** в шапке отчётов счёт пустышек привести к таблице S2 (9 позиций: loadTrack, setStemsEnabled, setStemPan, setStemsMode, setMicrophoneVolume, enable/disableMicrophone, enable/disableVocalMix, detachProgramSource, ensureInstrumentalBuffer) — «7» в диспатче занижено.
**У-С6 (запрет):** env.d.ts НЕ расширять типами `__belive`; никаких правок frozen-зоны; число tsc-ошибок 293 — не «чинить».

## РАМКА ВЕРИФИКАЦИИ ДЛЯ 007 (после Operator; точные команды и ожидания)

1. `git diff --stat` → ожидание: ровно 3 файла правок: `src/main.tsx` (+2), `js/audio-facade-v3.js` (переписаны ~20 методов + 2 свойства), `src/Rehearsal/bridge/rehearsal-trigger.bridge.ts` (+2); НОВЫЙ `src/audio/audio-facade-v3.test.ts`. Frozen (`src/audio/core/AudioEngineV2.ts`, `src/audio/compat/patchV1.ts`, `src/bridges/*`, `src/services/track.orchestrator.ts`) — diff ПУСТ. Иначе СТОП.
2. `git rev-parse HEAD` → `d024a41` (или merge-base содержит) — шапка отчёта.
3. `npx tsc --noEmit` (или канон-команда проекта) → ожидание: **293, дельта 0**. >293 — СТОП.
4. `npx vitest run` → ожидание: **801 pass (779+22), файлов 67, 0 int, 0 load**; окно допуска 793..801 (слияние asserts в кейсах 4/6/8/12/16/19/21), НО кейсы 10/11/19/22 (имя-мэппинги, resolve-контракт, guard-инвариант) — обязательны отдельными `it`. <793 или >801 — СТОП.
5. Специфичные проверки код-чтением (007, grep): (i) `__belive.transport` publish между main.tsx:91/:92 (У-С2); (ii) в фасаде: setStemMute→`setStemMuted`, setStemSolo→`soloStem`, setLoop/clearLoop→`return true` при живом transport; (iii) play без transport возвращает Promise (`.then`-able), НЕ reject; (iv) rehearsal-trigger: applyPlayPauseSeek play-ветка передаёт t.
6. Смоук-сценарии (по У-С1, НЕ через Space): (a) загрузка трека у учителя в live-сессии → студент начинает играть синхронно; (b) учитель seek после конца трека (state 'ended') → студент стартует с той же позиции (У-4); (c) Tempo Ladder (rate 0.85) → vclock.anchor студента получает rate 0.85 (playbackRate-свойство), линейного уплывания нет; (d) превью таке во время записи экрана → таке-дорожка присутствует в program-capture (attachProgramSource); (e) deleteTrack во время игры → звук останавливается (фасадный stop).
7. PARITY-канон: повторить канон-PARITY-команду проекта → PASS.
8. СТОП-условия S4 спеки остаются в силе дословно: tsc>293; vitest<793|>801; int>0|load>0; frozen-diff непуст; PARITY FAIL; кейс 22 красный.

— 009 · Суд Независимый · Круг-4 ARC-2d · 2026-08-30 · HEAD `d024a41`
