# D1-O3 · PRE-MORTEM D-1 (002 от лица системы) · 2026-09-03

**002 · HEAD dc082b0 · канон: tsc=201 · vitest 812/68 · frozen 2/2 · reach 126/0-ложных.** Исполнение долга системы (CEO_1 10:35: О-3 перед D-1) — стресс-прогоном 002; отчёт Опуса (если сдан) вливается доп. листом ДО применения.

## ВЕРДИКТ: ДЕРЖИТ С УСЛОВИЯМИ
Ядро подтверждено перемерами на dc082b0: кластер замкнут (К-2/К-5/К-12 — дрейфа НЕТ), атомарность манифеста, audioContext жив. Но спека содержит 2 гейта-ловушки и дрейф Δ-базы.

## КАРТОЧКИ-ПЕРЕМЕР
К-2 замкнутость ✓ · К-5 тесты ✓ · К-12 stemTypes ✓ (не расширился) · К-3 **дрейф базы: 201, не 202 → после D-1 = 191** · К-1 dist — гейт как записан краснеет (см. У-2/У-3).

## УДАРЫ (7)
- **У-2 [крит]:** гейт `rg "…|StemPlayer|…" dist/` = 0 — матчит живой **StemPlayerV3** в прод-чанках → ложный красный на зелёном. Патч: `-w`-границы + V3-исключение.
- **У-3 [выс]:** комментарий facade:22 уезжает в dist (cp -r js) → второй ложный красный. Патч: гейт «по свойству» (≤1 = комментарий) либо снос комментария в D-1.
- **У-1 [выс]:** Δ-таблица «202→192» vs канон 201 → Оператор видит 191 и STOPнется. Патч: база = живой замер шага-1 чек-листа.
- **У-4 [ср]:** `.gitattributes:7` — незаявленный якорь на сносимый файл → удалить строку в ТОМ ЖЕ коммите.
- **У-5 [ср]:** `known-ts-errors.txt:2-11` (10 кластерных строк) станут мёртвым списком → снести блок тем же коммитом (хвост конвоя D-5).
- **У-6/У-7 [лёгк]:** bLb/frozen-guard.mjs regex (не блокер) · audioContext: ensureResumed/setAudioContext/getSampleRate — 3 мёртвых экспорта (конвой D-5, НЕ D-1).

## СЦЕНАРИИ СВЕРХ §5 ОПУСА
1. reach после сноса = 121 (−5), не «Δ0» — иначе G-1 врёт в первую неделю.
2. HMR/optimizeDeps-кэш переживает снос → смоук тестирует кэш, не дерево: перезапуск dev + чистка .vite.
3. Service-worker-кэш старого чанка → «призрак V2» в стек-следах: bypass SW перед смоуком.

## ЧЕК-ЛИСТ ОПЕРАТОРА D-1 (15 шагов, исполняемый)
1. База-tsc-кластер ДО сноса: `grep -cE "AudioEngineV2|StemPlayer|AudioLoader|VocalMix|MicrophoneManager"` → **ровно 10** (иначе STOP — дрейф).
2. База-reach: зафиксировать N (канон 126) → ожидание N−5.
3. `git rm` всех 5 (НЕ audioContext.ts!).
4. Манифест: удалить guard-строку AudioEngineV2 (атомарно).
5. `.gitattributes:7` — удалить строку (У-4).
6. `verify:frozen` → **frozen OK: 1/1**.
7. tsc → **база−10** (при 201 = 191).
8. vitest → 812/68 Δ0.
9. `verify:ci` → PASS.
10. reach → **121** (Δ−5; «Δ0» = STOP).
11. `npm run build` → OK.
12. dist по свойству: `rg -w "AudioEngineV2|VocalMix|MicrophoneManager|AudioLoader" dist/` = 0 · `rg -w StemPlayer dist/ | rg -v StemPlayerV3` = 0 · `rg AudioEngineV2 dist/` ≤1 (комментарий facade:22).
13. verify:refs по 5 путям → только известные якоря; НОВОЕ живое = STOP.
14. Коммит = шаги 3+4+5 одной единицей; комменты-упоминания НЕ трогать (конвой D-5).
15. Смоук: перезапуск dev + чистка .vite + bypass SW → трек/стемы/мик/вокал-микс.
