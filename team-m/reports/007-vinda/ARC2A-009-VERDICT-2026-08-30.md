# ARC-2a · Круг-4 · 009 «Суд» — ВЕРДИКТ по делу VIS-19 (спека 001-Круг-3)

**БАЗА:** HEAD `d99609a` (проверен лично: `.git/refs/heads/main` = d99609a1531844949062d86eae712aadcb9afb5b) · 30.08.2026 · канон-снапшот: tsc=296 🔴 (4×TS2339) · vitest=761+0int+0load 🟢 (63 файла) · PARITY 🟢 · V3=дефолт.
**Дело:** β-синглтон `PitchEngine.get()` (P1) + `_listeners.clear()` в destroy (P2) + `initFromMic()` в store:49 (P3) + тест-файл 4 кейса (P4).

## ВЕРДИКТ: РЕШЕНО с условиями

Спека P1-P3 математически корректна (сверено по коду лично, каждый якорь). P4 содержит ОДНУ невыполнимую формулировку: кейс-4 требует `await expect(startPitch()).rejects.toThrow(...)`, но store глотает ошибку в catch (pitch.store.ts:72-77) — промис ВСЕГДА резолвится, ассерт красный в любом прогоне. Ни одна из сторон (001, 002, 001-Круг-3) этого не поймала — поймал Суд независимой сверкой. Патч формулировки однозначен, свойство кейса (проверка poison-пути β) не меняется → РЕШЕНО с обязательными условиями, без нового круга дизайна.

## ДОКАЗАТЕЛЬСТВА (сверка фактов, file:line)

**API PitchEngine (личное чтение `src/audio/pitch/pitch-engine.ts`, 300 строк):**
- `:20` `export class PitchEngine {` — подтверждено.
- Статических методов НЕТ (grep `static` → 0 совпадений в классе; только `initFromNode()` в комментарии :17). `static get()` отсутствует → TS2339 законен.
- `:30` `private _listeners = new Set<PitchListener>()` — подтверждено.
- `:46-50` `_getContext()`: `(window as any).audioEngine` → `ae?.audioContext` → `if (!ctx) throw new Error('audioEngine.audioContext not found')` (:49) — подтверждено.
- `:56` `async initFromMic(): Promise<void>` · `:111` `initFromNode(sourceNode)` · `:207` `destroy(): void` · `:282` `subscribe(fn)` → remover — подтверждены.
- **Poison-путь `:101-106`** (прочитан лично): `:101 catch (err)` → `:102 _status='error'` → `:103 console.error` → `:104 this.destroy()` → `:105 throw err` → destroy ставит `_status='idle'` на `:241`. Экземпляр после честного броска = живой, idle. Свойство β подтверждено кодом.
- **Якорь P2**: `:238 _ownStream = false` → `:239` (пустая) → `:240 ring.clear()` → `:241 _status='idle'`. Вставка `_listeners.clear()` между :238 и :240 — внутри destroy, перед ring.clear. Якорь однозначен.
- `destroy()` сегодня `_listeners` НЕ чистит (тело :207-242 — Set не тронут) — предпосылка P2 верна.

**4 точки VIS-19 (личное чтение):**
- `src/stores/pitch.store.ts:45` `const engine = PitchEngine.get();` · `:49` `await engine.init();` · `:85` `PitchEngine.get().destroy();` — подтверждено (файл 97 строк).
- `src/audio/pitch/pitch-visual-bridge.ts:17` `const engine = PitchEngine.get();` — подтверждено (файл 32 строки, guard pianoKeyboard :11-14).

**Обвязка store (критично для кейса-4):**
- `pitch.store.ts:72-77` — `catch (err) { set({ status:'error', error: err.message... }) }` — **ошибка проглатывается, rethrow НЕТ** → `startPitch()` никогда не reject'ит.
- `:41-43` guard `status==='running'|'starting'` → return; `'error'` проходит → повторный старт возможен.
- `:80-85` stopPitch: `_unsub?.()` → `_bridgeCleanup?.()` → `destroy()` — порядок подтверждён.

**Монтаж (grep лично):**
- `PitchModule` по src: ТОЛЬКО self-references (`PitchModule.tsx:97, :165`). НЕ смонтирован → живого UX-эффекта ARC-2a нет — рамка 001-Круг-3 честна.
- `src/deck/modules.ts:101-102` — регистрирует `PitchTab` (единственный pitch-компонент в UI).
- `pianoKeyboard\s*=` по src → **0 присвоений** → bridge-destination мёртв, guard :11-14 = вечный noop.
- `new PitchEngine(` → только `PitchTab.tsx:252, :307` (локальные инстансы, cleanup destroy :287/:319). Двойная истина singleton/new существует сегодня, пути изолированы.

**Подписки на PitchEngine (grep `.subscribe(` по src, лично):** ТОЛЬКО `pitch.store.ts:51`, `pitch-visual-bridge.ts:19`, `PitchTab.tsx:123` (useStableVocalData — на ЛОКАЛЬНЫЕ инстансы). Внешних подписчиков singleton'а вне store/bridge НЕТ.

**V2Adapter-канон:** `src/audio/engine-v3/V2Adapter.ts:19-23` — `private static instance` + `static getInstance()` — паттерн проекта, β канонична.

**Тест-окружение (для кейса-4):** `vitest.config.ts` — `environment: 'jsdom'`, `setupFiles: ['./src/test/setup.ts']`, include покрывает `src/**/__tests__/**`. `src/test/setup.ts` (прочитан целиком) — `window.audioEngine` НЕ создаётся (только AudioContext/Canvas/localStorage/navigator-моки) → предпосылка кейса-4 «audioEngine undefined → throw :49» ВЕРНА.
**tsc-контур:** `tsconfig.json` — `strict:true, noUnusedLocals:true, noUnusedParameters:true, include:["src"]` → новый тест-файл под tsc-проверкой. Удар-2/002 правомерен, инвариант ≠292 закрывает.
**Новый файл:** `src/stores/__tests__/pitch-store.test.ts` — НЕ существует (проверено stat: ENOENT). 63→64 по снапшоту корректно как Δ.
**Мёртвый `pitchEnabled`:** `medium-stores.test.ts:18-20` — подтверждено (setState поля, которого нет в PitchState; startPitch не дёргает — путь не задет).

## ОБВИНЕНИЕ (риски спеки)

**(a) `_listeners.clear()` в destroy убьёт чужие подписки — НЕ ДЕРЖИТ (сверено):** все подписки singleton'а (store:51, bridge:19) отписываются ДО destroy по порядкам stopPitch (:80-85) и bridge-cleanup (:27-31); PitchTab-подписка :123 — на локальные инстансы, unsub в cleanup эффекта идёт до destroy (:287/:319). Живой подписки на момент destroy НЕ СУЩЕСТВУЕТ ни по одному пути. Clear = чистка уже-пустого Set = самозащита от зомби, не оружие.
**(b) jsdom-зависимость кейса-4 — НЕ ДЕРЖИТ (сверено):** jsdom + setup.ts без `window.audioEngine` → реальный throw :49 гарантирован и детерминирован. Не flaky — контракт окружения.
**(c) тест-файл привнесёт strict-ошибки — риск РЕАЛЕН, но закрыт:** tsconfig strict-тройка проверяет новый файл; инвариант tsc==292 (любое ≠292 = СТОП, включая <292) ловит. Остаток — дисциплина ревью (см. чек-лист У-3).
**(d) JSDoc-сторож недостаточен против двойной истины — ДЕРЖИТ ЧАСТИЧНО, не как today-стоп:** JSDoc ≠ enforcement, но мина относится к ARC-2e (будущий перевод PitchTab на get()); today-дифф пути изолированы (grep подтверждает), каноничность паттерна (V2Adapter) + имя get() (3 сайта уже написаны) обоснованы. Риск фиксируется как мина ARC-2e, не как блокер ARC-2a.
**(e) ДЫРА, найденная Судом (пропущена всеми кругами): кейс-4 невыполним в текущей формулировке.** Спека: «`await expect(startPitch()).rejects.toThrow('audioEngine.audioContext not found')`». Факт: store:72-77 глотает ошибку — `startPitch()` всегда резолвится. Спека противоречит сама себе: её же кейс-2 ассертит `status==='error'` после catch — т.е. 001 ЗНАЕТ о проглатывании, но в кейсе-4 требует reject. Буквальное исполнение = гарантированный красный тест → vitest ≠765 → СТОП Operator'а. Это не риск «если» — это детерминированный провал прогона. Патч см. У-1.
**(f) Расхождение счёта тест-файлов (найдено Судом):** канон-снапшот = 63 файла; мой glob по include-паттерну vitest (src/**/*.{test,spec}.{ts,tsx}) = **66** (*.test.ts=66, *.spec/* 0, *.test.tsx 0). Разница 3. При read-only верификации Суд не запускает vitest; абсолютная база снапшота — данность Центра, но 007 обязан верифицировать по Δ, а не слепо по абсолюту (см. У-2). Не стоп-фактор: Δ-инвариант (+1 файл, +4 кейса) не зависит от базовой точки.

## ЗАЩИТА (сильные стороны)

1. **Минимальность β безупречна:** 3 сайта `get()` (store:45/:85, bridge:17) оживают без единой правки в них; bridge получает экземпляр store'а по построению (тот же static). α (смена сигнатуры моста) и γ (новая абстракция) честно хуже.
2. **P3 — единственное решение, следующее из кода:** у store нет node-источника (vocalsGain вне store), из доступного API остаётся только `initFromMic()` (сам берёт стрим :66-74). Комментарий-сторож о двойном захвате в V2 — по месту.
3. **Канон-инвариант — настоящий стоп-кран:** ≠292 (в обе стороны), ≠765+0int+0load, ≠64 файла, дифф вне 3 файлов = СТОП. Подгон чисел запрещён. Любая дыра исполнения (включая (e), если бы У-1 не применили) упирается в жёсткий стоп, а не в тихий дрейф.
4. **P2 каноничен и безопасен** (см. опровержение (a)); порядок `_unsub→destroy` сохранён как инвариант, кейс-3 его сторожит.
5. **Frozen не затронут:** AudioEngineV2.ts, patchV1.ts, src/bridges/*, track.orchestrator.ts не входят в дифф-план P1-P4 ни одной строкой.
6. **Честность рамки:** Круг-3 переписал «UX-улучшение» в «tsc-зелёный + контракт ARC-2e» после Удара-3 — сверка подтверждает (PitchModule не смонтирован, pianoKeyboard мёртв). Цепь умеет признавать промахи — а Суд нашёл то, что цепь проморгала даже после перепроверок.

## УСЛОВИЯ/ЧЕК-ЛИСТ ДЛЯ OPERATOR

**У-1 (БЛОКЕР, ДО старания P4):** заменить формулировку кейса-4 в P4. Старая: `await expect(startPitch()).rejects.toThrow('audioEngine.audioContext not found')` — НЕВЫПОЛНИМА (store глотает, :72-77). Новая (применить дословно, свойство кейса неизменно — poison-путь β без моков):
```
кейс-4 «poison-путь БЕЗ моков (стресс, главный для β)»: НОЛЬ spy-моков на engine.
Окружение jsdom: window.audioEngine undefined → реальный initFromMic() бросает в _getContext() (engine.ts:49).
Действия: `await startPitch();` (промис РЕЗОЛВИТСЯ — store глотает в catch, это контракт :72-77).
Ассерты:
  1) store `status === 'error'`, `error === 'audioEngine.audioContext not found'`;
  2) `PitchEngine.get().status === 'idle'` (poison-путь :102→:104→:241 отработал на реальном коде — singleton не отравлен);
  3) повторный `await startPitch();` снова резолвится с `status === 'error'` (guard :41-43 пропускает 'error', НЕ зависает в 'starting'; initFromMic честно бросает снова).
```
Число кейсов (4) и витest-таргет 765 НЕ меняются — правится только невыполнимый ассерт.

**У-2:** стартовая база тест-файлов по снапшоту (63) расходится с фактическим glob-счётом по include-паттерну vitest (66; *.spec=0, *.test.tsx=0). Operator работает по Δ-инварианту: файлов +1 ровно (новый `pitch-store.test.ts` — единственный новый), тестов +4 ровно. НЕ «подгонять» абсолютные числа; расхождение базы фиксирует 007 для Центра (коррекция канон-снапшота или пояснение метода счёта) — молчаливое принятие запрещено.

**У-3:** P4 strict-чист ДО передачи в прогон: 0 неиспользуемых переменных/параметров (noUnusedLocals/noUnusedParameters), включая кейс-4 из У-1 и обвязку beforeEach (`PitchEngine.get().destroy()` + полный сброс store-стейта) из спеки.

**У-4 (канон, без изменений):** порядок P1→P4; точки БЕЗ правок: pitch.store.ts:45, :85, pitch-visual-bridge.ts:17, порядок stopPitch :80-85; frozen = 0 диффа; любые правки вне pitch-engine.ts / pitch.store.ts / pitch-store.test.ts = СТОП.

**У-5:** имя `get()` НЕ переименовывать в getInstance (3 сайта уже написаны; канон Круга-2/3 подтверждён Судом).

## РАМКА ВЕРИФИКАЦИИ ДЛЯ 007

1. **tsc-дифф ПО СОСТАВУ, не только по числу:** итог 292; из списка ошибок исчезли РОВНО 4 TS2339 (3×`Property 'get' does not exist` [pitch.store.ts:45, pitch.store.ts:85, pitch-visual-bridge.ts:17] + 1×`Property 'init' does not exist` [pitch.store.ts:49]); ошибок других типов/файлов НЕ добавилось. Любое ≠292 (293+ или <292) или новая ошибка ≠списка = СТОП и разбор.
2. **vitest:** 765 passed + 0 unhandled/int + 0 load-ошибок, 0 failed, 0 skipped; файлов +1 ровно (`pitch-store.test.ts` — единственный новый, 4 `it()`); в кейсе-4 — 0 spy-моков на engine (проверить grep'ом по телу кейса); кейс-4 ассертит `PitchEngine.get().status === 'idle'` после честного броска.
3. **Poison-кейс зелёный по сути, не по букве:** если кейс-4 красный — сначала проверить формулировку У-1 (rejects-ассект = ошибка спеки, не кода); код β при этом не считается сломанным, пока poison-цепочка :102→:104→:241 не нарушена.
4. **Отсутствие правок вне спеки:** git-diff только `pitch-engine.ts` (+9 строк P1 после :20, +1 строка P2 между :238 и :240), `pitch.store.ts` (строка :49), новый `pitch-store.test.ts`. Точки store:45/:85, bridge:17 — без правок (diff пуст по этим строкам).
5. **Frozen git-diff = 0:** `src/audio/core/AudioEngineV2.ts`, `patchV1.ts`, `src/bridges/*`, `src/services/track.orchestrator.ts` — ни байта.
6. **Флаг У-2:** сверить фактическую базу файлов тестов с 63; при расхождении ≠ Central-канону — отчёт Центру (не молчать).
7. **Рамка эффекта:** живого UX-эффекта НЕТ (PitchModule не смонтирован, pianoKeyboard мёртв) — приём в ARC-2e под видом «питч работает у юзера» недопустим; ARC-2a = tsc-зелёный + контракт.

---
*009 · Суд · Круг-4 ARC-2a. Факты сверены лично (read/grep/stat, сборки не запускались — числа канона зафиксированы цепью). Ни один файл, кроме данного отчёта, не изменён. Frozen не тронут. HEAD d99609a.*
