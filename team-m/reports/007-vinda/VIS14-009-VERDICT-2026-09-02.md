# VIS14-009 · НЕЗАВИСИМЫЙ ВЕРДИКТ по цепи VIS-14 (001→002→001-финал) · 2026-09-02/03

**009 · Суд Независимый.** База: три отчёта кругов + живое дерево HEAD 23bed3d. Канон-снапшот диспатча: tsc=290 · vitest 68 файлов / 808 тестов · PARITY PASS · frozen 17/17. Read-only: правок нет. Выборочная верификация: 5 клеймов по живому дереву (все точны) + 2 контрольных.

---

## ВОПРОС
Готов ли «VIS-14 A/B/C-стенд» (спека круга-3 v1.0) к применению Оператором?

## ЗА
Архитектура «две машины, одно C-ядро» прошла стресс 002 без изменения сути: все 7 патчей — условия/поля/ассерты, ни один не трогает протокол пака; 5/5 проверенных клеймов финала точны по живому дереву, включая критические (no_pitch-расхождение, erratum-якоря, канон 68).

## ПРОТИВ
SSOT-прогон `vitest --run` не выполнен НИ ОДНИМ кругом — рантайм-базлайн гейта Δ0 не зафиксирован, есть только статика; версия пака в спеке (v1.4) расходится с основанием-задачей диспатча (v1.1), и это не заметил ни один круг.

## ДОКАЗАТЕЛЬСТВА (выборочная верификация по живому дереву 23bed3d)

1. **Якоря yin-detect.ts — все точны:** gate `:23`/`:66-69` · CMND `:75-83` · fallback te=minTau `:104-107` · subharmonic `:126-135` · autoCal-триггер `:147-149` · autoCal `:193-198` · octLock `:163-172` · rangeClamp `:174-179`. Erratum финала = истина.
2. **no_pitch-расхождение подтверждено:** `yin-detect.ts:104-107` (te=minTau → «питч из шума», conf 0.2–0.5) vs `yin-processor.js:132-135` (честный no_pitch, return). Клейм aSemantics обоснован фактами, не декоративен.
3. **Якоря pitch-engine.ts — все точны:** singleton `:27-31` · _initGen `:44` · gen-гварды `:72`/`:89` · fftSize 2048 `:156` · setInterval 46 `:165` · _computeSFM `:184-200` · _passiveTick `:202-233` · detect `:208` · refcount `:260-263`.
4. **PitchTab.tsx (372 строки):** isMatch `:338` · controls `:342-367` · PianoKeyboard `:369` · **`:378` не существует** — мета-находка круга-3 о дрейфе якорей самого 002 верна; `:226-242` = vocal-useEffect (спека-1 лгала о семантике mic-блока, erratum исправил); mic-блок `:281-310`.
5. **Канон-арифметика:** `vitest.config.ts:10` include = `src/**/*.{test,spec}.{ts,tsx}` + `src/**/__tests__/**` — статический glob даёт **68 файлов**; erratum-2 (68≠69) подтверждён; цель 68+4=72 файла / 808+14=822 теста сходится.
6. **Контроль-A:** `main.tsx:284-285` — прецедент `import.meta.hot.dispose` существует; FM-⑤a опирается на реальный прецедент.
7. **Контроль-B:** канон-снапшот диспатча (tsc=290 · 68/808 · PARITY PASS · frozen 17/17 · HEAD 23bed3d) согласован с BASE финала; противоречий с каноном нет → «БЛОКЕР: нет канона» не возникает.
8. **Вариант, не рассмотренный ни одной стороной (правило неоспоримости):** диспатч называет основание «MICRO-PACK-VIS14-PITCH-AB **v1.1**», все три круга ссылаются на «пак **v1.4**»; круг-2 сам пишет «пак v1.1 недоступен» — цепь внутренне несовместна в версии, вещество протокола (§2 [У5], §4, warm-up 1.5с) цитируется согласованно, но ярлык версии никем не сверен.

---

## ① ЦЕПЬ КОРРЕКТНА? — ДА
Процедура соблюдена: дизайн (001) → стресс с конкретными сценариями отказа и вычеркнутыми фальшивыми ударами (002) → финал с полным поглощением патчей FM-①…⑦ + erratum ЯКОРЕЙ ОБОИХ КРУГОВ (001-финал). Мета-находка «верификатор тоже дрейфует» (несуществующее `:378`) проверена и подтверждена — цепь самоисправляется. Все 7 находок 002 имеют статус в финале; 3 вычеркнутых удара не воскресли.

## ② СПЕКА ГОТОВА? — ДА, как имплементационная база
При условии, что FM-патчи исполняются как ОБЯЗАТЕЛЬНЫЕ пункты (не опции), а до первого коммита выполнен SSOT-прогон (см. У-1). Арифметика цели (72/822) и erratum-якоря верифицированы судом по живому дереву.

## ③ УСЛОВИЯ / РИСКИ

**Условия применения (нарушение любого = возврат):**
- **У-1 (SSOT):** прогон `vitest --run` на чистом дереве ДО имплементации; рантайм-число главнее статики 68; расхождение → коррекция спеки одной цифрой до первого коммита.
- **У-2 (вердикт-защита):** PV-6 CMND-инвариант — первый реализуемый тест; без него «пак-санкционированное дублирование» CMND = неуправляемый дрейф (прецедент расхождения yin-detect↔yin-processor живёт в дереве).
- **У-3 (вердикт-честность):** results-JSON обязан нести `sr` + `autoCalAt` + `aSemantics`; без любого — вердикт слеп (артефакт sr / излом autoCal / fallback-штраф A).
- **У-4 (прод-целостность):** правило §2 [У5] подтвердить Боссом ДО прогона; при победе C микропак проводки ОБЯЗАН включать worklet-близнец C ИЛИ feature-гейт «mic на A» + скрытие isMatch (`PitchTab.tsx:338`) — иначе двухголовый прод.
- **У-5 (версия пака):** сверить v1.4 (спека) vs v1.1 (диспатч) до первого коммита — см. Вопрос-1.
- **У-6 (frozen-дисциплина):** yin-detect.ts — только импорт; после сборки — проверка `dist` на отсутствие pitchy (двойной гейт lazy+DEV+dynamic оставляет остаточный риск).

**Остаточные риски (приняты, управляемы):**
- pYIN-CPU не измерен: >25% тика (11.5ms) → C отклоняется по §4; стенд меряет первым делом.
- Стемы вне git → ΔChroma-гейт = локальная дисциплина оператора (`SKIPPED-NO-STEMS`, exit=0 с маркером, не молча).
- HMR-dispose непроверяем юнитом (PS-2 расширен на create/destroy, но HMR-сценарий — ручная DEV-дисциплина).
- PV-2 зависит от синтетики — закрыт двойным ассертом (падение станет понятным, не загадочным).

## ④ ВЕРДИКТ

**РЕШЕНО С УСЛОВИЯМИ.**
Цепь корректна, спека готова к применению Оператором — при исполнении У-1…У-6 как обязательных пунктов первого коммита; возврат при нарушении любого.

*Почему одной строкой: все проверенные доказательства сходятся (якоря, расхождение семантик, 68 файлов, арифметика цели), ни один патч 002 не меняет суть, а два незакрытых пункта (SSOT-прогон, версия пака) — условия первого коммита, не переделка архитектуры.*

## ⑤ ВОПРОСЫ БОССУ

1. **Версия пака:** основание-задача = v1.1, спека = v1.4. Какая версия канон? Имплементация обязана бить по актуальной версии протокола §2/§4. (Не заметил ни один круг — 002 работал по цитатам, пак был недоступен.)
2. **LFS для стемов** (тогда ΔChroma-гейт работает в CI) или принять `SKIPPED-NO-STEMS` + локальную дисциплину оператора?
3. **noiseScore в витрине:** финал вычеркнул (прод-история `PitchEngine._computeSFM :184-200`). Нужен ли Никите для контента? Если да — отдельный микропак, не молчаливое подразумевание.
4. **Три 🔴 спеки:** (а) правило §2 [У5] как канон ДО прогона — чистая конъюнкция, «ничья» вычеркнута; (б) смоук-неделя = чек-лист сценариев + критерий отката «любой live-брек C → A без обсуждения»; (в) FFT-X рождается с инвариантом «FFT-CMND ≡ direct-CMND» (~1e-6) — иначе третий источник правды.

— 009 · Суд Независимый · VIS-14 · верификация по живому дереву 23bed3d · 2026-09-03 · read-only, правок нет
