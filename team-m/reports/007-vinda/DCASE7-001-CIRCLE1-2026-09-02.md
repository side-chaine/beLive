# DCASE7-001 · КРУГ-1 · АРХИТЕКТУРА FOUC-ГЕЙТА (001)

**001 · 2026-09-02 · HEAD b3cd866 · канон 282/803+0/67/frozen 2/2.**

## РЕШЕНИЕ
**Гибрид (в):** механика в lyrics.store — АДДИТИВНЫЙ блок ПОД :85 (ноль правок строк BAC-001), потребление в RehearsalLyrics (+2 строки: селектор structurePending + guard после :764). НОВЫЙ флаг читает ТОЛЬКО RehearsalLyrics — NowPlaying/LiveSubtitle/WagonTrain/KaraokeLyricsBoard тайминг не меняется (им raw-lines нужны раньше — остаются как были).

## СЕМАНТИКА
Окно «лирика готова, структура не отзеркалена» → return null (~300мс, не skeleton). Дискриминатор «структура ожидается» = **window.lyricsDisplay.textBlocks.length > 0** (тот же источник, что blocks-events) — НЕ hasBlockScenes (флаг картинок IDB, ошибка скаута 007 исправлена 001). Трек без textBlocks → false мгновенно (текущее поведение). Watchdog **500мс** (ожидаемый лаг 300мс blocks-events:47-49 + запас; 5000мс BAC-001 не подходит — там сигнал неограничен) → по истечении raw-рендер (деградация к текущему, не пустота).

## СОБЫТИЕ
Релиз = **прямая подписка на useBlocksStore** (blocks.length>0 → сброс watchdog + false; blocks.store чист, цикла нет). НЕ block-scenes-loaded (пайплайн картинок, не текст-структура — ошибка скаута исправлена). Второй track-loaded-listener: blocks уже пришли (lyrics-rendered-синк blocks-events:52-54) → false сразу. before-track-change → ре-арм.

## ТЕСТЫ
src/stores/__tests__/lyrics-structure-gate.test.ts (паттерн pitch-store.test, fake timers): К1 холд+релиз+ре-арм · К2 без структуры (мгновенно) · К3 watchdog 500мс. **Δ: vitest 803+3 / 67+1 · tsc 282 Δ0.**

## СТОП-ГЕЙТЫ
tsc 282 Δ0 · vitest 806/68 · frozen 2/2 · PARITY · смоук Никиты: ① структурированный трек — нет сырого текста, плашка ≤300мс; ② трек без блоков — raw сразу; ③ свитч подряд — не залипает; ④ второй трек — гейт ре-армится.

## ОТСТУПЛЕНИЯ ОТ СКАУТА (приняты, ошибки 007)
(i) hasBlockScenes ≠ displayBlock-детерминант · (ii) строитель = blocks-events-зеркало, не block-scene.service · (iii) block-scenes-loaded ≠ событие гейта.
