# DCASE7-000 · СКАУТ-ПАКЕТ Д-КЕЙСА 7 (FOUC-return: сырой текст до плашки/TrackMap) · 2026-09-02 · 007

**Собран:** 007 лично (rg/read по живому дереву HEAD `feb3e47`; логи смоука Никиты 17:5x в руках) · Входы: гипотеза 201-Н-3 (17:50) + моя дельта 18:10 (фантом-флаг) + история BAC-001 (184a3a3).
**Для:** цепи 001→002→001→009 (код-гейт FOUC) · все правки — в НЕ-frozen файлах (OVERRIDE не нужен).

---

## 1. СИМПТОМ (Никита, Д-КЕЙС 7 / 56f13f6)

Вспышка «сырого текста в центре» после загрузки трека, ДО формирования lyric-плашки/TrackMap. История: BAC-001 (гейт-по-стору + watchdog 5с, `184a3a3`) закрыл раннюю вспышку, но симптом жив.

## 2. АРХЕОЛОГИЯ ГЕЙТА (моё расследование, file:line)

**BAC-001-механизм (жив, работает):**
- `lyrics.store.ts:25-85`: ранние `lines` (raw mirror от загрузчика) буферизуются, пока `lyricsReady=false` (:70-75 hold-ветка); гейт открывает **document `track-loaded`** (:63) → flush буфера в стор (:76-80).
- `RehearsalLyrics.tsx:764`: `if (!lyricsReady || lines.length === 0) return null` — до гейта компонент скрыт. Гейт кроет ВСЕХ потребителей lines (NowPlaying, LiveSubtitle, WagonTrain...) — без правок frozen.

**Тайминг открытия (V3DataInterceptor, loadTrack-хвост):**
- `:200` `__setLoadingV3(false)` — ФАНТОМ (сеттер нигде не определён, 3 вызова = no-op; дельта 007 18:10).
- `:224` AudioBus.trackLoaded + `:231` document `track-loaded` → **в этот же тик** стор-гейт открывается и flush'ит строки.

**Найденная дыра (где рождается «сырой текст»):**
- `RehearsalLyrics.tsx:767+`: после открытия гейта рендер зависит от `displayBlock = getActiveBlock(blocks, ...)`. **Если blocks/сцены ещё не построены** (block-scene.service:355 слушает ТОТ ЖЕ track-loaded и строит сцены ПОСЛЕ в очереди: лог Никиты `[BlockScene] Preloaded 0 block + 52 line scenes` идёт после `V3 loaded`) → `displayBlock=null` → рендерится **else-ветка = сырые строки lines.map по центру** — ровно симптом.
- Окно: «гейт лирики открыт (track-loaded) → блоки/сцены ещё строятся (async, мс-десятки/сотни)» = вспышка raw-текста.
- 201-Н-3 «порядок двух строк :200/:224» — НЕТОЧЕН: перестановка ничего не даст (флаг-фантом, и событие-то уже «позднее»足够). Реальная причина: гейт лирики и строитель плашки слушают ОДНО событие, но строитель асинхронно позже → else-ветка мигает.

## 3. ФАКТЫ-ОГРАНИЧЕНИЯ ДЛЯ ДИЗАЙНА (001 обязан учесть)

1. **Треки БЕЗ сцен:** `hasBlockScenes=false` (RehearsalLyrics:57) → displayBlock=null всегда → else-ветка = ЛЕГИТИМНЫЙ рендер лирики (без плашки). Гейт «ждать displayBlock всегда» СЛОМАЕТ треки без сцен. Правильный гейт = «ждать сцен ТОЛЬКО когда они объявлены»: `hasBlockScenes && !displayBlock → скрывать/плейсхолдер` (секундный таймаут-фолбэк как watchdog-паттерн BAC-001).
2. **Уровень правки:** два кандидата — (а) RehearsalLyrics (else-ветка: рендер raw-lines → null/skeleton при hasBlockScenes-ожидании; файл живой, правился ARC-2a/e) ИЛИ (б) lyrics.store (не открывать гейт до `block-scenes-loaded`-события — есть! block-scene.service диспатчит, useBackgroundManagers:184 слушает). (б) кроет всех потребителей разом, но меняет семантику гейта для NowPlaying и др. (им raw-lines, может, нужны раньше?) — проверить потребителей.
3. **Событие готовности сцен существует:** `block-scenes-loaded` (document-CustomEvent, block-scene.service; useBackgroundManagers.ts:184 слушает) — можно слушать в сторе/компоненте.
4. **Watchdog-паттерн уже в сторе (:40-58)** — расширить тем же стилем (5с safety-net, чтобы не спрятать лирику навсегда на треках, где сцены не строятся).
5. **Смоук Никиты в логах:** сцены строятся (`Preloaded 0 block + 52 line scenes`) — т.е. мигание = окно между track-loaded и block-scenes-built, подтверждено живым порядком логов.

## 4. ВНЕ ДОКУМЕЙЛА (риск-реестр 002 для атаки)

- `__setLoadingV3` фантом — трогать/удалять ли (мусор) ИЛИ определить (живой индикатор) = отдельное решение; в скоупе Д-КЕЙСА 7 = только фикс FOUC, не чистка.
- 201-Н-2: 8 меток performance.mark в том же файле = отдельный микропак (синергия, но НЕ в одном прогоне с FOUC-фиксом — один баг = один поезд).
- Н-4 (хвост compat track-fully-loaded) — не трогать.

## 5. КАНОН

HEAD `feb3e47`: tsc=282 · vitest 803+0int+0load/67 · frozen 2/2 · PARITY PASS. Гейты фикса: канон-инвариант (Δ0), новые тесты на гейт-механику (по паттерну pitch-store.test BAC-эпохи).

— 007 · скаут-пакет Д-КЕЙСА 7 · цепь: вперёд
