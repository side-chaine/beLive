# OBS-1-000 · СКАУТ-ПАКЕТ «ОБСЕРВАТОРИЙ ЗАГРУЗКИ» (метки ступеней + фантом-флаг) · 2026-09-02 · 007

**Собран:** 007 (входы: диспатч 200 «007 atom-map code path» (aba2c69, концепция LOADING BUILDING) + эскиз 201 (Н-1/Н-2/Н-4, `28-ZDANIE-ZAGRUZKA-ESKIZ`, мост) + мой факт фантом-флага (дельта 18:10, подтверждена цепью Д-7) + досье 006 (§5)).
**Скоуп:** ТОЛЬКО инструментация в одном файле — `src/audio/engine-v3/integration/V3DataInterceptor.ts` (250 строк, весь путь загрузки; НЕ frozen). Рантайм-поведение не меняется.

---

## 1. ЧТО НАБЛЮДАЕМ (10 ступеней, file:line по 201 + мои сверки)

| # | Ступень | file:line | Хронология |
|---|---|---|---|
| 1 | before-change → setLoadingV3(true) | :58 | **бесплатно** (document-событие `before-track-change`) |
| 2 | стоп/сброс прошлого | :62 | ТЁМНО |
| 3 | сборка jobs | :66 | ТЁМНО |
| 4 | decode ×N (+ретрай 120мс :105) | :91-113 | ТЁМНО |
| 5 | WASM reset | :120 | ТЁМНО |
| 6 | WASM load ×N | :127-155 | ТЁМНО |
| 7 | активация + play (таймаут 5с :167) | :160-185 | ТЁМНО |
| 8 | загрузка завершена (фантом-флаг :200) | :200 | ТЁМНО |
| 9 | dispatch track-loaded | :220-247 | **бесплатно** (двойной транспорт Н-1) |
| 10 | хвост compat (stem-ready ×N, fully-loaded) | :236-247 | ТЁМНО частично (monitor-stream у 006) |

201-Н-2: бесплатно только 2 границы из 10 → **осветить = 8 меток performance.mark() в одном файле**.

## 2. ФАНТОМ-ФЛАГ (мой факт, подтверждён цепью Д-7)

`__setLoadingV3` вызывается :58/:116/:200 через `(window as any).__setLoadingV3?.(...)`, НИГДЕ не определён → все 3 = молчаливый no-op. Варианты: (а) удалить 3 вызова (ноль мусора; поведенческих читателей нет — верифицировано rg по src/+js/+index.html) · (б) определить сеттер (живой индикатор — продуктовое решение, вне скоупа OBS-1). **Рекомендация 007: (а) удаление, метка в комментарии.**

## 3. ИМЕНОВАНИЕ (техническое; город-класс ЗГ-* ждёт решения Никиты — НЕ смешивать)

Марки performance.mark (WHATWG): `belive:load:02-reset` … `belive:load:09-dispatch` (8 шт) + на :236-247 хвост — `belive:load:10-compat-done` (после последнего события; опционально 9-я). Консьюмер: `performance.getEntriesByType('mark')` / `getEntriesByName` — 006 синхронизирует с видео-таймкодами (§8bis ТЗ), бЛб-обсерваторий строит хронологию. БЕЗ performance.measure (пары даст консьюмер; минимализм).

## 4. ОГРАНИЧЕНИЯ

1. **Рантайм-нейтральность:** performance.mark = O(1) запись в буфер; НЕ менять порядок/логику ступеней. Защита try{}catch{} как у соседних строк (единый стиль).
2. **Буфер браузера:** performance buffer по умолчанию 150 entries — при частых загрузках треков старые метки вытесняются: допустимо (обсерваторий читает свежую сессию) ИЛИ clearResourceTimings после чтения — 001 решает (минимализм: не трогать).
3. **Тесты:** marks не наблюдаемы unit-тестами без perf-API мока — канон vitest 811/68 Δ0 ожидаем; tsc 282 Δ0. Проверка = консоль-лог? НЕТ — марк без логов (буфер читается по требованию). В смоук: `performance.getEntriesByType('mark').filter(m=>m.name.startsWith('belive:load'))` в DevTools.
4. **001 фиксирует точные строки вставки** по живому дереву (201-номера от HEAD f40aa91 — могли уехать: Д-7-фикс :200→сдвиги).

## 5. ВНЕ СКОУПА

Н-5 (мёртвый catalog-канал) · Н-6 (хардкод-сны) · P1 досье 006 · ЗГ-класс маркеров (решение Никиты) · Волна D.

— 007 · OBS-1 скаут-пакет · instrumentation-only · цепь: вперёд
