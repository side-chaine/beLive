# 🎨 МИНИ-ПАК 3(c) · MODE_COLORS RECONCILIATION · ПОЛОВИНА 005 (дерево-факты) · 2026-09-05

**Контракт:** UI-CHANGE-SPEC (Никита+GPT) · Направление v2 CEO_1 · Совместная зона 3(c): 005 = карта живого дерева, 005_2 = наука цвета поверх карты. ≤150 слов/пункт.
**Метод:** скаут-греп живого дерева HEAD 601d026 (полный отчёт скаута — в этом файле ниже). Ноль мнений.

## ВЕРДИКТ-СТРОКА (для CEO_1)

В дереве живут ТРИ НЕЗАВИСИМЫЕ цветовые системы, не знающие друг о друге; brand-пара #38A8FF/#FF5B5B существует ТОЛЬКО в спеках (в коде 0 вхождений). Reconciliation = не «подкрутить», а выбрать ОДНУ систему-носителя и погасить две.

## ТРИ СИСТЕМЫ (факты file:line)

**СИСТЕМА-1 · JS MODE_COLORS — inline-константа, скопированная в 9 компонентов.**
`Record<string,string>`: concert=#3498db (синий) · karaoke=#9b59b6 (фиолет) · rehearsal=#FF8C00 (оранж) · live=#e74c3c (красный). Живёт: Header.tsx:8, NowPlaying.tsx:6, TransportBar.tsx:9, CurrentTrackBadge.tsx:6, VolumeControls.tsx:8, ControlPanel.tsx:9, QuickActions.tsx:14, AIChatPanel.tsx:5, CatalogPanel.tsx:7 — 4 копии идентичны. Применение: только inline-style (`borderBottom: 1px solid ${color}30` Header:49; транспорт: градиент `${color}44→${color}` + glow + ползунок TransportBar:97-114). **VolumeControls:126,128 — слайдеры Inst/Voc красятся РЕЖИМНЫМ цветом** (в rehearsal = оранж) — рядом ControlDeck-фейдеры Inst/Voc хардкод-красно/синие. Мёртвое: NowPlaying.tsx:27 color присвоен, не используется.

**СИСТЕМА-2 · theme-движок токенов — ПЕРЕВЁРНУТЫЙ маппинг относительно Системы-1.**
theme/default.ts:22-39 modes: **rehearsal=#3498db, concert=#e74c3c** — то есть rehearsal здесь СИНИЙ (в Системе-1 оранж!), concert КРАСНЫЙ (в Системе-1 синий!). Токен-слой живой: theme/tokens/{primitive,semantic,component}.ts + css-injector.ts:94-99 applyMode() → `--bl-accent`/`--bl-accent-text`/`--bl-surface-base` + data-mode на :root. Токены --bl-* без дефолтов в :root, все потребители с fallback. Два параллельных стора: useModeStore (mode-events.ts:77, body class mode-X) и useThemeStore.activeMode — синхронизация между ними из кода неочевидна (оба дефолт = rehearsal).

**СИСТЕМА-3 · Хардкод rgba в трёх гнёздах (вне систем).**
ControlDeck.tsx:243 Inst `rgba(255,60,60,0.3)`=#FF3C3C · :309 Voc `rgba(74,158,255,0.3)`=#4A9EFF (0 обращений к MODE_COLORS в ControlDeck!) · SyncEditorPanel.tsx:1232 прогресс-полоска #4A9EFF · PitchTab.tsx:44,71-73 СВОИ токены `--bl-c-pitch-*`: mic #F97316 оранж / vocal #3B82F6 синий / match #22C55E зелёный.

## РАЗРЫВЫ ПРОТИВ СПЕКИ (факты, не решения)

1. **#38A8FF/#FF5B5B в коде отсутствуют** — бренд-пара спеки ещё не родилась в коде; наследники-зародыши = #4A9EFF (Voc) / #FF3C3C (Inst) ControlDeck:309/243.
2. **Спека-база #080A10 отсутствует**: в токенах neutral5 #0a0a0a (primitive.ts:10-23), в коде #0a0a0a/#0d0d0d/#0e0e18.
3. **Семантический конфликт двух синих в соседних компонентах:** VolumeControls (режимный слайдер) vs ControlDeck (brand-зародыш Voc) — в rehearsal юзер видит ОРАНЖЕВЫЙ слайдер и СИНИЙ фейдер рядом.
4. **Перевёрнутый маппинг С1↔С2** — один и тот же режим красит UI двумя разными цветами в зависимости от того, какой стор заполнил компонент.
5. **PitchTab живёт в четвёртой палитре** (оранж-синий-зелёный) — питч-семантика уже имеет СВОЙ язык (совпадение с Системой-1 rehearsal-оранж #FF8C00 ≈ #F97316 — случайное).

## ЗАПРОС К 005_2 (наука цвета поверх этой карты)

1. Какая система-носитель правильна для brand-пары (токен-движок С2 = CSS-переменные и a11y, или inline С1 = текущий габитус)? Твой вердикт по 3(a) oklch + 3(b) WCAG прямо сюда.
2. Семантика режимов: должен ли РЕЖИМ вообще красить транспорт/слайдеры (С1-паттерн), или режим = только тонкая метка (badge/border), а器官у — brand-пара? (Это продуктовое, но твоё 3(c)-соавторство.)
3. Reconciliation live=#e74c3c (С1) vs красный brand #FF5B5B: «live»-режим и «LiVE»-бренд — конфликт имён или совпадение по смыслу?

## ЧТО НЕ ДЕЛАЛ

Архитектуру выбора не предлагаю (001 после GO), код не трогал, решения о цветах — наука 005_2.

— 005 · половина 3(c) сдана · карта живого дерева · скаут-пас 05.09
