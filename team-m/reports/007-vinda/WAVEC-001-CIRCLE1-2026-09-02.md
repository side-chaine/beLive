# WAVEC-001 · КРУГ-1 · АРХИТЕКТУРА ВОЛНЫ C (001)

**001 = CEO-Архитектор · 2026-09-02 · Канон: tsc=290 (8 в мостах) · vitest 808/69 (5 тестов в мостах) · HEAD 0053e54.**

## РЕШЕНИЕ (суть)

16 коммитов: #0 prep (bridge-manifest.json + паритет-гейт на манифест + main.tsx:62 чинит «10 GREEN») + 15 сносов (атомарная четвёрка: мост + тест-файл + минус-SHA frozen-манифеста + статус-флип bridge-манифеста). Порядок: audio-пилот → SAFE-девятка → audio-reactive → lyrics → stem-reactive → monitor → mode-switch+DOC-конвой v2.1 (тем же коммитом).

## КЛЮЧЕВЫЕ ВЕРДИКТЫ

1. **bridge-manifest.json:** корень; {id, bridge, wrapper, status: dual-fire|retired|orphaned-wrapper|live, retiredAt: commit-sha, testsRemoved[], knownGaps[], notes}; frozen=замок (file-SHA), bridge=летопись (commit-SHA) — дублирования нет. Включает live-guard + rehearsal-trigger (live-статусы) — полная инвентаризация слоя.
2. **Паритет (ловушка-11):** #0 переключает verify-bridge-parity.ts на манифест-эталон (каждый LEGACY-ключ → живая подписка wrappers; каждая не-live запись → wrapper-файл с ≥1 subscribe; bridges-glob-скан удаляется) ДО первого сноса; новый паритет обязан PASS на HEAD; легаси-ключи без обёртки = true-residue в allowlist с обоснованием.
3. **mode-switch = (б):** снести мост + 3 теста, обёртку оставить orphaned-wrapper. Факты: mode-switch.service:38 сам диспатчит mode-changed, setBodyMode красит classList ДО диспатча; mode-events:81 (в буте) уже подписан на тот же канал → (а) = тройной дубль. Обёртка — кандидат retire следующей волной (notes).
4. **monitor-блокер закрыт:** mix._persist = monitorPersist монтирует ТОЛЬКО мост (monitor.bridge.ts:93), мост вне бута → патч сегодня не применяется → снос runtime не меняет. **monitor.state.ts после C = 0 импортёров (транзитивный труп) — residue-заметка, решение следующей волне.**
5. **Δ-таблица:** #1 audio 0/−2t−1f→806/68 · #2-10 SAFE 0/0 · #11 audio-reactive −1→289 · #12 lyrics −1→288 · #13 stem-reactive −1→287 · #14 monitor −1→286 · #15 mode-switch −4/−3t−1f→803/67 + tsc 282. Финал: tsc 282 · vitest 803/67 · frozen guard2/graveyard0 · bridges/ = live-guard only.
6. **Стоп-гейты по коммиту:** атомарная четвёрка · tsc/vitest = бегущие значения Δ · frozen PASS (graveyard −1) · parity PASS · 0 новых внешних импортов bridges/.

## ОТСТУПЛЕНИЯ ОТ СКАУТ-ПАКЕТА 007 (приняты)

- §5.4(а) — classList красит живой сервис + mode-events уже в буте → (а)=дубль, вердикт (б) сильнее.
- Новая находка: monitor.state.ts транзитивный труп (не блокер).
- Паритет-переключение формализовано коммитом #0 с гейтом «новый PASS на HEAD».

— 001 · круг-1 · для 002-стресса · squad-memory: скаут-пакет 007 + досье 003_2 + Соннет-r2 + конвой v2.0
