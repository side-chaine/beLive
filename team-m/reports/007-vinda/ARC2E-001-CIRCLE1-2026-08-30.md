# ARC-2E-001 · Круг-1 · Скоуп+дизайн питча на V3-тапы · 2026-08-30

**BASE: HEAD `9b6bf83` · канон tsc=293🔴 / vitest 801+0int+0load 🟢 (67 файлов) · PARITY PASS 🟢 · V3=дефолт**
**Зона прогона: pitch-модуль, PitchTab, pitch.store, HPS (не frozen ✅ — в FROZEN-списке только AudioEngineV2/patchV1/bridges/track.orchestrator), MonitorRouter, MicSourceV3, фасад, audio.store, ControlDeck.**

## СКОУП-РЕШЕНИЕ (что в slice / что отложено + критерий):

**В slice:**
1. **Vocal-тап** — ядро флагмана (референс-питч) И видео-моста: без живого vocal-тапа на студенте нечего сравнивать с педагогом (дельта питча ~0.4 КБ/с). Мост-критерий: ПОМОГАЕТ напрямую.
2. **Mic-дедупликация** — PitchTab-mic fallback (pitch-engine:82-85) создаёт ВТОРОЙ getUserMedia при живом REC — прямое нарушение инварианта mic-race-фикса 82e1c76 («один стрим на параллельные REC/🎤»). Мост-критерий: нейтрально, но ломает смоук СЕГОДНЯ (двойной захват = эхо-риск на student-машине). Цена фикса мала (один метод движка). ВКЛЮЧАЮ.

**Отложено (с честной записью):**
3. **Vmix-тоггл** — движковый `setVMix(on)` в MonitorRouter **уже существует** (:189-207: рампы 20ms + TASK-015b-гейт + событие vocalmix-state-changed). НЕдефицит — дефицит продуктовый: V2 `enableVocalMix` (vocal-mix) ≠ V3 `setVMix` (music-minus стерео, глушит defaultBranch :194!). Наивная проводка сломает пользователям основной микс. Решение Босса по семантике reference-listen не принято → отложить. UI-ложь TakesPanel:1012 задокументирована в фасаде (:142-143) — уже честно.
4. **Mic-volume** — движковый потребитель отсутствует; GainNode в static «0 disconnect»-графе MonitorRouter + мины G14 (mic-компенсация), F-2, TASK-015b (vmixMicIn-тап с micInput в обход будущего volume-узла). Мост-критерий: нейтрально. Косметика → следующий slice.

## ДИЗАЙН ПО ПОД-ЗАДАЧАМ (файл:строка → правка):

### A. Vocal-тап (флагман + мост)
- `HybridPipelineService.ts` (HPS, зона прогона):
  - **+1 строка-геттер** рядом с public getters (:132-138): `get vocalReferenceTap(): AudioNode | null { return this._vocalHallSource }`.
  - Выбор `_vocalHallSource` (НЕ `_vocalHallSend`): send-узел живёт с конструктора → без вокал-стема анализер читал бы тишину и ложно репортил hasVocals=true. `_vocalHallSource` = instance.outputNode, null до успешного chunkedLoad вокала (loadStem:220-225) → **сам является сигналом живости стема**, отдельный hasVocals-флаг не нужен.
  - **+1 строка в reset()** (~:696, рядом с `_deadStems.clear()`): `_vocalHallSource = null` — сегодня reset НЕ сбрасывает (сбрасывает только dispose:729) → после смены трека геттер вернул бы stale-узел прошлого трека (трек без вокала = вечная ложь). Устраняет retarget-гонку при быстрой смене трека.
- `PitchTab.tsx` tryInit (:239-265) — переподключение на V3:
  - `:241-242` вместо `ae?.vocalsGain` + `ae?.stems?.has('vocals')` → `const tap = (window as any).__belive?.pipeline?.vocalReferenceTap` (main.tsx:172 уже экспонирует pipeline).
  - `:243` `setHasVocals(tap !== null)` — та же семантика, один источник.
  - `:245` guard `if (!tap) return false`; `:248/:254` retarget/initFromNode(tap) — без изменений (движок вешает свой analyser fftSize=2048:133-136, pre-fader: instance.outputNode ДО stretchGain — mute/solo/volume НЕ влияют, фейдеронезависимый эталон Q6/BRG-4).
  - События: `track-fully-loaded` (:270, V3DataInterceptor:244 ✅) + `playback-state-changed` (:277, V3StatePublisher:130 ✅) — уже V3, НЕ трогаем.

### B. Mic-дедупликация (инвариант 82e1c76)
- `pitch-engine.ts` initFromMic (:76-86) — заменить блок поиска стрима: сначала `const micSource = (window as any).__belive?.micSource` → если жив, `this._stream = await micSource.acquire()` + `this._refNeeded = true` (новый приватный флаг, :42); иначе — существующий fallback getUserMedia (R9-константы), `_ownStream = true`. Импорт НЕ нужен (runtime-доступ), циклов нет.
- `pitch-engine.ts` destroy (:234-237) — ветка: `if (this._refNeeded) micSource?.release()` (вместо stop-tracks; трим `_refNeeded = false` в хвосте destroy, рядом :249).
- `pitch.store.ts` startPitch (:49) — дедуп получается АВТОМАТИЧЕСКИ (тот же initFromMic): REGISTRY-мина «двойной захват store+PitchTab» (комментарий :49) закрывается этим же изменением, store не трогаем. Комментарий :49 обновить текстом «решено ARC-2e: acquire-путь».
- Совместимость подтверждена: MicSourceV3.acquire() возвращает MediaStream (:33-47), refcount делим in-flight (:36) — REC + 🎤-питч получат ОДИН стрим; требование движка «MediaStream» удовлетворено.

## ТЕСТЫ (число X=4, таргет 801+4=805):

1. **HPS: vocalReferenceTap lifecycle** — null до loadStem('vocals'); AudioNode после; использует существующий mockAudioContext-паттерн (pipeline/__tests__ уже есть инфра). Покрывает: геттер + живость стема + мост-эталон.
2. **HPS: reset() чистит tap** — loadStem('vocals') → reset() → геттер null. Покрывает: retarget-гонку смены трека (трек без вокала).
3. **PitchEngine: initFromNode на фейк-узле** — mock ctx + GainNode: status='running', analyser подключён, passive-tick не бросает. Покрывает: vocal-ветку движка (первый тест движка за всю историю).
4. **PitchEngine: initFromMic → micSource.acquire** — mock `__belive.micSource` с acquire/release-счётчиком: getUserMedia НЕ вызван, acquire=1; destroy → release=1. Покрывает: инвариант 82e1c76 (один стрим), refcount-баланс.

PitchTab-компонент не тестируем (тяжёлый; вся логика ушла в движок/HPS — тестируемые единицы).

## САМОКРИТИКА/РИСКИ:

- **Stale-tap при смене трека**: лечится reset-строкой (тест 2); остаточное окно «reset→новый loadStem» даёт null → tryInit=false → UI-чип «—», не ложь.
- **Зомби-анализер при смене инстанса**: retarget движка (:263) сам disconnect старый анализер; окно до track-fully-loaded = тишина, безвредно.
- **MicSourceV3 отсутствует (V2-режим / V3 boot failed)**: acquire-путь не входит, fallback getUserMedia жив → деградация до статуса-кво, не хуже.
- ** pitching-visual-bridge / singleton-мина** (bridge:17 → window.pianoKeyboard мёртв; PitchTab-destroy убил бы singleton store): ВНЕ среза — PitchTab остаётся на локальных движках, singleton-путь (store-кнопка) мёртв UI-шно и не активируется. Зафиксировано как отсечение.
- **pitch.store destroy-баланс**: singleton.stopPitch зовёт destroy → release сработает только если store сам делал acquire (статусная защита :43 'running' не даёт двойного init).

## Требует решения CEO/Босса:

1. **Vmix-семантика (Босс)**: reference-listen (TakesPanel) глушит ВЕСЬ V-Mix (setVMix(false) с глушением defaultBranch=ON-инверсией) или только монитор-комфорт без program-truth? Без решения проводка в фасад = риск сломать микс.
2. **Mic-volume (Босс/001, следующий slice)**: куда класть gain — micInput (перед _micDelay, заденет vmixMicIn-тап :156) или новый узел после разветвления? Требует эскиза 3-узлового маршрута.

— 001 (Ювелир) · Круг-1 ARC-2e · 30.08.2026
