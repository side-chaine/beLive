# WAVEB-002-CIRCLE2 — Стресс-тест дизайна Круга-1 Волны B
**002 (Devil's Advocate) · Круг-2 · 2026-09-01 · HEAD `a8a3867` · канон: tsc=290(ошибок) · vitest=808+0int+0load/69 · frozen 18/18**

---

## ВЕРДИКТ: ТРЕБУЕТ ПАТЧА

Ядро дизайна (снос orchestrator+пары, DELETE-эффект, takes:108, duration-первичность) держит на доказательствах. Но инструкция №3 Круга-1 **сломает канон 808 как написано** (мок-чистка), а атом №1 неполон (note-строка манифеста + .gitattributes). Патчи точечные, суть волны не меняют.

---

## УДАРЫ (следствие + правка)

**УДАР 1 (тяжёлый — канон 808):** Дизайн №3 Круга-1: «сжать vi.mock до `{ getTransport: vi.fn() }`, убрать mockV2-объект и V2Adapter-ключ» → `mockV2` используется **4 живыми строками** теста: `:49 mockV2.getV2Engine.mockReturnValue({})`, `:50 expect(mockV2.delegateSync).not.toHaveBeenCalled()`, `:82` и `:99 mockReturnValue(null)` → удаление объекта = `ReferenceError` при первом же прогоне → падает весь файл stem-engine-sync.test → **канон 808−N, гейт волны красный**. ПРОВЕРКА: вход = `vitest run` после правки; ловит любая строка из четырёх. Удар реален. **Патч:** из vi.mock-фабрики (`:14-19`) удалить ТОЛЬКО ключ `V2Adapter`; `mockV2`-объект (`:11-14`) ОСТАВИТЬ (ассерты-тавтологии зелёны: vi.fn() никто не зовёт — и не должен, канал мёртв; пометить комментом «истор. заглушка, V2Adapter снесён Волной B»). Канон 808 Δ0.

**УДАР 2 (манифест-atom неполон):** Круг-1: «frozen-манифест 18→17, guard 3→2» — но note-строка манифеста зашита: `"Канон: 3 зоны · 18 файлов (FROZEN-AUDIT-301 §7, wc=5137)"` → после сноса verify:frozen печатает `17/17`, а note в ТОМ ЖЕ файле говорит «18 файлов · 3 зоны» → самопротиворечие в одном файле; 006-печать ловит. Плюс `.gitattributes:9` `src/services/track.orchestrator.ts -text` — мёртвый якорь на несуществующий файл. ПРОВЕРКА: вход = глаза 006/гейт-проверка текстов; реален (check-frozen.mjs note НЕ проверяет — человек-гейт, единственная защита = атомарность правки). **Патч:** атом №1 = guard-строка манифеста + note («2 зоны · 17 файлов», wc-строку пересчитать Оператору: 5137−592=4545 по формуле, факт — прогоном) + `.gitattributes:9` DELETE. Один коммит.

**УДАР 3 (гейт-хвост — список ×4 неполон):** Гейт волны «V2Adapter grep→0 в src/» НЕ закроется: `stem-engine-sync.ts:3` — шапка ФАЙЛА «Central Bridge: Zustand subscribe → V2Adapter.delegateSync» — выпала из списка ×4 Круга-1 (там position-sync:38, MonitorRouter:266, takes.time:22, WaveformCanvas:433/437). Это не истор-запись — это описание текущей работы файла, ложь уже сейчас (0 вызовов с W2b) и после сноса. ПРОВЕРКА: вход = grep-гейт волны; без правки гейт = ложный RED. **Патч:** добавить :3 в №7: «Central Bridge: Zustand subscribe → pipeline (V3) / legacy-движок».

**УДАР 4 (канон-шкала подменена):** Круг-1: «tsc 290→287 файлов / 0 ошибок» → канон-шкала 290 = **ОШИБКИ, не файлы** (доказательство: MICRO-PACK C «tsc 293→290, Δ−3 = 3 ошибки жили ВНУТРИ снесённых: TS2345/TS7016/TS6133»; DEFREEZE-A «ни одна из 290 ошибок не в patchV1»). Диспатч: «0 ошибок в сносимых» ⇒ все 290 живут в остающихся файлах ⇒ снос 3 файлов = Δ0. Прогноз «→287» = смена шкалы канона без полномочий, Оператор зафиксирует «дельту» там, где её нет. ПРОВЕРКА: вход = канон-строка отчёта волны; расхождение 287 vs 290 = ложный RED на приёмке. **Патч:** таргет = tsc 290 Δ0 (см. КАНОН-ТАРГЕТ).

**УДАР 5 (duration-цепочка: «V2-fallback сохранить» — наполовину дефект):** `ae?.audio?.duration` мёртв в ОБОИХ режимах: фасад не имеет ни `duration`, ни `.audio`, ни `getDuration` (grep audio-facade-v3.js = 0); настоящий V2 имеет `get duration()` (:1603) и `getDuration()` (:1737), но НЕ имеет поля `.audio` (grep — `.audio` только у StemPlayer.ts). Живой V2-fallback = только `ae?.getDuration?.()`. Сохранить `ae?.audio?.duration` = сохранить мёртвый член цепи. ПРОВЕРКА: вход = V2-режим + запрос duration → undefined → перескок к getDuration; безвреден, но врёт читателю. **Патч:** `const duration = (window as any).__belive?.pipeline?.duration ?? ae?.getDuration?.() ?? 0;` — dual-env честный, на 1 член короче. pipeline.duration подтверждён: HPS:149 геттер (max по стемам) + кэш `_lastKnownDuration` (:54, «защита от duration=0 при reset»); без трека = 0 — не хуже текущего.

**УДАР 6 (BusFader18:418 — имя врёт после сноса):** Тест «cage-инвариант: … канал V2Adapter.delegateSync работает при __v3Active» (`:418`) + шапка-покрытие `:14` — импорта V2Adapter НЕТ (grep import = 0), `delegateSync` = локальная `vi.fn()` (:425-428), тест ассертит свои же моки. После сноса имя описывает несуществующий канал. ПРОВЕРКА: вход = читатель теста; канон не падает (загрузка/ассерты не зависят), удар репутационный, но гейт «V2Adapter→0» его красит. **Патч:** в атоме №2+№3+№7 (текст-правка, канон 808 Δ0 — имя ≠ ассерт): `:418` → «cage-инвариант: гард задевает только ae.*, delegateSync-канал (истор., V2Adapter снесён Волной B)»; `:14` → «канал delegateSync не задет».

**ВЫЧЕРКНУТЫЙ УДАР (сам, до печати):** «Снос IV2PublicContract рвёт frozen AudioEngineV2 implements → новая tsc-ошибка в frozen → Δ0 невыполнимо». Проверка grep: `IV2PublicContract` в src/ = **только** V2Adapter.ts (:6,:12,:16,:26,:28 — сносимый) и сам файл. `implements IV2PublicContract` = 0 по всему src/. AudioEngineV2.ts интерфейс НЕ реализует. Удар фальшив — implements-цепи нет, frozen-файл не задет.

---

## УСИЛЕНИЯ

1. **Доказательство дефекта эффекта — усилить до «писал 1 в ЛЮБОМ режиме»:** `instrumentalGain`/`vocalsGain` отсутствуют у фасада (grep audio-facade-v3.js = 0) И у настоящего AudioEngineV2 (grep = 0, только VocalMix.ts-параметр) → `ae.instrumentalGain?.gain?.value ?? 1` = 1 всегда, эффект никогда не читал ничего реального → удаление = чистый фикс БЕЗ V2-регрессии, вопрос «а в V2-режиме работал?» закрыт навсегда. Заодно закрывает смоук-страх «слайдер СТАЛЕ до первого трек-евента»: эффекту нечего было терять — стале-сценарий невозможен, слайдер = zustand-селектор в render (:47-48), стор = user-intent, initStems обновляет при загрузке.
2. **Атом №1 расширить .gitattributes** (удар 2) — один коммит, ноль доп. движущихся частей.
3. **Гейт-формулировка против ретро-споров:** «V2Adapter runtime-imports → 0; текст-упоминания → 0 кроме истор-записей с явным маркером (истор.)» — прецедент M3 Д-3 («0 runtime-imports, комментарии = retain-класс») уже в каноне; иначе истор-комменты (:53/:67 test, rehearsal-trigger-writer:6) будут вечно красить гейт.
4. **Порядок коммитов — опция:** №4+№5 (SyncEditor, юзер-видимый фикс) не зависят от сносов — можно ПЕРВЫМ коммитом, дефект уходит в прод на шаг раньше. Не требую; выбор Босса.

---

## КАНОН-ТАРГЕТ 002 (числа с доказательствами)

- **tsc = 290 Δ0.** 290 = ошибок (MICRO-PACK C: «293→290, Δ−3 = 3 ошибки внутри снесённых»; DEFREEZE-A: «ни одна из 290 не в patchV1»). Диспатч: «0 ошибок в сносимых» ⇒ снос orchestrator+V2Adapter+IV2PC не трогает носители ошибок. Implements-цепи нет (grep: IV2PublicContract в src/ = 2 файла, оба сносимые; implements = 0). Прогноз Круг-1 «→287 файлов» — смена шкалы, ОТКЛОНЁН.
- **vitest = 808+0int+0load/69 Δ0** — при условии патча удара 1 (mockV2 остаётся, удаляется только V2Adapter-ключ фабрики) и переименования BusFader18:418 (имя ≠ ассерт, счёт тестов не меняется). Без патча удара 1: файл падает целиком → 808−(тесты файла) — НЕДОПУСТИМО.
- **verify:frozen = 17/17** (guard 2: AudioEngineV2 + live-guard; graveyard 15). Note-строка атомарно: «2 зоны · 17 файлов», wc-факт пересчитать прогоном (формула: 5137−592=4545). check-frozen.mjs правки НЕ требует (guard-список читает из JSON динамически; «отсутствует без удаления строки = ERROR» — самопринуждение уже в скрипте).
- **Гейт V2Adapter:** runtime-imports src/ → 0 (сейчас: index.ts:59 — единственный, grep); текст → 0 кроме (истор.)-маркированных.

## Что оставить

- Скоуп 7/7 и ядро правок Круга-1 — БЕЗ изменений: снос orchestrator (0 import-стейтментов), пары V2Adapter+IV2PC (0 живых вызовов), DELETE-эффект :102-110 (доказано: писал 1 всегда, в любом режиме), takes.recorder:108 (V3-ветка: pipeline.ctx + ae.audioContext живы, :109 V2-ветку не трогать), duration с pipeline-первичностью, 0 новых тестов (греп-инварианты вместо тавтологий).
- Порядок атомов Круга-1 (№2+№3+№7 → №1 → №4+№5+№6) — с расширением №1 (удар 2) и №7 (удар 3).
- Отложенное Круга-1 — подтвердить: rehearsal-trigger вне волны (диспатч явен).

**Итог: 6 ударов (1 канон-ломающий, 5 патч-обязательных), 1 вычеркнут сам (implements — фальшив), 4 усиления. Волна готова к 001-Кругу-3 после внесения патчей 1-3.**
