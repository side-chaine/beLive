# SLEPOK-009 · ВЕРДИКТ НЕЗАВИСИМОГО СУДА · цепь SLEPOK (001 круг-1 → 002 круг-2 → 001 круг-3) · финал-спека «SLEPOK-FINAL v1.0»

**009 = Independent Verification Agent · 2026-09-02 · Канон-снапшот: tsc=290 · vitest=808+0int+0load (69 файлов) · PARITY PASS · frozen 17/17 · HEAD c183abf.**
**Метод:** три отчёта кругов прочитаны по порядку; 10 клеймов финал-спеки выборочно верифицированы по живому дереву (file:line, read-only). Ноль правок кода и спеки. Frozen-файлы — только чтение.

---

## ВОПРОС

Прошла ли цепь кругов корректно и готова ли финал-спека «СЛЕПОК-ПАК v0» к применению Оператором после 🔴-решений Босса?

## ЗА

Спека самодостаточна и верифицируемо честна: 9/9 ударов круга-2 внесены с маркерами [002-У-N], оба убивающих несоответствия (У-1 aperiodicity, У-2 initFromMic) закрыты патчами в не-frozen файлах через уже живые механики (режим Б, RASP-канон-формула), машинные охраны (тест-9, К-7, aperiodicity-msg) защищают от дрейфа навсегда.

## ПРОТИВ

Спека стоит на дрейфующем дереве под чужой активной волной: RASP-SNAPSHOT-маркеры в тех же файлах = риск merge-конфликта, «живой прецедент» SPLIT_THRESHOLD=0.2 исчез из кода, 4 🔴-решения не приняты, tsc-гейт сформулирован противоречиво (ожидание ≤292 при гейте Δ≤0 против канона 290).

## ДОКАЗАТЕЛЬСТВА (выборочная верификация 009 по живому дереву)

| # | Клейм финал-спеки | Проверка 009 (file:line живого дерева) | Итог |
|---|---|---|---|
| Д-1 | У-1: `aperiodicity` нет в YinResult | `yin-detect.ts:10-19` — интерфейс без поля; return-объект без поля; grep `aperiodicity` по src — пуст (клейм круга-3 «grep пуст» точен); формула noiseProxy `clamp((cmnd[te]-0.18)/0.42, 0, 1)` канонизирована в RASP-SNAPSHOT-комментариях (`yin-detect.ts:120`, `pitch-engine.ts:183`) | ✅ точно |
| Д-2 | У-2: worklet не несёт телеметрию | `yin-processor.js` `_analyze()` шлёт ТОЛЬКО {type, frequency, confidence, rms, midi, timestamp}; subScore/noiseScore живут только в main-thread `_passiveTick` (`pitch-engine.ts:210-221`); `initFromNode` жив (`:144-176`), 46ms-тикер | ✅ точно |
| Д-3 | У-8: hold-500ms теряется при сносе | `useStableVocalData` держит hold (lastNoteRef/lastNoteTimeRef, holdMs=500); `useStableNote` — `else setNote(null)` сразу, hold-а нет | ✅ точно |
| Д-4 | У-3: deviceId-гвард слеп при '' | `MicSourceV3.deviceId` → `_deviceId` из localStorage `mic:deviceId` (`:34`); `_open()` НЕ читает `getSettings().deviceId` после захвата | ✅ точно |
| Д-5 | У-5: пороги 0.003/0.01 | `_gate = 0.01` (`yin-detect.ts:23`); `0.003` — no_pitch/silence-граница в `_passiveTick` (`pitch-engine.ts:227`) | ✅ точно |
| Д-6 | Персист `belive:voice-profile` | Паттерн `user-profile.store.ts:187` (`belive:user-profile`); ключи `belive:` канон (`main.tsx:812`, `metrics.store.ts:101`, ai-settings, mvsep-usage); коллизии ключа нет | ✅ точно |
| Д-7 | У-9: мок-паттерн К-3 | `makeFakeCtx` (`pitch-engine-modes.test.ts:21-38`) жив; createWaveShaper/createGain там НЕТ — спека честно требует добавить стабы, а не ссылается на несуществующее | ✅ точно |
| Д-8 | Frozen-граница чиста | Цели правок: yin-detect.ts, pitch-engine.ts, types.ts, PitchTab.tsx + 6 новых файлов; ни один не входит в frozen-список (AudioEngineV2.ts, patchV1.ts, bridges/*, track.orchestrator.ts) | ✅ чисто |
| Д-9 | Строки спеки = живое дерево | Выборочно: `yin-detect.ts:155/126-135`, `pitch-engine.ts:219/184-200/144-176`, `PitchTab.tsx:80-164/166-198`, `MicSourceV3.ts:73` — совпадают; дрейф RASP-маркеров кругом-3 учтён корректно | ✅ дрейф учтён |
| Д-10 | «Прецедент 0.2» для SPLIT_THRESHOLD | grep `0.2` по src/audio: порога сабгармоники 0.2 в yin-detect.ts НЕТ — прецедент круга-2 (`:189`) исчез в дрейфе; круг-3 признал смещение строки, но клейм «значение каноническое» — преувеличение: значение теперь чисто спек-уровня | ⚠️ дрейф-факт |

## ВЕРДИКТ

**РЕШЕНО С УСЛОВИЯМИ** (в терминах роли 009: РЕШЕНО — условия применения, не отложка: дефектов спеки, требующих нового круга, не найдено).

**Почему:** все проверенные клеймы точны, оба убийцы закрыты существующими живыми механиками, но применение гейтится 🔴-решениями Босса, дрейфом дерева и координацией с RASP-волной.

---

### ① Цепь прошла корректно? ДА

- **Круг-1 (архитектура):** самодостаточен, каждое решение = file:line или «новый файл с rationale», 4 🔴 вынесены Боссу — не самовольничал.
- **Круг-2 (стресс):** 9 атак, каждая верифицирована по живому дереву; два убийцы (У-1/У-2) реальны — 009 перепроверил оба лично (Д-1, Д-2); ранжирование честное, патчи-условия минимальные, frozen-граница проверена и чиста.
- **Круг-3 (финал):** 9/9 ударов внесено с маркерами [002-У-N]; дрейф RASP-маркеров честно зафиксирован, строки пересверены (Д-9 подтверждает точность); отклонена только альтернативная реализация У-2 (worklet-телеметрия), не сам удар — легитимно; тесты 8→9, мок-паттерн зафиксирован, migrate-тест расширен на version>текущей.
- **Сквад-память:** три отчёта — непрерывная цепь, информация не потеряна, ни один удар не проигнорирован, ни одно условие 002 не забыто (таблица §3 финал-спеки покрывает все 9 + оба условия-усиления).

### ② Финал-спека готова к Оператору? ДА, при условиях ③

Спека самодостаточна для реализации: 4 новых файла + 4 правки живых (~10 строк), 9 тестов поимённо, мок-паттерн, светофор, константа порога, dispose-обязанность. Оба убийцы закрыты через существующие механики: режим Б жив и протестирован (К-3), формула aperiodicity канонизирована RASP-комментарием в дереве — спека лишь поднимает значение в результат.

### ③ Условия применения (гейты) и риски (не-блокеры)

**ГЕЙТЫ (блокируют применение):**

- **Г-1 · 4 🔴-решения Босса** (§6 финал-спеки): мониторинг-маршрут, aperiodicity-патч, точка входа, снос крыльев. Спека сама их гейтит — до решений не применять.
- **Г-2 · Ревизия строк на дрейф перед реализацией.** Спека требует это сама; дерево дрейфнуло дважды за сутки (Д-9/Д-10 — дрейф реален и продолжится).
- **Г-3 · Координация с RASP-волной.** RASP-SNAPSHOT-маркеры в yin-detect.ts/pitch-engine.ts = чужая активная волна в тех же файлах. Патч У-1 аддитивен (сноса зоны-7 нет — маркеры запрещают снос, патч добавляет поле), но merge-конфликт риск. Синхронизировать правки yin-detect.ts/pitch-engine.ts до применения.
- **Г-4 · SPLIT_THRESHOLD=0.2 — как спек-канон** (живой прецедент исчез, Д-10): константа в voice-profile.service.ts + экспорт для тестов обязательны, иначе снапшоты несравнимы между сессиями.
- **Г-5 · tsc-гейт:** спека ожидает ≤292 при гейте Δ≤0 против канона 290 — противоречие требует формулировки Босса до прогона (вопрос-1).

**РИСКИ (не блокируют):**

- **R-1 · V2-fallback:** SplitFaderChain зависит от `__belive.pipeline.ctx`; в V2-режиме ctx нет. Зависимость та же, что у всего питч-стека (`PitchEngine._getContext()` кидает) — деградация = честная ошибка; Оператору предусмотреть catch + сообщение на экране.
- **R-2 · Observer-effect смягчён, не убит:** wetGain ×0.5 + светофор-дрейф rms снижают смещение С7, но юзер без наушников не слышит фейдер-фазу — спека честно предупреждает; снапшоты таких юзеров помечать quality-флагом.
- **R-3 · Пост-патч vitest:** спека назначает полный прогон после правки yin-detect.ts на «002», но круг-002 завершён — исполнителя назначить на фазу реализации (вопрос-2).

### ④ ВЕРДИКТ

**РЕШЕНО С УСЛОВИЯМИ** — гейты Г-1…Г-5 (см. ③). Цепь корректна, спека готова, применение — после гейтов.

### ⑤ Вопросы Боссу (нужны формулировки)

1. **tsc-гейт:** 290→292 проходит «Δ≤0»? Что считаем — строки/ошибки/иное? (закрывает Г-5)
2. **Пост-патч vitest:** кто гоняет полный прогон после правки yin-detect.ts на фазе реализации? (закрывает R-3)
3. **SPLIT_THRESHOLD=0.2:** подтвердить как спек-канон (живой прецедент исчез в дрейфе — Д-10)? (закрывает Г-4)
4. **4 🔴 из §6 финал-спеки:** жду формулировок — маршрут мониторинга / aperiodicity-патч / точка входа / снос крыльев. (закрывает Г-1)

---

### Вариант, не предложенный ни одной стороной (правило неоспоримости)

Ни один круг не предложил сместить опору применения со строк-номеров на машинные инварианты. Дрейф делает строки advisory (дважды за сутки — Д-9/Д-10), а тест-9 («initFromMic-кадры не содержат subScore/noiseScore/aperiodicity»), К-7-расширение (static-grep PitchTab) и aperiodicity-msg-тест — канон, который дрейф не рвёт. 009 фиксирует принцип применения: **строки — навигация, машинные охраны — закон**. Спека уже содержит все три охраны (§5); 009 подтверждает их обязательность — Оператору запрещается реализация без них.

---

*009 · вердикт · read-only: ни одного файла не правлено. Основа: SLEPOK-001-CIRCLE1 / SLEPOK-002-CIRCLE2 / SLEPOK-001-CIRCLE3-FINAL-SPEC (все 2026-09-02).*
