# ARC-2a · Круг-1 · VIS-19 — ДИЗАЙН ФИКСА (001 «Ювелир»)

**БАЗА:** HEAD `d99609a` · 30.08.2026 · Канон-снапшот: tsc=296 🔴 · vitest=761 🟢 · PARITY 🟢 · V3=дефолт
**Задача одной строкой:** 4 вызова несуществующего API `PitchEngine.get()`/`init()` → привести 3 файла к реальному API минимальным диффтом.

---

## ВЫБОР: β — singleton в классе (`PitchEngine.get()`), почему (≤3 предложений):

β добавляет ровно 5 строк в класс — и все 3 существующих вызова `PitchEngine.get()` (store:45, store:85, bridge:17) **зарабатывают без единой правки в них**: bridge:17 автоматически получает ТОТ ЖЕ экземпляр, что и store (требование (b) решено конструкцией, не пробросом). Гвоздь против α: α заставляет менять сигнатуру `activatePitchBridge(engine)` → +1 движущаяся часть в мосте. Гвоздь против γ: в проекте нет DI-контейнера (паттерны — singleton-класс / window-объект), γ = модульный setter/factory = новая абстракция без нужды. Бонус β: `initFromMic` при ошибке сам зовёт `destroy()` → `_status='idle'` (engine.ts:102-105 + destroy) — singleton НЕ отравляется после честного броска V3, повторный Start работает без пересоздания.

## ДИФФТ-ПЛАН (файл:строка → что меняем, 4 точки + bridge-синхронизация):

**Всего 2 реальных правки кода. Точки 45, 85, bridge:17 — ноль правок, они ожидаемо-корректны и оживают сами.**

1. **`src/audio/pitch/pitch-engine.ts:20` (после `export class PitchEngine {`)** — ДОБАВИТЬ:
   ```
   private static _instance: PitchEngine | null = null;
   static get(): PitchEngine {
     if (!PitchEngine._instance) PitchEngine._instance = new PitchEngine();
     return PitchEngine._instance;
   }
   ```
   5 строк, сигнатура идентична ожиданию всех 3 вызывов. PitchTab-путь (`new PitchEngine()` :252/:307) не затронут — `new` остаётся валидным.
2. **`src/stores/pitch.store.ts:49`** — `await engine.init()` → **`await engine.initFromMic()`** (единственная смысловая замена: у store нет node-источника (риск a) — initFromMic сам берёт `window.audioEngine.microphoneStream` или getUserMedia).
3. **`pitch.store.ts:45`** — `PitchEngine.get()` — БЕЗ ИЗМЕНЕНИЙ (оживает после п.1).
4. **`pitch.store.ts:85`** — `PitchEngine.get().destroy()` — БЕЗ ИЗМЕНЕНИЙ (`destroy()` инстансный, существует :207).
5. **`src/audio/pitch/pitch-visual-bridge.ts:17`** — `PitchEngine.get()` — БЕЗ ИЗМЕНЕНИЙ; получает экземпляр store'а бесплатно через тот же static (требование (b) выполнено по построению).
6. **Канон-прогноз:** tsc 296 → 292 (снимаются ВСЕ 4 TS2339: 3×`get` + 1×`init`; ср. п.6).

## ПОРЯДОК stopPitch (_unsub → destroy):

Зафиксирован кодом store:80-85, НЕ меняем: **`_unsub?.()` → `_bridgeCleanup?.()` → `PitchEngine.get().destroy()`**. Обязательно именно так: `destroy()` не чистит `_listeners` (engine.ts:30) — с β singleton живёт вечно, значит любая неотписка = течка замыканий и «зомби»-обновления store после Stop. С соблюдением порядка Set пуст после stopPitch → повторный Start добавляет только свежие слушатели. Отмечаю: порядок — инвариант, тест обязан его сторожить (ниже).

## ЧЕСТНАЯ ОШИБКА V3 (текст store.error до ARC-2d):

Проброс исходного сообщения без маппинга → юзер видит: **`audioEngine.audioContext not found`** (бросок `_getContext()` engine.ts:49 → catch store:72-77). Это честное состояние: кнопка не падает TypeError, а показывает диагноз, который дословно указывает корень ARC-2d (нет audioContext на фасаде). Маппинг в человекочитаемый текст — НЕ в ARC-2a: новая абстракция без нужды, текст будет заменён рабочим флоу в ARC-2d. PitchModule уже рендерит `error` (S.error, оранжевый) — UI-хук есть, CSS не трогаем.

## ТЕСТЫ (что/зачем/минимум):

**Один новый файл `src/stores/__tests__/pitch-store.test.ts`, 3 кейса, мок через `vi.spyOn(PitchEngine.prototype, ...)` (с β singleton не нужно инжектить):**
1. **startPitch happy-path:** mock `initFromMic: resolved`, `subscribe: () => unsub-spy` → ассерт `status==='running'`; guard повторного вызова при running → no-op.
2. **startPitch V3-бросок:** mock `initFromMic: reject(new Error('audioEngine.audioContext not found'))` → ассерт `status==='error'`, `error==='audioEngine.audioContext not found'` (фиксация «честного» текста как контракта до ARC-2d); **повторный Start после ошибки** → initFromMic вызван снова (singleton не отравлен — ключевое свойство β).
3. **stopPitch порядок:** spy на unsub-remover и `destroy` → ассерт порядка вызовов `unsub → destroy` (инвариант из-за нечистки `_listeners`).
Bridge-тест НЕ нужен: bridge:17 не меняется, его корректность = tsc (статик существует). Переписка мёртвого `pitchEnabled` в medium-stores.test.ts — ARC-2e (там же перевод PitchTab на V3-граф).

## САМОКРИТИКА/РИСКИ:

- **Смоук Босса / PitchTab:** не затронут — `new PitchEngine()` в PitchTab:252/:307 продолжает работать, static get() ничего не ломает (лишь бы PitchModule-кнопка не дергалась в смоуке: в V3 она честно даёт error-текст вместо TypeError — это И есть улучшение).
- **Параллельный store + PitchTab (риск d):** синглтон ≠ 2 инстанса PitchTab; в V3-дефолте store-ветка падает на audioContext раньше getUserMedia. В V2 параллельный запуск = два getUserMedia-стрима (браузер позволяет, нагрузка ×2, не фатально). Известно, разруливается в ARC-2e — вне скоупа.
- **Расхождение с диспатчем (не стоп-фактор, фиксирую):** канон-снапшот и задача говорят «3 ошибки VIS-19, −3 к tsc», но фактический счёт скаута = **4 TS2339** (3×get + 1×init). Дизайн закрывает все 4 → ожидание tsc **292, а не 293**. Прошу Центр подтвердить итоговый канон-таргет после прогона.
- **Мелочь β:** модульные поля singleton'а живут вечно — приемлемо, вся тяжёлая периферия (worklet/stream/analyser/timer) чистится `destroy()`.

## Требует решения CEO/Босса:

1. Подтвердить канон-таргет tsc после ARC-2a: **292** (−4 по факту, а не −3 из снапшота) — см. самокритику.
2. Не-стоп на frozen: НЕ требуется — фикс не упирается в frozen-zone (patchV1/AudioEngineV2/bridges/track.orchestrator не затронуты).

---
*001 · Ювелир. Диффт = 5 строк в pitch-engine.ts + 1 строка в pitch.store.ts. Остальные 3 точки — ноль правок.*
