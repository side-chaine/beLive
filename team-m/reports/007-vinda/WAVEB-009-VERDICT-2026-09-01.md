# WAVEB-009 — ВЕРДИКТ Суда (Круг-4, финал Волны B «КОНТРАКТ-ЗАКРЫТИЕ»)
**009 (Суд, независимый) · 2026-09-01 · Вход: спека 001-К3 (8 атомов/3 коммита) + стресс 002 (6 ударов) · Канон-снапшот диспатча: tsc=290 · vitest=808+0int+0load/69 · PARITY PASS · frozen 18/18 · «HEAD a8a3867»**

---

## ВЕРДИКТ:

**РЕШЕНО с условиями.** Спека 001-К3 исполнима без интерпретаций: все 8 атомов сверены с живым деревом дословно, канон-таргеты (tsc 290 Δ0 / vitest 808/69 Δ0 / verify:frozen 17/17 / PARITY) доказуемы, 6/6 ударов 002 закрыты, вычеркнутый implements-удар подтверждён фальшивым независимым grep. Условия — процедурные (чек-лист ниже), атомы не меняются. Два факта вне поля сторон найдены Судом и закрыты (см. ОБВИНЕНИЕ п.6).

---

## ДОКАЗАТЕЛЬСТВА (file:line):

Сверено лично, read-only, по живому дереву:

**(a) track.orchestrator — 0 импортёров.** Grep `track\.orchestrator` по src/ = 4 строки: `lyrics.store.ts:26` (коммент — правится А7.8), сам файл `:1`, `track.loader.ts:2` (истор-провенанс «1:1 копия», whitelist), `event-bus/README.md:60` (даш — правится А1.6). Import-стейтментов = 0. Замена (track.loader.ts) жива, включая глобалы: `:559` queueTrackJump, `:582` `window.queueTrackJump`, `:585` `window.trackOrchestrator`.

**(b) V2Adapter — 0 живых вызовов.** 16 упоминаний = сносимые файлы (V2Adapter.ts ×13, IV2PublicContract.ts ×3) + `index.ts:59` (единственный runtime-export, сносится А2.3) + комменты (takes.time:22, MonitorRouter:266, stem-engine-sync.ts:3, position-sync:38, WaveformCanvas:433/437, rehearsal-trigger-writer:6, test:9/:16/:53/:67) + BusFader18:14/:418 (имя-фикция, импорта НЕТ — grep `import.*V2Adapter` по src/ = 0). Runtime-импорт ровно один: index.ts:59.

**(c) implements-цепь = 0 — вычерк 002 подтверждён.** Grep `IV2PublicContract` по src/ = только V2Adapter.ts (:6,:12,:16,:26,:28) и сам IV2PublicContract.ts (:2,:15). `implements IV2PublicContract` = 0 по всему src/. Frozen AudioEngineV2.ts интерфейс НЕ реализует (grep по файлу: get duration :1603, getDuration :1737 — implements-строки нет). Frozen не задет.

**(d) instrumentalGain/vocalsGain — эффект писал 1 ВСЕГДА.** Grep по `js/audio-facade-v3.js`: instrumentalGain/vocalsGain/getDuration = 0 (есть только `get audioContext` :207). Grep по `src/audio/core/AudioEngineV2.ts`: instrumentalGain/vocalsGain = 0. Grep по src/sync/: только `SyncEditorPanel.tsx:106-107` — удаляемый эффект. → `ae.instrumentalGain?.gain?.value ?? 1` = 1 в любом режиме → DELETE (А4) = чистый фикс без V2-регрессии. Гейт (г) `src/sync/ → 0` закроется. (Остатки вне src/sync/ — if-guard'ы в frozen graveyard `audio-reactive.bridge.ts:34-35`, живом `audio-reactive.ts:40-41`, локальная переменная `TakesControlStrip.tsx:268` и параметры `VocalMix.ts` — гейт умышленно scoped, не красится.)

**(e) mockV2 — живых строк 6, спека 001-К3 права.** Grep по stem-engine-sync.test.ts: `:10` decl, `:17` (внутри удаляемого V2Adapter-ключа фабрики), `:40`, `:47`, `:50`, `:73`, `:86`. После патча А3.2 (удалён ТОЛЬКО ключ) живых = 6 (:10/:40/:47/:50/:73/:86). Счёт 002 «4» занижен (не нашёл :73/:86 в busVolumes-блоке) — суть У1 неизменна, спека стоит на grep-факте. Удаление объекта = ReferenceError → падение файла → 808−N; патч А3.2 обязателен и достаточен.

**(f) pipeline.duration существует.** `HybridPipelineService.ts:149` геттер `duration` (max по стемам chainA/chainB) + кэш `_lastKnownDuration` (:54-55, «защита от duration=0 при reset»). Цепь А5 `pipeline?.duration ?? ae?.getDuration?.() ?? 0` честна в обоих режимах: V2-хвост getDuration (:1737) жив, мёртвый `ae?.audio?.duration` удалён (`.audio` в src/audio есть только у StemPlayer.ts — не у движков).

**(g) Note-строка манифеста дословно:** `Канон: 3 зоны · 18 файлов (FROZEN-AUDIT-301 §7, wc=5137)` (frozen-manifest.json, поле note). Guard-строка orchestrator с SHA `b8818e66…` — совпадает с А1.2. `.gitattributes:9` = `src/services/track.orchestrator.ts                           -text` — совпадает с А1.4.

**Прочее сверенное:** А4 = SyncEditorPanel.tsx:102-110 дословно (9 строк); слайдеры :47-48 = zustand-селекторы → реактивность сохранена; useStemStore/useEffect (:90) остаются использованными → noUnusedLocals не сработает. А5 = :620-623 дословно. А6 = takes.recorder.ts:108 дословно, :109 (V2-ветка) не трогается; grep `_audioContext` по src/ = ровно :108+:109 → после А6 гейт (в) `→1 строка (:109)` сходится. А1.5/1.6 = README:55 «18/18» + :60 даш. А2.3 = index.ts:58-59 дословно. А8.1 = BusFader18.test.ts:418 дословно, тело :419+ не трогается. orchestrator = ровно 592 строки (файл кончается на :592). Операционный замок = `scripts/check-frozen.mjs` (package.json:25): guard-список читает из JSON динамически, «отсутствует без удаления строки = ERROR» (:18-21) — атомарность C1 достаточна, скрипт правки не требует.

---

## ОБВИНЕНИЕ:

1. **Слипание коммитов (C1+C2+C3 → 1).** Спека требует 3 коммита с полным гейтом после КАЖДОГО. Канонически слипание не ломает ничего (манифест-строка удаляется атомарно с git rm в любом случае), но разрушает диагностичность (bisect) и нарушает букву спеки. → Условие О-1: 3 раздельных коммита, после каждого tsc+vitest+verify:frozen. Слипание = отклонение отчёта волны.
2. **C3 зависает при падении C2.** Юзер-фиксы А4-А6 не зависят от сносов (подтверждено: правки в живых файлах, снос не задевает их носители), но едут последними. Вероятность падения C2 минимальна (0 живых вызовов, implements-цепи нет, mockV2 остаётся) — риск принят: STOP-условия волны сработают раньше, чем юзер-фиксы поедут по сломанному дереву. Откат-сценарий: STOP = стоп, юзер-фиксы НЕ применять самовольно, отчёт 007. Достаточно.
3. **CEO-добавки (README:55/60, index.ts:58, lyrics.store:26) — законны.** Все три = класс У2 «якорь чиним, сосед врёт», тот же коммит: README:55 после C1 утверждает «18/18» при живых 17 — самопротиворечие в операционном указателе канона; index.ts:58 без :59 = висячий коммент о несуществующем экспорте; lyrics.store:26 описывает снесённый файл как живой механизм. Это завершённость атома, не расширение скоупа: текст-правки, 0 канон-риска, 0 новых сущностей. Отказ = оставить враньё, которое 002 карал ударом 2. Подтверждаю все три.
4. **wc 4545 — допуск, не STOP.** Спека закрывает дважды (А1.3: «заменить число на факт ДО коммита (не STOP)»; СТОП-условия: «само число ≠ 4545 — НЕ STOP»). loop.bridge-ловушка (\n от 301) нейтрализуется определением: wc-факт = сумма `wc -l` по 17 ключам манифеста, прогоном, до коммита. Арифметика 5137−592=4545 верна. Достаточно.
5. **docs-дрейф — принятый риск, не дыра спеки.** Grep по docs/: ~79 строк с orchestrator, включая док-SSOT `frozen-zones-v2.md:3` («3 зоны · 18 файлов») и `:24`. Доки = истор. срезы, операционный канон = манифест; doc-sweep = движущиеся части в снос-волне. Гейты умышленно scoped to src/. Принято с фиксацией долга (У-4): frozen-zones-v2.md:3/:24 — первый кандидат doc-волны. Отдельно: `docs/modernization/handoff/verify-frozen.mjs:32` содержит жёсткий список с orchestrator — это мёртвая docs-копия, НЕ операционная (package.json:25 → scripts/check-frozen.mjs); запуск docs-копии после C1 даст ложный ERROR — не путать (У-5).
6. **Пропущенное цепью (найдено Судом — вариант, который не предлагала ни одна сторона): window-глобалы сносимого файла.** `track.orchestrator.ts:589-592` регистрирует `window.queueTrackJump` и `window.trackOrchestrator`. Снос убивает регистрацию — но заменитель `track.loader.ts` переподвешивает ОБА глобала (:582, :585), единственный живой потребитель `useKeyboardShortcuts.ts:35` зовёт опционально (`?.()`). Снос безопасен, спека этого не упоминает — пробел в обосновании, не в исполнимости. Закрыто доп-грепом чек-листа (У-6).
7. **HEAD-факт ≠ заголовок диспатча.** Живой main = `4db3f78` («200 PROCHTENO 17:25 — Wave A double-signed…»); `a8a3867` — на 3 коммита раньше (006 Д-КЕЙС 4, pull 003_2 FV-1, 200 PROCHTENO — все registry/docs, ни один не трогает src/ по git-log). Канон-числа снапшота этими коммитами не менялись. Не блокер: все атомы сверены по живому дереву, дерево = спеке. → Условие О-2: отчёт волны фиксирует фактический HEAD.

---

## ЗАЩИТА:

- **Минимальность:** 8 атомов = ровно скоуп 7 кандидатов Круга-1 + 3 CEO-добавки класса У2; ни одной новой абстракции, ни одного нового теста (греп-инварианты вместо тавтологий — обосновано: тест «компонент не пишет в стор» = тест отсутствия, цена >> цены).
- **Whitelist-гейты точны и проверяемы:** (а) V2Adapter → 3 (test:53, test:67 — истор., BusFader18:418 — маркер «истор.»); (б) track.orchestrator → 2 (track.loader:2, lyrics.store:26); (в) _audioContext → 1 (takes.recorder:109); (г) src/sync/ instrumentalGain|vocalsGain → 0. Каждый гейт сверен Судом по grep-факту ДО волны — числа достижимы.
- **Δ0-доказуемость:** tsc 290 = ошибки (прецедент MICRO-PACK C «293→290, Δ−3 внутри снесённых»); 0 ошибок в сносимых → Δ0. vitest: mockV2 остаётся (файл грузится), переименование :418 ≠ ассерт, тест-файлы не сносятся → 808/69 Δ0. verify:frozen: манифест-строка удаляется атомарно с git rm → 17/17.
- **У1-патч — самый тонкий момент закрыт:** удалён ТОЛЬКО ключ V2Adapter из фабрики (:14-19), mockV2-объект (:10-14) остаётся; 6 живых строк ассертят мёртвый канал (тавтологии зелёны — vi.fn() никто не зовёт и не должен). Спека стоит на grep-факте 6, а не на «4» 002.

---

## УСЛОВИЯ/ЧЕК-ЛИСТ ДЛЯ OPERATOR:

- **О-1. Порядок коммитов обязателен: C1 (А1) → C2 (А2+А3+А7+А8) → C3 (А4+А5+А6), три РАЗДЕЛЬНЫХ коммита.** После КАЖДОГО: `tsc` (0 новых, итог 290) + `vitest run` (808/69, 0int/0load) + `npm run verify:frozen` (после C1 — 17/17; до C1 — 18/18). Слипание = отклонение отчёта.
- **О-2. Фактический HEAD зафиксировать в отчёте волны** (ожидаю `4db3f78` или новее; расхождение с `a8a3867` = 3 registry-коммита, не блокер).
- **О-3. wc-факт до коммита C1:** сумма `wc -l` по 17 ключам манифеста (guard 2 + graveyard 15). ≠4545 → записать факт в note, НЕ STOP.
- **О-4. Зафиксировать долг doc-волны:** frozen-zones-v2.md:3 («3 зоны · 18 файлов») и :24 (таблица) врут после C1 — первый кандидат doc-волны; в волне НЕ трогать.
- **О-5. Замок = только `scripts/check-frozen.mjs`** (package.json:25). docs/modernization/handoff/verify-frozen.mjs — мёртвая копия с жёстким списком; её красный результат после C1 НЕ считать провалом verify:frozen.
- **О-6. Доп-греп после C1 (глобалы):** `git grep -nE "trackOrchestrator|queueTrackJump" src/` → ожидаемо ровно 5 строк: track.loader.ts:1 (коммент), :559, :582, :585 + useKeyboardShortcuts.ts:35 (опциональный вызов `?.()`). Отклонение = STOP.
- **О-7. Дословность атомов:** применять патчи А1-А8 как написано в 001-К3 (сверено Судом по дереву — все oldText совпадают). Импровизация = STOP.
- **О-8. STOP-условия 001-К3 — без самовольного фикса:** любое срабатывание → волна стоит, отчёт 007, откат последнего коммита, юзер-фиксы C3 НЕ применять по сломанному дереву.

---

## РАМКА ВЕРИФИКАЦИИ ДЛЯ 007:

1. `npx tsc --noEmit` (или канонический прогон проекта) → **290 ошибок, Δ0** (сверка с scripts/known-ts-errors.txt при его использовании каноном).
2. `npx vitest run` → **808 passed + 0 interrupted + 0 load-fail / 69 файлов, Δ0**; отдельная проверка: stem-engine-sync.test и BusFader18.test зелёны.
3. `npm run verify:frozen` → **17/17** (guard=2: AudioEngineV2 + live-guard; graveyard=15), 0 ERROR, 0 новых WARN.
4. Whitelist-грепы (дословно из 001-К3, все обязательны):
   - `git grep -n "V2Adapter" src/` → РОВНО 3: stem-engine-sync.test.ts:53, :67, BusFader18.test.ts:418 (маркер «истор.»).
   - `git grep -n "track.orchestrator" src/` → РОВНО 2: track.loader.ts:2, lyrics.store.ts:26 (маркер «снесён Волной B»).
   - `git grep -n "_audioContext" src/` → РОВНО 1: takes.recorder.ts:109.
   - `git grep -nE "instrumentalGain|vocalsGain" src/sync/` → 0.
   - Манифест: guard=2 · graveyard=15 · note «2 зоны · 17 файлов» + wc-факт.
   - Доп (О-6): `git grep -nE "trackOrchestrator|queueTrackJump" src/` → 5 строк (track.loader ×4 + useKeyboardShortcuts:35).
5. PARITY: прогон verify-bridge-parity по канонической команде проекта → **PASS**.
6. Frozen AudioEngineV2 нетронут: `git diff --stat a8a3867..HEAD -- src/audio/core/AudioEngineV2.ts` (или против фактического базового HEAD, см. О-2) → **пусто**; равно для всех graveyard-файлов.
7. Смоук (юзер-видимый эффект C3): открыть Sync-панель → слайдер инструментала показывает значение стора (не сбрасывается в 1 на маунт); LRC-пикер при загруженном треке → `duration ≠ 0`.

---

**Итог: РЕШЕНО с условиями О-1…О-8. Спека исполнима, атомы сверены с деревом дословно, канон Δ0 доказуем, дыр нет — два найденных Судом факта (HEAD-дрейф, window-глобалы) закрыты условиями, не правками атомов.**
