# 002 · СТРЕСС-ДОСЬЕ «ПУЛЬС-нервная система» · 2026-09-09 · ВЕРДИКТ: ТРЕБУЕТ ПАТЧА
Полный текст ударов в архиве прогона; ключи:
У1 §5 приёмка «1 ЧИСТО+1 НАБЛЮДЕНИЕ/живая аномалия» = ловушка (K-B уже пойман руками, живой аномалии нет → пилот «не состоится» при работающей системе, либо НАБЛЮДЕНИЕ доится; нарушение канона «зелёный до шага ≠ приёмка»). МИНА.
У2 §5 монитор③ (K-B рантайм) × «L1 vision нет» = центральный монитор неисполним пилотом. МИНА.
У3 §5 канал B01-HUB: три писца (зона/импульсы/T4) без триажа = свалка-риск. РИСК.
У4 §7.1 glob store+service (57) при определении ~120 (engine-v3 56, sync, exercises, deck, event-bus) = ложная зелёность охвата. МИНА.
У5 §2/AM-1(b) [SB|main] двойной трек без поля track → противоположные вердикты гейта на двух HEAD; первый merge-пак Волны-1 убьёт доверие. МИНА.
У6 §7 «по гейту за волну» → owner⇔ростер красный день-1 (6 L1 без промпт-файлов) = warn-G-1 реинкарнация. РИСК.
У7 §4 cold start: 30-дневного окна нет; первые 6 = ручное назначение, «рождение только по аномалии» нарушен на старте. РИСК.
У8 §3/§6 потоп: 6 L1×2 смены = ~135+ полных импульсов/нед в каналы. РИСК.
У9 §4 heartbeat=TRIGGERS: сам-себя не пиши; только CI-хук (мотор) пишет. ПРИНЯТЬ при CI-хуке.
У10 §6 бюджет 00: 23 потребителя (12 зон+5 пар+6 L1) на одном Никитином 00; не вычитается из Z-очереди. РИСК.
У11 AM-3 «4 golden signals» для STORE = категорная ошибка (нет латентности/QPS; 3 из 4 статичны → копипаст-ритуал). РИСК (AM-3 только для service-класса; сторы: ИНВАРИАНТ+ДРЕЙФ).
У12 §2 владение MAP при renames: git mv без MAP-diff = строки врут; писцов много → merge-войны. РИСК (единственный писец 007 + MAP-diff в rename-коммите, гейт mv-без-diff=fail).
ТОП-3 ОБЯЗАТЕЛЬНЫХ ПАТЧЕЙ: 1) §5 приёмка = детерминированные классы НАБЛЮДЕНИЯ (дрейф/near-miss/синтетика), ③ → статический контракт-чек; 2) §7.1 охват = все 4 класса, иначе переименовать verify:stores-services; 3) §2 track: main|SB|both + MAP-diff в коммите mv + гейт на обоих HEAD.
УСИЛЕНИЕ: скелет MAP генерится из glob(57 ядер)+types.ts(28 событий=словарь synapses); руки 007 = только tier/owner/zone-link.
Каркас держит: ярусность/reconcile/анти-поллинг = proven (005).
