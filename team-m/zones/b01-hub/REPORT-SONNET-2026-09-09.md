ПОЛНЫЙ ОТЧЁТ: РЕФОРМА РЕПЕТИЦИИ + ПУЛЬС — для Саннета (внешний архитектурный взгляд) — 2026-09-09

КАК ЧИТАТЬ. Отчёт фиксирует ВСЕ принятые решения последних двух суток (08.09-09.09), статус критического пути и список развилок, где мы хотим твой независимый ответ (раздел 7). У тебя нет доступа к WSL-песочнице; все кодовые утверждения даны с file:line нашими агентами (двойные проверки отмечены) — оспаривай логику и числа, не требуй скриншотов. Формат ответа: по каждому вопросу раздела 7 — вердикт + обоснование + что бы ты делал иначе.

=============== 1. СТАТУС И КРИТИЧЕСКИЙ ПУТЬ ===============

Цель: перестроить режим Rehearsal по референсу REF-2 (GPT-макет, SSOT). Порядок снизу вверх волнами Z1-Z5 в песочнице 007_2 (dev-сервер localhost:3000, ветка feature/yt-prep, отдельная от main). Полный экран песочницы = REF-2 и все гейты зелёные -> один трансфер-пак через 007 в main (волна V1 «большой переход дока» в HUB-QUEUE).

СДЕЛАНО (подтверждения в скобках):
- Z1 док применён в песочнице: DeckBar (Studio-таб, темпо-слайдер, неоны Inst/Voc 64px, 4 плитки, ...-меню), старая tabs-строка снесена (~500 строк), токены REF-2.
- Инцидент К1 закрыт: док рендерился «столбиком» с самого рождения Z1. Корень — отравленный CSS-комментарий в шапке DeckBar.css:5 (текст "--bl-p-*/" закрывал комментарий досрочно -> невалидный селектор .bl-deckbar -> Chrome молча дропал ВСЁ контейнерное правило). Диагноз с репро байт-в-байт в среде измерителя + изолированный эксперимент + скан 45 CSS (ядер ровно 1). Фикс 1 строка через микро-протокол (d99a55d), повторный независимый замер подтвердил: одна полоса, плитки прижаты вправо.
- STEP-1b (11/11): геометрия дока 88x88, aria 0-100 + valuetext, шрифт Inter единым стеком, мёртвые токены оживлены, legacy-заглушки ranges 0x0 закрыты как by-design (скрытый статический блок в index.html), иконки плиток: механизм img готов, глифы вырезаны из референса новым глазным агентом (K2 закрыт; подложные мокапы 1672x941 в папке ассетов — на карантине).
- Трек-блокер снят ЛОКАЛЬНО: vite-прокси (dev-сервер ходит к воркеру каталога сервер-сервер, CORS не применяется) — 54 трека живы в песочнице, ноль CF, ноль прода. Проверено: curl прокси 200/54, браузер-зонд без CORS-ошибок.
- Z2-СПЕКА (караоке slate-соседи + новая строка -1 + сине-белый glow; video-overlay: Show/Hide, BPM-степпер, PLAY 64 amber, WaveCanvas, Mini Catalog, тайм-коды) прогнана полной цепью 001->005->002->001->009 за смену и ЗАКРЫТА вердиктом 009 «РЕШЕНО С УСЛОВИЯМИ». Цепь нашла до применения 12+ мёртвых мест, включая 4 тихих убийцы: фидер 'music' не существует как id (role != stemId — волна рисовала бы пустоту молча); строка -1 ломала индексацию (off-by-one кластер); BPM «105» был бы выдуманным числом (в сторах BPM нет — статик-мок); темпо-действия ломали бы практику (две системы гейтов). Спека v1.1 (200 строк) готова к применению Оператором.
- 006 глаза реформированы: старый WorkBuddy-006 отчёты сняты по приказу Никиты; новый 006 = локальный vision-агент (MiMo) с 7 железными правилами (клик-маршрут а не консоль, [class*=] а не имена компонентов, числам нужен источник, стоп-не-импровизация, no-cache, без git-записей, без правок src) + формат журнала диагнозов. Первый прогон 008-в-роли-006: CDP-часть принята в кассу (находки: бюджет темпо-слайдера зависит от вьюпорта — 290px на 1600 при цели 500 в REF; узкое окно даёт media-ветку 1080), визуальная часть ОТВЕРГНУТА: анализ шёл по мокапу с десктопа вместо живого экрана (95% совпадение = мокап против мокапа). Пере-съёмка T4-v2 требует live-трек + computed КОНТЕЙНЕРОВ зон (урок К1 зашит в методику).
- Брифинг-реформа (круги 1-3): каркас вопросов, карта механики старого мира (12 объектов), двойная карта ХАБ x GPT, вердикты CEO, A1-A5 приняты Никитой.

В ОЖИДАНИИ (порядок жёсткий):
1) 006: T4-v2 — пиксель-база зон 4/5 (караоке-центр + видео) с живым треком, оба режима (karaoke/concert), контейнерные computed. Это последний гейт перед применением.
2) Оператор применяет Z2 v1.1 (~1 час) -> живая верификация 007_2 (гейты + CSSOM-зонд) -> 006 сверка STEP-2 против пиксель-таргета -> центр экрана = референс.
3) Параллельно открыт пред-Oператорный вопрос N3 (см. раздел 4, L0xexpanded) и вопрос объёма Q5 (K-A паритет).
4) Затем Z3 (хедер: dropdown режимов вместо 4 пилюль, поиск с инлайн-фильтром, медальон синий->розовый, монохромный вордмарк; + Show-вход переезжает в хедер), Z4 (левая панель), Z5 (AI-Coach), и — трансфер-пак V1 на main.

=============== 2. ПРИНЯТЫЕ ПРОДУКТОВЫЕ РЕШЕНИЯ (РЕЕСТР) ===============

- ДОК ЖИВЁТ (решение Никиты, повторно): Split/Studio/Pitch/Quest + ...-меню (Audio/Mic/VMix, Sync/DNA, Expand) — в доке; Sync настраивается доком. Умерла только старая tabs-строка (её роль забрала система плиток -> панель 240px).
- Show: на вкладке Show в доке была одна осиротевшая кнопка -> Show-вход ПЕРЕЕЗЖАЕТ в хедер главного экрана (пункт волны Z3). Хедер-строка в матрице замещения: ShowEditor больше не вызывается плиткой дока.
- КАНОН РАМКИ (GO Никиты по развилке «полный визуал сразу vs волны», решено гибрид): ПОСТОЯННАЯ трёхзонная рамка (левая колонка-зеркало прогресса + центр-караоке/видео + правая AI-Coach). Плитки дока НЕ подменяют экран: модуль всплывает ПОВЕРХ рамки. «Всё тут же, но отображается по-умному» = прогрессивное раскрытие, не переключение экранов. Синхронизм/Show-режимы остаются штатными замещениями дока до миграции.
- AI-Coach трактуется как вторая проекция ТЕХ ЖЕ данных что левая колонка (единый источник с Practice Plan), а его действия (замедлить/поднять громкость) идут через тот же actions-слой (EXTRACTION-СИГНАТУРА), что и клики — текстовый вход вместо мышечного. (Твой же системный разбор, принятый в план Z5.)
- ЛЕВАЯ ПАНЕЛЬ (твоя находка, принята): визуально значится (волна Z4, доноры реальны: metrics.store — rehearsals/exercisesCompleted/totalPlayTimeMs/elo/streakDays; UserRoom — аватар/имя). ОТКРЫТОЕ (K-#6): производные ФОРМУЛЫ нет — Lv из elo по какой шкале, «Current Track 48%» = прогресс чего, Practice Plan = из структуры трека (твой тезис: секционный прогресс ~85% = метрика тейков Quest наружу) или из practice-session. Контракт формул обязан родиться ДО спеки Z4.
- REF-2 = единственный SSOT дизайна; REF-1-эра артефакты помечены поколением (BUILD-скрины в архив, MOCK — живые; папка ассетов размечена после твоего A/B-голоса).
- collapsed/L0 модель дока (полоса без expanded / только транспорт) — REF-2 молчит ниже 186px; НЕ-решение, в Круг-3.
- Город = на паузе (кнопка в приложении, не режим); YouTube-вертикаль ждёт реформу.

=============== 3. ПРИНЯТЫЕ ПРОЦЕССНЫЕ КАНОНЫ (РЕЕСТР) ===============

- Цепь прогона: 001(архитектор) -> 005(внешняя сверка) -> 002(стресс) -> 001-рев -> 009(независимая приёмка с file:line); скауты — единственные разрешённые параллели. Строго последовательно.
- DUAL-MAP: любое крупное решение двумя независимыми руками, дифф карт богаче любой карты; внешние модели (ты, GPT) = второй взгляд, не исполнители.
- PIXEL-BASE-FIRST: ни одно изменение поверхности без базового замера глаз ДО; приёмка = дифф «было/референс/стало».
- EXTRACTION-FALLBACK: extract -> новый surface -> fallback легаси живёт -> сносите ПОСЛЕДНИМ; EXTRACTION-СИГНАТУРА actions: (value, opts={intermediate}) под будущую slider-семантику.
- SURFACE-SMOKE: машинный перебор комбинаций (syncOpen/showActive/featureActive/isPresenting/mode x5/expanded) -> ровно одна поверхность побеждает; консоль-warn запрещён как единственная защита (урок G-1).
- DUAL-TRACK: концептуал-визуал через песочницу с глазной сверкой; метрология (бенчмарки, стенды) идёт прямиком в 007-очередь — пиксель-сверка к консоль-вердиктам неприменима.
- МИКРО-ПРОТОКОЛ (рождён инцидентом К1, прецедент d99a55d): тривиальная правка идёт без полной цепи ТОЛЬКО при трёх опорах: доказательная база уровня диагноза К1 (репро + изолированный эксперимент + скан на тираж) + живая верификация пост-фактум (CSSOM/пиксель, формат 009) + повторный замер глаз. Слабо любое из трёх — полная цепь.
- КАНОН ВЕРИФИКАЦИИ ПОВЕРХНОСТИ: компилятор + CSSOM/пиксель; tsc/vitest НЕ ловят мёртвые селекторы — доказано живым кейсом. Кандидат гейта css-rule-count в очереди ПУЛЬС v0.2.
- 301-роутинг: все RED-вопросы агентов Никите идут сборщиком через 301; исключение CEO_1 (прямой доступ).
- разд.12 гонка реестра: мост = общий файл нескольких писцов; порядок любой записи: diff мост<->локал, мост новее -> сначала забрать его правду, блок вставляется python-проходом tmp+replace, cp, перечитать; слепой cp запрещён (два живых инцидента: мой 08.09 — 4 строки, восстановил из git, и класс «артефакт не там, где читают»: мост нёс мёртвую Z2 v1.0 пока v1.1 жил в песочнице — синхронизировано md5-в-md5; правило: спека и её мост-зеркало — одним актом).
- Правило каналов: артефакты — только в папку связки + короткая строка в канал; вердикты — в реестр.
- Экономика агент-часов: поллинг запрещён (урок: агент в цикле ожидания жжёт контекст и просыпается глупее), диспетч только событиями/диспатчем.
- Дисциплина ретраев: пустые выходы reasoning-агента — кап 2, дальше CEO-интеграция из префиксированных параметров + заявка владельцу (на очереди: -hi близнец 001, ловивший 2x  пусто на реВ-круге прогона В).

=============== 4. ПРОГОН A — МАТРИЦА СЦЕНАРИЕВ ДОКА (007_2, 001x002xмой скаут) ===============

24 клетки (поверхность x сессия x док x помехи), каждая в 3 слоях: сегодня (push-панель) / цель (overlay по канону) / минимальный мост. Зафиксирован КОНФЛИКТ #1: панель дока = PUSH (сдвигает лирику, 240px) сегодня vs канон = OVERLAY (рамка не ходит) — миграция отдельной волной, НЕ внутри В1.
Ключевые мины/находки матрицы:
- ДВА ЦЕНТРА: syncOpen x режим karaoke/concert дают одновременно SyncLyrics И KaraokeLyricsBoard (App.tsx:232-233, гейты не взаимоисключающи) -> XS-гейт.
- N3 БЛОКЕР: L0 (скрытый DeckBar) x expanded=true -> панель нечем закрыть (тоггл живёт на DeckBar). Дефолт-ремонт: кнопка закрытия в оверлее панели + deck-height=76 при скрытом баре. Это вопрос ОБЪЁМА Z2 перед Оператором.
- K-A паритет: interruptPracticeSession обёртки нужны в 4 местах (неоны DeckBar, Space, KeyK, ShowEntry/openScenario), часть уже обёрнута; спор: всё в Z2 или хвосты в Z2.5 (мелкий XS-батч уже укомплектован: 7 пунктов).
- delta-DV-предикат: константа 186 фальшива (188/186 по media-брейкпоинтам 1080), геометрия дока = функция вьюпорта; бюджет темпо-слайдера на 1600 = ~290px против 500 в REF (REF рисован на 1920) — либо целевые ширины-проценты, либо признать эластичность.
- Catalog-интерлок = скрытый третий писатель deck-состояния; live-виджеты (LiveControls bottom 40/100) погребены под доком 188; deep-link эффект живёт до ранних return'ов; reload x expanded=true = панельreopen поверх лирики (persist deck.store partialize: только expanded+activeTabId) — нужен reopening-канон.
- Фактология для Nervous-системы: deck-height = 3 файла-издателя / 4 set-операции, 5 потребителей; 25 вызовов interruptPracticeSession в 8 файлах; featureActive НЕ гейтит ControlDeck (док живёт под FeatureOverlay — двойное подтверждение двумя независимыми разведками).
Итог прогона: 9 RED вопросов Никите (поверхности-приоритеты в C4, live-док, overlay-тайминг+reopening-канон, sync-двойной центр, K-A объём, feature/presenting унификация, hidden x reload x feed издатель ДВ, N3, тайминг overlay-волны).

=============== 5. ПРОГОН Б — ПУЛЬС-НЕРВНАЯ СИСТЕМА (я, 001->005->002->v2->009) ===============

Принято с условиями, все закрыты. Конструкция:
- МЕХАНИЗМ = файл-ядро (store/service/engine-модуль/домен-ядро) + наблюдаемый контракт (экспорты/события/потребители). Домен-док из 38 архитектурных карт = КАРТА, не механизм. Пересчёт честным glob: ~162 строки.
- ЯРУСНОСТЬ (ответ на «у каждого механизма свой агент», не доводя до 162 агентов): L1 = свой агент-владелец; L2 = поле family в реестре, живой агент включается только по эскалации СЕМЬИ; L3 = без агента, только машинные гейты. Критерии L1: 3 из 4 (churn ядра за 30 дней; 2+ потребителя; наличие docs-карты; danger-соседство/аномалии). Ярус = режим наблюдения, не звание.
- ВНЕШНЕЕ ЯДРО ПОДТВЕРЖДЕНО 005 (proven-паттерны): реестр desired-state + CI = reconcile (K8s), сиротство легально/немота нет = Backstage orphan-аннотация, рождение/эскалация = OTP MaxR/MaxT intensity-period, owner-валидация = CODEOWNERS, событие-без-подписчика = NATS advisory + durable-запись, 4 золотых сигнала = SRE (с ограничением: для store-класса работают 2 — ИНВАРИАНТ + ДРЕЙФ, golden 4-ка только service).
- IMPULSE (нервный импульс) = обобщение журнала диагнозов 006: СОСТОЯНИЕ / АНОМАЛИЯ / СИНАПСЫ / ДИАГНОЗ file:line / РЕЦЕПТ (применяет только Оператор) / СТАТУС ЧИСТО-НАБЛЮДЕНИЕ-ЭСКАЛАЦИЯ. ЧИСТО = одна агрегированная строка в дневник зоны; полный формат только при отклонениях.
- MECHANISM-MAP.yaml: скелет ГЕНЕРИРУЕТСЯ (glob + types.ts event-bus = словарь из 28 синапсов), руки — разметка tier/owner/family/zone-link/track/seed; поля: source: fact|hyp (гипотезы стареют), track: main|SB|both (двойной трек реальность: [SB] HEAD и main живут разными числами), zone-link в 12 зон или явный ORPHAN (350 немых сирот покрываются осознанными).
- ПРИЁМКА 009 = ПРИНЯТО С УСЛОВИЯМИ: расширение охвата (src/**/*.store|service.ts, stem.store в приоритете — прямой импорт seed-ядра track.loader), frozen-референс трекуется (в main AudioEngineV2/live-guard снесены 03.09, в [SB] живут 2/2 — и наш собственный промпт-список frozen устарел, ждёт правки Никиты/200), граница «наблюдение vs соприкосновение» (L1-агент не правит НИЧЕГО, даже свой core; только импульс-рецепт), glob-числа = снимок с датой и воспроизводимой командой.
- ПИЛОТ: loop.store (история K-B «repeat угоняет авто-пасс»): детерминированная приёмка — (1) дрейф подписчиков при CI-смене (2) near-miss инварианта isLooping <=> loopSubBlockKeys (3) СИНТЕТИЧЕСКАЯ фикстура-аномалия (обязана ловиться; живая аномалия = бонус, «зелёный до шага != приёмка») (4) статический контракт-чек владения; рантайм-глаза — только через 006-пару по запросу.

=============== 6. ПРОГОН В — СОЗВЕЗДИЕ + АГЕНТ 004 (по слову Никиты; цепь 001->005->002->рев(CEO-интеграция)->009) ===============

ВЕРДИКТ 009: ПРИНЯТО С УСЛОВИЯМИ (5, все на исполнение, не на текст). Ключевое:
- 004 «РЕЕСТР-ПУЛЬС» (слот исторически пуст) = единственный писец реестров (MECHANISM-MAP, PULSE-ZONES hygiene, roster агентов MO-*, дедуп дневников, шаблоны). Писец смещён с 007 (писец+диспетчер в одних руках = риск; паттерн Backstage: провайдеры эмитят — конвейер пишет). Границы 004: не диспатч (это 007), не решения (301), не код (УЗ-3), не суждение об удалении (только механический sweep по ends-at). «Умнеет» = досье + метрики числами + МЕСЯЧНАЯ приёмка самого 009 (запрет самосуда).
- ВОЛНА-1 = ровно 5 агентов: MO-dock (seed-досье = 24 клетки прогона A — формула «погружение->доклад» уже оплачена), MO-mode-switch (ВЕРНУТ в L1 после мины 002: dual-transport mode-changed без наблюдателя = тихий убийца), MO-loop, MO-track-loader, MO-v3-interceptor. Event-bus — без агента (буфер-очередь 004; диагнозы несёт verify:events + synapse-warn wave-2). Upload — wave-2 (зона B09 закрыла пилотом). Волна-2: 7 кандидатов; волна-3: ~150 механизмов навсегда L3-гейтные.
- ПРАВИЛО СЛОЯ (закрыто двойное дно зона x механизм): аномалию ЯДРА MAP-строки пишет только МО-агент; зональный патруль = стыки/компоненты/не-ядра. Fingerprint дедупа = mechanism-id + класс (обязательное поле каждой записи; дедуп/слой/stale/MAP-diff = один ключ).
- ПЛОСКОСТИ БЕЗ БЮРОКРАТИИ: таблица «сигнал -> ровно один адресат»: аномалия -> NERVOUS-LOG -> 004 дедуп (порог: до 3 НАБЛЮДЕНИЙ/семья/неделя — сквозной проход в дайджест, дедуп-бюрократия не раньше шума) -> эскалация в 301; задача -> 007; RED -> 301 -> Никита; архитектурный спор -> CEO_1; применение -> Оператор через цепь; heartbeat: v0 = ручной «00» Никиты (честно), [ФАЗА-2] = cron-workflow + коммиттер TRIGGERS + workflow для [SB] (~30 строк yml, очередь ПОСЛЕ Z2). Цепи прогонов — только на изменение системы; рапорты — без цепей. Разделение органов: ХАБ = поток работы (очередь реформы), 004 = состояние реестров, 007 = триаж в работу, CEO_1 = архитектура, 301 = вопросы; слияния запрещены (SPOF + саботаж-риск). Проверка 0-циклов пройдена.
- COLD START: скелет стартует ВСЕГДА tier:L3 + unclassified:true (всезнание одного мозга запрещено); разметка волны-1 — ВТОРЫМ коммитом (первый: генератор+скелет+гейты, автор Оператор; второй: разметка+5 MO-промптов, автор 004, приёмка-триаж 007). Дайджест не стартует без СПИСКА ЗАМЕН (что умирает: MAP-diff нотесы в LOG, пороговые INBOX-отчёты, зональные недельные — всё свёрстано в дайджест).
- ХОЛОДНЫЙ ПУЛЬС v0 застрахован: «застрявшая запись» машинно (НАБЛЮДЕНИЕ mtime>48ч без перехода статуса — grep-уемо), stale-алерт о самом 004 идёт в триггеры 301/007; ручные [SB]-прогоны verify:ci обязаны логироваться (иначе v0-носитель фиктивен — условие-4 009).
- ИНЦИДЕНТ КРУГА: 001 на реВ-круге выдал 2x  пустой вывод (tokenrouter free); кап ретраев -> финальная сшивка сделана CEO_1 из префиксированных параметров цепи (новые концепты не вносились, журнал 20/20). Открытая заявка: -hi близнец 001 или узаконить практику CEO-интеграции.

=============== 7. РАЗВИЛКИ — ВОПРОСЫ САННЕТУ (по каждому ждём независимый вердикт) ===============

Q1 ТАЙМИНГ PUSH -> OVERLAY. Панель дока сегодня сдвигает контент (push 240px + deck-height chain в 5 потребителях); канон — overlay поверх постоянной рамки. Переносить ли main-трансфер (В1) с push-семантикой (быстрее, механика проверена) и делать overlay отдельной волной, или НЕ transferring пока оверлей не собран в песочнице? Наша склонность: переносить push (он верифицирован), overlay — отдельная волна с собственным гейтом. Что видишь ты?
Q2 N3 (L0 x expanded): скрытый DeckBar = панель нечем закрыть. Дефолт: close-кнопка в оверлее + deck-height=76 в скрытом состоянии. Альтернативы: в L0 не скрывать DeckBar полностью; авто-collapse панели при скрытии. Какой вариант не ломает прогрессивное раскрытие?
Q3 ОБЪЁМ K-A паритета (4 interrupt-обёртки): неоны+Space в Z2 (они в тех же компонентах что правим), KeyK+openScenario в Z2.5. Согласен, или все 4 сейчас дешевле чем два захода внедрения?
Q4 ФОРМУЛЫ ЛЕВОЙ ПАНЕЛИ (K-#6): Lv из elo — шкала (пороги/log)? «48% Current Track» = покрытие плейбэка секций трека? Practice Plan = генерится из верс-структуры (твой тезис: секционные метрики = тейк-прогресс Quest наружу) или кураторский список упражнений? Предложи минимальную связку БЕЗ нового бэкенда (есть metrics.store, practice-session, markers/sections).
Q5 АКТИВАЦИЯ СОЗВЕЗДИЯ: 004+волна-1 — запускать параллельно Z2 (событийные окна, плановые смены запрещены) или переждать до трансфер-пака V1? Наша склонность: параллельно, кроме генератора-кода (он в очереди после Z2).
Q6 РАЗМЕР ВОЛНЫ-1: 5 агентов vs минимум-доказательство (002 требовал счесть экономику). Оставить 5 (модели бесплатные, событийные окна) или порезать до 3 (dock, mode-switch, loop)?
Q7 v0 РУЧНОЙ ПУЛЬС: «00» Никиты — осознанное узкое горло до фазы-2. Тащить ли cron+коммиттер (~30 строк yml) ВПЕРЁД Z2 ради аудируемости (условие-4), или живой процесс не стоит отвлечения 007?
Q8 ГОVERNANCE: таблица «сигнал->адресат» + разделение ХАБ/004/007/CEO/301. Найди глазами снаружи: где возможен тихий цикл или прокидывание сигнала между органами (мы проверили 0-циклы, нужен третий взгляд).
Q9 ДЕГРАДАЦИЯ 001-rev (2x  пусто): держать кап-практику «CEO-интеграция из префиксированных параметров» как норму, или платный -hi близнец на реВ-кругах окупается независимостью?
Q10 ОБЩАЯ КОНТРА-РЕВЗИЯ: посмотри весь реестр решений (разделы 2-6) и назови 1-2 решения, где ты видишь системный риск, который мы сами не назвали.

=============== 8. ЦИФРЫ И АРТЕФАКТЫ (для сверки) ===============

Канон main: tsc=181 - vitest 812/68 - PARITY PASS - reach 21 hold - junction 3 viol в GRACE (CF-долг). Песочница: tsc=276 - vitest 67/803 - frozen 2/2 (свой HEAD). Z2 009: 12/12 клеймов + 9 бонусов лично проверены, ноль галлюцинаций. MAP-скелет: ~162 (46 stores + 28 services + engine-v3 26 + домены). Event-bus: 28 событий / 6 каналов / 19 wrappers. Зоны: 12 (покрытие src 26%, 350 осознанных сирот после MAP). Агенты: 12 зонных патрулей + цепные (001/002/005/006-vision/009/operator) + созвездие в проекте (5 в волне-1). Ключевые артефакты: K1-DIAGNOSIS / MICRO-PACK-K1FIX / MICRO-PACK-Z2 v1.1 / REF2Z2-009-VERDICT / SCENARIOS-DOCK-A-FINAL (24 клетки) / PULSE-NERVOUS v2+final / CONSTELLATION-V v2 (+цепь) / SCOUT-DOCK+SCOUT-PULSE — все в bridge zones/reform/ + team-m/zones/b01-hub/. Хронология: 07.09 брифинг-реформа (9 вопросов, 3 круга); 08.09 К1-диагноз->фикс->R2, Z2-цепь, трек-разлок, трек-вердикты; 09.09 прогоны А/Б/В + реформа 006.

=============== 9. ФОРМАТ ОТВЕТА, КОТОРЫЙ НАМ НУЖЕН ===============

По каждому вопросу Q1-Q10: (а) твой вердикт, (б) 2-4 строки обоснования, (в) «делаю иначе, если...» где не согласен с нашей склонностью. Отдельно: общий контр-взгляд Q10. Ответ можно частями. После тебя: твои вердикты войдут в пакет Никите через 301 с пометкой SOURCED-EXTERNAL (твоя карта против наших карт = двойная карта, расхождения — на арбитраж).
