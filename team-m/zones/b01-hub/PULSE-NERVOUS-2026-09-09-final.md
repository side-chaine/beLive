# ПУЛЬС-НЕРВНАЯ СИСТЕМА · ФИНАЛ v2.1 (приёмка 009 закрыта) · 2026-09-09
**Статус:** ПРИНЯТО С УСЛОВИЯМИ (009) → условия вшиты здесь; тело = PULSE-NERVOUS-2026-09-09-v2.md (не переписывается, это надстройка-патч). Цепь прогона Б: 001→005→002→001-рев→009→(этот файл).

## УЗ-1 (009 условие 1): охват verify:mechanisms расширяется
Классы 1-2 v2 заменяются:
- 1. `src/**/*.store.ts` минус `__tests__/**` = **46** (было 33; добавились avatar/performance/ai-settings(js)/catalog/trigger/stem/feed×3/rehearsal-session/blockEditor.store/takes/billy-runtime и пр.)
- 2. `src/**/*.service.ts` минус тесты = **28** (было 19; +blocks-bridge/blockEditor/deep-link/trigger-visual/billy)
- классы 3-4 без изменений (26/24/15/3/27 по команде УЗ-4)
- **ИТОГО ≈162** строки MAP (вместо ≈147). **stem.store — явный приоритет разметки** (прямой импорт seed-ядра track.loader:12 — невидимость для reconcile(a) закрыта).
Проверка: §7.1(a) glob = число MAP-строк, расхождение = fail.

## УЗ-2 (009 условие 2): frozen-референс трекуется
§9 строка frozen переписывается: «Frozen по **track**: [SB] main-дерево не содержит AudioEngineV2/live-guard (glob=0, эпоха сносов 03.09: D-1/D-2, frozen-zones-v2.md:3 «вся frozen-зона снесена»); на ветке **feature/yt-prep** они ЖИВЫ (check-frozen 2/2, мой ls 09.09) — потому что песочница меряет свой HEAD. **Nervous-system правила: (i) L1-агент над frozen-LIVE объектами текущего track не создаётся; (ii) живой охраной считаются G-гейты (reach/junction/inert) того же track.** Это У5 на теле самого документа — показательно: условие 1 про track в строке MAP спасло бы 009 от двойной правды.»

## УЗ-3 (009 условие 3): граница «наблюдение vs соприкосновение» (строка в §4/§5)
«L1-агент НЕ правит НИЧЕГО, включая свой core (даже комментарий). Единственный допустимый выход — ИМПУЛЬС с РЕЦЕПТОМ; исполнение только Оператором через цепь. Соприкосновение seed-шестёрки с живым кодом (V3DataInterceptor в engine-v3, track.loader 12 импортов) — чистое наблюдение: чтение файлов и CI-выводов. Любое предложение правки в ядро НЕ СВОЕЙ строки MAP = эскалация 301, не импульс.»

## УЗ-4 (009 условие 4): glob-метод воспроизводим
Команда-источник §7 (фиксация в шапке MECHANISM-MAP.yaml комментарием):
`find src -name '*.store.ts' -o -name '*.service.ts' | grep -v -E '(__tests__|\.test\.|\.bench\.)'` + класс3-4 пересчёт по: `-not -path '*__tests__*' -not -name '*.test.ts*' -not -name '*.tsx' -not -name '*.json' -not -path '*vendor*' -not -name '*.tar.gz' -not -name 'README.md'`
Пер-классовые числа фиксируются как СНИМОК с датой (09.09: 46/28/26/24/15/3/27 ⇒ ≈162); расхождение следующего снапшота = MAP-diff, не молчание.

## Итог прогона Б
- Конструкция: ярусы L1(6 seed)/L2→family-поле/L3-гейты; MECHANISM-MAP (генерящийся скелет, один писец 007, поля track/source/seed); импульс = обобщение DIAGNOSTICS-LOG 006; анти-поллинг (CI-хук, K8s-watch-паттерн); пилот loop.store с детерминированной приёмкой (дрейф/near-miss/синтетика + контракт-чек); волны гейтов 1+2+5 → 3+4 после пилота.
- Провен-ядро подтверждено 005: reconcile(K8s), orphan(Backstage), MaxR/MaxT(OTP), CODEOWNERS, advisory→durable(NATS), golden-signals-с-ограничением(SRE).
- 🔴 к Никите (через 301): 7 вопросов v2 §8 (seed-набор, бюджет-формула, носитель NERVOUS-LOG, носитель MAP, эскалация-301, порог 3/4, заморозка-без-удаления) + НОВЫЙ-8: **активация прогона Б = после Z2-применения или параллельно CI-окнами?** (дефолт: параллельно — бюджет-формула У10 позволяет, Z-очередь не трогает).
- Стык с прогоном А: 006-T4-v2 находки → строки MAP (триаж §5); loop×practice×repeat контракт = клетки матрицы дока; mode-switch.service L1 = кандидат на общие сценарии с дока-матрицей.

— CEO_1 (интеграция 001/005/002/009) · прогон Б ЗАКРЫТ · ждёт GO Никиты на активацию 🫀
