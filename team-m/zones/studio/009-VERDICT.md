# 009 · ВЕРДИКТ ПРИЁМКИ прогона Studio · этап-5

**Дата:** 2026-09-06 (вечер) · **Автор:** 009 (верификатор, независимый) · **Вердикт: ПРИНЯТ С ПОПРАВКАМИ** (прогон честен, 2 поправки к 001-финалу, 1 к 002)

## Пруфы (все клеймы проверены по коду лично)
| Клейм | Пруф | Вердикт |
|---|---|---|
| Вкладка уже 'Studio' | modules.ts:74 | ✅ коллизия реальна |
| Третий вход setTab('mixer') | featureRegistry.ts:66-72; вызыватели: show-editor.store:598,621 · StepWorkspace:485 | ✅ три входа реальны |
| Тап vmixMicIn ДО delay/FX (сухой V-Mix) | MonitorRouter.ts:156 vs :140-141 | ✅ |
| release() не трогает _monitorGain | MicSourceV3.ts:50-53 | ✅ код / ⚠️ удар ослаблен: UI-тоггл 🎤 УЖЕ глушит монитор ДО release (ControlDeck.tsx:395-396); ghost-echo реален для echo-петли DSP и не-UI смертей мика (setDevice degraded :62, hardware unplug); REC-V3 монитор не включает (takes.recorder.ts:80-96, только V2-ветка :95) |
| bpm null/'none' + bpm-ramp | track-meta.types.ts:21 · track-meta.service.ts:196,294 · practice-session.store.ts:338,367-368 | ✅ fallback-120 прецедент: useBillyLocomotion.ts:101 |
| Гейты оверлеев без studioActive | App.tsx:232-249 (в 002 заявлено 237-249 — неточность диапазона, суть верна) | ✅ |
| Нет UI-библиотек | package.json:5-13 | ✅ @base-ui/react будет первой |
| z-реестр занят | 999995-999999 все заняты (ControlDeck → ShowEditor) | ✅ слот требует выбора |

## Попадки (применены к 001-финалу)
1. **A.3 условен:** «спека DSP готова; применение блокировано B1 — при выборе Никитой B1(б) „полка моком" пункт A.3 замораживается до решения».
2. **A.1 условен по месту кнопки:** монтаж по C3, место = B5.
3. **К 002 (история):** удар-3 переформулирован — UI-тоггл уже глушит; усл.4 (ghost-протокол) остаётся в силе для echo-петли и не-UI смертей мика.

## Смоук-предсказания (для 006 на POC/первой сборке)
1. Touch-drag вертикального фейдера на 390px не прокручивает страницу (touch-action:none).
2. VU живой: meterFill >0% и пульсирует при играющих стемах (rAF); 0% на статике ≠ отсутствие.
3. probe:bundle после POC: gzip-число ≤30KB (бюджет B3) зафиксировано, main chunk Δ0 при React.lazy.
4. Ghost-echo (после DSP-POC): Echo ON → mic-off → хвост обрывается ≤20ms; V-Mix ON = тишина самоконтроля (гейт-инвариант).
5. bpm=null → Echo не крашится, delayTime=60/120×coeff, нет TypeError Infinity.
6. Третий вход: Show-шаг open-studio-mixer открывает вкладку, deactivate → restoreSnapshot возвращает таб (featureRegistry:73-80).

## Канон-риск (гейты Δ0)
tsc=181 (новые файлы — 0 новых ошибок) · vitest=812/68 (только добавление pass) · 6 CI-гейтов (4-я ветка App.tsx + z-слот могут зацепить reach/inert) · probe:bundle (main Δ0; package.json+lockfile — единственное касание) · **опасность №1:** C1-патч MonitorRouter до заморозки B04 — только через Оператора по контракту.

## Формула готовности к спеке
`Спека = A.1-A.5 (с поправками) ∧ B1∧B2∧B3 закрыты Никитой ∧ C1 подписан с B04 до его заморозки ∧ канон Δ0`. B4/B5 (состав плагинов, место кнопки) могут решаться внутри спеки; B1/B2/B3 — только до неё.

— 009 · этап-5 сдан · полный вердикт в цепи прогона (task ses_f8776cc6fffeETqs6NA820QCIe)
