# HANDOFF-TO-HUB-B03 · пакет зоны Show для ХАБа → очередь 007_2 (песочница) → потом 007 (main) · 2026-09-07

> **От:** agent-show [B03] · **Кому:** agent-rehearsal [B01-HUB] (диспетчер очереди 007_2 по слову Никиты)
> **Протокол Никиты:** все решения сначала отрабатываются в песочнице **007_2 — он применяет**; только после этого отдаём **007 (main)**. 007_2 один → ХАБ выстраивает порядок для него. Этот пакет = мой вклад в очередь.
> **Источник:** REPORT-2026-09-07.md (полный итог прогона, цепь 001→005×5→002→001-рев→009, вердикт 009: ЦЕПЬ ДЕРЖИТ, 40/40 клеймов).

---

## 1. СТАТУС ЗОНЫ (для твоего среза, обнови строку Show)

Show [B03]: ④ **ВСЯ ЦЕПЬ ЗАВЕРШЕНА** (13:17, `26f95f5`): этап-0 скауты ×4 → 001 «СТЕК-СЕССИЯ» → 005 ×5 → 002 (ДЕРЖИТ С УСЛОВИЯМИ, У1-У5) → 001-ревизия (каркас v2, стоп-краны R1-R7) → **009: ЦЕПЬ ДЕРЖИТ, готовность к Оператору = ДА**. Ждём: 🔴-развилки Никиты ×4 → GO → очередь 007_2.

## 2. ЧТО СТРОИМ (суть одной строкой)

**СТЕК-СЕССИЯ v2** — манифест записи (сценарий+трек+HTML+источники+пресет) как first-class концепт: префлайт до REC → полный кадр (музыка+голос) → HTML-мост оживляет слайды → пресет-ZIP с шарингом → ИИ собирает тот же стек. Фаза-1 закрывает все условия стресса У1-У5.

## 3. ОЧЕРЕДЬ ДЛЯ 007_2 (моё предложение порядка; финальный порядок — твой, ХАБ)

**Принцип сортировки:** сначала максимальная ценность/доказательство (геймчейнджер), сначала Show-local (нулевые стыки с чужими зонами), стыки — после 🔴 Никиты.

| # | POC (песочница) | Слой | Что применяет 007_2 | Файлы зоны (Show-local) | Смоук-глаз (Show_006) | Статус готовности |
|---|---|---|---|---|---|---|
| **P1** | **HTML-мост + авторинг-контракт** ⭐ геймчейнджер | L4 | MessageChannel+port-transfer+rAF-батч (0 зависимостей); runtime-сниппет приёмника; 2-3 встроенных live-слайда; liveness: freeze-last-value + `--bl-rec` при REC, `--bl-t` без трека; per-URL revoke | PresenterDock.tsx · show.html.service.ts (:24-27 кость) · сниппет+слайды = новые файлы в components/Show/ | **S4** | ГОТОВ к применению (все удары 002 закрыты в v2) |
| **P2** | **Полный кадр: музыка+голос** | L3 | WebAudio pre-mix одним аудиотреком (program-capture tap + raw-mic MicSourceV3.acquire) → MediaRecorder start(1000); попутно умирает утечка displayStream (009-находка); перекрёстный гейт с takes-REC | recording.store.ts · (takes-стор: только гейт-хук) | **S1, S2** | ГОТОВ |
| **P3** | **Пресеты-ZIP + шаблоны** | L5 | `belive-show-preset` v1 (show.json + assets/, transitionPreset=ID-ссылка); zod safeParse (уже в deps); пре-гарды бомб (size/count/ratio по центр-директории ДО extraction + magic-bytes); идемпотентная миграция на импорте; 3 шаблона (Урок/Разбор/Эпизод) в src/data/showTemplates.ts | зона Show + upload.service (гарды — общий лифт, аккуратно) | **S6, S7** | ГОТОВ |
| **P4** | **Префлайт-бейдж** | L2 | бейдж готовности на REC-кнопке PresenterDock (mic/сценарий/HTML/tier); отказ с причиной, не молча | PresenterDock.tsx | **S3** | ГОТОВ |
| **P5** | **Persist-delta стека** | L1 | слоты sources/presetId в существующий автосейв (ShowScenarioPersisted расширяется); SessionStack = derived-view, без нового стора | show-editor.store.ts · idb.service.ts | — (тихий) | ГОТОВ, но сначала проверить дубль show-presentation.store (флаг 009) |
| **P6** | **ИИ-тулзы ACTION-мин** | L6 | 4 макро-тулза (propose_session_stack, set_stack_source, set_stack_preset, preflight_stack) на живом парсере [ACTION:] + Show-контекст-билдер (паттерн TRACK CONTEXT); гейт подтверждения по образцу PLAYER_TOOLS | ai-tools.ts · AiExpertPanel.tsx | **S8** | ГОТОВ (tools-param = фаза-2, gateway-развилка у Никиты) |

**Порядок мой: P1 → P2 → P3 → P4 → P5 → P6.** Логика: P1 — доказательство геймчейнджера (и твой Z-видео-контракт хаба, см. §4), P2 — вторая половина флагмана (запись), P3-P4 — продукт-целостность, P5-P6 — фундамент для ассистента. Но **очередь организуешь ты** — если твой Z-план (Z1→Z2→…) требует другой порядок, я подчиняюсь: мои POC'и Show-local не конфликтуют с твоими Z (кроме §4-стыков).

## 4. СТЫКИ С ТОБОЙ (хаб-территория — не моя, только флаги)

1. **BillyDock в кадре записи** (App.tsx:77,246 — твой файл): маскот рендерится при презентации и попадает в getDisplayMedia-запись. **🔴 Никиты**: прятать при REC vs маскот-фича. До решения — P2 не трогает App.tsx (гейт внутри Show-стора).
2. **Мёртвый rec-код** (ControlDeck.tsx:137,147,152 — твоя зона B01-HUB): ветка t.id==='rec' без модуля. **🔴 Никиты**: вычистить vs зарегистрировать живой модуль. Show не зависит от неё (префлайт = бейдж на REC-кнопке), но при твоём Z1/Z7 (плитки дока) она всплывёт.
3. **Stem-вары при REC** (stem-reactive wrapper — foundation, общий): мой R6 требует liveness-политику. **🔴 Никиты**: freeze-last-value (дефолт цепи) vs ноль. Влияет на твой video-zone: если HTML-слайды в кадре видео, freeze смотрится живее нулей.
4. **Твой Z-контракт (video-zone REF-2)**: ты пометил мой T1/L4 «если postMessage недостижим → мост в песочницу, не в прод». 005+002+009 подтвердили: **достижим** (MessageChannel native, opaque origin — легальный wildcard-handshake + port). НО: мост оживляет только HTML с нашим сниппетом (авторинг-контракт) — пользовательские старые слайды статик. Если твой video-zone предполагает произвольные HTML — стыкуем мои сниппет-слайды с твоим Z-контрактом при P1.
5. **Gateway tool_calls** (gateway/src/index.ts:294 — бэкенд 007/200): фаза-2; ACTION-грамматика уже живая. **🔴 Никиты**: стык-флаг vs ACTION-only.

## 5. СМОУК-ЛИСТ ГЛАЗ (S1-S8) — уже в CHANNEL-show (13:20)

Show_006 готов проверять: S1 музыка+голос в webm · S2 гейт параллельной записи · S3 бейдж · S4 live-слайд анимируется внутри iframe / замирает при REC+--bl-rec / живёт на --bl-t без трека · S5 чистый пере-handshake · S6 экспорт/импорт с превью, бомба отклоняется · S7 3 шаблона · S8 чат собирает стек карточками. **P1-P6 закрывают все 8.**

## 6. ЧТО МНЕ НУЖНО ОТ ТЕБЯ

1. Вставь P1-P6 в общий порядок 007_2 (твой приоритет — закон; мои аргументы в §3).
2. Решения по §4-стыкам 1-3 — через твои 🔴 к Никите (они и мои, но файлы твои — слово хаба весомее).
3. Когда 007_2 начнёт P1 — скажи в реестре, я подниму Show_006 на смоук S4 сразу после применения.

— agent-show [B03] · 07.09 14:0x · пакет собран, очередь в руках хаба 🎭🏗
