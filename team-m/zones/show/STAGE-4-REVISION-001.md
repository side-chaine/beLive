# STAGE-4-REVISION-001 · B03 Show · «СТЕК-СЕССИЯ v2» · 2026-09-07

> Этап-4 зонного прогона. 001-ревизия принимает удары 002 (все 6 проверены по живой кости, клеймы держат; пути 002 местами уточнены: stem-reactive = foundation/event-bus/wrappers:43-51, PresenterDock = components/Show/:115-129,300, AiExpertPanel = TrackInfoBoard/:373-379).

---

## МЕТА-СТРОКА 001-РЕВ

**001-РЕВ: каркас v2 = СТЕК-СЕССИЯ с авторинг-контрактом моста и ACTION-first ассистентом; фаза-1 = L1 persist-delta, L2 бейдж+гейт, L3 pre-mix, L4 мост+сниппет+встроенные слайды, L5 ZIP+гарды+3 шаблона, L6 ACTION-мин; стоп-кранов = 7; вердикт = УСИЛЕНИЕ С РЕФОРМО-КРАЯМИ; готовность к 009 = спящие консьюмеры ЗИМОЙ (не проверены), Chrome multi-audio-track, AEC×WebAudio, runtime tools-модели, gateway-флаг.**

## 1. РАЗБОР УДАРОВ 002 (все ПРИНЯТЫ)

| Удар | Вердикт | Чин в v2 |
|---|---|---|
| U-T1 приёмник без контракта (крит) | ПРИНЯТ | L4 += авторинг-контракт: runtime-сниппет приёмника + 2-3 встроенных live-слайда в поставке; rec_html_* честно статик до пере-авторинга |
| U-T4 gateway режet tool_calls (крит) | ПРИНЯТ | фаза-1 L6 на [ACTION:]-грамматике (ai-tools.ts:124-152 жив); tools-param = фаза-2 через стык-флаг |
| U-stem-гашение при REC | ПРИНЯТ | liveness-контракт моста: при REC freeze-last-value + --bl-rec; no-track — мост шлёт --bl-t (время) для не-аудио анимации |
| U-zip-бомба | ПРИНЯТ | пре-гарды ДО extraction: size-cap, entry-count, ratio по metadata центр-директории JSZip + magic-bytes (ныне по расширению upload.service.ts:718-722) |
| U-двойной рекордер | ПРИНЯТ | перекрёстный гейт в обоих start-путях (У3), фаза-1 |
| U-rec-призрак (модуля rec нет) | ПРИНЯТ | префлайт = бейдж на REC-кнопке PresenterDock:303-314, не таб; мёртвая ветка → развилка Никите |

## 2. КАРКАС v2 (дифф от v1)

- **L1:** + persist-delta (слоты sources/presetId в существующий автосейв idb.service.ts:536-539); SessionStack остаётся derived-view без нового стора.
- **L2:** − rec-таб (призрак); + префлайт-бейдж на PresenterDock; + гейт двойной записи.
- **L3:** + WebAudio pre-mix одним аудиотреком + start(1000) (T2 как есть); mic только listen-tap (R3 держит).
- **L4:** + авторинг-контракт (сниппет + встроенные слайды); + liveness/freeze + --bl-t; + per-URL revoke вместо revokeAll (show.html.service.ts:24-27 готов); re-handshake на шаг — ПРИНЯТЬ как дешёвый (раз на смену шага, не на кадр).
- **L5:** ZIP держит; transitionPreset = ID-ссылка на TRANSITION_PRESETS (не инлайн-тип чужой зоны); + пре-гарды.
- **L6:** фаза-1 = ACTION-грамматика 4 макро-тулз + новый малый Show-контекст-билдер (паттерн TRACK CONTEXT AiExpertPanel:373-379); tools/require_parameters → фаза-2.
- **L7:** без изменений — интерфейс-декларация; кодо-переиспользование bridges запрещено явно.

## 3. ФАЗИРОВАНИЕ

- **Фаза-1 (для Оператора):** L1+L2+L3+L4(сниппет+встроенные слайды)+L5(гарды+3 шаблона, «Стемы»-шаг → open-studio-mixer — единственная фича featureRegistry.ts:63)+L6-min(ACTION). **У1-У5 все закрыты в фазе-1.**
- **Фаза-2:** tools-param через gateway-флаг, Billy-монтаж, html-шаги шаблонов, camera-PIP, URL-шаринг, оценка реформы фотолинии.
- **Фаза-3:** L7-реализация (стрим поверх rehearsal-транспорта, интерфейсно).

## 4. СТОП-КРАНЫ v2 (7)

- **R1** sandbox — держит, заострён: сниппет ≠ ослабление; не принят авторами → мост в песочницу.
- **R2** tsc-рост — держит.
- **R3** mic vs V3 — держит (pre-mix снимает часть).
- **R4** ZIP — расширен гарды-бомбы.
- **R5 (нов)** двойной рекордер: без гейта L3 не включается.
- **R6 (нов)** liveness: мост не публикует мёртвые вар.
- **R7 (нов)** per-URL: revokeAll запрещён в мост-пути.

## 5. НАХОДКИ 005 В ФИНАЛЕ

T1 — транспорт берём как есть, приёмник = адаптация (сниппет-контракт новое). T2 — как есть. T3 — формат берём, transitionPreset → ID-ссылка, + пре-валидация. T4 — адаптация: ACTION-first, tools → фаза-2. T5 — берём, «Стемы»-шаг заменён, html-шаги фаза-2.

## 6. ВЕРДИКТ: УСИЛЕНИЕ С РЕФОРМО-КРАЯМИ

Принята честность-поправка 002: L4-авторинг меняет смысл html-шага, L6-gateway = стык чужой зоны (отложен). Ядро L1-L3/L5 — чистое усиление. Реформа фотолинии — фаза-2+ после доказательства моста.

## РАЗВИЛКИ НИКИТЕ (🔴)

- 🔴 BillyDock в кадре записи (App.tsx:246): прятать при REC vs маскот-в-видео как фича.
- 🔴 Мёртвый rec-код ControlDeck.tsx:137-152: вычистить vs зарегистрировать живой rec-модуль.
- 🔴 Gateway delta.tool_calls (gateway/src/index.ts:294): дать стык-флаг на фазу-2 vs жить на ACTION-грамматике.
- 🔴 Stem-вар при REC: freeze-last-value (дефолт 001) vs честный ноль.
