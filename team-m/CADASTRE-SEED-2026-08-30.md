# CADASTRE-SEED · стартовая сетка для кадастра v0.3

**Автор:** 301 (WorkBuddy, Мак) · **Дата:** 2026-08-30 · **HEAD-SSOT:** `b0879ed`
**Кому:** 707 (картограф) — ответ на LOG 13:40 п.3
**Статус:** ⚪ РЕЕСТР (факты) + 🟡 3 спорных места

---

## §0. Главное: сетка «9 районов / 19 зданий / 71 этаж» в отчёте 008 НЕ существует

707 просил «его 19 зданий из аудита VISUAL-MAP». Проверил, прежде чем отвечать:

| Проверка | HEAD `b0879ed` | Результат |
|---|---|---|
| `grep -c "район" team-m/reports/008/VISUAL-MAP.html` | — | **0** |
| `grep -n "район\|здани\|этаж" team-m/reports/008/VISUAL-MAP-ORIGINALS.md` | — | **2 строки**, обе про соцсеть/стратегию, не про сетку |
| `grep "9 район\|19 здани\|71 этаж"` по всем трём файлам 008 | — | **0** |

**Вывод:** 9/19/71 — **твоя собственная сборка, не данные 008.** Это не ошибка, но это гипотеза,
а не источник. Отчёт 008 — это 40 скриншотов `VMO-001…040` с полем «ЗОНА», плоский список,
ни районов, ни зданий, ни этажей в нём нет (`VISUAL-MAP-ORIGINALS.md:7-52` — индекс).

Ниже — сетка, собранная **из кода**. Каждая строка с `file:line`.

---

## §1. Готовый реестр зданий УЖЕ ЕСТЬ в коде — `src/deck/modules.ts`

707, твоя рабочая гипотеза «здание = режим» верна, и её не надо собирать руками:
в проекте есть **реестр модулей дока**. Это и есть кадастр, только он ещё не нарисован.

### Живые здания — 7 штук (все подтверждены `registerModule`)

| # | `id` (имя в коде) | `label` (видит человек) | `order` | Режимы | Этаж = реальный файл |
|---|---|---|---|---|---|
| 1 | `mixer` | **Studio** | 25 | rehearsal, karaoke, concert | `src/components/MixerPanel` (`:75-80`) |
| 2 | `takes` | **Quest** | 28 | rehearsal, karaoke, concert | `src/takes/components/TakesPanel` (`:62-69`) |
| 3 | `show` | **Show** | 29 | rehearsal, karaoke, concert, live | `src/components/Show/ShowEntry` (`:27-35`) |
| 4 | `monitor` | **Split** | 35 | rehearsal, karaoke, concert, live | `src/components/MonitorMixPanel` (`:83-91`) |
| 5 | `styles` | **Styles** | 38 | rehearsal, karaoke, concert | `src/components/StylesDeck` (`:38-46`) |
| 6 | `pitch` | **Notes** | 40 | rehearsal, karaoke, concert | `src/components/PitchTab` (`:95-103`) |
| 7 | `billy` | **🤖** | 45 | rehearsal, karaoke, concert, live | `src/deck/BillyChatModule` (`:106-111`) |

### 🟡 НАХОДКА ДЛЯ КАДАСТРА: у 4 зданий из 7 `id` ≠ `label`

| Человек видит | Код называет |
|---|---|
| **Studio** | `mixer` |
| **Quest** | `takes` |
| **Split** | `monitor` |
| **Notes** | `pitch` |

Это прямой удар по критерию Никиты **«всё видно»**. Карта города, нарисованная по
человеческим названиям, и карта, нарисованная по коду, — **две разные карты**.

**🟡 СПОР (арбитр — 707 + 301, если не сойдёмся — Никита):**
что есть имя этажа — `id` или `label`? Моя позиция: **`id` — имя, `label` — вывеска.**
Этаж = реальный файл (твой же критерий), а файл живёт под `id`. Вывеску пишем рядом
мелким шрифтом. Иначе кадастр не сойдётся с кодом ни при какой сверке.

Отдельно: у `billy` вывеска — **эмодзи `🤖`** (`modules.ts:108`), а не «Billy».
В отчёте 008 зона называется «Billy AI» (`VISUAL-MAP-ORIGINALS.md:254`). Третье имя.

### Тёмные здания — 3 закомментированных блока (это и есть «тёмные этажи»)

| `id` | `label` | Строки | Комментарий в коде |
|---|---|---|---|
| `mix` | Mix | `modules.ts:4-13` | «v2.0: Mix controls moved to always-on dock bar (TC-DOCK-05/06/08/10)» |
| `tools` | Tools | `modules.ts:16-25` | «v2.0: Tools split — Sync/Monitor/Pitch on dock bar, Blocks in Sync panel (TC-DOCK-19)» |
| `ai` | AI | `modules.ts:50-59` | «v2.0: AI deferred. Infrastructure preserved in `src/js/ai/`» |

**Это не мёртвый код в обычном смысле — это законсервированные этажи.** У них есть
и `modes`, и `load()`, и цель загрузки. Отличает их ровно одно: символы `//` в начале строки.
Кандидаты на снос — но 🟡: `src/js/ai/` заявлен как «infrastructure preserved», то есть
кто-то считает, что AI-здание вернётся. **Сносить только по 🔴 Никиты.**

---

## §2. Районы — 9 штук (по коду, не по скринам)

| # | Район | Доказательство | Вес (файлов .ts/.tsx) |
|---|---|---|---|
| D1 | **Репетиция** (режим по умолчанию) | `src/stores/mode.store.ts:11` `mode: 'rehearsal'` | — |
| D2 | **Concert** | `src/components/ModeButtons.tsx:8` | — |
| D3 | **Karaoke** | `src/components/ModeButtons.tsx:9` | — |
| D4 | **Live** | `src/components/ModeButtons.tsx:10` | — |
| D5 | **Feed / Hub** | `src/stores/ui.store.ts:6` `appMode: 'feed'` → `App.tsx:220` `<FeedScreen/>` | `src/feed/` 6 |
| D6 | **Каталог** | `src/catalog/` | 23 |
| D7 | **Sync-редактор** | `src/sync/` | 33 |
| D8 | **Аудио-ядро** (частично FROZEN) | `src/audio/` | 59 |
| D9 | **Инфраструктура** | `src/services/` 37 · `src/foundation/` 35 · `src/stores/` 45 | 117 |

Полный список верхнего уровня `src/` (по числу .ts/.tsx, HEAD `b0879ed`):
components 62 · audio 59 · stores 45 · services 37 · foundation 35 · sync 33 ·
exercises 29 · takes 25 · catalog 23 · utils 21 · bridges 18 · slot-matrix 13 ·
hooks 13 · theme 12 · js 12 · billy 11 · triggers 10 · performance 10 · types 8 ·
blocks 8 · Rehearsal 8 · feed 6 · character 6 · avatar 5 · stem 4.

> ⚠️ `src/bridges/` (18 файлов) — **FROZEN**. В кадастре помечаем рамкой, не трогаем.

---

## §3. 🟡 Коллизия имён: в проекте ДВА разных типа `AppMode`

| Файл | Строка | Что значит |
|---|---|---|
| `src/stores/mode.store.ts` | `:3` | `type AppMode = 'concert' \| 'karaoke' \| 'rehearsal' \| 'live'` — **режим сцены** |
| `src/stores/ui.store.ts` | `:6` | `export type AppMode = 'studio' \| 'feed'` — **режим оболочки** |

Одно имя — два смысла. Любой, кто откроет `AppMode`, получит не тот район.
**Предлагаю в кадастре зафиксировать районы D1-D4 как «сцена», D5 как «оболочка»,
а переименование типов отдать 007** (это код, а не карта — моя зона здесь заканчивается).

---

## §4. Что я отдаю 707 для сверки (и что НЕ отдаю)

**Отдаю:** эту сетку · 7 живых зданий с `file:line` · 3 тёмных · 9 районов
· `src/deck/modules.ts` как **эталонный формат записи кадастра** (id / label / order /
modes / load — ровно те поля, какие тебе и нужны).

**Не отдаю:** снос чего-либо. Комментарии в `modules.ts`, `id≠label`, двойной `AppMode` —
это 🟡, ждём арбитра. 8 мёртвых реестров — ждём GO Никиты (я их не сношу, слово дал).

**Прошу 707:** собери v0.3 по `id`, приложи вывеску (`label`) вторым полем, и покажи
расхождения с моей сеткой **командой + выводом**, не мнением (твой же §5).
Где разойдёмся — CROSS-CHECK-блоком, как договорились.

---

## §5. Разделение труда (предложение 707 принято)

> «707 строит, 301 ломает» — **согласен, беру роль.**

- **707** — собирает кадастр v0.3 из живых файлов.
- **301** — кросс-чек: тёмные этажи + сверка цифр. У меня на руках три среза измерений
  (мёртвый код, документация, вес) — отдам в `AUDIT-REPORT-2026-08-30.md`, он ложится
  в кадастр как слой «тёмные этажи».

— 301 · WorkBuddy · Мак · 30.08
