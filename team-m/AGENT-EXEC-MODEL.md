# 🤖 МОДЕЛЬ ИСПОЛНЕНИЯ АГЕНТНОЙ СИСТЕМЫ beLive
> Автор: 007_Винда. Статус: **ОБЯЗАТЕЛЬНО к исполнению** (коррекция Босса, 26h).
> Дополняет прежнее неявное «chain 001→002→009»: добавляет фазу скаутов и ЖЁСТКОЕ правило параллелизма. SSOT-контекст цепи — `team-m/prompts/SHARED.md`.
> **Слиты поправки Мака (Far Light), BRIEFING-AGENT-SYSTEM-UPDATES-2026-08-26:** §8 (скауты параллельно ДО 001 И по ходу, ведущий решает момент/число), §9 (полный контекст на блюдечке). Плюс реальность среды (деградация каналов) из журнала 25.08.

## 🚫 ЖЕЛЕЗНОЕ ПРАВИЛО ПАРАЛЛЕЛИЗМИИ
- **Параллельно запускаются ТОЛЬКО СКАУТЫ** (`arch-scout`, `gateway-scout`, `sync-scout`, `explore`, `general` как research-скаут). Только по **РАЗНЫМ фронтам** (независимый сбор сырой инфы). Скауты — **и ДО 001 (подготовить фактуру), и ПО ХОДУ прогона** (по решению ведущего агента: 007/Hub на PC, Мак/Far Light на Far). Скауты НЕ решают — только собирают/подсвечивают факты (file:line, grep/git-выводы, инвентари).
- **001 / 002 / 005 / 008 / 009 / Operator — СТРОГО ПОСЛЕДОВАТЕЛЬНО.** Каждый агент читает вывод предыдущего и накапливает общий контекст. Параллельный диспатч этих агентов **ЗАПРЕЩЁН** — даже по разным волнам/задачам. Суть цепи именно в накоплении контекста от звена к звену; параллельный спавн ломает этот смысл.

## 🔄 ПОТОК (для каждой задачи / волны)
- **Фаза 0 — SCOUTS (parallel, ДО 001 И mid-run):** 007 запускает нужных скаутов параллельно по разным фронтам — как ДО старта 001 (готовая фактура), так и ПО ХОДУ, если вскрылась новая дыра/фронт. Скауты read-only, решений не принимают, frozen не трогают.
- **Фаза 1 — 001 (CEO Co-Architect):** читает выводы скаутов + контекст, организует/аудит, выдаёт дизайн.
- **Фаза 2 — 002 (Stress-Test):** читает вывод 001, атакует, ищет failure modes. Вердикт: ДЕРЖИТ | ТРЕБУЕТ ПАТЧА | ПАДАЕТ.
- **Фаза 3 — 001 re-check:** читает вывод 002, консолидирует патчи, финализирует дизайн.
- **Фаза 4 — 009 (Independent Verification):** читает все предыдущие, runtime-аудит + DOC-CHECK + Registry + frozen re-check. Вердикт: РЕШЕНО | ОТЛОЖЕНО | ОТКЛОНЕНО.
- (опц.) **005 Booster** — по решению 001/007 на этапе дизайна (context7-поиск подходов из других проектов).
- (опц.) **Operator** — только на GO владельца, слепо применяет финальный MICRO-PACK (tsc / vitest / guard-гейты).

## 🎯 РОЛЬ 007 (координатор)
- Решает, **каких скаутов и когда** запускать — в т.ч. **ПО ХОДУ прогона** (mid-run scouts), если вскрылась новая дыра/фронт.
- Диспатчит агентов 001→002→001→009 **строго последовательно**.
- Упаковывает финальный MICRO-PACK + делает **doc-reconcile** (SSOT: MICRO-PACK + WAVE-HANDOFF-INDEX + REGISTRY) после вердикта 009, чтобы закрыть doc-drift (иначе 009 выдаёт ОТЛОЖЕНО).

## ⚖️ ЭСКАЛАЦИЯ / РЕТРАИ
- Счётчик провалов роли; 2 провала reasoning → близнец `-hi`; кап ретраев 2 → СТОП + доклад владельцу.

## 📌 ПРИМЕР (волна миграции)
`scouts(arch-scout + sync-scout + gateway-scout, parallel)` → `001(recon+дизайн)` → `002(stress)` → `001(recheck)` → `009(verify)` → `007(doc-reconcile)` → **[GO владельца]** → `Operator(apply)`.

## 🍽️ ПОЛНЫЙ КОНТЕКСТ НА БЛЮДЕЧКЕ (§9, поправка Мака)
Каждому основному агенту — **ВСЁ сразу на входе**, чтобы агент бил по существу и не тратил токены на re-derivation. 007 вкладывает в диспатч:
- полные выводы **всех** предыдущих агентов цепи (не саммари, а целиком);
- карту «кто кого слушает» (event/listener map): `ASSISTANT_RESPONSE_COMPLETED` → `CharacterSoundManager` + будущий `avatar.store`; цепь `aiHub.sendMessage.onDone` → звук + аватар;
- структуру волн **W1–W5**, grep-гейты, точные `file:line`;
- зафиксированные решения: REGISTRY/SYNC/вердикты 009/flip-коммит `2395c1e7`/канон `306/767+5int+2load`.
Цель — **ноль re-derivation**.

## 📡 РЕАЛЬНОСТЬ СРЕДЫ (деградация сабагентных каналов — журнал 25.08)
- **named-spawn мёртв**: `ox-alpha-free` как named-спавн не существует; скауты/`console-go` **блипают** (эпизодически недоступны). Деградация: `named → general + Функция → инлайн-роли`.
- **Следствие для 007**: не блокировать цепь на скауте. Если нужный скаут блипнул → (а) повторить; (б) заменить на `general`/`explore` или инлайн-сбор (007 сам делает grep/read); (в) `arch-scout` может выступать «носителем» (SCOUT-OK-канал) для research-ролей при мёртвом фритере.
- **Стабильные каналы**: `Operator` (`big-pickle`, 49/49) и инлайн-роли 007 — идём уверенно. Frozen-guard/`tsc`/`vitest` — локально, без сабагентов.

## 🔗 КАНОНИЧЕСКИЕ ЦЕПИ (registry §6, не менялись)
- `GO_001` = `[001 → 002 → 001 → 009]`
- `GO_005` = `[005 → 002 → 009]`
- `GO_009` = standalone `[009]`
- `GO_002` = standalone `[002]`

## 🔗 СВЯЗЬ С ДРУГИМИ ДОКАМИ
- Цепочка исполнения волн: `team-m/WAVE-EXEC-PLAYBOOK.md`.
- Спецификации цепей/счётчиков: `team-m/SUBAGENT-SETUP.md`.
- SSOT-реестр: `team-m/REGISTRY.md`.
- FROZEN-ZONE + вердикты цепи: `team-m/prompts/SHARED.md`.
