# MICRO-PACK-DOCS-REFORM v1.0 — реформа системы документов перед постройкой города

**Автор:** Опус-5 (внешний со-Архитектор) · **HEAD:** `93eb1e6` · **Дата:** 2026-09-03 07:4x (время машины Linux)
**Метод:** машинная перепись (`find`/граф-обход ссылок между 586 .md), чтение шапок, сверка с CI. Ноль правок доков.
**Развилка ④ Никиты — принята:** `docs/architecture/frozen-zones-v2.md` → `history/` целиком.
**Потребители:** 003 + 003_2 (исполнение), 201 (архитектура гейта G-3), 301/707 (город — потребитель результата).
**Статус:** спека-сырьё для цепи. Не истина.

---

## §0 · ПРИНЦИП, ИЗ КОТОРОГО СЛЕДУЕТ ВСЁ

**У каждого дока ровно одно состояние, и оно объявлено в первой строке.**

| Состояние | Правило | Что делает машина |
|---|---|---|
| 🟢 **LIVING** | контракт с кодом: числа и `file:line` обязаны совпадать с деревом | **G-3 проверяет каждый коммит** |
| 🗄 **HISTORY** | заморожен датой; **правка = фальсификация замера** (правило E1-3 пары 003 — уже ваше) | G-3 **не смотрит** |
| 📚 **REFERENCE** | внешнее: исследования, выжимки, статьи | G-3 не смотрит |

Сегодня 236 из 254 доков в `docs/` **не говорят о себе ничего** — читатель (человек или агент) обязан догадываться, контракт перед ним или воспоминание. Это и есть корень всех дрейфов вида «доки 18 / машина 17».

---

## §1 · ДИАГНОЗ ЧИСЛАМИ (карточки клеймов)

| # | Утверждение | Доказательство | Проверено |
|---|---|---|---|
| Д-1 | **765** `.md` в репо без `node_modules`: **254** `docs/` · **332** `team-m/` · 5 корень | `find . -name "*.md" -not -path "./node_modules/*" -not -path "./.git/*" \| wc -l` | Опус |
| Д-2 | **101 из 254** доков в `docs/` — **сироты**: ни один другой `.md` (из 586 в `docs/`+`team-m/`) на них не ссылается ни по пути, ни по имени файла | скрипт-граф по basename+path, корпус 586 файлов | Опус |
| Д-3 | **589** упоминаний `frozen/FROZEN/❄` в `docs/` | `rg -o "frozen\|FROZEN\|❄" docs --glob '*.md' \| wc -l`. Топ: `n-stem-architecture.md` 51 · `architecture-map-2.1.md` 44 · `architecture-doctrine.md` 36 · `ADR-0015` 20 · `ADR-0001` 19 | Опус |
| Д-4 | Лишь **18 из 254** несут маркер `HISTORICAL/SUPERSEDED/устарел` ⇒ 236 молчат о своём статусе | `rg -l "HISTORICAL\|SUPERSEDED\|устарел\|ИСТОРИЧ" docs --glob '*.md' \| wc -l` | Опус |
| Д-5 | **CI охраняет документ, который сам себя объявил мёртвым** | `doc-sync-check.yml` читает `docs/sync/MASTER-SYNC-REGISTRY.yaml`; строка `:2` того файла дословно: «⚠️ HISTORICAL (31.08): снапшот эры 2026-06/07. Живой SSOT — `team-m/SHARED-REGISTRY.md`. CI-гейт `doc-sync-check.yml` инертен (судьба — 🔴)» | Опус |
| Д-6 | Док-гейт **не стреляет ни разу**: он на `pull_request`, а волны идут коммитами в `main` | `.github/workflows/doc-sync-check.yml:2` `on: [pull_request]` | Опус |
| Д-7 | **В `docs/` лежит исполняемый код:** 22 файла в `docs/modernization/handoff/` — `.ts`, `.mjs`, `.patch`, `.bundle`, `errors-212.txt` | `find docs/modernization/handoff -type f` | Опус |
| Д-8 | Три скрипта из `handoff/new-files/scripts/` **уже живут** в `scripts/` (дубли), а `.d.ts` и патч-близнец CaptureWorklet — **нет** (это и есть неприменённый актив на −80 tsc) | `check-native-bindings.mjs`/`check-sri.mjs`/`check-manifest.mjs` есть в `scripts/`; `src/types/audio-worklet-global.d.ts` отсутствует | Опус, эмпирика |
| Д-9 | **Разные вещи с одним именем, все расходятся:** три `REGISTRY.md`, три `README.md`, два `INDEX.md`, два `00-INDEX.md` — **0 идентичных, 4 расхождения** | `docs/agents/REGISTRY.md` (самопомечен STALE, mtime 15.06) · `docs/modernization/REGISTRY.md` (29.08) · `team-m/REGISTRY.md` (01.09, живой) | Опус |
| Д-10 | **Архив по имени, но не по месту:** `docs/operations/packs/era-08-2x/` = 54+ файла эпизодных паков внутри живого `docs/`; они же — большинство сирот Д-2 | `ls docs/operations/packs/` → era-08-20/21/22/23 (13+21+20+3) | Опус |
| Д-11 | Возрастной профиль: **54** файла от 2026-06 · **163** от 08 · **37** от 09 | `find docs -name "*.md" -printf "%TY-%Tm\n" \| sort \| uniq -c` | Опус |
| Д-12 | `docs/screenshots/` — **12 бинарников**, `.md` = 0 | `find docs/screenshots -type f \| wc -l` | Опус |
| Д-13 | Пустых каталогов под `docs/` — **0** (структурного мусора нет, есть смысловой) | `find docs -type d -empty \| wc -l` | Опус |
| Д-14 | `docs/INDEX.md` **уже почти годный индекс** (30+ строк таблицы архитектуры со статусами) — реформе нужна не новая точка входа, а генерация существующей | `docs/INDEX.md:1-40` | Опус |

**НЕ ПРОВЕРЕНО (для 002/009):**
- Содержательная актуальность каждого из 254 доков — это и есть работа R-1, машинно не решается.
- `team-m/` (332 файла) в этом паке **не переписан**: там правило 48ч-архива уже действует, отдельная зона ответственности 200.
- Читает ли кто-то `docs/` глазами регулярно — неизвестно; сиротство (Д-2) — про ссылки, не про чтение.

---

## §2 · МАРШРУТ РЕФОРМЫ

### R-1 · ПЕРЕПИСЬ (003 + 003_2, ~полдня)

Каждый из 254 доков получает **одно** состояние. Правило разрешения споров, снимающее 90% обсуждений:

> **Если док утверждает что-то о коде — он LIVING, либо его надо пометить HISTORY. Третьего нет.**

Выход: **`docs/DOC-CENSUS.yaml`** — `path · state · owner · date · claims:[]`. Это же — вход для R-3 (генерация индекса) и R-4 (гейт).

Пара работает как всегда: 003 переписывает, 003_2 бьёт выборку (прецедент: он поймал хвост ФВ-4).

### R-2 · ФИЗИЧЕСКОЕ РАЗДЕЛЕНИЕ

```
docs/
  architecture/   🟢 LIVING — контракт с кодом (~38 файлов)
  adr/            🟢 LIVING — решения (ADR-0004 и др. — ЖИВЫЕ, не эра)
  guides/         🟢 LIVING — как работать
  reference/      📚 внешнее (исследования, SOUNDTOUCHJS-FULL, GPT-выжимки)
  history/        🗄 всё HISTORY:
                     operations/packs/era-08-2x/  (54+)
                     archive/superseded/           (5)
                     sync/  + sync/reports/        (43+2)
                     agents-hub/ · agents/         (20+7)
                     modernization/ — по переписи (37, часть ADR → adr/)
```

Точечные решения:
- `docs/modernization/handoff/` **расформировать**: `.d.ts` + патч-близнец CaptureWorklet → `src/` (это шаг **D-0b** волны де-фриза, не реформа) · скрипты-дубли → удалить (Д-8) · `.patch`/`.bundle`/`errors-212.txt` → `history/`
- `docs/screenshots/` (12 бинарников) → вне `docs/`
- `docs/architecture/frozen-zones-v2.md` → **`history/` целиком** (развилка ④). Причина: после D-3 понятия frozen в проекте нет; живой док про несуществующее — приглашение к следующему дрейфу. В `docs/INDEX.md` остаётся **одна строка-надгробие** со ссылкой в `history/`

### R-3 · ОДИН ИНДЕКС, ОДИН РЕЕСТР

- `docs/INDEX.md` — **единственная** точка входа, **генерируется** из `DOC-CENSUS.yaml`, не пишется руками (Д-14)
- Переименование двойников (Д-9):
  - `docs/agents/REGISTRY.md` → `history/agents-registry-2026-06.md`
  - `docs/modernization/REGISTRY.md` → `history/modernization-registry-2026-08.md`
  - **имя `REGISTRY.md` остаётся ровно за одним файлом — `team-m/SHARED-REGISTRY.md`-соседом**
- То же для `README.md` (три штуки) и `INDEX.md`/`00-INDEX.md` (по два): в каждом каталоге имя допускается, но в индексе явно указано, что чем является

### R-4 · G-3 — ГЕЙТ ДОК-КЛЕЙМА (идея пары 003, реализация минимальная)

Не моя идея: 003 предложил в опросе — *«док-клейм ↔ дерево как CI-шаг в одном конвейере с verify:frozen»*. Беру как есть и свожу к минимуму.

В LIVING-доках проверяемое число помечается якорем:
```md
<!-- claim: frozen-files -->0<!-- /claim -->
<!-- claim: tsc -->192<!-- /claim -->
```
`scripts/verify-doc-claims.mjs` сверяет значения с выводом `canon.mjs`. Первые клиенты — **7 носителей frozen-чисел** из PRE-D §D1: `frozen-zones-v2.md:2,3` (уходит в history) · `docs/architecture/README.md:15` · `src/foundation/event-bus/README.md:55` · `frozen-manifest.json note` · `WAVE-FROZEN-INVARIANTS.md:6` · `WAVE-EXEC-PLAYBOOK.md:10` (последние два реально лежат в `team-m/`, не в `docs/` — путь в PRE-D неточен, проверено).

Режим: **`warn` неделю → `fail`**. Без исключений.

### R-5 · УБРАТЬ ИНЕРТНЫЙ CI

- `doc-sync-check.yml` → заменить на G-3 (Д-5/Д-6: он валидирует мёртвый файл и не срабатывает по триггеру)
- `docs/sync/MASTER-SYNC-REGISTRY.yaml` + `.md` → `history/` (файл сам просит: `:2`)
- G-3 работает **на push в main**, не только на PR — иначе повторится Д-6

### R-6 · КОНВОЙ ВОЛНЫ D

**589 упоминаний frozen массово НЕ переписывать** — урок B++ (масс-правка = риск новой волны malformed). Правится **только**:

1. **слой-ядро** — 7 носителей чисел (см. R-4)
2. **`ADR-0004`** — живая ADR, не эра: `:14` «🔒 FROZEN, 7 вечных ошибок» устарело вдвойне (ошибок 7 → 0 в этом файле; судьба ≠ «шаг 8 удаляется»)
3. **`frozen-zones-v2.md`** → `history/` + надгробие в INDEX (развилка ④)
4. **хвост V1A — 6 K2-строк** из PRE-D §D0 (`takes-system.md:194`, `audio-engine.md:14,248,433,843,1067`) — **от волны не зависит, можно догнать раньше**
5. **`frozen-zones-v2.md:25`** «js/*.js (5 файлов) | ~1,000» — после Волны A живых 5, число неточно → wc-факт

**НЕ вычищать:** 16 вхождений вида `AudioEngineV2.ts:NNNN` в доках — это спецификация паритета V2→V3 (PRE-D §D3.2, директива подтверждена).

Остальные ~570 упоминаний живут как **история эпохи** — и это правильно: они в HISTORY-состоянии, G-3 их не смотрит, фальсифицировать замеры прошлого запрещено.

---

## §3 · ПОЧЕМУ ЭТО ДО ГОРОДА, А НЕ ПОСЛЕ

Аргумент не мой — он в вашем собственном кадастре. `team-m/bLb/houses.yaml:22-24`:

> `floors: каждый этаж = реальный файл/модуль живого дерева`
> `КРИТЕРИЙ ЭТАПА 2: 19+ зданий = живые режимы, ни одного этажа без файла`

Это **контракт живости** — ровно то, что не проверяет ни один существующий гейт, и ровно то, что делает G-1. Пока G-1 нет, «этаж = живой файл» держится на том, что кто-то посмотрел глазами.

Живой пример дрейфа уже в кадастре: `deck/modules.ts:4-13` — здание «Mix» закомментировано, компонент `VolumeControls.tsx` **не в бандле** (проверено немифицированной сборкой: 0 вхождений в `dist/`), а в `houses.yaml` `mix` числится законсервированным зданием **по комментарию, а не по факту достижимости**.

⇒ Город, построенный до G-1, — это карта с домами, про которые нельзя машинно сказать, стоят ли они. Порядок: **гейты → де-фриз → реформа доков → город.** Тогда город получает не только чистое дерево, но и машину, которая держит его карту правдивой — то, что 201 сам назвал: «маркер должен проверяться машиной, а не мной».

---

## §4 · СВОДНЫЙ ПОРЯДОК (обе программы вместе)

| # | Блок | Кто | Выход |
|---|---|---|---|
| 1 | D-0 гейты G-4+G-1 (`warn`) · **D-0b CaptureWorklet** | 007 + Оператор | **tsc 282 → 202** |
| 2 | D-0c три живых бага (решение Никиты ③) | 007 + смоук Никиты | Билли танцует, мик-фейдер живой |
| 3 | D-1 созвездие · D-2 фантом-шестерня · D-3 упразднение замка | 007, цепь 001→002→001→009 | **frozen 0/0 · tsc ~192 · −2756 строк** |
| 4 | **R-1** перепись · **R-2** разделение · **R-3** индекс | 003 + 003_2 | 254 дока в трёх состояниях, ноль двойных имён |
| 5 | **R-4** G-3 · **R-5** замена инертного CI · **D-4** G-1/G-2 `warn→fail` | 201 (архитектура) + Оператор | доки и живость держит машина |
| 6 | **R-6** конвой D | 003 | ноль живых расхождений |
| 7 | **ГОРОД** | 301/707/201 | кадастр на проверяемом контракте |

Параллельно, никого не блокирует: батч Никиты (Д-3 CF-скрины + Д-4 SEC-ротация + Search Console — одна 20-минутная сессия с распечатанным чек-листом).

---

## §5 · РИСКИ РЕФОРМЫ (сырьё для 002)

1. **Массовое перемещение файлов ломает ссылки** внутри доков (101 сирота — как раз те, кого не жалко; остальные 153 связаны). Защита: R-2 идёт **после** R-1, где связи уже описаны в `DOC-CENSUS.yaml`; перемещение — скриптом с переписыванием относительных ссылок, не руками.
2. **`git log` по перемещённым файлам** становится менее очевидным. Защита: `git mv`, один коммит на каталог, не «удалить+создать».
3. **G-3 в `fail` слишком рано** блокирует деплой на ложном срабатывании якоря. Защита: неделя `warn`.
4. **Перепись превращается в переписывание.** Защита: R-1 **только присваивает состояние**, не редактирует содержимое. Правки — отдельными шагами R-6.
5. **HISTORY-каталог станет свалкой** и через месяц кто-то начнёт «чинить» историю. Защита: правило E1-3 (фальсификация замера запрещена) — вписать в шапку `history/README.md` один раз.

---

*Опус-5 · MICRO-PACK-DOCS-REFORM v1.0 · каждое число — карточка клейма §1 с полем «проверено»; список «НЕ ПРОВЕРЕНО» — часть спеки. Идея G-3 принадлежит паре 003, я лишь свёл её к минимальной реализации.*
