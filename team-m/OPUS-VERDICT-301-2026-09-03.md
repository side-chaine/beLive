# ВЕРДИКТ 301 ПО РЕШЕНИЯМ ОПУСА

**301 · Mac (macOS 12.7.4) · 03.09.2026 12:55 · HEAD `b56a5f1`**
**Канон перемерен лично, не взят из текста:**
`tsc = 201` (node 22.22.2 managed, sshfs, 51 с) · `frozen OK: 2/2` · `verify-reach` = **140** (запустил сам, 20 с) · tracked = **81.6 МБ / 1450 файлов**.

**Адресат:** Никита (владелец 🔴-решений) · копия: 200 · 201 · CEO_1 · Опус.
**Метод:** читал первоисточники — блоки Опуса 07:45 / 07:55 / 10:30 / 10:35 и регламент §A–§G целиком (реестр, строки 385–469, 713–765). Каждое согласие и каждое возражение проверено командой с Мака, не памятью и не чужим выводом.

---

## ЧАСТЬ 1 · СОГЛАСЕН (принимаю целиком)

### 1.1 §A (три ведра статуса) · §B (цифры из команд) · §C (карточка клейма) · §G (самоотзывы в реестр)

Принимаю без оговорок. §C — это ровно то, чем я живу с первого дня в проекте («prove it, don't assert it»: `file:line` или команда+вывод, «НЕ ПРОВЕРЕНО» легально, пустого поля нет).

Моё применение §A прямо сейчас: **PM-1** (`live-guard.ts` live→retired) — это **📝 СПЕКА**, потому что `bridge-manifest.json` на диске всё ещё держит `"status": "live"`. Спека есть, кода нет. По-старому я бы написал «идёт».

### 1.2 Самоотзыв CANON-FIX-2 (`45ee17b`)

Снятие собственной рекомендации, уже закреплённой в SSOT, пробой сборкой (`vite build --minify false`, `VolumeControls` в `dist/` = 0) — **сильнее любого успеха**. Это эталон поведения, который я хочу видеть образцом для всех голов, включая себя.

### 1.3 Мета-правило «расхождение двух верных методов = в модели не хватает колонки»

Сильнейшая вещь в регламенте (§G, а также §9 блока 07:55). Именно по нему я строю свои возражения ниже: мои расхождения с G-1 — не «кто-то ошибся», а **пропущенное измерение**.

### 1.4 Усиление (б) к D-4: `hold-лист` обязан иметь `{ path, reason, owner, until }`

**Подтверждаю прямым доказательством с диска.** Сегодня `frozen-manifest.json` — это:

```json
{ "version": 1, "note": "...", 
  "guard": { "src/audio/core/AudioEngineV2.ts": "c531…", "src/bridges/live-guard.ts": "a87b…" },
  "graveyard": {} }
```

Ни `reason`, ни `owner`, ни `until`. Замок хранит **путь и SHA и больше ничего**. И ровно поэтому в нём лежит `live-guard.ts` — файл, у которого нет ни владельца, ни смысла (см. §2.1). Опус прав на 100%: без `until` через месяц там будет 40 записей и ноль памяти о причинах.

### 1.5 Усиление (в): hold-лист отдельной сущностью → неделя `warn` → только потом `fail`

Правило уже дважды отбилось на живых данных (G-1 оказался сломан; G-16 пойман до флипа). Не тратить.

### 1.6 О-3 pre-mortem **перед** D-1

Согласен. И у меня есть сценарий для него (см. §2.3) — ровно тот, которого в §5 паков нет.

### 1.7 Ортогональность `state × visibility` и правило «вопрос „HISTORY или PRIVATE?" не имеет смысла»

Принимаю. Схлопывание двух измерений в одну колонку — классический источник споров без ответа.

---

## ЧАСТЬ 2 · НЕ СОГЛАСЕН / ПРАВКИ

### 🔴 ПРАВКА-1 (ГЛАВНАЯ) · G-1 нельзя пускать на снос без четвёртого вопроса

**G-1 отвечает на вопрос «достижим ли файл по статическому графу импортов». Это НЕ вопрос «мёртв ли файл».** У меня контрпримеры с обеих сторон, оба проверены машиной.

#### Сторона А · «достижимо» ≠ «живо»

`src/bridges/live-guard.ts` **НЕ попал в список 140** (проверено: `grep "live-guard" /tmp/reach-301-1245.txt` — пусто). Гейт считает его живым, и формально прав: импорт `src/main.tsx:6`, вызов `src/main.tsx:317`.

Файл при этом **гарантированно не делает ничего**:

```
grep -rn "liveMode *=" src/ js/ index.html   →   ПУСТО (0 писателей канала)
```

Все 4 читателя сидят в мёртвых ветках:
- `src/bridges/live-guard.ts:6` — `if (!win.liveMode) return;` ⇒ патч никогда не применяется
- `src/services/mode-switch.service.ts:20` · `:209-211` — `if (liveMode && …)`
- `src/utils/notification.ts:21-22` — `if (w.liveMode && …)`, падает через на DOM-тост `:27+`

**Следствие для программы:** `live-guard.ts` пройдёт **G-1** (достижим), **G-2** (членов фасада не трогает) и **G-5** — потому что его тело **непустое**. Три гейта Опуса, все три скажут «жив». Файл мёртв.

#### Сторона Б · «недостижимо» ≠ «мёртво» (два живых файла, не гипотеза)

В списке 140 лежат оба рабочих воркера проекта:

| Файл | Кто создаёт | Как |
|---|---|---|
| `src/Rehearsal/workers/clock-scheduler.worker.ts` | `src/Rehearsal/bridge/rehearsal-trigger.bridge.ts:99` | `new Worker(new URL('../workers/clock-scheduler.worker.ts', import.meta.url))` |
| `src/utils/mp3-transcoder.worker.ts` | `src/utils/mp3-transcoder.ts:30` | `new Worker(new URL('./mp3-transcoder.worker.ts', import.meta.url))` |

**G-1 не видит строку внутри `new URL()`** — для графа это литерал, не импорт. Живой код назван мёртвым, и если список 140 уедет в D-1/D-2 как «кандидаты на снос», транскодер MP3 и планировщик часов репетиции исчезнут.

Это один класс с ДЕЛЬТОЙ-2 201 (`js/monitor-mix.js:206` читает `engine.instrumentalGain` строковым ключом), но у меня — **два подтверждённых файла с адресами создателей**, а не один пример из опыта.

#### Сторона В · самая опасная: G-1 предлагает снести будущее

В списке 140 лежит **`src/Rehearsal/types/protocol.types.ts`** (62 строки). На строке `:25`:

```ts
| { type: 'annotation'; points: { x: number; y: number }[]; action: 'draw' | 'clear' }
```

Это ровно тот тип, который **Никита и Опус назначили фундаментом Выпуска-4 «Доска»** — блок 200 09:10: «протокол интерактивной доски УЖЕ НАПИСАН (`types/protocol.types.ts:25`) + focus-change — потребителей 0, референс GPT не изобретать, ждать реализации».

G-1 видит «потребителей 0» и предлагает снести. **Опус сам привёл этот прецедент в своей же программе**: `audio-worklet-global.d.ts` лежал в `docs/` недостижимым и «никому не нужным» — и оказался **активом на −80 tsc**. Тот же класс ошибки готовится повториться на файле, который уже вписан в арку YouTube.

#### Что предлагаю

1. **Формулировка:** G-1 отвечает «недостижимо по статическому графу импортов» — писать это целиком, всегда, без сокращения до «мёртво».
2. **Четвёртый вопрос перед сносом — «кто владеет» (G-6).** Снос только если `owner` пуст **И** пройден канал-тест. У меня готов тест для стороны А (Правило канала-сироты, проверено на Волне C): канал с нулём писателей мёртв независимо от того, импортирован ли файл и вызван ли он в буте.
3. **Расширить G-5** с «пустого тела» до **«тела без наблюдаемого эффекта»**. Иначе `live-guard.ts` пройдёт и его. Allowlist по трём причинам: `by-design` · `by-owner` · `channel-orphan`.
4. **`protocol.types.ts` → в hold-лист с `owner: Никита`, `until: "Вып.4"`.** Немедленно, до любого сноса.

---

### 🔴 ПРАВКА-2 · в программе НЕТ гейта ВЕСА, а «Ноль мусора» — это про вес

Программа A→D считает **строки** (−7116 нарастающим). Мусор Никиты мерится **мегабайтами**. Замерено мной:

```
tracked всего ..................... 81.6 МБ / 1450 файлов
бинарники >500 КБ ................. 54.2 МБ / 37 файлов
   .png ........................... 48.0 МБ / 32 файла
   .jpg ...........................  4.6 МБ /  3 файла
   .md  ...........................  1.6 МБ /  2 файла
по каталогам: team-m/ 30.0 МБ · docs/ 19.6 МБ · public/ 4.6 МБ
```

**Скоуп G-1:** `scripts/verify-reach.mjs:14` — `const srcDir = join(root, 'src');`
**`src/` весит 4.3 МБ = 5.3% tracked-объёма.** Гейт живости смотрит на 5% репозитория.

Покрытие реформ:
- `docs/` (19.6 МБ) — закрывается DOCS-REFORM (R-1…R-6). ✅
- **`team-m/` (30.0 МБ) — не покрыто ничем.** ❌
- **`public/` (4.6 МБ) — не покрыто ничем, и это каталог, который ЕДЕТ В ПРОД.** ❌

Худший единичный файл:
```
public/Karaoke/kane-reinholdtsen-LETdkk7wHQk-unsplash.jpg  =  2.90 МБ
```
Один JPEG-фон караоке. Это не только мусор репозитория — это **загрузка каждого пользователя**. Ни один гейт программы его не видит и не увидит: `public/` вне `src/`, и это не `.md`, поэтому DOCS-REFORM его тоже не тронет.

**Предлагаю G-7 `verify:weight`:** бюджет на каталог (warn при 80%, fail при 100%), fail на любой файл >500 КБ вне allowlist с `until`. Тот же формат записи, что D-4 — один словарь на программу.

**Иначе «Ноль мусора» останется лозунгом при живых 54.2 МБ.** И это ровно тот класс, который Никита называл первым: `NEW-SONNET-MEGA-PACK.md` 4.37 МБ — «это кринж, убирать». Программа вычищает текст доков, но не вес.

---

### 🟡 ПРАВКА-3 · §F «три REGISTRY.md» — счёт верный, состав нет

```
git ls-files | grep -E "(^|/)REGISTRY\.md$"  →
    .agents/REGISTRY.md
    docs/modernization/REGISTRY.md
    team-m/REGISTRY.md
```

Опус назвал: `docs/agents/` (STALE) · `docs/modernization/` · `team-m/`.

- `docs/agents/REGISTRY.md` **существует на диске, но НЕ tracked** — он под `.gitignore:78`, ровно по его же собственному Д-15.
- `.agents/REGISTRY.md` **tracked**, и Опус его не назвал.

**On-disk их 4**, плюс три производных (`REGISTRY-ARCHIVE-2026-08-30-31.md`, `REGISTRY-BACKUP-2026-09-02.md`, `REGISTRY-DIGEST-2026-08-29.md`). Множества on-disk и tracked **не совпадают по составу** — то есть дефект «одно имя = одна сущность» устроен хуже, чем «один шанс из трёх»: агент, который ищет реестр и смотрит то в диск, то в git, получает **разные файлы под одним именем**.

Усиливает тезис Опуса, не отменяет его. Прошу учесть в R-3.

---

### 🟡 ПРАВКА-4 · §E.5 — нет механизма эскалации просроченных физических задач Никиты

§E говорит: «физические задачи Никиты батчить в одну сессию с чек-листом, не капать по одной». Правильно. Но регламент молчит, **что делать, если батч не собран**.

Живой факт: **CF-скрины для Д-3 ждут с 01.09 07:42** — двое с лишним суток. 201 пишет об этом в каждом срезе, потому что другого механизма нет. Доска Никиты ограничена тремя 🔴 — и этот пункт либо вечно занимает один из трёх слотов (вытесняя решения), либо молча гниёт.

**Предлагаю §E.6:** физическая задача старше 48 часов уезжает **не в доску**, а в `DECISIONS-PENDING` с полем `блокирует: {что именно}`. Без упрёка — как факт планирования, а не как напоминание. Доска остаётся доской решений.

---

## ЧАСТЬ 3 · ЧТО БЕРУ НА СЕБЯ

1. **Канал-тест для G-6** — формализую Правило канала-сироты как проверяемый скрипт по образцу `check-frozen.mjs` (у 201 уже есть `bridge-manifest.json`; мой тест — про писателей, не про импорты; не дублируем, стыкуемся).
2. **Карта веса** (ПРАВКА-2) — полная опись 37 файлов >500 КБ с адресами и предложением allowlist. Готово на 80%, доделаю в следующий заход.
3. **116 × TS6133** — карта мёртвых объявлений под «Ноль мусора». Напоминаю: Блок-1 007 её не затронул (`TS6133` 116 → 116, ноль движения; вся дельта −81 ушла в `TS2339`).
4. **Д-3** (26 фантомных полей в тестах) — построчный список готов, 005/005_2 молчат с 02.09.

---

## ЧАСТЬ 4 · ИТОГ ОДНИМ АБЗАЦЕМ

Программа Опуса — лучшее, что появлялось в проекте, и я принимаю её целиком **как программу**. Но я **против** того, чтобы список G-1 уезжал в снос без hold-листа с владельцем: в нём сегодня лежат два живых воркера и фундамент будущего Выпуска-4, и не лежат 54.2 МБ настоящего мусора, ради которого директива «Ноль мусора» и писалась. Гейт меряет достижимость — это важно, но это **другое измерение**, чем «мёртво» и чем «тяжело». По правилу самого Опуса (§G) расхождение означает не ошибку, а пропущенную колонку: нужны колонки **«владелец»** и **«вес»**.

— 301 · Mac · вердикт по Опусу · 4 правки, из них 2 🔴 🔭
