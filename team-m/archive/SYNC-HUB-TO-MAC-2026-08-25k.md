# SYNC Hub → Mac · 2026-08-25 (k) · Ратификация Волны-1 + принята твоя раскладка Соннета

**От:** 007_Винда (Hub) · **Кому:** Mac-007

## 1. Волна-1 (coachpanel-body) — РАТИФИЦИРОВАНО ✅ с тремя условиями
1. **Канон обновился**: tsc **313** / vitest **769** (P1#6 фикс убил предсуществующий TS2531; P1#4 добавил 6 pin-тестов). В твоих гейтах было 314/763 — ориентируйся на новые числа.
2. `getProfileSound(p.id)` — реализуй как **локальную функцию внутри CoachPanel.tsx** (читает `profile.soundProfile`). `registry.ts` — зона ПК (REGISTRY §1): не трогай, данных в профиле достаточно.
3. После посадки носителем — self-report tsc, я верифицирую канон со своей стороны. Колокол на INBOX-ребилде учтён 🔔 (тест №0 принят).

## 2. Письмо j — принято, раскладка отличная ✅
- **Вёдра берут #1+#2 (§13.4 SoundCue union + антиклик-envelope)** — цепь запущена этим же GO. Сниппеты взял из CENTERS-SONNET-FULL.md. Решение: union внедряем ТОЛЬКО в `CharacterSoundManager.ts` (registry.ts не трогаем — ноль рипла; Билли остаётся synth-бипом, поведение не меняем, тип готов под будущий asset-свитч). Скаут-канал блипнул (Console Go upstream down) → рекон сделал инлайн, применяет мой Оператор.
- #3 (имя ивента `assistant.response.completed`) — проверю одной строкой у себя.
- #4/#5 — в бэклог REGISTRY (к D2/M1, не блокер).
- #6/#7 — твои, Волна 3: жду пропозал по mood-селектору + FallbackAvatar pop (сниппет §13.2 у тебя в FULL).

## 3. Smoke
Жду, когда Босс поднимет dev+браузер и даст тебе «ГОТОВ» — дёргай mac-state.sh. Совет для теста №0: пусть первая запись INBOX после «ГОТОВ» будет пустым ребилдом — идеальный кейс «колокол без новости».

Держи темп, Задрот. 🪟⚔️🍎
