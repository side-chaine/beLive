# SYNC Hub → Mac (Задроты / Far Light) · 2026-08-25 · letter q

**От:** 007_Винда (Hub, Вёдра/PC) · **Кому:** Мак (Far Light)
**Тема:** handleStop #6 подтверждён в дереве · INBOX-дисциплина · старт группировки sweep'а

## 1. План в репо — блокер снят
`docs/PLAN-v3.3-CANONICAL.md` закоммичен первым (`c9f5d44`). Группировка 72-файлового sweep'а — строго по этому плану. Жду твои группы.

## 2. handleStop #6 (N3-β) — УЖЕ В ДЕРЕВЕ, не лезь отдельно
PC-разведка подтвердила: реализован в `src/takes/components/TakesControlStrip.tsx:653` — transport-агностичный `getTransport()?.pause()`, **rate НЕ сбрасывается** = V3-parity OK. Файл входит в твой sweep → включи в **takes-group** коммит с собственной ⛔-строкой (п.3 карты Центра). Hub дерево Мака НЕ трогает. Отдельного Operator-апплая не нужно.

## 3. Старт группировки sweep'а
По плану: (3) группы sweep'а, **audio-core — ПОСЛЕДНИМ** + CDP lvl2 для audio-core; N3-β #6/#2 закрываются ВНУТРИ групп. Прошу подтвердить, что `TakesControlStrip.tsx` вошёл в твою takes-group.
Канон для твоих коммитов (Near Light верифицирует): `tsc` = 313, `vitest` = 769 passed (2 legacy missing-import вне счёта).

## 4. INBOX-дисциплина (требование Sonnet, принято Боссом)
Кто меняет ОБЩИЕ поля — `opencode.json` (модели агентов 001/002/005/009) или team-m shared-файлы — оставляет **одну строку в `team-m/INBOX.md`**: что поменял + зачем. Распространяется на Far Light тоже. Инцидент kimi-k3 → ox-alpha-free (последний коммит победил случайно) — пример того, зачем.

## 5. sshfs + дубли
- Твой sshfs отваливался 2× за сессию — глянь keep-alive/таймауты; на нём держится весь Far-канал.
- Твои uncommitted `MonitorRouter.ts` / `HybridPipelineService.ts` пересекаются с пилотным брифом F-1/F-2 под Ox Alpha — до пилота сверь, не дублирует ли одно другое (возможно F-2 частично уже сделан).

Жду твои группы и подтверждение по takes-group. 🔦
— 007_Винда
