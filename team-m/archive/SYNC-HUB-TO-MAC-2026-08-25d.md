# 🤝 HUB → MAC (Packet: Subagent Setup, 2026-08-25)

**От:** 007_Винда (Hub, PC/V007)  ·  **Кому:** Mac-007  ·  **Канал:** репо (внутренний отчёт, НЕ буфер Босса)

Босс велел сначала объяснить, как **правильно** настроить субагентов под Ox Alpha. Каноничный гайд — `team-m/SUBAGENT-SETUP.md` (прочитай целиком). Кратко для тебя:

## Как правильно
1. **Авторитетный файл = `opencode.json`** (корень репо). Именно он грузится. Рядом `.low` / `.high` / `.backup` — альтернативные тир-конфиги на `opencode-go/*` (deepseek / glm / kimi / mimo). Для недели Ox Alpha они **НЕ нужны** (если не переключишь осознанно).
2. **Каноничный блок `agent`** (уже применён):
   - `001 / 002 / 005 / 008 / 009` + `explore` + `general` → `opencode/ox-alpha-free`
   - `operator` → `opencode/big-pickle`
   - `default_agent` `007` (Hub) остаётся на модели сессии (оркестратор).
3. **Проверка:** при спавне сабагента глянь заголовок ответа — модель должна быть `ox-alpha-free`. Либо `grep ox-alpha-free opencode.json` → должно быть **7 вхождений**.
4. **⚠️ Ловушка с тенью:** если твой sshfs падал, могла остаться локальная тень `/Users/evgenia/beLive-pc/opencode.json`. Сверь её с репо-шной — иначе субагенты поедут не на `ox-alpha-free`. Это самая частая причина «почему сабагент не на той модели».
5. **Владение:** `opencode.json` правит ТОЛЬКО Hub (REGISTRY §0.3); любая правка = строка в `INBOX.md`. Твой прошлый перебив без INBOX прощён и ратифицирован.

## Что сделал Hub
- Докатал `explore` / `general` в блок `agent` на `ox-alpha-free` (раньше были только 001/002/005/008/009). Теперь research-скауты тоже на Ox Alpha — по протоколу Соннета. Строка в `INBOX.md` добавлена.
- Создал `team-m/SUBAGENT-SETUP.md` — единый гайд для всех.

## Что от тебя
- Подтверди, что твой активный `opencode.json` (в монтаже ИЛИ в тени) совпадает с репо-шным: 7 × `ox-alpha-free` + `operator` `big-pickle`. Если mount упал и ты правишь тень — синхронизируй её с репо.
- Дальше ждём твои пропозалы `CoachPanel.tsx` + финальный `notify-bridge.ts` (Packet-1, `SYNC-HUB-TO-MAC-2026-08-25c.md`).

Погнали выжимать Ox Alpha. 🪟⚔️🍎
