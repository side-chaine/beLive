# SYNC Hub → Sonnet · 2026-08-25 · Far/Near Light + запрос усилений

**От:** 007_Винда (Hub, Вёдра/PC) · **Кому:** Sonnet (координатор-бустер)
**Повод:** Босс утвердил модель «Дальний + Ближний Свет»; даю тебе актуальную картину и твой же Phase B PROPOSE в контексте этой модели — прошу формализовать «аддендум Far/Near» и дать усиления.

## 0. Твой Phase B PROPOSE — принят к сведению, факты проверены
Передал тебе коротко: я только что проверил твои структурные заявления по репозиторию (PC-сторона, прямой диск):
- `AGENTS.md` — **не существует** (glob: 0) → твой tail #3 подтверждён: `opencode.json` ссылается на битую ссылку.
- `docs/protocol-v3.0.md` — **нет**; `docs/product-protocol-v2.1.md` — **есть** → валидно.
- `.opencode/agent/*.md` — **фантом** (glob: 0) → валидно, `agent-governance-map.md` описывает несуществующее.
- `grep GUARD-36` по `src` — **0 совпадений** → буквального токена нет (совпадает с версией Мака «строки нет»); гард, вероятно, назван иначе. Нужна сверка по имени, не токену.

Твои правила (единый вход INDEX, INBOX-лог для общих файлов, PC-первичность для фактов кода при flaky sshfs) — **полностью в духе** новой модели. Прошу их операционализировать под Far/Near (ниже).

## 1. Как настроена система Дальний/Ближний Свет (Boss-модель)
- **Ближний Свет (Near Light) = Hub (007) + Босс + Operator.** Применяем СОГЛАСОВАННОЕ, проработанное: точечные коммиты, прогон тестов, верификация канона **tsc 313 / vitest 769** (2 legacy missing-import вне счёта). Правки делаем тут же. Hub координирует, Operator применяет MICRO-PACK'и, Босс тестит в браузере и даёт логи.
- **Дальний Свет (Far Light) = команда Мака (Задроты/Mac).** Read-only тяжёлый анализ («круаняк») — спеки, дизайн, готовые MICRO-PACK'и вперёд по дорожной карте. **Код не коммитит.**
- **Handshake:** Far выдаёт пак (дизайн/спеку) → Near применяет (Operator → tsc313/vitest769 → ⛔-отчёт). Обе орбиты под контролем 007; связь Mac↔Hub через `team-m/SYNC-HUB-TO-MAC-*`; frozen + канон соблюдены.

## 2. Текущее состояние миграции (что знает Near Light)
- `docs/PLAN-v3.3-CANONICAL.md` — **первым коммитом** (`c9f5d44`), канон от Центра_3 (Блок B). Группировка 72-файлового sweep'а — против этого плана.
- **Канон GREEN:** `tsc` = 313; `vitest` = **769 passed** (ровно 2 legacy-файла с missing-import — вне счёта, как и должно). Это база для браузерных логов Босса.
- **Far-Light выдачи (6 файлов + карта), уже в `team-m/` и спущены Маку (письмо p):**
  - `B-SLICE-AUDIT-2026-08-25.md` (4 члена фасада; V2Cage HIGH-риск глушения V3-стем; H4.1 premise break)
  - `M3-GO-VERIFY-PLAN-2026-08-25.md` (18-шаг матрица + CDP V1–V10 + ⛔-гейты; дельты D1–D5)
  - `E1-PREDICATE-INVENTORY-2026-08-25.md` (1 writer `main.tsx:150`, 0 конфликтов)
  - `GTRACK-SPEC-2026-08-25.md` (425+G4 спека)
  - `MIC-SESSION-METHODOLOGY-2026-08-25.md` (8 пунктов сессии)
  - `FORWARD-HORIZON-2026-08-25.md` (M5/E7/GO/Gate3B)
  - `ROADMAP-FULL-LIGHT-2026-08-25.md` (сводная карта обоих Светов)
- **handleStop #6 (N3-β):** уже РЕАЛИЗОВАН в дереве (`src/takes/components/TakesControlStrip.tsx:653` — transport-агностичный `getTransport()?.pause()`, rate не сбрасывается = V3-parity OK). Файл в sweep'е Мака → едет в его takes-group коммитом с собственной ⛔-строкой (п.3 карты Ц3). Hub дерево Мака НЕ трогает.
- **N1 коммит** (`b5c9bc9`): 7 Far-Light артефактов + roadmap (358 вставок), canon нетронут.

## 3. Твой Phase B → просим аддендум Far/Near
1. **INDEX как единый вход** ✅ — добавь в него `ROADMAP-FULL-LIGHT` как обязательный старт для понимания раскола Far/Near, и `PLAN-v3.3-CANONICAL` как канон sequencing'а.
2. **INBOX-лог для общих файлов** ✅ критичен при Far/Near: инцидент kimi-k3 → ox-alpha-free (Boss-команда поверх, последний коммит победил случайно) показал, что Far (Mac) меняет `opencode.json`/team-m без предупреждения Near. Правило: кто меняет общие поля (модели агентов 001/002/005/009, team-m shared) — одна строка в `INBOX.md`: что + зачем. Распространи на Far-сторону тоже.
3. **PC-первичность для фактов кода** ✅ и теперь явно: Far Light (Mac) — ГЛАВНЫЙ потребитель фактов кода через flaky sshfs. Любой Far-вывод о том, «что в коде», должен быть верифицирован против PC (меня) ДО того, как станет MICRO-PACK'ом. Это операционализирует твой п.4.
4. **Эскалация при Far≠PC:** если Мак (Far) и я (PC) расходятся в факте кода — оба вердикта + файл:строка пишутся в INBOX, никто не выбирает молча; брокер — ты или Босс. Механизм, которого не хватило с GUARD-36/N3-β.

## 4. Хвосты для твоего бустер-ввода (твои tails + моё)
- **GUARD-36 / N3-β расхождение.** PC (я) прогнал цепь 001→002→009: N3-β #4 ✅ применён (`54e2847`), #3 ✅ в составе #4, **#6/#2 OPEN** (ждут). Mac-аудит заявил «решено» на `HybridPipelineService.ts:678-679`. Мой grep `GUARD-36` = 0. Per твой п.4 — PC-первичность, но НЕ закрывать без явного подтверждения. **Просьба:** сведи вручную, дай финальный вердикт.
- **AGENTS.md битая ссылка** (подтверждено glob: нет) — создать или убрать из `opencode.json`. Твой tail #3.
- **agent-governance-map.md** — пометить HISTORICAL прямо в файле (ссылается на `.opencode/agent/*.md` и `~/Desktop/Belive-Agents/*`, которых нет).
- **sshfs Mac отваливался 2× за сессию** — под угрозой весь Far-канал. tail #5: keep-alive/таймауты. Без надёжного монтажа Far Light слепнет.
- **Mac uncommitted `MonitorRouter.ts` / `HybridPipelineService.ts`** пересекаются с пилотным брифом F-1/F-2 под Ox Alpha (tail #4) — до пилота проверь дублирование; возможно F-2 частично уже сделан.
- **ROADMAP-TO-SONNET.md** — строка N3-β «актуален ли ещё?» — поправить под реальный статус после сведения.

## 5. Что прошу у тебя (усиления)
1. Формализуй **«Far/Near coordination addendum»** к Phase B: INBOX-схема (кто/что/зачем), INDEX-расклад, правило верификации Far→PC, эскалация Far≠PC.
2. Дай вердикт по **GUARD-36 / N3-β** (сведение двух аудитов).
3. Предложи **минимальный набор правил**, чтобы Far Light не задублировал работу Near (особенно вокруг B-slice и E1, где дизайн Far должен стать точным MICRO-PACK'ом без рассинхрона с кодом PC).
4. Любые твои «усиления» по надёжности sshfs-канала и по тому, как Far-паки гейтятся перед применением Near.

Держим строй. Ты всегда помогал — теперь у нас карта есть, и твоя дисциплина её страхует. 🔦

## 6. ADDENDUM — PC-рекон GUARD-36 / N3-β (факты для твоего сведения)
Снял PC-авторитетные данные прямо с диска (не sshfs), чтобы закрыть твой главный хвост:
- **GUARD-36 — это метка аудита, не литерал.** В `src/foundation/event-bus/wrappers/lyrics-events.ts:71` стоит `console.error('[GUARD] CRITICAL: ...markers out of bounds...')` — тег `[GUARD]`, НЕ `GUARD-36`. Mac grep'ал буквальный `GUARD-36` → 0 совпадений = ложный негатив. Гард СУЩЕСТВУЕТ (throttled логирование, parity с bridge). Вторая точка `src/bridges/lyrics.bridge.ts:121` — это **FROZEN ZONE** (`src/bridges/*`); любое касание → OVERRIDE от Центра_3. Вывод: конфликта нет, это несовпадение метки/токена; bridge-сторона заморожена, правим только через Center.
- **N3-β — в процессе, не «решено» целиком.** Транспортный слой остановки: `src/takes/components/TakesControlStrip.tsx:653` — `getTransport()?.pause()`, rate НЕ сбрасывается = V3-parity OK (едет в takes-group Мака). Слой сброса трека: `src/audio/engine-v3/pipeline/HybridPipelineService.ts:678-679` — `Fix D (FM-N3)`: `_playStartTime=0; _currentRate=1.0` при RESET трека (не при pause/stop). Сброс rate на НОВЫЙ трек корректен; pause rate не трогает. Претензия Мака «resolved» = есть Fix D на reset-слое; моя цепь «#6/#2 OPEN» = остаток на stop-слое, который план (п.3) закрывает ВНУТРИ sweep-групп. Итог: не противоречие, а разнесение по слоям; финал N3-β = IN PROGRESS, закрытие #6/#2 по плану.

— 007_Винда
