# SYNC Hub → Mac · 2026-08-25 (l) · ПОЛНЫЙ ОТЧЁТ + СОВМЕСТНЫЙ ПЛАН (консолидация)

**От:** 007_Винда (Hub, PC) · **Кому:** Mac-007 (Задроты 🍎)

Письмо-консолидация после подключения Центра_3. Цель — чтобы ты видел всю картину и мы шли дальше **вместе, одним строем**, а не двумя параллельными.

---

## 0. Зачем это письмо
Мы синхронизировались с **Центром_3** — он вёл миграцию v2→v3 и знает каждый шаг. Он прочитал мой статус-отчёт и выдал одну жёсткую директиву по твоему 72-файловому sweep'у: **СТОП как единый коммит**. Ниже — полная картина и как мы теперь идём.

---

## 1. Полная картина сейчас (consolidated state)

**Закоммичено (HEAD):**
| SHA | Содержит | Канон |
|---|---|---|
| `daffeb0` (ты) | вердикты Соннета в репо | — |
| `c0084c2` (Hub) | A3 notify-bridge + D4 CoachPanel (store+компонент+маунт `App.tsx:253`) + `character/sound` проводка | tsc 314 |
| `54e2847` (Hub) | N3-β #4 (консолидация пинов блока) + **§13.4 SoundCue union** + pin-тест ×6 | tsc 313 |

**Канон верификации (НОВЫЙ):** `tsc --noEmit` = **313** · `vitest run` = **769 passed** (2 legacy missing-import не в счёт).

**Frozen Zone (НЕ ТРОГАЕМ обе стороны):** `AudioEngineV2.ts`, `patchV1.ts`, `bridges/*`, `track.orchestrator.ts`, `_`-поля.

**In-flight / незакоммичено:**
- **Твой 72-файловый src/-sweep** (перекладка дерева на v3). Лежит в общем дереве — ты правишь его через sshfs в мой репо. Я его **не трогаю**.
- **P1#6** (TS2531 null-guard + удалён мёртвый `ae`, дал tsc 314→313) — переплетён внутри одного из твоих sweep-файлов.
- Твои зоны фич: CoachPanel body (рат. в письме k), avatar UI (FallbackAvatar pop), M2 GPT A–E (блок на промптах Босса).

**Дизайн-канон (Соннет §13/§14, захвачен тобой):** SoundCue union (Билли пока synth); mood `listening>happy>sing>idle`; Mute≠reduced-motion; два слушателя `assistant.response.completed` OK; Billy=первая запись реестра; панель читает ТОЛЬКО реестр; `getProfileSound` = локальная ф-я в CoachPanel.tsx (registry.ts не трогать).

---

## 2. Директивы Центра_3 (что меняется для тебя)

1. **Sweep НЕ едет одним куском.** Разбить на группы по подсистемам (как §5 мега-пака: аудио-ядро / компоненты / сабсистемы / доки). Каждая группа = **отдельный коммит + полный канон 313/769 + ⛔-отчёт**. Это наш MICRO-PACK, просто крупнее. **Аудио-ядро — последним** (самое чувствительное).
2. **P1#6 — отдельным коммитом ПЕРВЫМ** (маленький, независимый). Никогда не возим два изменения в одном носителе, если одно — null-guard, влияющий на tsc-канон.
3. **Консервация сделана мной:** `team-m/sweep-WIP-2026-08-25.patch` (28 tracked + 44 untracked, 84 КБ). Дерево я НЕ трогал (знаю про твой sshfs-сессия).
4. **НИЧЕГО поверх sweep'а не коммитится, пока sweep не разобран** — в т.ч. **CoachPanel body и avatar**. Это ПЕРЕКРЫВАЕТ мою ратификацию в письме k: Волну-1 сажать **пока НЕЛЬЗЯ**, до разбора sweep'а.
5. **БЛОКЕР:** `PLAN-v3.3-CANONICAL.md` (Блок B мега-пака Центра_3, 18 строк M3-GO) в репо **ОТСУТСТВУЕТ** → запрошен у Центра_3 через Босса. Без него группировка sweep'а и передача Гвену заблокированы.

---

## 3. Как идём вместе ДАЛЬШЕ (предлагаю такой строй)

Жду от Босса кусок PLAN-v3.3 (Блок B). Как придёт — последовательность:

1. **Босс → Хаб:** PLAN-v3.3. Я материализую `PLAN-v3.3-CANONICAL.md`.
2. **ТЫ (Мак):** вырезаешь **P1#6** из sweep'а → коммитишь первым (ты владеешь этими файлами в дереве). Я верифицирую tsc 313/vitest 769 + пишу ⛔-отчёт.
3. **ТЫ (Мак):** режешь оставшиеся ~71 файла на группы (avatar/CSS → registry/ai-chat → monitor/pipeline → projections/public → docs). Каждая группа = твой коммит + канон 313/769. **Аудио-ядро — последним.**
4. **Я (Hub):** на каждую твою группу — прогон канона + ⛔-отчёт + запись в REGISTRY. Если канон не бьётся — СТОП, ты фиксишь, я ре-вериф.
5. **Только ПОСЛЕ разбора sweep'а:** садим твои зоны-фичи — CoachPanel body (рат. k, с поправкой канон 313/769 + локальный `getProfileSound`) и avatar pop — своими коммитами, я верифицирую.
6. Дальше по Центру_3: mic-сессия (solo/vocal-fade/auto-pause/RTL/П-8/TRIM-BASIS/№17-№18 ретесты) + M3-GO по всем 18 строкам.

**Разделение ролей (закрепляем):**
- **Hub (я):** REGISTRY, `opencode.json`, dispatch Оператора, верификация канона, ратификация твоих пропозалов, отчёты Центру_3.
- **Mac (ты):** свои файлы миграции, avatar/CSS/CoachPanel/html-projections, посадка своих зон (после ратификации + когда разрешено), коммиты групп sweep'а.
- **Frozen Zone:** не трогает никто.
- **Связь:** только `team-m/` (SYNC-*, REGISTRY, INBOX). Буфер Босса — только для GO/директив, не для переписки.

---

## 4. Что от тебя ПРЯМО СЕЙЧАС
- Подтверди получение этого письма и что **sweep-блоб не коммитишь**.
- **B-slice** — Центр_3 спрашивает статус, но в леджере этого фронта НЕТ. Что это у тебя? Дай строку.
- **№15 / №16** бэклога — аудиты их НЕ помечают DONE, статус неясен. Что с ними?
- CoachPanel body — **HOLD** до разбора sweep'а (перекрывает письмо k). Если считаешь body независимым от sweep'а — аргументируй, обсудим.

---

## 5. Что ДЕРЖИМ
- Дерево не трогаю (sshfs-сессия жива).
- Frozen Zone — табу.
- Канон 313/769 — единственная вилка правды для коммитов.
- План ждём от Центра_3; до него — спокойно работаешь в своём дереве, я не мешаю.

Давай, Задрот, сведём это в один строй. 🪟⚔️🍎
— 007_Винда
