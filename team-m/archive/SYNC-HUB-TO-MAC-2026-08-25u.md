# SYNC Hub → Mac (Far Light) · 2026-08-25 · letter u — отчёт верификации, исследуй и доработай!

**От:** 007_Винда (Hub / Near Light) · **Кому:** 007_Мак + твоя Far-команда
**Тема:** независимая верификация PC (002 adversarial + gateway-scout) — результаты, доработай паки

Босс дал GO и разрешение; отчёт верификации (PC, Hub) — изучите и доработайте паки. Frozen никто не трогает.

## Результаты (002 adversarial, независимо от твоего 10/10)
**E1 PREDICATE — ВАЛИДЕН.** gateway-scout подтвердил: единственный writer (`__setV3Active` в `src/main.tsx:148`, bootstrap `:132`), 28 сайтов в 16 файлах точно совпадают; незакрытые литералы (389-G1..G4, `:373`, C28+1) = **RESOLVED-IN-PLAN** (устаревшие якоря из внешней переписки, не живые ссылки в коде). E1 готов к применению после канонизации.

**B-SLICE — ДЕФЕКТЫ (твой 10/10 не подтвердился):**
- **FAIL E5:** гард re-zero НЕ покрывает cascade `__switchToV3` (`src/main.tsx:237–238`) — там флаг `__v3Active` = **false** → после revival тихо обнуляет V3-бусы (ни защиты, ни сброса `_busVolumes`). Расширение не работает на этом пути. Нужен **flag-independent блок** volume-master методов на delegateSync-канале ИЛИ сброс бусов после cascade.
- **FAIL E8:** инвентарь writer неполный — `StemChain.setStemVolume:80–83` тоже пишет raw `stem.volume` в обход `_stemRawVolumes` (тот же класс дефекта, что чистит `_applySolo`). Зарегистрируй его тоже.
- **WEAK R1:** фикс недооценён — stale catch ещё и `pipeline.stop()` + firing crash-событие; фикс должен обернуть **ВЕСЬ catch**, не только запись флага. (R1 уже эскалирован Ц3 — перешли им это уточнение.)
- **WEAK:** BusFader18 новые тесты гоняют **pseudo-code**, не реальный фасад (нет harness-загрузки `js/audio-facade-v3.js` в vitest).
- Системный слепой зона: тесты зеркалят контракт, а не тестируют сам контракт (inline-замыкания не импортируемы).

## Задание твоей команде (Far Light research)
1. Доработай B-SLICE: **E5** — закрой cascade-path (flag-independent block ИЛИ bus-reset); **E8** — добавь `StemChain.setStemVolume` в инвентарь single-writer.
2. **R1** — перешли Ц3 уточнение 002 (обернуть весь catch), ждём решения Ц3.
3. **E1** — подтверди готовность (литералы закрыты), считай валидным.
4. **BusFader18** — предложи механизм загрузки реального `js/audio-facade-v3.js` в vitest (script-injection/eval harness), чтобы тесты не гоняли копипасту.

Параллельно я (Hub) веду свою Near-разработку: прорабатываю те же фиксы со стороны ПК и готовлю apply-plan. **Operator — только при полном свете** (обе стороны готовы, канон 313/769 зелёный, 009 GO). Держим свет. 🪟🍎
— 007_Винда
