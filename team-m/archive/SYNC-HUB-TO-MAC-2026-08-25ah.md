# SYNC-HUB-TO-MAC · 2026-08-25 · letter ah · Prompt-инжиниринг v2 применён на PC — настрой у себя то же

> От: 007 Hub. Кому: Mac-007. Статус: применено на PC по GO Босса. Тебе: синк + рестарт + проверка.
> Полное дело: `team-m/prompt-eng-run/` (VERDICT-009 ← ATTACK-002 ← BOOST-005 ← PROPOSAL-001 ← SCHEMA-FINDINGS ← CENTER5-ANSWERS). Коммит **04c71a3**.

## 1. ЧТО ПРОИЗОШЛО
Реквизиция вскрыла: субагенты работали «слепыми» — домен (frozen-zone, канон, правила) впрыскивался вручную каждым вызовом 007, и любой пропуск = агент не знал. Прогнали полный adversarial-gate: 001 спроектировал → 002 атаковал оба варианта (наш = ТРЕБУЕТ ПАТЧА, внешнего Центра_5 = ПАДАЕТ) → 005 верифицировал документацией И ИСХОДНИКАМИ opencode → 009 вынес вердикт РЕШЕНО. Внешний Центр_5 одобрил финал. Босс дал GO — применили.

**Ключевой факт уровня схемы:** поля per-agent `instructions` в AgentConfig НЕ существует (проверено webfetch официальной схемы + чтением исходников variable.ts). Работают только markdown-агенты `.opencode/agent/<name>.md` (тело = системный промт) + топ-level `instructions[]`. Битый `{file:}`-реф: проектный конфиг = падение старта, глобальный = ТИХАЯ потеря всего конфига.

## 2. КАК ТЕБЕ ПОЛУЧИТЬ
Ветка `backup/win-V3-finish_2`, коммит **04c71a3**:
- `.opencode/agent/{001,002,005,007,009}.md` — НОВЫЕ тела агентов + frontmatter тюнинг (**force-tracked**: `.opencode` был gitignored из-за node_modules — после pull приедут нормально)
- `team-m/prompts/SHARED.md` — общий домен-слой
- `opencode.json` — `subagent_depth: 1` + SHARED.md в `instructions[]`
- Если работаешь по sshfs с тем же деревом PC — файлы уже у тебя, просто рестартни opencode.

## 3. КАНОН РОЛЕЙ (Босс verbatim, зашито в тела)
- **001 Ювелир**: минимум движущихся частей, ≤40 строк ответа; формат РЕШЕНИЕ/СТРУКТУРА/ПОЧЕМУ-МИНИМАЛЬНО/ЦЕНА-РИСК/АЛЬТЕРНАТИВА-Y. temp 0.2, steps 30.
- **002 Адвокат Дьявола**: ≥3 удара по убыванию тяжести + самопроверка каждого удара + ≥1 усиление. Вердикты ТОЛЬКО: ДЕРЖИТ | ТРЕБУЕТ ПАТЧА | ПАДАЕТ. temp 0.2, steps 20.
- **005 Бустер**: context7 ДО памяти (ответ без цитаты источника = невыполнен), «не получится» запрещено, фантазия после фактов. Формат ФАКТЫ(источники)/ВОЗМОЖНОСТИ/ОБХОД/НЕ-НАЙДЕНО. temp 0.8, steps 25.
- **009 Суд Независимый**: ВОПРОС→ЗА→ПРОТИВ→ДОКАЗАТЕЛЬСТВА→ВЕРДИКТ + обязательный «вариант, который НЕ предложил никто». Вердикты: РЕШЕНО | ОТЛОЖЕНО | ОТКЛОНЕНО. Цифры канона — ТОЛЬКО из КАНОН-СНАПШОТА диспатча (нет снапшота → «БЛОКЕР»). temp 0.1, steps 12.
- **007**: += ШАБЛОН ДИСПАТЧА (РОЛЬ / FROZEN-СТОП / КАНОН-СНАПШОТ / КОНТЕКСТ / ДОСТАВИТЬ+few-shot+«Думай шаг за шагом» reasoning-ролям / ВЕРИФИКАЦИЯ / НЕ ДЕЛАТЬ) + дисциплина эскалации (счётчик провалов роли в строке REGISTRY, порог 2 → диспатч близнецу `-hi`, кап передосудных ретраев = 2 → СТОП владельцу).
- Все сабы: `hidden: true`, `task: deny`, edit/bash deny.

## 4. SHARED.md — общий слой (инжект всем через instructions[])
Худой домен БЕЗ цифр канона и БЕЗ универсального формата ответа: проект V2→V3; FROZEN-ZONE «читать можно и нужно; менять/предлагать — никогда» (AudioEngineV2.ts, patchV1.ts, bridges/*, track.orchestrator.ts); REGISTRY=SSOT; push 🔒 до GO владельца; `_`-поля легальны только в V3-файлах; context7-first; **Реестр вердиктов цепочки** — ровно два набора: ДЕРЖИТ|ТРЕБУЕТ ПАТЧА|ПАДАЕТ (002) и РЕШЕНО|ОТЛОЖЕНО|ОТКЛОНЕНО (009); PASS/FAIL/GUILTY запрещены.

## 5. ТВОИ ШАГИ
1. Синк до 04c71a3 (или проверь видимость файлов).
2. Полный рестарт своего opencode из корня репо (конфиги читаются на старте).
3. Претест: `grep -m1 "^Ты —" .opencode/agent/*.md` (каждая строка = правильная роль); JSON valid check; опц. `opencode debug config`.
4. Ролевое эхо: тривиальный диспатч каждому субу «назови роль одной строкой» — сверка с п.3.
5. CHAIN-SMOKE: один диспатч через 007 со спавном ВСЕХ агентов (эхо роли от каждого), 009 замыкает вердиктом. Зелёный = цепочка жива.

## 6. НЮАНСЫ (отклонения от буквы вердикта, залогированы в REGISTRY)
1. Промты в md-телах вместо `{file:}` — md-агенты уже работали и перекрывают JSON (доказано описаниями живой сессии).
2. **008 НЕ ТРОНУТ** — ему нужен edit:allow для MDF-handoff протокола (Суд не видел 008.md, материала не было в деле).
3. task-glob белый список 007 отложен (merge md+json не верифицирован) — вложенность уже заперта subagent_depth:1 + task:deny сабов.
4. МОДЕЛИ НЕ МЕНЯЛИ: frontmatter `opencode-go/ox-alpha-free` vs JSON `opencode/x-preview-f-free` — приоритет источников не выяснен, трогать без верификации нельзя.
5. Старые `.md.high` в .opencode/agent/ — легаси ручной подмены, не загружаются. Новая эскалация = близнецы `<id>-hi` по накоплению отказов.

## 7. ЗАЧЕМ
Рычаг владельца: настройка входного контекста агентов = 5–70% успеха цепочки. Было ~30% потенциала Ox Alpha, стало ~85–90% на дизайне. После твоего зелёного CHAIN-SMOKE — обе машины в одной конфигурации, возвращаемся к миссии: mic-уши-сессия → M3-GO.

— 007 Hub (PC)
