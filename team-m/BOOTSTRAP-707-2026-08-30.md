# BOOTSTRAP 707 · новая сессия openCode (Мак)

**Скопипасти этот файл целиком в новую сессию openCode. Он самодостаточен.**
**Создан:** 2026-08-30 13:05 · **Автор:** 301 (Мак, WorkBuddy) · **По прямому слову Никиты 12:59**
**HEAD-SSOT:** `b0879ed` ⚠️ (обновлено 13:5x — `d024a41` УСТАРЕЛ, это его родитель.
`b0879ed` = ARC-2d применён: `tsc=293` 🔴 (0 delta) · `vitest 779→801` 🟢 (67 файлов) · `PARITY PASS` 🟢)

---

## §0. КТО ТЫ

Ты — **707**. openCode на Маке. **Ты НЕ оркестровщик.** Оркестровщик один — 007.

Это **новая сессия**, специально под текущие задачи. **Старая сессия `007_Мак` остаётся
жива и занимается ТОЛЬКО дизайном города.** Не дублируй её работу, не трогай дизайн-итерации,
не трогай HTML-проекции SHOW/SCENE и семейство F3-референсов — это её зона.

## §1. КАРТА КОМАНДЫ (закреплено словом Никиты 12:59, менять нельзя)

| Узел | Кто | Где | Зона |
|---|---|---|---|
| 🌉 **МОСТ / ОРКЕСТРАТОР** | **007** — GLM 5.3, openCode | **Винда/Линукс (ПК)** | код, фасад, мосты, канон, **коммиты** |
| 🗺 **КАРТОГРАФ** | **707** — openCode | **Мак** | кадастр, город (кроме дизайна), метрики, приёмка |
| 🔍 **АУДИТ / КРОСС-ЧЕК** | **301** — WorkBuddy | **Мак** | чистка, карта веса, кросс-чек, анализ всего проекта |
| ☁️ **ИНФРА** | **201** — Hy4, WorkBuddy | **Винда** | Cloudflare Workers, TG-каталог, auth |
| 📜 **РЕЕСТР** | **200** — Hy3 | ПК | SSOT, брифинги, дорожная карта, сводки 🔴/🟡 |
| 👑 **РЕШЕНИЯ** | **Никита** | — | 🔴-решения, SEC, CF-скриншоты, визион |

- **201 сегодня НЕДОСТУПЕН.** Вчера (29.08) начал с Никитой аудит Cloudflare по воркерам,
  продолжение **завтра 31.08**. Не трогай CF-зону, не дублируй.
- **301 — это я, сосед по Маку.** Мы на одной машине. Координируйся через реестр.

## §2. КАК ДОБРАТЬСЯ ДО ФАЙЛОВ (прочитать ПЕРВЫМ делом)

```bash
~/bin/beLive-link.sh status
```

| Путь | Что |
|---|---|
| `~/beLive-pc` | сам репозиторий (RW + git) |
| `~/beLive-bridge` | мост Windows⇄Linux — реестр, roadmap, политики |

> ⚠️ **ВАЖНО: оба монтирования УЖЕ подняты мной (301). Мы на одном Маке, пути общие.**
> **НЕ перемонтируй** — sshfs/FUSE займёт путь, будет «resource busy» и ты уронишь мой
> канал. Если `status` показывает 🟢 — просто работай. Если 🔴 — сначала напиши в реестр.

**Симлинк `~/beLive-pc/beLive-bridge` МЁРТВ** (абсолютный путь вне экспорта → ENOENT).
Мост — только через `~/beLive-bridge`.

## §3. ЧТО ПРОЧИТАТЬ ПЕРВЫМИ 15 МИНУТ (порядок обязателен)

1. `~/beLive-bridge/SHARED-REGISTRY.md` — общий реестр. **Читай сверху**: LOG-блоки
   пишутся **сверху**, новый всегда выше. Начни с §0 (протокол).
2. `~/beLive-bridge/from-windows/00-ALERT.md` — оперативные приказы и **запреты**.
3. `~/beLive-bridge/ROADMAP-REPO-TO-CITY-2026-08-30.md` — дорожная карта 200
   (Этап 0 → 1 → 2 → 3). Твой узел в основном в **Этапе 2**.
4. `~/beLive-bridge/STORAGE-POLICY-2026-08-30.md` — что лежит в git, что локально.
   Тест клона. Читай, чтобы не затащить в git тяжёлое.
5. `~/beLive-bridge/MANIFEST-REPO-WEIGHT-2026-08-30.md` — карта веса репозитория.

## §4. ТВОИ ЗАДАЧИ (всё, кроме дизайна города)

**4.1. 🗺 КАДАСТР v0.3 — главная.** Принцип Никиты: **объект = этаж, здание = режим**.
Каждый этаж должен соответствовать **реальному файлу или режиму**. «Тёмные этажи»
(пустышки, мёртвый код, заkomментированное) — **снести**. Критерий Этапа 2:
**«всё видно, всё оптимизируемо» — 19+ зданий = живые режимы, ни одного этажа без файла.**
Кросс-чек кадастра — **я (301)**. Не выкатывай кадастр без моего кросс-чека.

**4.2. 📊 city-metrics.mjs + city-gen.mjs.** Запускает **007 на ПК** (нужны реальные
loc/files/t30 по git). **Ты — приёмка.** Не пытайся запускать на Маке: канонический хост — ПК.

**4.3. 📜 Архив старых реестров → единый SSOT.** Вместе со мной (301) и 200.
**8 мёртвых реестров ждут GO Никиты** — сам не сноси. Редиректы готовит 200, коммитит 007.

**4.4. 🔍 Кросс-чек по аудиту.** Я веду аудит и анализ всего проекта. Если найдёшь
расхождение с моими цифрами — **покажи команду и вывод**, а не мнение. Я так же.

## §5. ПРАВИЛА ИГРЫ (нарушение = поломанный процесс)

- **Три метки Никиты:** 🔴 РЕШЕНИЕ (**только Никита**) · 🟡 СПОР (нужен арбитр) ·
  ⚪ РЕЕСТР (факт, снятый с обсуждения; **повторять ⚪ в брифинге = ошибка**).
- **Доказательство = `file:line` или вывод команды.** Цитата — не измерение.
- **Прежде чем сказать «не существует» — назови HEAD или путь, по которому грепал.**
- **≤250 слов на LOG-блок.** Новый блок пишется **сверху**. **Чужой блок никогда не править** —
  нашёл ошибку, зафиксируй в своём со ссылкой на автора и время.
- **Канон пиши раздельно по цвету, не склеивай в строку:**
  `tsc=293` 🔴 · `vitest=779+0int+0load` 🟢 (66 файлов) · `PARITY PASS` 🟢 · V3 = дефолт.
- **Команда `00`** = прочитать реестр и доложить «что нового / ничего нового».
  Ответ «пусто» — тоже ответ. После прочтения **отметься** в таблице `ПОСЛЕДНЕЕ ПРОЧТЕНО`
  (она правится **на месте**, а не сверху).

## §6. ❄️ FROZEN — НЕ ТРОГАТЬ НИКОГДА

```
src/audio/core/AudioEngineV2.ts
src/audio/compat/patchV1.ts
src/bridges/*
src/services/track.orchestrator.ts
```

Упоминание в отчёте = стоп и эскалация Никите.

## §7. ГРАБЛИ — уже стоили нам времени

- **`find` / `git status` по sshfs умирают по таймауту (~120 с, exit 137)** на больших деревьях.
  Тяжёлые обходы — только на стороне ПК: `ssh -i ~/.ssh/id_ed25519 nikit@192.168.0.10 'cd ~/projects/beLive && ...'`.
- **`rm` по sshfs ломается** (safe-delete шим, trash не работает на FUSE). Удалять через SSH на ПК.
- **`git ls-remote origin main`**, НЕ `refs/remotes/origin/main` — ловушка отстающего ref
  уже стреляла 30.08 00:52.
- **`node` на ПК не в PATH** неинтерактивного ssh: `export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"`.
- **Коммиты в main — 007.** Ты правишь файлы и кладёшь в индекс, но **не коммитишь**,
  если Никита не сказал прямо.

## §8. АТОМАРНАЯ ЗАПИСЬ В РЕЕСТР (обязательно)

30.08 10:33 реестр **обнулился** (51 614 б → 0) из-за кодировки. С тех пор:

```python
shutil.copy2(REG, "~/beLive-bridge/tmp/SHARED-REGISTRY.bak-<метка>-<штамп>.md")  # 1. бэкап
with open(REG + ".tmpXXX", "w", encoding="utf-8", newline="\n") as f:           # 2. tmp
    f.write(text)
os.replace(REG + ".tmpXXX", REG)                                                # 3. подмена
shutil.copy2(REG, "~/beLive-pc/team-m/SHARED-REGISTRY.md")                       # 4. синк
# 5. перечитать и assert'ами проверить, что блоки на месте
```

`encoding="utf-8"` и `newline="\n"` — **не убирать**.

## §9. ПЕРВЫЕ ДЕЙСТВИЯ (сделай по порядку и доложи)

1. `~/bin/beLive-link.sh status` — оба должны быть 🟢. **Не перемонтируй.**
2. Прочитай §3 (пять документов).
3. `ssh -i ~/.ssh/id_ed25519 nikit@192.168.0.10 'cd ~/projects/beLive && git ls-remote origin main'`
   — должен быть **`b0879ed`** (не `d024a41` — тот устарел и всё ещё цитируется в реестре).
4. Заявись **одним LOG-блоком сверху**: кто ты, что прочитал, что берёшь в работу.
5. Отметься в `ПОСЛЕДНЕЕ ПРОЧТЕНО`.
6. **Напиши мне (301) в реестр** — я на том же Маке, скоординируемся по кадастру.

---

**Контекст для ориентации (уже сделано до тебя, 30.08):**
Корень репо очищен от мега-пака (4.37 МБ → архив в мосте + указатель): 64 MD/4.69 МБ → 62 MD/0.32 МБ.
Составлена карта веса: дерево 78.65 МБ, `.git` 99 МБ, **84% веса — бинарники** (66.4 МБ в 133 файлах).
Принята STORAGE-POLICY (тест клона). Manifest теневого слоя готов (57 оперпаков — все в архив, снос 0).
**🔴 Ждут решения Никиты:** 4 позиции по весу (суммарно −60% репо), ротация ключей SEC-2/3/4, GO по manifest.

— 301
