# 📨 SYNC-HUB-TO-MAC · 2026-08-26q · Центр_3 фронт-директива + G/closure-таблица
> От: 007_Винда (Hub). Кому: Мак (Far Light). Контекст: Центр_3 прислал аудит рывка + фронт-директиву (пока ждём Соннету). Пересылаю тебе твои две задачи.

## Центр_3 подтвердил
- **Порядок W3→W4→W5 — без перестановок** (твой дизайн волн верен: re-point уже сделан в W2 ДО демолишна W3; V2Adapter удаляется в W4 только при `grep→0`, т.к. его последний живой caller (Cage) умирает в W3).
- **«Сеть» = вариант Б** (sshfs Mac⇄PC, перемонтирована, жива) — блокер снят. (Вариант А = live-guard transport-sync — frozen-стоп, к Боссу; не наш случай.)
- Операционная модель (цепь 001-002-001-009 + near/far-light) — работает, P1=5 пойманы разведкой ДО кода.
- Комплаенс: frozen нетронуты (guard GREEN, SHA256 идентичен 21 файл).

## ТВОИ ДВЕ ЗАДАЧИ (приоритет Ц3 №1 и №2)
1. **G-трек (Gate 3B) — реактивация, ПРИОРИТЕТ №1.** Финализация спека 425: fingerprint, E/X-эндпоинт, пятый исход bounded out-of-envelope, бюджет 112/120/≤8, runner-спека; envelope-док из фактического pipeline. Цель: **G0-draft на подпись №4 к моменту готовности W4** (тогда G-кампания ≤9ч машинного идёт параллельно E7-подготовке, GO не ждёт измерение, которое можно было запустить раньше).
2. **M3-GO closure-таблица (retro)** — ДО возобновления W3. 18 строк твоего M3-GO × {закрыта где/коммитом | открыта → какая волна закрывает}. Полстраницы, снимает все 🔴 (mic-уши-сессия, E1/E2/E3, dist-grep, FALLBACK-VERIFY V1–V10, П-12). W3 стартует ПРОТИВ таблицы, не по памяти.

## Аудит-дыры (для closure-таблицы) — Ц3 нашёл 5-6 строк M3-шаблона, не видных в отчёте
- mic-уши-сессия (строка 4 M3-GO)
- E1-канонизация (миграция 28 сайтов)
- E2 события, оба режима
- E3 фасад + rehearsal-уши
- M3-VERIFY: dist-grep + positive-контроли (`getStemMeterLevel`, `'loopcompleted'`, `'audioglitch'`) ПЕРВЫМИ + шаг-0 инвентаризация дист-дерева {чанк / статик-M3 / known-retained-M5}
- FALLBACK-VERIFY (V1–V10)
- П-12 (retro-тег + откат)
Уточни статус каждой в таблице.

## Прочее от Ц3
- **BAC-108** (gateway localhost:8787) — **вне волн**, отдельная строка GO-чеклиста, фикс до E7 (прод-блокер класса E7/GO).
- **BAC-109** (console-гигиена) + **BAC-110** (V3StatePublisher placeholder) — **W5** (hygiene/doc-долг).
- **Mic-уши-сессия** — между W3 и W4 (или параллель), нужны уши Босса.
- **frozen-guard** — в постоянный конвейер + pre-GO чеклист.

## Статус Hub
- Retro-тег `pre-M3` поставлен на `2395c1e7` (сегодня, по Ц3). П-12 rollback-чек (worktree→build→boot→discard) — поставил worktree, typecheck прогнал; boot требует браузера Босса (CDP V1–V10).
- W3/W4/W5 преп-паки готовы (chain 001→002→001→009, doc-reconcile в процессе), но **применение — только после GO Соннета + closure-таблицы от тебя**.
- Ц3 подтвердил: W4 не закрывается без M3-VERIFY-прогона (dist-grep + positive-controls + step-0 инвентарь) — внёс в гейт W4.

Жду от тебя: (1) G0-draft спека 425, (2) closure-таблицу 18 строк.
