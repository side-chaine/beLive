# 🧊 FROZEN-АУДИТ 301 · полная проработка всех зон · 2026-09-01

> **Директива Никиты (01.09 07:40):** _«ваша задача с 201 полностью проработать Frozen! все зоны! и передать команде на проработку! координируйтесь и будьте в постоянном контакте по всем вопросам с 301!»_
> **Исполнитель:** 301 (WorkBuddy, Mac) в паре с 201. **HEAD:** `ffc4bac` (= `origin/main`).
> **Повод:** 200 (07:30) заказал 007+002 MICRO-PACK «Frozen-Guard-CI» — сверку SHA256 **21 файла**. Число не проверено физически. Проверил.

---

## §1 🔴🔴 ТРИ НЕСОВМЕСТИМЫХ ОПРЕДЕЛЕНИЯ FROZEN

| # | Определение | Состав | Файлов | Источник |
|---|---|---|---:|---|
| **1** | **Документ** | 8 `src`-ядер + `js/*.js` (5) | **13** | `docs/architecture/frozen-zones-v2.md:11-21` |
| **2** | **Операционное (003)** | 3 ядра + 16 мостов без тестов | **19** | 003 (07:30), **подтверждено мной** |
| **3** | **SHA-практика (007/200)** | то же + 2 теста | **21** | диспатч 200 (07:30) |

**Состав определения №1 (по доку):**
`AudioEngineV2.ts` 2 178 · `patchV1.ts` 162 · `StemPlayer.ts` 210 · `AudioLoader.ts` 110 · `VocalMix.ts` 88 · `MicrophoneManager.ts` 149 · `audioContext.ts` 35 · `track.orchestrator.ts` 592 · `js/*.js` ×5.

🔴 **Пересечение №1 ∩ №2 = всего 3 файла** (`AudioEngineV2`, `patchV1`, `track.orchestrator`).
⇒ Док замораживает `StemPlayer`/`AudioLoader`/`VocalMix`/`MicrophoneManager`/`audioContext`, которых **нет в операционной зоне**.
⇒ Операционная зона замораживает **16 мостов**, которых **нет в доке** — доку прямо пишет: _«🔄 Bridges (3 осталось — **не frozen**)»_ (`frozen-zones-v2.md:25`).

**Это не «три числа одного». Это три разных множества.**

---

## §2 ✅ 19 ПРОТИВ 21 — РАЗНИЦА = ТЕСТЫ (той же природы, что 71/36 против 72/37)

| Счёт | Состав | Строк | Проверено |
|---|---|---:|---|
| **19** | 16 мостов (без `__tests__`) + 3 ядра | **5 299** | ✅ мной, `wc -l`, совпало с 003 ровно |
| **21** | 18 файлов `bridges/` (с тестами) + 3 ядра | 5 445 | ✅ мной |

Исключаемые тесты:
- `src/bridges/__tests__/audio.bridge.test.ts` (73 строки)
- `src/bridges/__tests__/mode-switch.bridge.test.ts` (73 строки)

### 🔴🔴 МАНИФЕСТ ДОЛЖЕН БЫТЬ **19**, НЕ 21
Если `check-frozen.mjs` запрепит 21 файл, он **запретит писать регрессионные тесты на замороженный код**.
Тесты — это и есть страховка, доказывающая, что frozen не сломали. **Запереть страховку = разрушить страховку.**
📌 **Канон: 19.** Тесты исключены явно, с комментарием почему.

---

## §3 🔴🔴 FROZEN-ЗОНА НА 94% — КЛАДБИЩЕ

Проверил живые импорты всех 16 не-тестовых мостов.

```
grep -rn "^import.*bridges/" src --include="*.ts" --include="*.tsx" | grep -v "__tests__"
→ ./main.tsx:6: import { installLiveGuard } from './bridges/live-guard';
```

**Ровно один живой импорт на всю папку.**

| Мост | Ссылок | Кем заменён (registered в `main.tsx`) |
|---|---:|---|
| `live-guard.ts` | 1 | — **🟢 ЖИВОЙ** (`main.tsx:6`) |
| `audio.bridge.ts` | 13 | `audio-events` (`main.tsx:77`) |
| `track.bridge.ts` | 9 | `track-events` (`:64`) + `position-sync` (`:76`) |
| `mode-switch.bridge.ts` | 9 | `mode-events` (`:75`) |
| `lyrics.bridge.ts` | 7 | `lyrics-events` (`:69`) |
| `markers.bridge.ts` | 5 | `markers-events` (`:68`) |
| `blocks.bridge.ts` | 5 | `blocks-events` (`:67`) |
| `loop.bridge.ts` | 5 | `loop-events` (`:78`) |
| `audio-reactive.bridge.ts` | 4 | `audio-reactive` (`:71`) |
| `stem-reactive.bridge.ts` | 3 | `stem-reactive` (`:72`) |
| `cover-theme.bridge.ts` | 3 | `cover-events` (`:65`) |
| `mode.bridge.ts` | 3 | `mode-events` (`:75`) |
| `monitor.bridge.ts` | 2 | `monitor-events` (`:70`) |
| `plate.bridge.ts` | 2 | `plate-events` (`:66`) |
| `textStyle.bridge.ts` | 2 | `text-style-events` (`:74`) |
| `time-sync.ts` | 2 | — ⚠️ не имеет прямой замены, проверить |

**Все 15 заменены обёртками `foundation/event-bus/wrappers/*`,** которые регистрируются через `registerInit` в `main.tsx:61-78`. В `main.tsx` прямо помечены: _«// retired: blocks.bridge → blocks-events (DUAL FIRE)»_ (`:5`), _«// retired: loop.bridge → loop-events»_ (`:7`), _«// retired: audio-reactive.bridge → audio-reactive wrapper»_ (`:9`).

⇒ **Мы запираем 15 трупов и одного живого одинаковым замком.**

---

## §4 🔴 ДВА РАЗНЫХ СМЫСЛА ЗАМОРОЗКИ СМЕШАНЫ В ОДНУ ЗОНУ

| Смысл | Файлы | Почему заморожено | Что с этим делать |
|---|---|---|---|
| **(а) Драгоценное ядро** | `AudioEngineV2.ts` (2 178) · `patchV1.ts` (162) · `track.orchestrator.ts` (592) | незаменимо до V3 | **охранять** машиной |
| **(б) Retired-мосты** | 15 файлов, 2 367 строк | **уже заменены**, ждут удаления | **хоронить** (Д-2 / В-2), не охранять |

Одна машина `check-frozen.mjs` не может обслуживать оба смысла: трупам нужен не замок, а могила.
📌 **Предложение:** манифест разделить на два блока — `guard` (3 ядра + `live-guard` = 4 файла) и `graveyard` (15 файлов, кандидаты на снос в Д-2).

---

## §5 🔴 КОНФЛИКТ: FROZEN БЛОКИРУЕТ ФИКС УТЕЧКИ AudioContext (ДГ-13)

`frozen-zones-v2.md:18` замораживает **`src/audio/core/audioContext.ts` (28 строк)** как **«Permanent frozen (никогда не трогать)»**.

Но найденная мной утечка (ДГ-13, пост 07:22) живёт ровно там:
```
audio/core/audioContext.ts:11-16
export function getAudioContext(): AudioContext {
  if (!_ctx) { _ctx = new (window.AudioContext || window.webkitAudioContext)({ latencyHint: 'interactive' }) }
  return _ctx
}
```
Файл **не входит** в операционную зону (19/21) ⇒ де-факто править можно. Но документ says «никогда не трогать».
⇒ 🔴 **Противоречие док ↔ практика требует решения Никиты.** Формально: поправка в `frozen-zones-v2.md:18` или явное исключение.

---

## §6 🔴 ДОК ЗАМОРАЖИВАЕТ ТРУП И ВРЁТ О ЧИСЛЕ `js/`

| Что | Док | Реально |
|---|---|---|
| `js/*.js` | «5 файлов, ~1 000 строк» | **6 файлов**: `audio-engine.js` · `audio-facade-v3.js` · `lyrics-display.js` · `marker-manager.js` · `monitor-mix.js` · `track-catalog.js` |
| `src/js/*.js` | (подразумевается) | **1 файл** (`main.js`) — док имеет в виду корневой `js/` |

🔴 **`js/audio-engine.js` — мёртв.** Это `ДГ-01`: по доказательству ЦП-01 (201) файл **не грузится** в `index.html` ⇒ `window.audioEngine` не из него.
⇒ **Постоянно-замороженный документ держит в списке неприкасаемых мёртвый файл.**

---

## §7 📦 ГОТОВЫЙ МАНИФЕСТ — 19 ФАЙЛОВ, SHA256 (для 007 + 002)

Формат: `SHA256  строки  путь`. Сумма строк = **5 299** (совпало с 003 ровно).

```
377b2e5f5244289ba0601d92026b2f4b34124b1eec94a61d7143fdccd5fc156a     150  src/bridges/audio-reactive.bridge.ts
f2ad1cabd829edc50dca4b39cf5f7ce54e66e2107e68c8a83672d4d2ead9e4f3     301  src/bridges/audio.bridge.ts
8eb0cf3f5f2f512e38db02ed2788099a4f3c747405777741e27ab5d840ec4a27      80  src/bridges/blocks.bridge.ts
84cd0a54a7585ab8dcb28888e668b0921be4c528ac1cc7f90d2d682ca33be2f4      43  src/bridges/cover-theme.bridge.ts
a87b3fd22dca826e5d51df90eb5fa91c5ffb5f9aa38b32e067e3fb1299886ba8      21  src/bridges/live-guard.ts
1375957a2b391b798570b22eb74bb7e2728108e14794b2088dfafc032c784d1c      94  src/bridges/loop.bridge.ts
059944b4ee16066be77d95fd9e8a7ad831dcd6dafa30b9b0e39dcc99c6334aa7     184  src/bridges/lyrics.bridge.ts
37ba77ac692c9cb2fdf235d1ce986a45acb1b3bc1bf302ed7775cea78b8ab81b     106  src/bridges/markers.bridge.ts
72efdf8e219df2726cfec1328364da2bbcfa78b847202c74128f15348678b7df     384  src/bridges/mode-switch.bridge.ts
ebd056555ccc1f07ed51a4af629887a49fafaed68ebb493eab8a68aa3fc89c50     121  src/bridges/mode.bridge.ts
5030cf93c67e8f45c9f6ac2f67e504e3cb5bc29ba37749cd436289d23938516e     170  src/bridges/monitor.bridge.ts
6909a7e748e35fa71acbbbb90825c9f15be119409046f22cc999874a2dff44e3      52  src/bridges/plate.bridge.ts
473e506882aea9fa2b4ecad609d6dabe3fe202a9d8c6341a4f3e297867461cab     271  src/bridges/stem-reactive.bridge.ts
670ffa4cc42bd79972cc0204cf50b6004a3c55fb5fc39337601b8419f660f1c0     218  src/bridges/textStyle.bridge.ts
6d2bece240187d05cdb9093f1153ba3e9f6118191a3560556c0d8e226c31e876      31  src/bridges/time-sync.ts
8e30b54d5ba79f1e110d685dd949024973df0ec00bd484d0c5cdde4793ec28c9     141  src/bridges/track.bridge.ts
c5311543716de2f9c2317b85dee0c676c219e0242702a23fe11a435778ed6f92    2178  src/audio/core/AudioEngineV2.ts
0e599c349d104e7737ecd902d4fc64da89f2c278fae61602b2562b32f9924a30     162  src/audio/compat/patchV1.ts
b8818e66e5cd5ff6ce158cdc6d02f824c9190c1cce4a6c555b73ad94946199bd     592  src/services/track.orchestrator.ts
```

**ИСКЛЮЧИТЬ ЯВНО (не замораживать — это страховка):**
```
7b104b6439eb8a2e55bded3377cc2e5be91c741797b4fa7e6d3353ae66f2ed56      73  src/bridges/__tests__/audio.bridge.test.ts
9cbe81aab490ee229441205d7067a6de5932ecfc993f88c4d319232c05d849da      73  src/bridges/__tests__/mode-switch.bridge.test.ts
```

---

## §8 📮 ПАК ЗАДАЧ КОМАНДЕ (передать)

| Кому | Задача | Блокировщик |
|---|---|---|
| **007 + 002** | «Frozen-Guard-CI»: манифест **19** (§7), тесты исключить, `scripts/check-frozen.mjs` + `deploy.yml` + `npm run verify:frozen` | манифест готов, берите как есть |
| **007 + 002** | Манифест разделить: `guard` (4 файла: 3 ядра + `live-guard`) и `graveyard` (15 retired) | — |
| **200** | Канон-число frozen: **19 без тестов / 21 с тестами** (не 17, не 30). В гейт-4++ заложить сверку «4 зоны» **и** число 19 | 003 ждёт подтверждения |
| **003 / 003_2** | F-15: правка `frozen-zones-v2.md:2` (17→19) и `README.md:15` (30→19) + `frozen-zones-v2.md:25` («bridges не frozen» — устарело, они в зоне) | — |
| **006** | Д-КЕЙС 5 «FROZEN-АУДИТ»: сверить манифест 19 × SHA; отдельно — 15 retired, `time-sync.ts` без прямой замены | ждёт манифест 007 |
| **201** | SHA-инвентарь для манифеста — **уже готов** (§7), брать отсюда, не пересобирать | — |

---

## §9 🔴 НИКИТЕ — ЧТО ТРЕБУЕТ РЕШЕНИЯ

1. **Какое определение FROZEN канон:** документ (13 файлов, другое множество) или операционное (19/21)? Сейчас они расходятся на 13 из 16 позиций.
2. **`audioContext.ts`** — снять замок с документа, чтобы закрыть утечку AudioContext (ДГ-13)? Сейчас док says «никогда не трогать».
3. **15 retired-мостов (2 367 строк)** — охранять или хоронить? Они уже заменены обёртками и не имеют живых импортов.
4. **`js/audio-engine.js`** — мёртв (ДГ-01), но числится в «Permanent frozen». Снять?

---

*— **301** · 2026-09-01 · HEAD `ffc4bac` · FROZEN проработан полностью · манифест 19 готов к вставке в CI*

---

## §10 🔄 ОБНОВЛЕНИЕ 14:30 — ЧТО ПОМЕНЯЛОСЬ ПОСЛЕ ВОЛНЫ A (`b0fa1d5`) · HEAD `76bb061`

Все утверждения ниже перепроверены **после** применения Волны A и патча гейта `9c887a7`. HEAD **`76bb061`** = `origin/main`.

### 10.0 Статус манифеста — **применён и зелёный**

- `frozen-manifest.json` = **18 файлов** (guard **3** + graveyard **15**), `note` обновлён.
- Прогон: `node scripts/check-frozen.mjs` → **`frozen OK: 18/18`, EXIT=0**.
- SHA: **18/18 совпали, 0 отсутствующих**. Гейт стоит в `deploy.yml` **до** Build.
- Реальная сумма строк **5 138** (мои файлы), документ says **5 137** — см. 10.3.

### 10.1 ⚠️ САМОКОРРЕКЦИЯ §3 — «94% кладбище» верно по цифре, **причина не та**

В §3 я объяснил отсутствие чтений тем, что мосты **не импортируются**. Это неполно. Поправка 201 (принята): мосты не импортируются **потому что** им это не нужно — они **дёргают god-object напрямую**:

`(window as any).audioEngine` внутри graveyard: **7 файлов / 14 вхождений** —
`audio.bridge` 6 · `loop.bridge` 3 · `mode-switch:145` 1 · `mode.bridge:54` 1 · `stem-reactive:59` 1 · `audio-reactive:22` 1 · `time-sync:19` 1.

⇒ **graveyard = неисполняемый код под контрактом V2.** Кладбище — по факту исполнения; бомба — по факту контракта. Формулировка в `note` манифеста (применена): «Graveyard = мёртвый код под контрактом V2; воскрешение требует проверки контракта, не только SHA (201, 01.09)».

### 10.2 ✅ КАНОН ЧТЕНИЙ — ПЕРЕКРЁСТНАЯ ПРОВЕРКА (мои числа против 201)

Считано независимо, своими методами, на разных HEAD (201 — `99f2a825`, я — `76bb061`):

| | 201 | 301 | дельта |
|---|---|---|---|
| всего насчитано | 125 | 127 | +2 |
| из них комментарии (не чтения) | 4 | 0 | −4 |
| алиас `w.audioEngine` | 0 | 6 | +6 |
| 🔒 frozen | 14 (7 мостов) | **16** | +2 |
| **🟢 живые** | **111** | **111** | **0 ✅** |

**Живые 111 подтверждены двумя независимыми методами.** frozen = **16 чтений / 8 файлов**: 7 graveyard-мостов (14) + guard `src/services/track.orchestrator.ts` (2).

Разбор дельты: 125 = `(window as any)` 122 + `window.` 3, и **все 3 `window.` — комментарии** (`audio-events.ts:16` · `stem-engine-sync.ts:58` · `rehearsal-trigger.bridge.ts:122`); ещё **1 из 122** — комментарий (`rehearsal-trigger-writer.ts:6`) ⇒ реальных 121; плюс **6 пропущенных** через алиас `w` (см. 10.5) ⇒ **127**. −4 +6 = +2 ✅.

### 10.3 🧩 РАЗГАДКА «+1 СТРОКА» — `loop.bridge.ts` БЕЗ `\n` НА КОНЦЕ

Документ says **5 137**, файлы дают **5 138**. Причина: `loop.bridge.ts` — **единственный файл frozen-зоны без завершающего перевода строки**; `wc -l` считает только символы `\n` и теряет последнюю строку.

⇒ **документ прав, канон = `wc`.** Этим же файлом объясняется утреннее расхождение 5299/5300. **Свою правку «5299→5300» снимаю** — она сломала бы синхронизацию доков. Корректное решение (🔴 на усмотрение Никиты): либо дописать `\n` в `loop.bridge.ts` (станет честные 5138), либо зафиксировать правилом «канон = `wc`».

### 10.4 ✅ ДГ-13 (утечка AudioContext) — **ЗАКРЫТ САМ СОБОЙ**

§5 этого аудита ставил ДГ-13 как конфликт: frozen-файл `patchV1.ts:11` был **единственным** вызывающим `setAudioContext()` и перезаписывал `_ctx` без `close()` ⇒ контекст-сирота, 1 утечка на запуск.

**Волна A удалила `patchV1.ts`.** Проверено: у `setAudioContext()` **0 вызовов**, осталось только объявление `audioContext.ts:8`. Подменщика нет ⇒ создаётся ровно один контекст ⇒ **утечки нет**.

Остаток → **Д-2**: `setAudioContext` — мёртвый экспорт. Жадное создание контекста без жеста пользователя (`audioContext.ts:11-16`) остаётся, но это уже не утечка.

### 10.5 ⚠️ ЛОВУШКА №2 ДЛЯ ВСЕХ, КТО СЧИТАЕТ (после экранирования точки)

`grep "w\.audioEngine"` ловит **хвост от `window.audioEngine`** — буква `w` на конце слова `window`. Нужна граница слова: `(?<![\w$.])w`. Вдобавок строка `stem-engine-sync.ts:58` начинается с « * » (продолжение блочного комментария) — детектор по `//` её **не берёт**; я поймал это только на второй итерации.

Реальный алиас: `const w = window as any` (`upload.service.ts:205,394,1058,1102,1228`; `track.actions.ts:16,43,99,128,148`) ⇒ чтения `upload.service.ts:582` · `track.actions.ts:58` ×2 · `:160` ×2 · `useBackgroundManagers.ts:48` = **6**, все настоящие.

### 10.6 🔴 ОТКРЫТО — ДВА ПРОТИВОРЕЧИЯ «ДОК ↔ МАШИНА» (пакет **F-16**, конвейер 003/003_2)

1. **`docs/architecture/frozen-zones-v2.md:28`** — «Bridges (3 осталось — **не frozen**)» при **16 мостах** в манифесте. Строка `:3` уже исправлена (3 зоны · 18 файлов), `:28` — нет.
2. **🆕 Трёхстороннее противоречие по `rehearsal-trigger`.** `src/foundation/event-bus/wrappers/rehearsal-trigger-writer.ts:4` утверждает `// ORIGINAL: src/Rehearsal/bridge/rehearsal-trigger.bridge.ts ❄️ FROZEN`. При этом в `frozen-manifest.json` файла **нет** (ни одного frozen-пути со словом `rehearsal`), а код его **импортирует и инстанцирует**: `main.tsx:38` → `:803` `new RehearsalTriggerBridge(pc, role)` и `deep-link.service.ts:5` → `:91`.
   ⇒ док «frozen» · машина «не frozen» · код «живой в бут-цепи».

### 10.7 🎯 ПОЗИЦИЯ ПО В-3 (ответ 201, запрос 12:45)

`rehearsal-trigger.bridge.ts` = топ-1 живого читателя (**17** чтений; «18» у 201 = 17 + комментарий `:122`). **Первым кандидатом В-3 — против**, по двум фактам:

- он **в бут-цепи** (два `new`), это не graveyard-паттерн;
- его объявленная замена `rehearsal-trigger-writer.ts` имеет **0 импортов** ⇒ **посадочной площадки нет**, откатываться некуда.

Предложение: первым в В-3 брать **самый подготовленный** мост — тот, у которого замена **подключена** и есть смоук 006. Для `rehearsal-trigger` порядок: сначала подключить writer + смоук, потом двигать мост.

### 10.8 Что осталось открытым (на 14:30)

| # | Что | Кому |
|---|---|---|
| 1 | `frozen-zones-v2.md:28` + противоречие `rehearsal-trigger` | **F-16** → 003/003_2 |
| 2 | Канон «+1 строка»: дописать `\n` или зафиксировать «канон = wc» | 🔴 Никита |
| 3 | `setAudioContext` мёртвый экспорт | **Д-2** |
| 4 | Подключить `rehearsal-trigger-writer` перед В-3 | 201 / 006 |
| 5 | Т-4 (DOM+postMessage, `ПР-600…899`) | **301** (в работе) |

---

**— 301 · Mac · 01.09 14:30 · HEAD `76bb061` (= origin/main) · §10 — обновление после Волны A · самокоррекция §3 · ДГ-13 закрыт · 111 живых подтверждены**

---

## §11 🔎 ОБНОВЛЕНИЕ 02.09 09:55 — ПРАВИЛО «КАНАЛ-СИРОТА» (В-3-КОНСАЛТ) · HEAD `98a276e`

### 11.0 Статус замка после Волны B — **зелёный, замыкается полностью**

| Проверка | Результат |
|---|---|
| `check-frozen.mjs` | **`frozen OK: 17/17`, EXIT=0** |
| Манифест | **guard 2 + graveyard 15 = 17** |
| `src/bridges/` (без `__tests__`) | **16 файлов** |
| Расхождение | **0 фантомов, 0 сирот** — полное замыкание |
| Канон чтений `window.audioEngine` | **123 всего / 14 frozen / 109 живых** (пересчитано независимо, сошлось с 201) |

`src/bridges/` = 15 graveyard + 1 guard (`live-guard.ts`). Ни одного файла в каталоге вне
манифеста, ни одного пути в манифесте без файла. **`event-bus/README.md` точен** (`src/bridges/*`
= 16/16), но сливает guard и graveyard в один список — `live-guard.ts` это guard, хотя лежит
в `bridges/`.

### 11.1 🔴 ГЛАВНОЕ: «мёртв по импортам» ≠ «безопасен». Правило «канал-сирота»

Волна C сносит мосты по признаку **«импортов нет ⇒ мёртв ⇒ сносим»**. На `mode-switch.bridge.ts`
проверено: этого признака **недостаточно**.

Мост не импортирует никто — но он **пишет живой канал**:

```
mode-switch.bridge.ts:369   (window as any).beLiveSwitchMode = switchMode;   ← труп
mode-switch.service.ts:247  (window as any).beLiveSwitchMode = switchMode;   ← живой сервис
```

**Читателей канала — 6, все живые:**

| Читатель | Строка |
|---|---|
| `src/stores/practice-session.store.ts` | `:144` |
| `src/components/TrackInfoBoard/ai-tools.ts` | `:963` (фолбэк `:967`) |
| `src/Rehearsal/services/deep-link.service.ts` | `:69` |
| `src/catalog/components/CatalogLayout.tsx` | `:114`, `:242`, `:243` |

Сегодня канал пишет только сервис — работает. Но мост это **бомба с двумя взрывателями**:
загрузись он после сервиса, ① читатели уйдут на труп, ② состояние сервиса
(`currentMode :92`, `previousMode :93`) протухнет и `getCurrentMode()` солжёт, ③ мост
дополнительно навесит DOM-слушатели на `.mode-button` (`:372-383`, немедленный вызов `:383`)
поверх React-компонента `ModeButtons.tsx`. Сегодня селектор `.mode-button` не рендерит никто
(0 совпадений в `src/` кроме самого моста) — это **повезло, а не защищено**.

> ### 📏 ПРАВИЛО «КАНАЛ-СИРОТА» (предложено гейтом Волны C)
>
> **Снос моста разрешён только при двух условиях:**
>
> **① Импорты мертвы.** Динамический импорт из `__tests__` не считается живым.
>
> **② Канальный тест.** Для каждого `window.*`-присваивания внутри моста доказано одно из:
> - **(а)** канал **не имеет живых читателей** (замкнут внутри моста) — тогда мост сносится
>   сразу;
> - **(б)** есть **живой альтернативный писатель** — тогда **мост сносится вторым**, убедившись,
>   что живой писатель загружается.

### 11.2 🟢 Правило проверено на кандидате №1 — `audio.bridge.ts` проходит чисто

Единственный глобал, который он пишет: `__stemsMuteListener` (`:106` = null,
`:108` = `applyStemsMute`).

**Всего 5 упоминаний во всём репо:**

| Строка | Что |
|---|---|
| `audio.bridge.ts:81` | чтение (`if`) |
| `audio.bridge.ts:82` | `removeEventListener` |
| `audio.bridge.ts:106` | запись `null` |
| `audio.bridge.ts:108` | запись `applyStemsMute` |
| `audio-events.ts:17` | **комментарий**: «`__stemsMuteListener` (window.\* костыль — V3 не нужен)» |

⇒ **4 из 5 внутри самого трупа, 5-й — комментарий. Живых читателей нет.** Канал замкнут в себе.
**Снос `audio.bridge.ts` безопасен.** Выбор 201 (брать его первым в В-3) **подтверждён
доказательством**, не только соображением.

Для `mode-switch.bridge.ts` правило даёт вариант **(б)**: снос возможен, но **вторым**, после
того как убедимся, что сервис загружается.

### 11.3 🆕 Экстракция `mode-switch` неполная — но потерянный код мёртв

`wrappers/mode-switch-events.ts:4` сам признаёт: «ORIGINAL: `src/bridges/mode-switch.bridge.ts`
**(partial)**». Тело `switchMode`: мост **24 строки** (`:342`), сервис **19** (`:225`).

Потеряно два фрагмента:

1. комментарий `// W4b: Save rehearsal volumes BEFORE applying new mode`;
2. блок:
   ```js
   const we = (window as any).waveformEditor;
   if (we?.isVisible) we.hide();
   ```

🟢 **Потеря — гарантированный no-op.** У `window.waveformEditor` **0 присваиваний во всём
репо** — проверено включая `js/` (5 файлов, каталог существует) и `index.html` (32 КБ, файл
существует): это **истинное отсутствие**, а не ложное. Единственное чтение —
`mode-switch.bridge.ts:354`, в том же трупе. `we` всегда `undefined` ⇒ `we?.isVisible`
всегда `undefined` ⇒ `we.hide()` не выполнится никогда. **Экстракция полная по факту.**

### 11.4 🆕 Доки врут снова — новый экземпляр Д-1 (пакет F-16, конвейер 003/003_2)

| Дока | Строка | Что утверждает | Реальность |
|---|---|---|---|
| `architecture-map-2.1.md` | `:407` | «`window.waveformEditor` → `waveform-editor.stub.ts`, Compat stub, `sync.bridge.ts` intercept» | **файла не существует** |
| `interaction-schema-2.1.md` | `:318` | «`window.waveformEditor` — TS stub if missing» | **файла не существует** |

`find -name "*waveform*"` даёт только `src/takes/waveform/*` и `src/sync/canvas/draw-waveform.ts`.
Две архитектурных доки описывают compat-слой, которого нет в репозитории.

### 11.5 ✅ Врущая шапка ×10 — подтверждена 1:1, но у 003/003_2 её нет

- **Подтверждено независимо:** врапперов **20**, шапку «Активируется когда Facade
  отключается» несут **10**, **список файлов совпадает со списком 201**: `blocks-events` ·
  `cover-events` · `exercise-events` · `markers-events` · `mode-switch-events` ·
  `monitor-events` · `plate-events` · `position-sync` · `rehearsal-trigger-writer` ·
  `text-style-events`.
- **В `RAWDUMP-WAVE-BC-003_2-2026-09-01.md` (HEAD `44fbfeb`) этой темы НЕТ** — секции W0–W4
  (сырьё скаута · якоря Волны A · src-комментарии · SSOT-слой · рекомендация). Унести паре
  003/003_2.
- 🆕 **Пункт §W3 п.4 (SSOT-список в `event-bus/README.md`) уже закрыт в коде:** README
  переписан (`:54` отсылает к `frozen-manifest.json`, «verify:frozen, 17/17»), `patchV1` и
  `track.orchestrator` из списка убраны, `js/*.js` заменён точной формулировкой.

### 11.6 🔧 Побочно: детектор склеек заголовков (ловушка протокола)

Опубликованная строка 201 — «`grep -c '^## LOG'` против `grep -o '## LOG' | wc -l`, разница =
число склеек» — **даёт 9 при реальных 2**: остальные 7 это текст §0, где формат блока описан
словами. Рабочая строка отсекает цитаты:

```
grep -v '^>' REG | grep -cE '^## LOG [0-9]{4}-[0-9]{2}-[0-9]{2}'            # 204
grep -v '^>' REG | grep -oE '## LOG [0-9]{4}-[0-9]{2}-[0-9]{2}' | wc -l      # 206
```

Разница **2** = реальное число склеек. 🔴 **Склейка 452 (блок 003, 23:48) не расклеена**,
склейка **276 (блок 200, 07:55)** — новая.

### 11.7 Что осталось открытым (на 02.09 09:55)

| # | Что | Кому |
|---|---|---|
| 1 | `frozen-zones-v2.md:28` + противоречие `rehearsal-trigger` | **F-16** → 003/003_2 |
| 2 | Канон «+1 строка»: дописать `\n` или зафиксировать «канон = wc» | 🔴 Никита |
| 3 | `setAudioContext` мёртвый экспорт | **Д-2** |
| 4 | `waveform-editor.stub.ts` — фантом docs (§11.4) | **F-16** → 003/003_2 |
| 5 | Внести правило «канал-сирота» гейтом в спеку Волны C | **201 / 007** |
| 6 | Т-4 (DOM+postMessage, `ПР-600…899`) | **301** (в работе) |


## §12 🔎 ОБНОВЛЕНИЕ 02.09 20:10 — ФАНТОМНЫЙ КАНАЛ `window.liveMode`: ЗАМОК ОХРАНЯЕТ НЕСУЩЕСТВУЮЩЕЕ · HEAD `59cd990`

### 12.0 Статус замка после Волны C — **2/2, кладбище пусто**

| Проверка | Команда | Результат |
|---|---|---|
| Каталог мостов | `ls src/bridges/` | **1 файл** — `live-guard.ts` (578 б) |
| Манифест | `frozen-manifest.json` | `guard` = 2, `graveyard` = `{}` |
| Замок | `node scripts/check-frozen.mjs` | `frozen OK: 2/2`, EXIT=0 |

**Волна C закрыта полностью.** 15 graveyard-мостов снесены; правило «канал-сирота» (§11.1) отработало на `mode-switch`: канал выжил, слепых зон нет.

Осталось два guard-файла:
- `src/audio/core/AudioEngineV2.ts` — ядро звука, жив.
- `src/bridges/live-guard.ts` — **см. 12.1: не то, чем кажется.**

### 12.1 🔴 ГЛАВНОЕ: последний охранник охраняет фантом

`window.liveMode` — **ноль писателей во всём репозитории.**

Проверено по: `src/`, `js/` (5 легаси-файлов), инлайн-блоки `index.html` (`:38 :57 :192 :369 :406`), внешние CDN.
Независимое подтверждение из команды: `team-m/MICRO-PACK-WAVE5.md:53` — `N6 grep -rn "window.liveMode =" src` → 0.

Четыре читателя — **все четыре мёртвые ветки**:

| Файл:строка | Что делает | Почему мертво |
|---|---|---|
| `src/bridges/live-guard.ts:6-15` | патчит `liveMode.activate` | `if (!win.liveMode) return` — патч не накладывается **никогда** |
| `src/services/mode-switch.service.ts:19-22` | `deactivateLiveIfActive()` | `lm?.isActive` всегда `undefined` |
| `src/services/mode-switch.service.ts:209-211` | `activateLive()` → `liveMode.activate()` | ветка не выполняется |
| `src/utils/notification.ts:21-22` | приоритет 2 показа ошибки | всегда мимо → работает приоритет 3, DOM-тост (`:27+`) |

**Итог:** `live-guard.ts` — файл из **frozen-зоны**, подключённый в буте (`main.tsx:6 → :317`), который на каждом запуске гарантированно ничего не делает.

### 12.2 🔴 `bl-live-camera` — гейт согласия, который никто не выставляет

Единственное чтение ключа: `src/bridges/live-guard.ts:10`.
Писателей — **0**. По всему репо ключ встречается дважды: в самом `live-guard.ts` и в минифицированной копии guard'а внутри сборки (`dist/assets/main-ClHPKwif.js:583`).

Следствие: `cameraAllowed` всегда `false` ⇒ даже будь `liveMode` жив, `activate()` всё равно вернул бы `Promise.resolve()`. Охранник **нерабочий дважды**.

### 12.3 🟢 Камера живёт мимо `liveMode` — реальный путь цел

`src/stores/camera.store.ts:27` → `navigator.mediaDevices.getUserMedia({ video: { facingMode }, audio: false })`.
Потребитель: `src/components/LiveControls.tsx:2,7,8,9`.
`bl-live-camera` этот путь **не пишет** — он с `liveMode` не пересекается вообще.

Вывод: `liveMode` — остаток легаси-слоя, а не недостроенная фича. Снос канального кода = no-op. Но `live-guard.ts` лежит в **frozen guard-зоне** ⇒ снос = 🔴 решение Никиты, не моё.

### 12.4 ⚠️ САМООТЗЫВ 301: mediapipe **не** грузится

Заявил: «9 CDN-скриптов mediapipe — мёртвый вес на каждой загрузке страницы». **Неверно, отзываю.**

`index.html:24` открывает `<!-- FINAL-W1: MediaPipe removed (mask-system disabled since S0) -->`, закрывается `-->` на `index.html:35`. Скрипты `:26-34` лежат **внутри HTML-комментария** — не грузятся: 0 байт, 0 запросов.

Причина ошибки: grep по `<script` дал 9 хитов, и я принял хиты за загрузку, не глянув контекст. **Урок: хит grep ≠ исполняемый код.** Ровно та ловушка, про которую §11.1.

### 12.5 🆕 Семейство ложно-зелёных тестов (пакет **Д-3**, новое)

Тест сетает и читает поле, которого нет в типе стора. Механика ложной зелени: zustand `setState` сливает объект как есть ⇒ фантомный ключ попадает в стейт, `getState()` его же и читает. Тест проходит, **не проверяя ничего**.

| Тест:строка | Фантомное поле | Реальное поле |
|---|---|---|
| `simple-stores.test.ts:15,17` | `cameraActive` | `cameraOn` (`camera.store.ts:4`) |
| `simple-stores.test.ts:24,26` | `pianoOpen` | `open` (`piano.store.ts:5`) |
| `simple-stores.test.ts:51,53` | `activeTab` | `activeTabId` (`deck.store.ts:6`) |
| `medium-stores.test.ts:6` | `loopStart` | `loopStartTime` (`loop.store.ts:14`) |
| `medium-stores.test.ts:64,66` | `isWordSyncReady` | отсутствует в `WordSyncState` |

Масштаб: **26 ошибок** класса «фантомное поле» в тестах — `medium-stores` 12, `simple-stores` 10, `batch-remain` 4.
`startCamera` / `stopCamera` / `togglePiano` этими тестами **не покрыты**.

### 12.6 📊 Канон tsc — независимый прогон 301 (с Мака, не с ПК)

| Метрика | Значение |
|---|---|
| Всего ошибок `tsc --noEmit` | **282** — сошлось с каноном 007 |
| Файлов с ошибками | 87 |
| **TS6133 «объявлен, но не используется»** | **116 (41%)** — чистый мёртвый код |
| TS2339 «свойства нет в типе» | 89 |
| Ошибок в тестах | **67 (24%)** |
| Из них фантомные поля | 26 |

Прогон шёл **с Мака через sshfs**, другим рантаймом (node 22.22.2 managed) — число сошлось 1:1 с ПК. Канон `tsc=282` подтверждён независимо, а не принят на веру.

**41% ошибок tsc — не баги, а мусор.** Это прямой замер под директиву «Ноль мусора»: 116 объявлений можно удалить без изменения поведения.

### 12.7 Что осталось открытым (на 02.09 20:10)

| # | Что | Кому |
|---|---|---|
| 1 | `live-guard.ts` + 3 мёртвых читателя `liveMode` — снос или оставить | 🔴 Никита (frozen-зона) |
| 2 | `bl-live-camera` — 0 писателей, гейт согласия всегда закрыт | 🔴 Никита |
| 3 | Семейство ложно-зелёных тестов (26 ошибок, **Д-3**) | **005 / 005_2** |
| 4 | 116 × TS6133 — мёртвый код под снос (карта веса) | **301** (в работе) |
| 5 | `frozen-zones-v2.md` противоречит сам себе (§11.7 п.1) | **F-16** → 003/003_2 |
| 6 | `waveform-editor.stub.ts` — фантом docs (§11.4) | **F-16** → 003/003_2 |
| 7 | Т-4 (DOM + postMessage, `ПР-600…899`) | **301** (в работе) |

---

**— 301 · Mac · 02.09 20:10 · HEAD `59cd990` · §12 — канал `window.liveMode`: 0 писателей, 4 мёртвых читателя, frozen-guard охраняет фантом · самоотзыв по mediapipe · канон tsc=282 подтверждён независимо (116/282 = мёртвый код) · Д-3: 26 фантомных полей в тестах**
