# 📦 MICRO-PACK «ДИСТОРШН-ХИРУРГИЯ» (снос вердикт-слоя + миграция сырья в слепок-контур)

**Номер:** D-CLN-1 · **Дата:** 2026-09-02 (ночная смена) · **Автор:** 005_2 (Research-2) на базе границы-51 (301) + вердикта-④ пилота
**Для:** цепь 001→002→001→009 → Оператор · **Тип:** SPECA · **Статус:** 🟡 К прогону · **v1.3 ФИНАЛ** (6 условий 001-К1 + 5 патчей 002-К2 + 2 усиления + 2 микро-условия 009-СУД: гейт-коллизия и К-7-размещение закрыты, ночь 02.09)
**Основание:** 🔴 Никита 31.08 13:10 (LOG 200 13:20): детектор «бред», под снос; условие «снос после контура замены» — **ВЫПОЛНЕНО** (контур = зона-7 PUZZLE + С1-С8, пилот 4/4).

**⚖️ OVERRIDE 301-ОТКРЫТОЕ-2 (002-УДАР-1):** снос yin:147-148 (noiseProxy) санкционирован вердиктом-④ 005_2 (02.09): «CMND-апериодичность = валидный черновик, НО её UI-интерпретация = бред; снос из UI, метрики остаются внутри» — при TS6133-мине (после сноса :149/:152 у noiseProxy 0 читателей → tsc≠290) и живом-своём SFM-канале (engine:223) выбираем снос как мёртвого кода. **Якорь-формула восстановления (обязательна в Шаге-3):** `noiseProxy = clamp((cmnd[te] − 0.18) / 0.42, 0, 1)` — НЕ «сырой cmnd[te]», восстановление = нормировка+кламп. **Следствие до Этапа-3:** шумовой контур = SFM-монопризнак; перекрёстная проверка (два признака) возвращается в слепке восстановлением noiseProxy по формуле.

---

## ЦЕЛЬ

Снести «вердикт-слой» дисторшн-детектора (одномерный UI-приговор «расщеп/шум» в PitchTab — то, что Никита видел и назвал бредом), сохранив всё DSP-сырьё для будущего «Расщёп-слепка» (Слой 3 по 301). Чистый фикс без изменения поведения аудио.

**Почему слои, а не всё:** философия 02.09 00:05 — «анализ прежде советов»: сырьё (анализ) живёт, вердикт (совет/приговор) уходит. Плюс правило Никиты: не остаться без ничего — снапшот-контур строится на Слое 3.

## БАЗА И ГЕЙТЫ

- HEAD-база: `c183abf` · канон: **tsc=290** 🔴 (Δ=0 СТРОГО) · **vitest 808+0int+0load (69 файлов)** 🟢 · PARITY PASS · frozen 17/17
- **Слои из DISTORTION-BOUNDARY-2026-08-31.md (301):** 🟥 Слой-1 мёртвый код 18 строк (риск 0) · 🟧 Слой-2 вердикт-визуал 32 строки (то, что «бред») · 🟩 Слой-3 сырьё ~58 строк (НЕ ТРОГАТЬ)
- **Ключ-поправка 301 (принята в спеку):** subScore/noiseScore из yin-detect.ts:151-152 МЁРТВЫЕ (затираются в pitch-engine:222-223); истинные источники: sub ← yin-detect:134-145 (CMND), noise ← pitch-engine:183-199 (SFM). Слой-1 включает эти мёртвые поля.

## ДЕЙСТВИЕ (три шага, порядок важен)

### Шаг 1 · Слой-1: снос мёртвого кода (18 строк, риск 0)
- `yin-detect.ts`: `:135-136` (let subFrequency/subMidi — осиротеют) · `:147-148` (aperiodicity/noiseProxy — осиротеют; ⚠️ noUnusedLocals → TS6133) · `:149` depth-формула · `:151-152` subScore/noiseScore (мёртвые копии) · `:171-174` порог → subFrequency/subMidi · `:182-184` return-поля subFrequency/subMidi/depth. YinResult-поля: **subharmonicRatio ОСТАЁТСЯ** (живой источник С7, читает engine:222); уходят depth/subFrequency/subMidi/subScore/noiseScore. **Якорь для Этапа-3:** CMND-апериодичность восстановима одной строкой `cmnd[te]` — отметить в RASP-SNAPSHOT-комменте.
- `pitch-engine.ts`: `:218-221` depth/subharmonicRatio-in-msg/subFrequency/subMidi из msg-объекта
- `types.ts`: `:10-17` depth?/subFrequency?/subMidi? (+комменты) · **+:12-13 `subharmonicRatio?` в WorkletMessage-типе** (писатель engine:219 сносится → поле мёртвое). YinResult.subharmonicRatio (`:16-17`) ОСТАЁТСЯ.
- ⚠️ Тонкость: `pitch-engine.ts:222` читает `(result as any).subharmonicRatio` — источник ОСТАЁТСЯ в yin-detect return (`:181`). `:223` noiseScore = SFM (свой, живой) — остаётся.

### Шаг 2 · Слой-2: снос вердикт-визуала (32 строки)
- `PitchTab.tsx` — ТОЧНЫЕ границы: сносятся **крылья-div `:374-377` и `:391-394`** + **textShadow-IIFE `:380-387`** (width-вычисления :376/:393 — внутри сносимых div). **ОСТАЮТСЯ: span-обёртка `:378-379` и `:388-390`** (после сноса :380-387), **vocalDisplay :389 жив** (нота вокала — функция таба!), контейнер `:373/:395`, стили wingsContainer/noteInWings. Отдельный vocalDisplay-IIFE `:350-357` в теле компонента — не трогается (жив).
- `noteInWings:88`: снос `transition: 'text-shadow 0.15s ease-out'` (транзишенит несуществующее после сноса IIFE свойство) — минус 1 строка.
- `:60-79` стили wingLeft/wingRight (20 строк по факту) — СНОС
- ⚠️ `useStableVocalData`-хук (`:101-185`) — Слой-3-D (EMA-канал) — **ОСТАВИТЬ ЦЕЛИКОМ, ВКЛЮЧАЯ стейт subScore/noiseScore и их return** (решение 001-Круга-1: канал доставки сырья для слепка; снос = потеря доставки Этапа-3). Хук не трогать вообще.
- `:376` больше не матчится — проверить ретраки типа «больше нет крыльев» вPitchTab-тестах (их нет — 0 тестов PitchTab по 00-пас 30.08).

### Шаг 3 · Слой-3: фиксация сырья (0 правок, только якоря)
- НИ ОДНОЙ правки кода. В комментарии над `yin-detect.ts:134-145` и `pitch-engine.ts:183-199` добавить якоря: `/* RASP-SNAPSHOT: сырьё снапшота (С5/С7), слепок-контур зона-7 — снос запрещён без OVERRIDE. noiseProxy-восстановление: clamp((cmnd[te]-0.18)/0.42, 0, 1) */`
- Это защита от будущих волн де-фриза («провода с маркерами» — директива).

## ФАЙЛЫ

| Действие | Файл | Строк-дельта |
|---|---|---|
| Править | `src/audio/pitch/yin-detect.ts` | −24 (Слой-1: 14 код + 10 интерфейс) + 2 коммента (:113/:129) + 1 якорь |
| Править | `src/audio/pitch/pitch-engine.ts` | −4 (Слой-1) + 1 якорь |
| Править | `src/audio/pitch/types.ts` | −8 (Слой-1) |
| Править | `src/components/PitchTab.tsx` | −36 (Слой-2: 16 JSX + 20 стилей) + коммент :99 −1 transition :88 |
| НЕ трогать (код) | Слой-3-КОД: yin-detect:134-145 · pitch-engine:183-199/222-223 · useStableVocalData (тело) · MicSourceV3 · vocalDisplay :350-357/:389 | 0 |

**Итого (002-пересчёт, единый канон): yin −24 · engine −4 · types −8 · PitchTab −37 (36 + transition:88) = −73 строк, 0 новых сущностей (гейт-тест сущностью не считается — Усиление-1), 2 якоря. Гейт объёма: СТРОГО перечень Шагов 1-2 (численный потолок снят). Атомарность: Шаг-1 = ОДИН коммит (yin→types→engine одной транзакцией, tsc-гейт финальный — Усиление-2).**

## ЗАПРЕЩЕНО

- ❌ Трогать frozen: `src/bridges/*`, `src/audio/core/AudioEngineV2.ts`, `src/audio/compat/patchV1.ts`, `src/services/track.orchestrator.ts` (по манифесту 17/17; orchestrator уже снесен Волной B — манифест актуален).
- ❌ Трогать Слой-3-КОД (сырьё) — единственное исключение: вставка якорь-коммента над блоком (Шаг-3); нумерация ниже точки вставки сдвигается +1 → все последующие гейты — grep по содержимому, НЕ по номерам строк.
- ❌ Менять поведение аудио/питч-движка: detect()-результат для живых потребителей (frequency/confidence/midi/rms/subharmonicRatio) НЕ меняется.
- ❌ Сверх перечня Шагов 1-2 не заходить (спека = хирургия; численный итог −73 = сумма перечня, не потолок).

## ТЕСТ (Operator-гейты)

```bash
npx tsc --noEmit                      # 290, Δ=0 (снос полей из интерфейса = TS-чистка, проверит потребителей)
npx vitest run                        # 808/69, 0int, 0load (pitch-store-тесты живы: subharmonicRatio-путь не тронут)
# depth-гейт (код-вхождения; 3 коммента-омонима yin:113,129 + PitchTab:99 — НЕ входят): переформулировать комменты «depth дисторшн» → «сабгармоника» ИЛИ гейт = grep по коду-строкам без комментарий. Оператор: rg -n "depth" --glob '!*.md' затем ручной отсев комментов. Итог: 0 КОД-вхождений.
rg -n "subFrequency|subMidi" src/ --glob '!**/__tests__/**'   # 0 вхождений (К-7-литерал в тестах исключён — гейт-коллизия 009-У1)
grep -c "wingLeft\|wingRight" src/components/PitchTab.tsx  # 0
grep -c "RASP-SNAPSHOT" src/audio/pitch/yin-detect.ts src/audio/pitch/pitch-engine.ts  # 2
git diff --stat src/bridges src/audio/core src/audio/compat  # пусто (frozen)
```
**К-7-расширение (009-У2: ТОЧНОЕ размещение — канон 808 не меняется):** в `pitch-engine-modes.test.ts` ДОБАВИТЬ 2 expect-строки **внутрь существующего it() К-7** (статический импорт `pitchTabSrc` уже в :13 — БЕЗ await, БЕЗ нового it()):
```ts
expect(pitchTabSrc).not.toMatch(/wing(Left|Right)/)
expect(pitchTabSrc).not.toMatch(/subFrequency|subMidi/)
```
→ вечная регресс-защита + 0 новых кейсов = vitest остаётся 808/69 (новый it() дал бы 809 ≠ канон).

## КОНТЕКСТ (для 001)

- 301-граница = основа разбивки; его поправка о мёртвом сырье принята (двойная шумовая метрика SFM+CMND — перекрёстная проверка для слепка).
- К-7-подобный static-grep: `expect(src).not.toMatch(/wing(Left|Right)|subFrequency|subMidi/)` — регресс-гейт в духе BusFader18.
- Слой-2-снос = «бред»-визуал уходит, PitchTab остаётся чистым (note + confidence уже там).
- ⚠️ TS-проверка потребителей — ИЛЛЮЗИЯ для engine:222 (читает через `as any`); канон держит noUnusedLocals-гейт (Условие-1), а не потребительский.
- Е2Е-следствие: скрин 11:07 Никиты больше не воспроизводим — вердикт-крыльев нет.

**Кто съест / когда:** Оператор по диспатчу 007 (после прогона цепи) · слепок-контур (Этап-3) строится на сохранённом Слое-3.
