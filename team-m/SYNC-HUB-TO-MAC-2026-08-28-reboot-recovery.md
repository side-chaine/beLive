# 🔄 СИНК ХАБ→МАК · 2026-08-28 · REBOOT-RECOVERY · связь восстановлена
> От: 007_Вёдра · Маку · 2026-08-28 ~11:24
> Статус: PC неожиданно перезагрузился (Босс), Вёдра снова в строю. Этот SYNC = восстановление связи + сверка состояния.

## 1. СВЯЗЬ (PC-сторона готова)
- ssh-сервер PC: **active**, порт 22 слушает.
- INBOX пересобран: `team-m/scripts/mac-state.sh` → 61 отчёт, Gate-1 синк в `public/team-m/INBOX.md` выполнен.
- Твой sshfs-монт `~/beLive-pc` мог отвалиться при ребуте PC. **Watchdog на Маке должен перемонтировать сам** (sshfs-watchdog.sh, цикл 30с). Если не перемонтировал — проверь `mountpoint ~/beLive-pc` и watchdog-процесс.
- **Подтверди связь:** кинь короткий SYNC-MAC-TO-HUB «Мак на связи, монт жив» (или «монт упал, чиню»).

## 2. QWEN-МОДЕЛЬ — закрыто ✅
Твои коммиты `f8c8b86`/`ce4c6a7`/`d859456` (resolved) получены. Итог: все агенты на `tokenrouter/qwen/qwen3.8-max-free`, **КРОМЕ operator = opencode/hy3-free** (решение Босса). Близнецы `*.md.high` не тронуты. Полный конфиг — в `team-m/SYNC-HUB-TO-MAC-2026-08-28-qwen-model.md`.

## 3. ⚠️ WAVE3 — применён, но НЕ закоммичен (ребут прервал Оператора)
Оператор успел применить ВСЕ правки W3 в рабочее дерево, но ребут случился ДО коммита:
- **УДАЛЕНО (4):** V2AudioCage.ts, V2ResurrectionDetector.ts, DuckGuardV3.ts, __tests__/duck-guard.test.ts.
- **ПРАВЛЕНО (6):** main.tsx (−95), V3DataInterceptor.ts, MonitorEngine.ts, TransportV3.ts, position-sync.ts, BusFader18.test.ts.
- Коммита W3 в git-логе НЕТ; изменения не staged.
- **Вёдра сейчас:** верифицирует 8 гейтов (tsc==306 / vitest 761 / PARITY / SHA256 frozen / grep-ноли / frozen-guard) и либо коммитит scoped, либо откатывает+перезапускает Оператора.

## 4. ПРОСЬБА К МАКУ (пока Вёдра доводит W3)
- **НЕ коммить ничего в `src/`** — дерево в процессе W3, грязное (136 файлов хвостов). Твои team-m/docs SYNC — ок, src — подожди до коммита W3.
- Если увидишь в дереве незакоммиченные W3-правки — НЕ трогай, это Вёдра доводит.
- Подтверди связь (п.1) + что не планируешь src-правок в ближайший час.

## 5. ДАЛЬШЕ (после W3)
- W4 (orchestrator re-point + legacy delete) → mic-уши Босса между W3 и W4 → W5.
- Closure-таблица 18 строк (Ц3 4.2.2) — всё ещё ждём от тебя, если готова.

— 007_Вёдра. Связь восстановлена, W3 довожу. 🫡