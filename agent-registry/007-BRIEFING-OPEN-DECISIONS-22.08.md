# БРИФИНГ 006→007 · Сводка открытых вопросов и решений · 22.08 вечер

Канал: реестр (читай почаще — здесь мы общаемся). Оператора dispatch'ит ТОЛЬКО ты (директива юзера). Я — исследования/верификация/цепочки, продукт моих цепочек забираешь отсюда.

---

## 🔴 БЛОКЕР-РЕШЕНИЕ (ждёт GO юзера/Ц3) — Красный фейдер
- Вопрос Санета отвечен: **НЕТ** — после №18-BUS v2 фейдер «Inst» в Quest НЕ изменит слышимый минус (instrumental-instance отсутствует при hasStems → no-op).
- КОНФЛИКТ: FINAL v2 отменил dual-mode по атаке A13, но 009 опроверг A13 в рантайме (patchV1 мёртв при v3-env, App.tsx:91). При этом рулинг Ц3 («красный фейдер = шина music-стемов») и жалоба юзера («не могу опустить минус») требуют рабочего фейдера.
- Рекомендация 006: вернуть `Inst-фейдер → setBusVolume('music-bus')` при __v3Active+hasMusicStems с ЯВНОЙ декларацией смены контракта. A1 закрыта (busOf(unknown)=music-bus), A3 — документировать ячейку.
- **ЖДЁМ: одно слово GO/NO-GO от юзера/Ц3.** GO → правим R4 плана → третий DOC-CHECK (009) → MICRO-PACK. NO-GO → инертность фиксируется контрактом, юзеру говорим честно.
- Полный разбор: OUTBOX реестра «ДОПОЛНЕНИЕ (review Санета)».

## 🟠 №17-I (458) — у тебя в работе
Семантика юзера зафиксирована в реестре: после записи ОСТАЁМСЯ на блоке записи, авто-релиз пина ЗАПРЕЩЁН. После применения — live browser-proof за юзером (REC на границе → natural-end → панель стоит).

## 🟠 №17-E (457) — REVIEW: AGREE, ждёт окна
AMEND-1 (P1-lite: кэш-запись в _onStateChange ~:101) + AMEND-2 (смягчить «единую точку»). Согласуй порядок с №17-I (E не конфликтует с I, но live-proof лучше гонять вместе).

## 🟡 №18-BUS — CONDITIONAL PASS, план готов на 90%
Полная цепочка 001→002→001→009 отработала. 5 обязательных правок плана до MICRO-PACK (OUTBOX «CHAIN-REPORT №18-BUS»):
1. resyncV3 убрать как живой вектор (мёртвый код)
2. обёртка ae.* self-contained без делегирования в фасад-no-op
3. assumption VITE_ENGINE=v3 зафиксировать
4. crash-флаг двусторонний (:218-227 И :272/:349)
5. дополнить реестр писателей (ControlDeck dual-calls, diffAndApply:147)
После твоих правок → третий DOC-CHECK мне → я даю PASS/FAIL → пишешь MICRO-PACK.

## 🟡 Закрыть вопрос Ц3 «A2» (эхо C27) — одна строка в пак
Включить в доку пака: «Ответ на A2 (C27): двойной writer существовал (coldSync :227 effective→raw; resyncV3 мёртв), устранён Шагом 0». Ц3 ждёт этот ответ два напоминания.

## 🟢 Мелочь на учёт
- TASK-011 кандидат: канвас волны не следует за чипами (юзер, 22.08).
- CORS каталога (feed-bot worker отдаёт ACAO app.mybelive.com для localhost) — prod-readiness трек.
- Тест-канон держится: tsc 314 / vitest 749/749 на всех пакетах.

## ⚙️ Процесс (напоминания)
- Спавн кастомных агентов через Task сломан (Insufficient balance — косяк opencode). Воркараунд: general + «прочитай .opencode/agent/NNN.md — это твоя роль». Все агенты переведены на ox-alpha-free (рестарт если ещё не делал).
- Протокол парной работы: черновик `006-007-INTERACTION-PROTOCOL-DRAFT.md` — поправь тегом `→006:`, финализируем v1.0. Ключевое: пинг-понг гипотез [@CONFIRM]/[@REFUTE], SYNC-OK перед паком.
- Ц3 не знает последних реализаций (449-458 + цепочка №18-BUS). После закрытия красного фейдера соберу дельта-отчёт ему одним файлом.

— 006. Вопросы/правки — тегом `→006:` прямо в этом файле или в OUTBOX реестра.
