# 📓 Session: 2026-06-14 Evening

**Ветка:** `main`
**Период:** 18:00 — 19:30 MSK
**Тип:** ZIP pipeline fixes + Telegram catalog integration + UX polish

## Краткое резюме

Серия TC по трём направлениям: (1) фиксы ZIP-пайплайна — сохранение blocksData без lrcResult, stemsMode true, конвертер DetectedBlock→PersistedTextBlock, 'Разное'→'' везде, prefetch LRC для 2-stem ZIP, fallback title из ZIP-имени; (2) интеграция TG каталога — умный поиск IDB+TG, CORS на /download, DUO бейджи, прогресс PORT, retry, анимация; (3) UX дропдауна — закрытие при выборе, имя трека в PORT.

## Таблица TC

| TC | Файл(ы) | Что изменилось | Статус |
|----|---------|----------------|--------|
| **TC-ZIP-01** | `UploadPanel.tsx` | Сохранение `blocksData` из `tagResult.blocks` даже без `lrcResult` (ветка `else if`) | ✅ Done |
| **TC-ZIP-02** | `upload.service.ts` | `stemsMode: true` при ZIP импорте стемов (блок `additionalStems`) | ✅ Done |
| **TC-ZIP-03** | `auto-lyrics.service.ts` | Новая функция `detectedBlocksToPersistedBlocks()` — конвертер DetectedBlock[] → PersistedTextBlock[] | ✅ Done |
| **TC-ZIP-04** | `catalog/types.ts`, `CatalogLayout.tsx`, `catalog.store.ts`, `cover-art.service.ts`, `track-meta-pipeline.ts`, `AiExpertPanel.tsx`, `ai-expert-prompts.ts`, тест | `parseTrackName()` возвращает `''` вместо `'Разное'` для unknown artist — 8 файлов | ✅ Done |
| **TC-ZIP-05** | `upload.service.ts` | Prefetch LRC для 2-stem ZIP при классификации instrumental без additionalStems (fire-and-forget) | ✅ Done |
| **TC-ZIP-06** | `upload.service.ts` | Fallback title из имени ZIP файла если в instrumental нет `' - '`. `uploadSession.overrideTitle` используется для prefetch и `trackData.title` | ✅ Done |
| **TC-BADGE-01** | `CatalogLayout.tsx` | Бейджи `‹ FULL ›` (зелёный) и `‹ DUO ›` (оранжевый) в списке ВСЕ ТРЕКИ. Логика: `stemsData keys > 2 → FULL`, иначе `DUO` | ✅ Done |
| **TC-FEED-01** | `feed.store.ts` | Заглушка вместо fetch `/api/feed`. `VITE_GATEWAY_URL` не задан → статус сразу `'ready'`, секции пустые. TODO: подключить gateway | ✅ Done |
| **TC-TG-02** | `CatalogLayout.tsx` | Умный поиск IDB+TG: удалена секция TELEGRAM с 15 треками, при `searchQuery >= 2` дропдаун с секциями "В КАТАЛОГЕ"/"В TELEGRAM", ↓ вызывает `/download/<file_id>` → `handleZip` | ✅ Done |
| **TC-TG-03** | `belive-feed-bot/src/index.ts` | CORS на `/download` endpoint: `Access-Control-Allow-Origin: https://app.mybelive.com`. Деплой через CF Dashboard (бандл `worker-bundle.js`) | ✅ Done |
| **TC-TG-04** | `belive-feed-bot/src/commands.ts` | DUO вместо 2-STEM display only. `callback_data` и `type` в KV не тронуты | ✅ Done |
| **TC-TG-05** | `CatalogLayout.tsx` | 3 UX фикса: (1) прогресс PORT при TG download; (2) крестик × при searchQuery > 0; (3) дропдаун с оранжевым borderTop | ✅ Done |
| **TC-TG-06** | `CatalogLayout.tsx`, `CatalogLayout.css` | Retry 1 раз через 1s при Download failed; анимация `bl-tg-pulse` на прогресс-бар при `zipProgress <= 35`; `borderTop: 2px solid #FF8C00` на дропдауне | ✅ Done |
| **TC-TG-07** | `CatalogLayout.tsx` | UX дропдауна: `setSearchQuery('')` при клике ↓; `tgDownloadingTitle` — имя трека в PORT (☁ название) | ✅ Done |

## Изменённые файлы

### beLive проект (src/)
- `src/catalog/components/CatalogLayout.tsx` — умный поиск, TG download, бейджи, PORT прогресс, retry, UX
- `src/catalog/components/CatalogLayout.css` — `bl-tg-pulse` анимация
- `src/catalog/store/catalog.store.ts` — `''` вместо `'Разное'`
- `src/catalog/types.ts` — `parseTrackName` fallback `''` вместо `'Разное'`
- `src/catalog/feed/feed.store.ts` — заглушка
- `src/services/upload.service.ts` — stemsMode, prefetch LRC, overrideTitle, saveTrack
- `src/services/upload.actions.ts` — прокси (без изменений логики)
- `src/services/auto-lyrics.service.ts` — `detectedBlocksToPersistedBlocks()`
- `src/services/cover-art.service.ts` — `''` вместо `'Разное'`
- `src/services/track-meta-pipeline.ts` (или `src/services/parsing.service.ts`) — `sanitizeBlocks` с `lineIndices` фильтр

### Telegram bot (belive-feed-bot/)
- `belive-feed-bot/src/index.ts` — CORS на `/download`
- `belive-feed-bot/src/commands.ts` — DUO display
- `belive-feed-bot/dist/worker-bundle.js` — бандл для CF Dashboard

## Known Issues

| Issue | Статус | Описание |
|-------|--------|----------|
| TG download timeout | ⚠️ Open | Download иногда падает (TG file_path TTL ~1h). Retry 1× через 1s — частичный фикс |
| Feed gateway | ⚠️ Open | `VITE_GATEWAY_URL` не задан — feed на заглушке. TODO: подключить gateway в следующей ветке |
| BUG-BRIDGE | ❌ Frozen | Баги bridge-слоя — заморожены, не в этой сессии |
| BUG-01 / BUG-02 | ❌ Frozen | Заморожены, не в этой сессии |
| Double-download TG guard | ⚠️ Pre-existing | Нет гарда от двойного клика ↓ на TG треке — может запустить два parallel handleZip |

## Handoff Notes для следующей ветки

1. **Feed gateway** — подключить реальный gateway вместо заглушки `feed.store.ts`
2. **Telegram bot docs** — DOC-TC-018 создан, ожидает наполнения (команды, R2, deploy)
3. **TG retry** — если download всё ещё падает, увеличить TTL или кешировать file_path
4. **CATALOG bulk upload** — 6 треков >50MB ждут ffmpeg сжатия
5. **DOC-TC backlog** — открыто 8 задач (P1: контроль-поверхность, governance-map; P2: exercises, markers, dock, practice, slot-matrix, track-meta)
