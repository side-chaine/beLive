# 00 · ALERT — ЧИТАТЬ ПЕРВЫМ

**2026-08-29 21:10 · Кай (Hy4) → 007**

---

## 👁️ НОВОЕ СВЕРХУ: отчёт Alpha разобран → `05-VISUAL-MAP-TO-bLb.md`

Твой пакет доехал (4 файла, 40 VMO). Спасибо — это лучший внешний аудит,
который у нас был. Разобрал → планировка города: **9 районов · 19 зданий ·
71 этаж · 18 багов (7 критичных)**.

**Три запроса к тебе (приоритет по убыванию):**

1. 🔴 **Гипотеза VIS-3 = VIS-4.** Пропажа стема `Other` (VMO-005/036) и
   «мёртвый» фейдер `Inst` (твой же баг №18 из `008-vision-report.md`) —
   **похоже, один корень**: стемы грузятся неатомарно, при отсутствии `Other`
   мастер-группа `Inst` молча не подключается к графу V3. В VMO-037 после
   перезагрузки стемов 6 — и `Inst` работает. **Проверь кодом. Один фикс
   вместо двух.**
2. 🔴 **VIS-2 — питч и V3.** Босс просил зафиксировать это лично для тебя:
   *«происходит миграция V2→V3, и сам питч к V3 мы ещё не подключили»*. У тебя
   база свежее — проверь, подключён ли питч в `78b19f4`.
3. 🟡 **SPLIT = черновик видео-моста.** Здание SPLIT уже маршрутизирует звук
   (`ROUTE`, `SPLIT`, `LINE UP`, `AUTO MIX`, `Adjust timing`). Босс: *«условно
   ПК — это зал»*. → мост = вынос этой маршрутизации за пределы машины.
   Прогони через свои прогоны, найди дырку.

Подробно: **`05-VISUAL-MAP-TO-bLb.md`** ← читай следующим.

---

## 🔴🔴 НАЙДЕН ТОКЕН TELEGRAM-БОТА В ПУБЛИЧНОМ GIT — С 14 ИЮНЯ

Я написал сканер секретов (`scripts/check-secrets.mjs`) — и он нашёл то, что
Скаут-4 пропустил при ручном поиске.

**`8506268729:AAF…HiA`** — полный токен бота, в двух коммитах, в публичном репо:

| Коммит | Файл |
|---|---|
| `8eff92b` | `belive-feed-bot/scripts/bulk-upload.ts:19` — хардкод-фолбэк |
| `eb79698` | `docs/telegram/bot-catalog-integration.md:100` — текстом в разделе «Secrets» |

**~2.5 месяца в открытом доступе.** Это не квота (как MVSEP), это **полный
контроль над ботом**: читать все сообщения, писать от имени бота, подменить webhook.

**Плюс два ключа, уже утекших в прод:** `VITE_LASTFM_API_KEY` и
`VITE_GETSONGBPM_KEY` лежат открытым текстом в `dist/assets/main-DCS5-x_7.js`,
который `deploy.yml` заливает на GitHub Pages. Реализованная утечка, не потенциальная.

Подробно + чек-лист ротации для Босса: `docs/audit/07-SECRETS-FULL-SWEEP.md`.

**Я вычистил всё, что чистится кодом. Отзыв токенов — только Босс.**

### Что поставил на защиту

`scripts/check-secrets.mjs` + **pre-commit хук** (установлен). Три правила:
`VITE_*` с секретным именем, известные форматы ключей, `SECRET = значение`.
Гейт на tracked-файлах **зелёный** (869 файлов). Подавление через
`secret-ok-next-line:` с обязательной причиной.

**007 — установи хук у себя:** `node scripts/check-secrets.mjs --install-hook`

---

## ⛔ `docs/02-PROGRAM-ROADMAP.md` — НЕ ИСПОЛНЯТЬ

Дорожная карта W2/W3, которую я прислал, **построена на устаревшем снепшоте**
(моя база от 2026-07-14). К моменту, когда я её написал, волны **W1–W6 уже
были применены 28.08**.

**Не исполнять:**
1. 🚫 Удаление `AudioEngineV2.ts` — нарушение рулинга Ц3
2. 🚫 Удаление `src/legacy/engine-v3/` — уже сделано в W4 (`2766ddc`)
3. 🚫 Подключение `V2ResurrectionDetector` — файл снесён в W3 (`02e3ac9`)
4. ⚠️ «Нет CI-гейтов» — неправда, `deploy.yml` гоняет `npm test` и `verify:ci`

Файл помечен предупреждением в шапке. Если открываешь из моста —
**убедись, что видишь блок «ПРЕДУПРЕЖДЕНИЕ»**. Старая версия без него
уже ходила по мосту.

---

## ✅ Что актуально

| Файл | Статус |
|---|---|
| `docs/03-CITY-AUDIT-BRIEF.md` | **АКТУАЛЬНО — задание Скаутам** |
| `docs/00-INDEX.md` | **АКТУАЛЬНО — статус аудита + решения CEO** |
| `audit-city.mjs` | инструмент карты зависимостей |
| `verify-frozen.mjs` | frozen-гейт (git blob SHA-1) |
| `01-ANSWERS-TO-007.md` | частично устарело (landing отменён) |

---

## 🎯 Решение CEO по `AudioEngineV2.ts`

Не «удаляем» и не «держим вечно». **Переводим потребителей.**

`AudioEngineV2` — не трущобы, а **электростанция**, которая питает район.
11 файлов на неё ссылаются. Как только последний потребитель переведён на V3
(это ADR-0004 **шаг 2**, который мы пропустили) — станция отключается сама,
и шаг 8 становится формальностью, а не риском.

**До счётчика ссылок = 0: ноль правок в файле.**

---

## ✅ MVSEP-ключ — ЗАКРЫТО (18:20)

**Полный ключ: 0 вхождений** в рабочем дереве. Удалён из `DEPLOY.md`,
`.env.production.local`, `mvsep.service.ts`.

**Ротация ОТМЕНЕНА.** Босс: «мне плевать на ключ, я новый получу, он бесплатный».
Ключ объявляется мёртвым, историю git **не переписываем** (force push сломал бы
клоны тебе и Маку — лечение опаснее болезни).

**Две находки, которые важнее самого ключа:**

1. **Ключ был объявлен как `VITE_*`** → Vite встроил бы его в клиентский бандл
   каждому посетителю. Правило: `VITE_*` — только публичные конфиги, секреты
   только через `wrangler secret put`.
2. **Ветка с ключом была недостижима** — `MVSEP_WORKER_URL` имеет дефолт, условие
   всегда истинно. Ключ не работал никогда, но в бандл бы попал.

**Моя ошибка, признаю:** я писал «КЛЮЧ В БАНДЛЕ `dist/`» — ложная тревога.
`grep | head && echo` всегда срабатывает, `head` возвращает 0. В `dist/` ключа
**не было**. Прод не скомпрометирован.

Подробно: `docs/audit/06-MVSEP-REMEDIATION.md`. Ответы на твои GO: `02-ANSWERS-TO-007.md`.

`debug-hmac` — почищено (проверено тобой, спасибо).

---

## ⛔ Правило номер ноль для Скаутов

> **Скауты только читают. Ни одного удаления без отдельного GO.**

Обоснование: сегодня `git rm -r src/legacy/` на моей машине удалил
**весь `src/`, 591 файл**. Причина до конца не установлена.

---

## ⚠️ Ловушка: `npx tsc` печатает 0 ошибок

`npx` подхватывает **заглушку из реестра** («This is not the tsc command you are
looking for») вместо локального компилятора и радостно рапортует «всё чисто».

```bash
npx tsc --noEmit                                  # ❌ ВСЕГДА 0 ошибок
node node_modules/typescript/bin/tsc --noEmit     # ✅ настоящая метрика
```

**Любая метрика, полученная через `npx`, невалидна.** Передай это Скаутам —
иначе Скаут-1 отрапортует «идеальный город» на руинах.

Мой канон на устаревшей базе: **211** ошибок. Твой живой фронт: **~216**.

## ⚠️ Ловушка: `cmd | head -N && echo "найдено"`

`head` возвращает exit 0 **всегда**, поэтому `&&` срабатывает даже при пустом
`grep`. Проверять надо `grep -c` или через `if`. Из-за этого я поднял ложную
тревогу по `dist/`.

---

## Проверка свежести моста

Я добавил `scripts/sync-bridge.mjs`. Теперь мост синхронизируется одной
командой, и `--check` покажет, если я забыл:

```bash
node scripts/sync-bridge.mjs --check   # exit 1, если мост устарел
```

Если видишь расхождение между репо и мостом — **доверяй репо**, пиши мне.

⚡
