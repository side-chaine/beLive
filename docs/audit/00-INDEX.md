# 00 · INDEX — Полный аудит репозитория («Город»)

**Ведёт:** 007 (Hub) · **Курирует:** Кай (Hy4), CEO реформы
**Старт:** 2026-08-29 · **База:** живой фронт Linux-main, HEAD `41e6280` ↔ `78b19f4`
**Бриф:** [../modernization/03-CITY-AUDIT-BRIEF.md](../modernization/03-CITY-AUDIT-BRIEF.md)

---

## ⛔ Правило номер ноль

> **Скауты только читают. Ни одного удаления, перемещения или правки без
> отдельного GO от CEO.**

Обоснование: инцидент 2026-08-29 16:51 — попытка удалить 9 orphan-файлов
задела 591 файл. Подробности в брифе §0.

---

## Статус Скаутов

| # | Скаут | Что | Статус | Отчёт | Находок |
|---|---|---|---|---|---|
| 4 | 🔥 **СЕКРЕТЫ** | ключи, debug-роуты, git-история | ✅ сдан + ⏳ доп. проход по истории | `04-SECRETS.md`, `07-SECRETS-FULL-SWEEP.md` | 1 → 4 |
| 2 | КОММУНИКАЦИИ | граф импортов, циклы, хабы | ⏳ ждёт запуска | `02-DEPENDENCIES.md` | — |
| 1 | КАДАСТР | опись каждого файла | ⏳ ждёт запуска | `01-CADASTR.md` | — |
| 3 | ЗАБРОШКИ | мёртвый код (кучи A/B/C) | ⏳ ждёт 2 | `03-DEAD-CODE.md` | — |
| 5 | ДОКУМЕНТАЦИЯ | паспорта объектов, белые пятна | ⏳ ждёт запуска | `05-DOCS.md` | — |

---

## Уже найдено (первый проход Hy4, база 2026-07-14 — требует перепроверки)

### 🔥 Секреты

| Находка | Статус | Действие |
|---|---|---|
| 🔴 **Токен Telegram-бота** — `belive-feed-bot/scripts/bulk-upload.ts:19` + `docs/telegram/bot-catalog-integration.md:100`, **в коммитах `8eff92b`/`eb79698` с 14 июня** | ✅ код вычищен | 🔴 **БОСС ОТОЗЫВАЕТ** через @BotFather → `/revoke` |
| 🟠 **Last.fm API key** — `.env:1`, уехал в `dist/assets/main-DCS5-x_7.js` → GitHub Pages | ✅ помечен | 🟠 Босс перевыпускает ключ |
| 🟠 **GetSongBPM API key** — `.env:2`, уехал в тот же бандл | ✅ помечен | 🟠 Босс перевыпускает ключ |
| MVSEP-ключ — `belive-mvsep/DEPLOY.md:21` | ✅ **закрыто** | Босс получает новый при желании |
| `belive-api` роут `/auth/debug-hmac` | почищено на живом фронте | — |

⚠️ **Удаление строки из файла не отменяет ротацию.** Всё, что попало в
git-историю публичного репо или в опубликованный бандл, считать
скомпрометированным. Чек-лист ротации: **`07-SECRETS-FULL-SWEEP.md` §7**.

**Все четыре секрета — одна и та же ошибка:** секрет, объявленный с префиксом
`VITE_`, публикуется Vite в клиентский бандл. Правило закреплено в
`scripts/check-secrets.mjs` и в CI.

### Циклы импортов (4, требует верификации на живом фронте)

```
1. markers.store  ↔  utils/markerUtils
2. practice-session.store → practice-scenarios → billy-action-runner
                          → TrackInfoBoard/ai-tools → practice-session.store
3. practice-session.store  ↔  practice-scenarios
4. services/mvsep.service  ↔  stores/mvsep.store
```

### Хабы (топ-8 по числу импортёров)

| Файл | Импортёров |
|---|---|
| `src/stores/track.store.ts` | 34 |
| `src/foundation/event-bus/types.ts` | 32 |
| `src/foundation/event-bus/event-bus.ts` | 29 |
| `src/stores/audio.store.ts` | 29 |
| `src/stores/blocks.store.ts` | 26 |
| `src/stores/mode.store.ts` | 26 |
| `src/stem/stem.store.ts` | 23 |
| `src/stores/lyrics.store.ts` | 21 |

### Разное

- **512 файлов** в `src/` (415 `.ts`, 105 `.tsx`) на базе Hy4
- **60 тестов, из них компонентных (`.test.tsx`) = 0** — подтверждённая дыра
- **93 кандидата в orphan** — ⚠️ большинство ложные (алиасы, `lazy()`,
  строковые пути). Фильтрует Скаут-3.
- **44 района** верхнего уровня в `src/`
- `src/audio/engine-v3/V2Adapter.ts` — 13 импортёров (живой, не трогать)

---

## Инструмент

```bash
node scripts/audit-city.mjs           # карта в консоль
node scripts/audit-city.mjs --json    # машиночитаемо → docs/audit/graph.json
```

Что считает: orphans, hubs (≥8), циклы (DFS), раскладку по модулям.
**Ограничение:** не знает алиасы (`@/...`), `lazy()` с шаблонными строками,
строковые ссылки. Результат — **черновик для ручной проверки**, не приговор.

### Сканер секретов

```bash
node scripts/check-secrets.mjs            # tracked-файлы = гейт коммита
node scripts/check-secrets.mjs --staged   # только то, что сейчас коммитится
node scripts/check-secrets.mjs --all      # + gitignored (.env, *.pem) = аудит
node scripts/check-secrets.mjs --install-hook
```

R1 `VITE_*` с секретным именем · R2 известные форматы ключей ·
R3 `SECRET = значение`. Подавление — `secret-ok-next-line:` с причиной.

**Ограничение:** смотрит только **текущее состояние файлов**, историю коммитов
не трогает. Проход по `git log --all -p` — задача Скаута-4.

---

## Решения CEO (приняты)

| Вопрос | Решение |
|---|---|
| `AudioEngineV2.ts` удалять? | **НЕТ сейчас.** Перевести 11 потребителей на V3 (ADR-0004 шаг 2). Когда счётчик ссылок = 0 — шаг 8 выполняется без риска. До этого — ноль правок в файле |
| `src/legacy/engine-v3` удалять? | Уже удалено в W4 (`2766ddc`) |
| `V2ResurrectionDetector` чинить? | Файл снесён в W3 (`02e3ac9`). FINDING-001 аннулирован |
| Дорожная карта 02 | ⛔ **НЕ ИСПОЛНЯТЬ** — построена на базе 2026-07-14 |
| Секьюрити-правка MVSEP | ✅ **закрыто** — ключ удалён везде, 0 вхождений. Ротация отменена: Босс получит новый сам, ключ бесплатный |
| Токен Telegram-бота | 🔴 **ОТОЗВАТЬ** — единственное, что его убивает. Историю не переписываем (force push сломает клоны 007 и Мака) |
| Last.fm / GetSongBPM | 🟠 **Перевыпустить.** Радикальное решение — CF Worker-прокси (задача O5), как уже сделано для MVSEP |
| Гейт на секреты | ✅ **Введён** — pre-commit хук + шаг в `quality-gates.yml`. Зелёный на 869 файлах |
| Landing 7 файлов Hy4 | Отменён — файлов нет на живом фронте |
| Мак | Перенацелен: legacy-аудит отменён → security-аудит + база-синк + **описание «города»** |

---

## Ждём от 007_Мака

**Расширенное описание метафоры «город».** Босс указал, что Мак больше всех
занимался этой темой. Нужно: районы и их назначение, какие «коммуникации»
считаются магистральными, где границы города (что внутри, что внешние
сервисы), как выглядит «стройнадзор». Это ляжет в основу Генплана.

---

## Что будет после аудита

**Генплан города** (`docs/modernization/04-GENPLAN.md`):
- районы и границы между ними
- какие коммуникации тянуть заново
- что сносим, в каком порядке, с какими гейтами
- где ставим стройнадзор (CI-гейты)
- расписание и владельцы

**Генплан — следствие кадастра, а не фантазия.** Сначала опись, потом план.

---

## Канал

```
Windows:  C:\Users\nikit\beLive-bridge\
WSL/007:  /mnt/c/Users/nikit/beLive-bridge/
  from-windows/  ← Hy4 пишет
  to-windows/    ← 007 пишет (отчёты Скаутов дублировать сюда)
```

⚡
