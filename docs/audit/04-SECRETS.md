# 04 · СКАУТ-4 · СЕКРЕТЫ — ОТЧЁТ

**Скаут:** 4 (СЕКРЕТЫ) · **Дата:** 2026-08-29 · **База (HEAD):** `41e6280` ↔ `78b19f4` (Linux-main, живой фронт)
**Файлов просмотрено:** весь `src/`, `belive-*`, `gateway/`, `team-m/`, `*.md`, `*.toml`, `*.json` (исключая `node_modules/dist/build`)
**Находок:** 2 (1 критичная, 1 самоутечка в REGISTRY — устранена) · **Критичных:** 1
**Время:** 2026-08-29 17:50 GMT+3

> ⚠️ **КОНФИДЕНЦИАЛЬНО.** В отчёте — только первые 6 символов ключей. Полные значения НЕ публикуются.

---

## 🔥 КРИТИЧНО: MVSEP_API_KEY в открытом тексте

| Поле | Значение |
|---|---|
| Файл | `belive-mvsep/DEPLOY.md:21` |
| Значение (маск.) | `kVnx54…` |
| Статус | **в коммите, публичный репо** |
| Git-история | введён в `4d59e67` («fix: route MVSEP through CF Worker») как новый файл `belive-mvsep/DEPLOY.md` |
| Риск | сливают платную квоту MVSEP прямо сейчас |

**Действие (GO от CEO дан):**
1. **Босс ротирует ключ у провайдера MVSEP** — обязательно, т.к. ключ уже в публичной git-истории = скомпрометирован.
2. **007 убрал plaintext из рабочего файла** (`DEPLOY.md:21` → `${MVSEP_API_KEY}`, без реального значения). Сделано в этом же коммите-пакете.
3. Удаление из файла НЕ отменяет ротацию — история `4d59e67` остаётся. Рекомендация: `git filter-repo` / BFG для вычистки истории ИЛИ считать ключ мёртвым и только ротировать.

---

## Найдено при сканировании (ложные срабатывания — НЕ секреты)

- `belive-api/src/auth/google.ts` — совпадение по словам `clientSecret`/`access_token`, но это **типы TypeScript** (`clientSecret: string`), хардкода нет. Чисто.
- `belive-api/src/auth/jwt.ts` — `secret: string` параметр функции подписи, значение приходит из env. Чисто.
- `team-m/REGISTRY.md` — **самоутечка**: 007 записал полный MVSEP-ключ в пивот-блок. **Устранено**: замаскировано до `kVnx54…` тем же коммитом.

---

## Проверка debug-hmac (из ALERT)

`belive-api/src/index.ts:93` — `GET /auth/debug-hmac` **отсутствует** на живом фронте (строка 93 = `/health`). Подтверждает: роут уже почищен. Доп. grep `debug-hmac` по `belive-api/src/` — пусто.

---

## Что НЕ нашёл (но надо держать в уме)

- `JWT_SECRET` (`DEPLOY.md:24`) — плейсхолдер «тот же secret что в belive-auth Worker», реального значения нет.
- Прочие воркеры (`belive-api`, `gateway`, `belive-feed-bot`, `belive-auth`) — иных литеральных ключей (AIza/sk-/ghp_/xoxb/postgres:///mongodb://) в текущих файлах **не обнаружено**.
- Рекомендация Скаута: после ротации MVSEP прогнать `git log --all -p` по расширенным паттернам повторно (узкий скан здесь — первый проход).

---

## Требует решения CEO

1. **Ротация MVSEP-ключа у провайдера** — только Босс. Каждый час = деньги.
2. **Очистка git-истории** от `kVnx54…` (filter-repo/BFG) ИЛИ официально считать ключ мёртвым.
3. Добавить `belive-mvsep/DEPLOY.md` в `.gitignore`? Нет — он нужен как инструкция; реальные значения только через `wrangler secret put`, не в файле.
