# 454-CORRECTION · ОТМЕНА ГИПОТЕЗЫ ДРЕЙФА + №17-F (ФИНАЛ САГИ №17) → Ц3

## ⚠️ КОРРЕКЦИЯ 452-REPORT (честность протокола)
Гипотеза «дрейфа __belive.currentTime» из 452 — **ОТМЕНЕНА самим 007** после полного чтения V3StatePublisher:
- `_tickLoop` читает `transport.currentTime` НАПРЯМУЮ каждые ~50мс (`V3StatePublisher.ts:125`) → кэш зеркалит транспорт, врать отдельно не может.
- «Дрейф» был **ловушкой соседних строк консоли**: между `[TRIM-BASIS] REC ARM` и `[PS Travel]` юзер ПЕЛ тейк ~9-12 секунд. Транспорт легитимно дошёл до триггеров.
- Проверка по маркерам: 450-run — nsFMT=29.65, выстрел на ct=28.89 (= за 0.76с ДО субблока ✅); nbFMT=38.66, выстрел на ct=37.69 (= за ~1с ДО блока ✅). Финальный run: tt=36.98 при границе 37.98 ✅. PS Travel работал ТОЧНО во всех прогонах.
- **№17-E (publishSeek внутрь движка) ОТМЕНЕНА. Движок НЕ тронут.** Инвентаризация publishSeek-коллеров (WagonTrain:101/126, TransportBar:45, WaveformCanvas:443, Space:53) остаётся в леджере как справка.

## РЕАЛЬНЫЙ ИТОГ САГИ №17 (продуктовая семантика)
Весь феномен «переброса» = легитимное движение песни + auto-follow панели по дизайну + (до 442/445) реальные side-effect advance'ы. Юзер по скриншоту зафиксировал финальное желаемое поведение: «если песня перещёлкнула в другой блок — волна должна показывать активный блок».

## 453-MICRO-PACK · №17-F — ПРИМЕНЁН (tsc 314 / vitest 749/749 ✅)
Авто-релиз `blockPinRef` (TakesPanel rAF): когда честное время транспорта выходит из диапазона запиненного блока (вперёд `t >= endTime`, назад `t < startTime - 0.5`) → пин снимается → существующий auto-follow переключает панель на активный блок. Guard `isRecording` сохранён.

## ИТОГОВАЯ СЕМАНТИКА №17 (для подписи)
1. Во время записи → панель стоит на блоке записи.
2. Сразу после стопа → панель стоит, зелёный best виден/играется.
3. Песня легитимно ушла в след. блок → чипы/текст/панель+волна уходят ВМЕСТЕ; слоты нового блока чистые; записанный тейк сохранён в своём блоке (возврат кликом чипа).

## ПОЛНЫЙ СПИСОК ПРАВОК САГИ (кандидат на коммит после финального ретеста)
442 (TakesControlStrip:365) + 445/B2 (exercise-events:39) + 447/C (blockPinRef + PS Travel V3-clock) + 451/D (PS Transport truth clock) + 453/F (pin auto-release) + трейсы 449/450 (DEV-логи). Канон А4 соблюдён на каждом шаге: tsc 314 / vitest files 61/63, tests 749/749.

## ПРОСЬБА Ц3
1. Санакция итоговой семантики №17 (три пункта выше).
2. GO на коммит саги после финального browser-proof юзера (push 🔒).

*007 · координатор/упаковщик.*
