# 444-REPORT · №17 КОРРЕКЦИЯ (442 НЕДОСТАТОЧЕН) + 008-VISION → Ц3

**Источник:** `agent-registry/008-vision-report.md` (008 = vision-агент, параллельная сессия как 006, читает скриншоты; скрины удалены после фиксации).

---

## №17 — ВИЗУАЛЬНО ПОДТВЕРЖДЁН (008 кадры)
- Verse1 запись (0:28) → коммит тейка, ЗЕЛЁНЫЙ best (0:30, TASK-009/441 proof жив) → Pre-Chorus (0:46) авто-переход. Юзер дословно: *«что не правильно!»*.
- 441/TASK-009 подтверждён визуально (Take 1 зелёная рамка best сразу без Play).

## 442 ПРИМЕНЁН НО НЕДОСТАТОЧЕН — НАЙДЕН ВТОРОЙ (РЕАЛЬНЫЙ) ТРИГГЕР
- 442 убрал `advanceToNextStep` из `TakesControlStrip.tsx:365` (handleIntermediateWindowEnd) — НО этот вызов не единственный/не активный путь перехода.
- **РЕАЛЬНЫЙ активный триггер:** `src/foundation/event-bus/wrappers/exercise-events.ts:37-40`:
  ```ts
  // recording → completed step
  if (prevIsRecording && !isRecording && exercise.phase === 'recording') {
    exercise.onStepCompleted()   // → advanceToNextStep() → ПРЫЖОК НА СЛЕД. БЛОК
  }
  ```
  Срабатывает на переходе `isRecording` true→false при `exercise.phase === 'recording'` — т.е. ровно «при остановке записи», симптом юзера.
- `exercise-events.ts` **НЕ в frozen-списке** (frozen = AudioEngineV2 / patchV1 / bridges/* / track.orchestrator / private _). Правка допустима.
- `initExerciseEvents` **АКТИВЕН**: `src/main.tsx:62` `registerInit({ id: 'exercise-events', init: initExerciseEvents })` + `runAll()` (`docs/architecture/init-registry.md:48`). Т.е. подписка реально висит в рантайме.
- grep `onStepCompleted`: живые коллеры = `exercise-events.ts:23` (Track before-change) и `:39` (recording-stop). `TakesControlStrip.tsx:365` удалён 442.
- **СЛЕДСТВИЕ:** прыжок после записи живёт в `exercise-events.ts:39`, НЕ в `TakesControlStrip:365`. 008-F3 (свежесть сборки/HMR) теперь НЕ релевантен — это не stale-сборка, а реальный активный путь. 442 был применён в неверном месте.

## РЕКОМЕНДАЦИЯ (в рамках уже DECIDED Вар.B — явная прогрессия)
- **№17-B2 MICRO-PACK (corrective):** в `exercise-events.ts` убрать блок `:37-40` (`if (prevIsRecording && !isRecording && exercise.phase === 'recording') exercise.onStepCompleted()`).
- Прогрессия остаётся ЯВНОЙ через `ExerciseStrip.tsx:107` (`onClick={advanceToNextStep}`) / `skipStep` / ручной `onStepCompleted` — согласно Вар.B.
- **ФЛАГ для Ц3:** `exercise-events.ts:23` (`store.onStepCompleted?.()` на Track before-change) — тоже авто-advance, но на СМЕНЕ ТРЕКА, не записи; предлагаю НЕ трогать в №17-B2 (отдельный скоуп) — решение за Ц3.
- 442-коммит `8df03a3` можно оставить как есть (удаление `:365` безвредно) либо откатить — на усмотрение Ц3.
- **НУЖЕН GO Ц3 на №17-B2** (это исполнение Вар.B в верном месте, не новое арх. решение).

---

## №18 — КОНКРЕТИЗИРОВАН через 008 (вариант (б))
- Юзер опустил красный **Inst до 37 ВО ВРЕМЯ записи** (кадр 2) — **ЗВУК НЕ ИЗМЕНИЛСЯ**. Объяснение юзера: красный фейдер = instrumental-мастер, который **ВЫКЛЮЧЕН когда играют стемы**; играют стемы, а не instrumental-мастер. → совпадает с **MX-01 / TASK-008**: при `hasStems` instrumental-мастер не грузится/не играет → фейдер крутит «мёртвый» слой.
- **F-1 (008):** Inst 37 (кадр 2, 0:30) → Inst 100 (кадр 3, 0:46, после смены блока). Нужна проверка персистентности значения Inst при смене блока (релевантно дизайну №18 и B-slice).
- **F-2 (008):** нет скрина Studio-микшера среди кадров (юзер упоминал — запросить отдельно при необходимости).
- **ИТОГ №18 = (б):** быстрый фейдер уровня музыки в практике, который реально глушит **СТЕМЫ** (а не мёртвый instrumental-мастер) + персистентность между блоками. Входит в F-2 / v-Mix-дизайн (стерео-разводка + контроль music-стемов). НЕ блокирует B-slice/F-2.

---

## 008 интегрирован в координацию
Vision-агент, параллельная сессия (как 006), пишет в тот же локальный реестр (`agent-registry/008-*.md`), читает скриншоты (007/006 не могут), удаляет скрины после фиксации. Координация = те же INBOX/OUTBOX через реестр. 008 — только визуальные наблюдения + слова юзера (диагнозы/фиксы вне компетенции).

## Канон А4
`tsc 314` (diff IDENTICAL) · `vitest` files 61/63 (2 legacy load-error), tests 749/749 — не нарушен. 442 не добавлял ошибок; №17-B2 будет проверен тем же каноном.

## НУЖЕН ОТ Ц3
1. **GO на №17-B2** (`exercise-events.ts:37-40` remove). 
2. Решение по `:23` (Track before-change `onStepCompleted`) — трогать в №17-B2 или нет.
3. Подтверждение статуса №18 = (б) (быстрый фейдер музыки → стемы + персистентность), входит в F-2.

*007 · координатор/упаковщик · без правок кода.*
