# 452-REPORT · №17 ROOT CAUSE ДОКАЗАН ЦИФРАМИ + №17-C/D ПРИМЕНЕНЫ → Ц3

## ЭВОЛЮЦИЯ ДИАГНОЗА (честная хронология)
1. **442** убрал advanceToNextStep из TakesControlStrip:365 — недостаточно.
2. **445/B2** убил onStepCompleted на stop записи (exercise-events:39) — недостаточно, но путь был реальным.
3. **447/C** пин блока панели (blockPinRef) + PS Travel переведён с V2-часов на __belive.currentTime — панель починена ([SET-BLOCK] в ретесте молчит), но текст продолжал лететь.
4. **449-TRACE + 450-DIGITS** дали протокол с места преступления.

## ВЕРДИКТ ЦИФР (трейс юзера, track 1787200209918)
```
TRIM-BASIS: engineNow = 20.014          ← транспорт РЕАЛЬНО здесь
[PS Travel] tt=28.85 ct=28.89 v2clock=28.89   ← часы на +8.9 вперёд
[PS Travel] tt=37.66 ct=37.69 v2clock=37.69   ← на +17.7 вперёд
```
**ROOT CAUSE:** `__belive.currentTime` (кэш V3StatePublisher) НЕ сбрасывается внутренними seek'ами. `publishSeek()` зовётся только из клика по чипу (WagonTrain.tsx:99); все прочие seek'и (REC-preroll через `HybridPipelineService.seek`, возврат транспорта после превью тейка) публикатор молча пропускает → база часов не пересинхронизируется → накопленный дрейф ровно равен непublished seek'ам → PS Travel «досрочно» достигал триггера след. блока = видимый переброс текста во время записи.

## ЧТО УЖЕ ПОЧИНЕНО И КАК ДОКАЗАНО
| Симптом | Фикс | Proof |
|---|---|---|
| Панель прыгала на след. блок | 447 blockPinRef (pin на arm, unpin на смене трека) | ретест: [SET-BLOCK] молчит после записи, тейк остался в слотах ✅ |
| Текст улетал к след. блоку при записи | 447 V3-clock + **451/D честные TransportV3.currentTime** | цифры трейса: дрейф +8.9…+17.7 устраняется чтением transport truth |
| Авто-advance шага при stop записи | 442+445 | exercise-events:39 удалён; [STEP-ADVANCE] в ретесте не звенел ✅ |
| Звук/подсветка тейка | C29–C33, 440, 441 | PLAY-TAKE→GUARD PASS→SRC-START gain=1, natural-end ✅ |

Канон А4 на каждом шаге: tsc 314 (diff IDENTICAL) · vitest files 61/63 (2 legacy load-error), tests 749/749.

## СИСТЕМНАЯ НАХОДКА → НУЖНО РЕШЕНИЕ Ц3 (№17-E)
Дрейфующий `__belive.currentTime` едят и ДРУГИЕ потребители — минимум playhead тейков в TakesPanel (паттерн C21/418 читает именно кэш!). Предлагаю пак **№17-E**: `publishSeek(newTime)` внутри `TransportV3/HybridPipelineService.seek()` (единая точка) — чинит всех потребителей разом, PS Travel-фикс D остаётся как страховка. Затронет engine-v3 pipeline (НЕ frozen). Жду GO.

## ОЖИДАЕМЫЙ ФИНАЛЬНЫЙ RETEST (юзер)
Запись тейка на куплете → стоп → текст СТОИТ на куплете до реального подхода границы (~1с до неё — легитимный pre-scroll) → панель на месте, зелёный best, звук есть.

## ПРОЦЕССНОЕ
Урок 442 отработан дважды: полная инвентаризация двигателей блока (6 записей) + трейс-инструментация ДО фикса. Гейт proof-of-change поймал оба ложных закрытия (008-визуал + цифры).

*007 · координатор/упаковщик.*
