# 429-REPORT (C28 РЕТЕСТ) — ИНДИКАЦИЯ ЖИВАЯ ✅ + CDP R1-PROOF ✅

**Контекст:** ретест C28 ушами + CDP R1-proof (сниппет 428). Вторая v3-конфиг-сессия.
**Статус:** оба гейта закрыты. Бандл: 1/7 ✅ · 2/7 ✅ · 3/7 R1-half ✅ (CDP) · 4+ ждут.

---

## 1. Ретест индикации (уши, C28)
**Вердикт пользователя: «кайф! все как надо!»**

- Обычный режим: индикация **живая**, плавно движется с музыкой (rAF-цикл вместо setInterval 10Hz)
- Отжатие solo → индикация остальных стемов **сразу оживает** («застревание» ушло — корень был в узком гейте `t3.state !== 'idle'`, читавшем заглушенный V2)
- Бонус-чек C27: mute/solo стема → его индикация падает к 0 (метры читаются ПОСЛЕ stretchGain — effective-маска видна)

## 2. CDP R1-proof (числа, без ушей)
Сниппет 428 (mute vocals в UI → два метра через `__belive.pipeline`):
```
vocals meter (после mute): 0            ✅ основной микс заглушён
vocalHall level (после mute): 0.111     ✅ вокал ЖИВЁТ в зале (pre-fader тап)
```
**Инвариант R1 (mute не убивает зал) — доказан числами.** Это первая половина 3/7 SPLIT: R1-proof закрыт CDP-драйвом, к живому уху добавим при SPLIT-сессии с железом (conditional). SPLIT-минимум (crossfade default↔main) — conditional по наличию времени.

## 3. Консоль сессии — чисто
- Бут v3: 7/7 StretchInstance, Phase F ACTIVE, Cage + watchdog 3 прохода ✅
- Трек 1787084231170: 5 stems (vocals/bass/drums/guitar/keys — без 'other'), RouteCheck **5 routes**, RECON-1 ×1.000, seek 24.88 → 68.05 (пробел-навигация), PS Travel все триггеры с `hasMeasurement: true` ✅
- M2-latency: **1235ms** — внутри [1156–1476] (2-я точка; re-measure на 'interactive' — ledger #3)
- `[VOC] skip 15s` — по треку с dataVersion < 4 (А3: штатно, не баг)
- Единственная ошибка: CORS feed-bot (известное, не наш)

## 4. Точки для M3-GO (по Ц3-шаблону)
| Строка | Статус |
|---|---|
| solo/mute-инвариант ✅ (уши) | закрыта (C27, 24.08) |
| индикация обоих режимов живая (уши) | закрыта (C28, 24.08) |
| R1 (mute не убивает зал) | ✅ CDP-числа; ухо при SPLIT |

## 5. Следующий шаг
Бандл 4/7: **П-8 снапшот** (обе половины на 'interactive') — шаги выданы пользователю в сессии. Далее: Visual-карточки → 389 → loop-cleared → takes/TRIM-BASIS → RTL → §H → полный 425 (с правкой G4-инварианта) → M3-GO.

## 6. Реестр
C28: мост читает существующие метры; R1-геттер — AnalyserNode (параллельный тап, не в сигнальном пути, живёт весь lifecycle). Ноль аудио-узлов в сигнальном пути добавлено обоими пакетами (A: 0 узлов; B: 1 параллельный тап).